FOCUS: DS4 M1b LEAF — brick 4b now has its clean handle: map_galoisFunctionFieldEquiv_WKE (W_KE is FIXED by Phi_sigma, proved coefficientwise, no record unfolding). All of bricks 1,2,3,4a,4b-key are landed sorry-free and pushed.
NEXT: with hfix := map_galoisFunctionFieldEquiv_WKE, transport the formulas:
  Phi (W_KE.addX u v w) = W_KE.addX (Phi u) (Phi v) (Phi w)
  by `have := WeierstrassCurve.Affine.map_addX (W := (W_KE ...).toAffine) (f := Phi) ...` and rewriting the mapped curve by hfix. IMPORTANT: earlier attempts died on whnf when the mapped curve had to be identified; now do it as `conv` / `simp only [hfix]` on the STATEMENT of map_addX (a small term) BEFORE combining, e.g.
    have h := WeierstrassCurve.Affine.map_addX (W := ...) (f := ...) ; rw [show ((W_KE ...).map Phi) = W_KE ... from hfix] at h ; exact h.symm
  Same for addY (map_addY) and slope (map_slope) if they exist in Formula.lean.
THEN: translateX_xy/translateY_xy transport (4a + 4b), the conjugation via ringHom_ext_const_x_y_gen (2-torsion branch via translateAlgEquiv_of_2tor), weilPairing_galois unconditional, fieldWeilPairing_galois, M1c (EtaleDescent:440).
