FOCUS: Gap A / (LOCAL-SQUARE). STEP 1 DONE + axiom-clean:
  EllipticCurve/WeierstrassCover.lean
    LocallyWeierstrass.exists_cover
    EllipticCurve.exists_weierstrass_cover_baseChange
  -- the base T of E x_S T -> T is covered by affine opens U x carrying a Weierstrass model
     W over Gamma(T, U x), with pullback (pullback.snd E.pi t) (U x).iota =~ projModel W.
     This is EXACTLY the cover nonempty_unitObj_iso_of_normalized_glue consumes.
STEP 2 (next): on one such U, transport the three section divisors D_Q, D_Q', D_{Q+Q'} and
D_0 across the iso to projModel W, and exhibit the generating section l/v of the difference.
  - the group law on projModel W must be matched with E's: see EllipticCurve/ModelGroupUniq.lean
    and RecordGroupUnique.lean (canonicity of the group law over a connected base).
  - the ideal identity to prove over R = Gamma(T, U x):
      I(D_P).I(D_Q).I(D_{-(P+Q)}) = (line)   and   I(D_{P+Q}).I(D_{-(P+Q)}) = (vertical)
STEP 3: normalize on the zero section, glue, convert with picClass_mul_eq_of_nonempty_tensor_iso.
STATUS: Step 2 is the large one — it is the relative (base-ring) theorem of the square for
Weierstrass curves. HasseWeil MillerAllChar.lean (726 lines) is the FIELD analogue and is
valuation-theoretic, so it does NOT transport.
