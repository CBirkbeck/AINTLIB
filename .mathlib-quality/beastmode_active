FOCUS: T802 — NOW the (3.3) limit lemma canonicalMap_Qa_sq = 0: hWsplit (rho W = rho t * g via mk_spec), hkey (per-n rewrite via Wa_pow_mul_yQ + map_pow), hbdd (range (g^n * rho(yQ n)) bounded: g-powers pb-range .mul {rho yQ n}-bounded [via tA^k-scaling into unitBall=podA.A0 + coeRingHom_image_locSubring_isBounded + u := unit-inverse of rho tA singleton-mul]), hnull (tA-powers norm-null + continuity + map_pow), then absorption: forall U nhd-0, rho(Q2) in U ⇒ rho(Q2) = 0 by T2-singleton-closed. THEN rev-eval assembly per earlier sentinel; roundtrips; chartEquiv; T804.
