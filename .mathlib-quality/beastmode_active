FOCUS: DS4 M1b-3b-viii step e — equivMapDomain (galoisProjPointEquiv) (pullbackDiv f h Q) = pullbackDiv f h (galoisPointEquiv Q).
Steps (a)-(d) are DONE and pushed (galoisProjPointEquiv_toProjectiveSmoothPoint, equivMapDomain_kappaDivisor, galoisPointEquiv, galoisFiberEquiv). File is green.
ATTEMPT NOTE: a show-based unfolding of pullbackDiv (Pullback.lean:34 = sum over {P // f P = Q} of single P.toProjectiveSmoothPoint 1) got stuck on elaboration (Fintype instance spelling f P = Q vs N smul P = Q, then an AddCommMonoid metavariable in the big show). NEXT ATTEMPT: avoid show — use `unfold HasseWeil.WeilPairing.pullbackDiv` (or simp only [HasseWeil.WeilPairing.pullbackDiv]) on both sides first, then push equivMapDomain through with Finsupp.equivMapDomain_eq_mapDomain + Finsupp.mapDomain_finset_sum, rewrite singles with step (a), and finish with Fintype.sum_equiv (galoisFiberEquiv W sigma N Q). Provide the Fintype instances via letI in BOTH spellings before unfolding.
THEN step f: pullbackDivisor (Finsupp.sum) transport + weilFunction_divisor_eq_pullbackDivisor_kappaDivisor => div(Phi_sigma g_T) = div(g_{sigma T}); THEN pairing equivariance; THEN M1c.
