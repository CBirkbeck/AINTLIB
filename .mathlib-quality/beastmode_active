FOCUS: DS4 M1c. LANDED: WeilPairing/GaloisFibre.lean —
  algHomEquivSpecOver_comp_algEquiv (algebra-side sigma-action = precomposition with Spec sigma),
  specMap_algEquiv_comp_specMap_algebraMap, GammaSpecIso_hom_appTop_specMap_comp,
  EllipticCurve.torsionAlgebraFibreEquiv (+_coe, +_comp_algEquiv),
  muNAlgebraFibreEquiv (+_val, +_comp_algEquiv)  — all axiom-clean.
REMAINING for M1c:
 (4b) compare the SCHEME point action P |-> Spec.map (ofHom sigma) >> P on
      E.Point (Spec.map (ofHom (algebraMap k R))) with mathlib Affine.Point.map sigma
      through chartAffinePointEquiv (WeilPairing/FibrePointDict.lean), using
      EllipticCurve/GlobalChartOverField.lean for the global chart over Spec k.  <-- the hard leaf
 (5)  assemble p := (torsionAlgebraFibreEquiv)^2 -> fibreWeilPairing -> (muNAlgebraFibreEquiv)^-1
      and call exists_pairingAlgebraHom_of_galoisEquivariant (EtaleDescent.lean:440).
TECHNIQUE NOTE (cost me several iterations): `rw` will NOT match a lemma whose statement
mentions Gamma(X,top) against a goal whose term has the defeq-but-not-syntactic
(muNAlgebra k N hk).obj; state the instance as a `have` with EXPLICIT type ascriptions and
then close by `Eq.trans (congrArg F h) ?_` instead of `rw`.
Also: put the Algebra k Gamma(...) structure in a SECTION-LEVEL `instance` so the def and its
lemmas share one instance term.
