FOCUS (2026-08-09, night): THREE results today, and ONE HARD STOP.

CLOSED:
  1. **AP-D7** — `torsionSplittingEval_add`. The Katz–Mazur construction of the relative Weil
     pairing is complete except for AP-D4 ⊇. (c6281f9d5)
  2. **STACKS 00ME** — `ForMathlib/LocalFlatnessCriterion.lean` is ENTIRELY SORRY-FREE, every
     declaration axiom-verified standard-three. (12b5355a5, a5af3b7da)

*** B2 — TWO STATEMENTS ON THE CRITICAL PATH ARE FALSE AS STATED. USER DECISION NEEDED. ***
`b2_log.jsonl`: `AP2-B2-evalgen`, `AP2-B2-blocker4`. Warnings are in both docstrings; handover §6.5.
  * `evalGenerator_mem_nonZeroDivisors` — `EllipticCurve/AbelEquivalence.lean:836` (sorry :848)
  * `relEffCartierDiv_of_degreeOne_package` — `:960` (sorry :971) — **this is BLOCKER 4 itself**
`HasDegreeOneFibreCohomology` (`AbelSkeleton.lean:53`) is PURELY COHOMOLOGICAL — positive-degree
Čech exactness + `finrank (ker d⁰¹) = 1` over every field over the base ring. It constrains the
geometry of E not at all, so the fibres may be disconnected and reducible.
ONE COUNTEREXAMPLE KILLS BOTH: `R = k`, `E = ℙ¹_k ⊔ Spec k`, `M = 𝒪(-1)` on the ℙ¹ and `𝒪` on the
point. Then `h⁰(M) = 1`, `H¹(M) = 0`, so `hpkg` holds; take σ the nonzero section supported on the
point, so `hσ` holds, and σ vanishes IDENTICALLY on the ℙ¹.
  – evalGenerator: on any nonempty affine `V ⊆ ℙ¹`, the evaluation ideal is `span {0}`, so `hspan`
    holds with `f = 0`, and `0` is never a nonzerodivisor.
  – Blocker 4: the vanishing subscheme is the whole ℙ¹, not finite over `Spec k`; `D.finite` fails,
    and `IsIso (… .subschemeι ≫ π)` is false for the same reason.
FIX (statement change — owner's call, `theorem_statement_protected`): add smooth geometrically
integral fibres + `deg M_s = 1`. Statements left byte-identical. `:981` and `:999` NOT audited —
suspect for the same defect.

=== DO NOT ATTACK BLOCKER 4 UNTIL THAT IS SETTLED. ===
The Stacks 00ME work is NOT wasted — the fibrewise route genuinely needs it — but it cannot be
wired up against a false statement. Nothing in the tree consumes the criterion yet.

=== WORK THAT IS UNBLOCKED RIGHT NOW ===
`exists_torsionPoint_of_mem_kerMulByN` — `WeilPairing/KMPairing.lean:302` — AP-D4 ⊇ = Abel
(rigidity + surjectivity) + a fibrewise degree function on `Pic` the tree lacks. NOTE it routes
through the same AbelEquivalence declarations, so it is downstream of the B2 too.
Group E — the DS4 register (AP-E1, AP-E2…E6) — is independent.

=== PATTERNS THAT PAID OFF TODAY ===
* When a proof needs an instance package only to supply one `Prop`, EXPOSE THE `Prop`
  (`injective_of_lTensor_residueField_injective_of_separated`).
* An injective map of vector spaces is SPLIT (`LinearMap.exists_leftInverse_of_injective`);
  `lTensor Q` of a split injection is injective by functoriality.
* `Scheme.Hom.appLE` instead of `Scheme.Hom.app` puts an open-comparison in a `Prop` argument, so
  `subst` + proof irrelevance replaces dependent rewrites (`WeilPairing/Translation.lean`).
* SPEND TEN MINUTES TRYING TO BREAK A STATEMENT BEFORE SPENDING A SESSION PROVING IT.

GOTCHA: inside `namespace Module.Flat`, bare `lTensor_exact` resolves to `Module.Flat.lTensor_exact`
with a DIFFERENT signature; `_root_.lTensor_exact` is load-bearing.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone; NEVER `git add -A` while a subagent is mid-run; full
`lake build ModularCurves` before adding any import.
