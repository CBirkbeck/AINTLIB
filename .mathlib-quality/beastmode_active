FOCUS: Gap A. BOTH LEAVES NOW REDUCED to their descent-compatible form, and everything
else in SelfAdjointN.lean is PROVED:
  exists_pic_map_snd_kappa_add                    <-- LEAF (i), the only sorry left for kappa_add
  exists_pic_map_snd_picMap_mulByHom_kappa        <-- LEAF (ii)
Derived (no longer sorried): kappa_add, picMap_mulByHom_kappa_pow, picMap_mulByHom_kappa_eq_one.
Mechanism: eq_of_mul_inv_eq_picMap_snd — Ker(0^*) cap Im(f^*) = 1 (GME p.109 splitting), so
"the discrepancy comes from the base" upgrades to an equality. That is EXACTLY what
nonempty_unitObj_iso_of_normalized_glue (Picard/RigidDescent.lean) produces.
NEXT AND ONLY REMAINING MATH: (LOCAL-SQUARE). On a chart of the base with a Weierstrass
model, exhibit the generating section l/v of the difference bundle, normalized on the zero
section; feed to nonempty_unitObj_iso_of_normalized_glue; convert with
picClass_mul_eq_of_nonempty_tensor_iso / picClass_eq_of_nonempty_iso.
NOTE: mulByN E t N was introduced because Pic-valued products need
(E.baseChange t).E and pullback E.pi t SYNTACTICALLY equal, not just defeq.
