FOCUS (2026-08-09): **BLOCKER 3 STEPS 1-3 ARE PROVED. `hhigh` AND STEP 5 REMAIN.**

`WeilPairing/TheoremOfSquareUniversal.lean` —
`exists_invertible_tensor_idealModule_add_projModel` IS `SelfAdjointN.lean`'s
`exists_invertible_tensor_idealModule_add` for the tautological family, with `Rs` pinned to
`mulModelHom` so `P+Q` is the real group sum. `hfib` is FULLY DISCHARGED. Only `hhigh` is
hypothesised. Axiom-clean, zero sorries. Also `exists_invertible_tensor_idealModule_add_of_hfib`
(general form) and the universal-base instances `isNoetherian_pairBase`/`isNoetherian_pairCurve`/
`isSeparated_pairCurve` (the boarded spelling trap, now bridged).

*** STRUCTURAL FIX LANDED (66b573f7f) — READ THIS ***
`ModularCurves.sectionVanishingIdeal` was declared TWICE (an `IdealSheafData` in
`EllipticCurve/AbelEquivalence.lean`, an `Ideal R` in `LevelStructure/Incidence.lean`). That clash
is WHY THE WHOLE `ForMathlib/Seesaw*` LINE WAS AN ORPHAN — and its consumer `Picard/SelfAdjointN.lean`
IS in the root, so it would have blocked the entire workstream from landing. Renamed the sheaf one to
`sectionVanishingIdealSheaf`. `lake build ModularCurves` is now 9760 jobs (was 9745) and the Seesaw
line is genuinely built. The old board note "some ForMathlib modules are orphans, build them
explicitly" was a SYMPTOM of this, not a quirk.
**Run a full `lake build ModularCurves` as a name-clash detector before adding any import** — a
conclusion-grep has twice missed what the clash caught.

=== CURRENT TARGET: STEP 4 — discharge `hhigh` (in flight) ===
`hhigh` = exactness of the ordered base-Čech complex at positions ≥ 2, i.e. `H^{≥2}(X_s,M_s) = 0`,
automatic because the fibres are CURVES.

*** THE BOARDED ROUTE DOES NOT WORK — CORRECTED, WITH THE FIX ALREADY VERIFIED ***
Imitating `kernel_data_of_hasDegreeOneFibreCohomology` FAILS:
`HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
(`ForMathlib/CochainComplexBoundedFlat.lean:275`) demands `hfield : ∀ n, n < N → …` — EVERY degree
below `N`, INCLUDING `n = 0`, which for this discrepancy asserts `H¹(E_s,𝒪) = 0`, FALSE on a genus-1
fibre. (That is exactly why `Seesaw.lean` leaves `hhigh` open and `HasDegreeOneFibreCohomology` is
unavailable here.)
THE FIX, verified verbatim in a scratchpad probe: `boundedFlatCokerAndExactOfFiniteHomology`
(`ForMathlib/BaseChangeKerCoker.lean:900`) is a DECREASING induction consuming `hfield k`/`hfinite k`
only for `n ≤ k < N`. Thread a LOWER BOUND `n₀ ≤ n` through it and it goes through unchanged.
Generalise three lemmas to a lower-bounded `hfield` (ADD generalised versions, keep the existing ones
as `n₀ = 0` specialisations so no consumer moves):
  1. `boundedFlatCokerAndExactOfFiniteHomology` (`BaseChangeKerCoker.lean:900`)
  2. `LinearMap.exact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
  3. `HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
At `n₀ = 1` the fibre input is then exactly `subsingleton_H_add_two_of_two_affine_open_cover`.
SECOND OBSTACLE, route around it: `HasHighCechExactnessOpens` quantifies over ALL opens of the base
including NON-AFFINE ones, but the Čech surrogate needs an affine base. **Target the affine-base
`HasHighCechExactness` only** — that is what `exists_invertible_tensor_idealModule_add_projModel` uses.

=== THEN: STEP 5 — base-change down ===
To an arbitrary, possibly NON-REDUCED base, stated and glued ZARISKI-LOCALLY on `T` (moduli-stack
issue globally). `pairClassify` (`WeilPairing/TautologicalPair.lean:79`) already proves "every pair of
sections is a base change of the universal pair", which is most of it.
`RelEffCartierDiv.sectionDivisor_baseChange` (`EllipticCurve/PoleSheaf.lean`) is part of the bridge.
That closes `Picard/SelfAdjointN.lean:267` and unblocks AP-D4/D5-existence/D6/D7.

=== STANDARDS THAT HAVE PAID OFF — KEEP THEM ===
* **Verify fit by ELABORATION, not assertion**: every transport step so far shipped with a scratchpad
  probe that actually applies the result to its consumer and compiles.
* **Full `lake build ModularCurves` as the duplicate/clash detector** before writing.
* Gotcha: passing `UniversallyOConnected` positionally makes Lean re-unify `π`, breaking `haveI`s —
  bind it with an explicit type ascription first.

DEAD ENDS — LOGGED, DO NOT REVISIT:
  * `ProjIsPrincipal` / `kappaDivisor_add_linEquiv` is UNSOUND over non-closed fields
    (`b2_log.jsonl`, T10-asm) — `projectiveDivisorOf` sees only `k`-rational points. It is no longer
    needed at all: the field theorem is now proved by the explicit chord-and-vertical route.
  * `WeilPairing/LineVertical.lean` is NOT about the chord and the vertical despite its header.
  * "cover, generators and ratio must be produced together" was a WRONG DIAGNOSIS.
  * Recovering the chord/vertical shape from the ideal identity alone is BLOCKED — it needs the
    coordinate ring to be Dedekind. Split the call site instead.

REUSABLE BY-PRODUCTS FROM THIS SESSION:
  * `OverlapRel W b a j` (`b = a·tʲ` on the `Y ⊓ Z` overlap) with `.mul/.sub/.add/.pow/.cancel` and
    base cases — the overlap dictionary this line has wanted for a long time. Plus `germY`/`germZ`
    with injectivity, `infChartOpen`, `projModelZero_ker_ideal_infChartOpen`.
  * `ker_ideal_of_fromSpec_factor` — replaces the whole `appLE` apparatus whose helpers are
    `private` in `PoleFiltration.lean`.
  * mathlib's `XYIdeal_mul_XYIdeal` IS the affine-chart theorem of the square.
  * `isIso_idealGenHom`, now named (copy-pasted in three other places — cleanup ticket).

BLOCKER 4 (independent, does NOT gate AP-D): `IsIso ((sectionVanishingIdeal M hM σ).subschemeι ≫ π)`
at `EllipticCurve/AbelEquivalence.lean`. Route REPLACED after review: étale transversality
(`dh̄ ≠ 0` on the smooth fibre ⟹ `h : U → 𝔸¹_S` étale ⟹ `Z` finite étale of degree one ⟹ iso;
Stacks §37.38 / 055S — prove the special case from a standard-smooth Jacobian presentation, since the
general slicing lemma routes through fibrewise flatness). The old nonzerodivisor route IS Stacks
00MF and does not dodge Artin-Rees; producing the section from fibre points is not viable.
Hypothesis audit: `h⁰ = 1` and `H¹ = 0` alone are NOT enough — also needs smooth geometrically
integral genus-one fibres, `deg M_s = 1`, and the base-change iso giving `σ_s ≠ 0`.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone. **NEVER `git add -A` while a subagent is mid-run** (it swept two
scratch files in this session). Some `ForMathlib/` modules are ORPHANS that `lake build ModularCurves`
skips — build them explicitly.
