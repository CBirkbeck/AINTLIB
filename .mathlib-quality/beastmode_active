FOCUS: Gap A. RIGIDIFIED DESCENT COMPLETE + AXIOM-CLEAN:
  Picard/GlueTrivialization.lean : nonempty_unitObj_iso_of_glue (glue sections, sheaf axiom)
  Picard/RigidDescent.lean       : nonempty_unitObj_iso_of_normalized_glue
    -- L with generating sections over f^-1(U i) for a cover U of the BASE, whose overlap
       comparison units restrict to 1 on the zero section, is TRIVIAL.
The workhorse is done. Both remaining leaves are now LOCAL ON THE BASE.
NEXT: leaf (i) kappa_add. Plan:
  A. Identify the difference module D = O(D_Q) (x) O(D_Q') (x) O(D_{Q+Q'})^-1 (x) O(D_0)^-1
     as an object of (pullback E.pi t).Modules built from PoleSheaf idealModule.
  B. Locally on T (Weierstrass chart): the line through Q,Q' and the vertical through Q+Q'
     give an explicit generating section of D (Silverman III.3.5 / HasseWeil
     Pic0.TheoremOfSquareDivisorForm.kappaDivisor_add_linEquiv).
  C. Normalize each local section along the zero section, apply
     nonempty_unitObj_iso_of_normalized_glue, then push to Pic / picRelProj.
START WITH A — survey what O(D) as a Modules object looks like: PoleSheaf.idealModule +
Picard/IdealModule.lean, and how picClass turns it into a Pic element (DivisorClass.lean).
