FOCUS: Gap A. Pic-level bridge DONE + axiom-clean:
  Modules.IsInvertible.unit_eq_one / unit_eq_unit_of_iso   (GlueTrivialization.lean)
  RelEffCartierDiv.picClass_eq_of_nonempty_iso              (DivisorClass.lean)
CHAIN NOW COMPLETE FROM MODULE-ISO TO kappa:
  cover-local generating sections normalized on 0-section
    -> nonempty_unitObj_iso_of_normalized_glue (RigidDescent)
    -> Nonempty (idealModule D =~ idealModule D')
    -> picClass_eq_of_nonempty_iso
    -> equal picClass -> equal kappa.
NEXT STEP: build the tensor-level version needed by kappa_add, i.e.
  picClass D_Q * picClass D_Q' = picClass D_{Q+Q'} * picClass D_0  (mod ker picRelProj),
which needs Skeleton.toSkeleton_tensorObj (already used in PicComparison
.IsInvertible.isUnit_toSkeleton) to turn a product of classes into the class of a tensor.
Then the ONLY remaining input is (LOCAL-SQUARE): the relative Miller/line function.
NOTE: HasseWeil field proof (Foundation/Curves/Divisor/MillerAllChar.lean, 726 lines) is
valuation-theoretic and does NOT transport to a base ring — the relative statement must be
the IDEAL identity I(D_P).I(D_Q).I(D_{-(P+Q)}) = (line) and I(D_{P+Q}).I(D_{-(P+Q)}) = (vert)
on a chart where the sections are in general position.
