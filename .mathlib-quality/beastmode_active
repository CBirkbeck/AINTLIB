FOCUS: Gap A. LEAF (i) is now at the DIVISOR-CLASS level (no kappa), which is the right
interface for the Weierstrass work:
  exists_pic_map_snd_sectionCls_add        SelfAdjointN.lean:~182
  exists_pic_map_snd_picMap_mulByHom_kappa SelfAdjointN.lean:~275
Those are the ONLY two sorries in the file; kappa_add / picMap_mulByHom_kappa_pow /
picMap_mulByHom_kappa_eq_one (star-prime) are all PROVED from them.
NEW helpers: sectionCls, zeroCls, kappa_eq_picRelProj (rfl), picRelProj_eq_of_mul_inv_eq_map_snd.
GOTCHA RECORDED: never AC-normalise (simp [mul_comm,...] / ac_rfl / group) on unfolded
sectionCls terms — it times out. Pre-arrange the STATEMENT so the derivation is `exact hM`.
NEXT AND ONLY REMAINING MATH: (LOCAL-SQUARE). Produce, locally on the base (Weierstrass
chart), the generating section l/v of the difference bundle, normalised on the zero section;
feed nonempty_unitObj_iso_of_normalized_glue (Picard/RigidDescent.lean); convert with
picClass_mul_eq_of_nonempty_tensor_iso / picClass_eq_of_nonempty_iso (Picard/DivisorClass.lean).
Survey next: EllipticCurve/WeierstrassModel.lean + LocallyWeierstrass for what a base chart
supplies (coordinates x,y and the section coordinates).
