FOCUS: DS4 M1b LEAF brick 4b — transport of translateX_xy/translateY_xy under Phi_sigma.
LANDED (sorry-free, pushed): bricks 1,2,3 and 4a (galoisFunctionFieldEquiv_translateSlope).
ATTEMPT NOTE (reverted; file green): `rw [translateX_xy_eq_addX]; unfold Affine.addX; simp only [...]; congr 1` hits a whnf TIMEOUT (200k) because W_KE unfolds into the base-changed record. Heartbeat bumps are FORBIDDEN.
FIX TO TRY: prove a tiny standalone helper first, e.g.
  private theorem map_addX (u v w : KE) : Phi (addX u v w) = addX (Phi u) (Phi v) (Phi w)
  by `simp only [WeierstrassCurve.Affine.addX, map_sub, map_add, map_mul, map_pow, galoisFunctionFieldEquiv_a1, galoisFunctionFieldEquiv_a2]` (NO unfold of W_KE, NO congr), and similarly map_addY with a3/a4 as needed; then brick 4b is a two-line rw using 4a and these helpers.
THEN: the conjugation via ringHom_ext_const_x_y_gen (2-torsion branch separately), then weilPairing_galois unconditional, fieldWeilPairing_galois, M1c.
