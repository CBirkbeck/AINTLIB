FOCUS: T802 step 3 — 1-var polynomial density polyKW_denseRange (mirror polyToP_denseRange; finiteness of {n | eps <= coeff-norm} from IsRestricted-atTop eventually) then evalRescale_eq: chartEval (rescaleRestricted t _ f) = ρ (constNN-lift of f)-form via equalizer (chartEval_continuous ✓, RHS-cont via ρ∘(KW→A const-embed)-cont, agree on C r + X); then step 4 rev∘theta = ρ; steps 5-7.
