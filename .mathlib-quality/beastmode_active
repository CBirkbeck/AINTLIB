FOCUS: D-iii-4e — isLocalHom_stalkMap for yFrobHom, then the VPreHom bundle. HAVE: yFrob_val_compat + ringStalkMap_yFrob_conj + all germ naturalities (FrobeniusValuation.lean, all axiom-clean, pushed). PLAN for isLocalHom: ROUTE A (iso): show ringStalkMap (yFrobHom k) x is bijective — from the conjugation square: rSM-yFrob = iso-x.hom-inv COMP rSM-ambient COMP iso-Fx.hom (all in CommRingCat) so bijective iff rSM-ambient bijective; ambient: rSM (ambientFrobHom k) at any w — bijective because ambientFrobNat's components are ring ISOS (limitFrobHom has the two-sided inverse limitFrobHom (-k) modulo frobOpens-roundtrip-cast — OR simpler: stalk-level two-sided inverse via ringStalkMap_comp + the k/-k composite = id at PresheafedSpace-Hom level — needs ambientFrobHom_comp : ambientFrobHom k COMP ambientFrobHom (-k) = id (PresheafedSpace.Hom-ext: base spaFrob_spaFrob + c-components roundtrip) — HEAVY). ROUTE B (direct local-hom): IsLocalHom f := isUnit (f a) -> isUnit a: on stalks a unit iff stalkValue-nonzero-ish? we have not_stalkValue_vle_zero_of_isUnit + the 8.14 locality (maximalIdeal_stalk_Y): a in stalk is a unit iff its stalkValue-vle-zero fails (isUnit_iff via maximalIdeal = supp!): from val_compat: stalkValue(image-pt)(a) nonzero iff stalkValue(x)(rSM a) nonzero -> UNITS CORRESPOND exactly -> isLocalHom DIRECTLY from yFrob_val_compat + isLocalRing supp-characterization (val_supp field of yVPreObj!). ROUTE B is 20 lines: IsLocalHom.mk fun a ha => by unit-iff-not-in-maximalIdeal (IsLocalRing.mem_maximalIdeal/not_not) + val_supp (supp = maximalIdeal) + supp-membership via comap (val_compat rewires) — WRITE B. Then: yFrobVPreHom k : VPreHom (yVPreObj) (yVPreObj) := {toHom := yFrobHom k, isLocalHom_stalkMap := ..., val_compat := yFrob_val_compat}. THEN the iso/X. Scratch r68.
