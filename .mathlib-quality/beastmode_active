FOCUS: DS4 M1b-3b-x — extract the sigma-naturality constant, then assemble the pairing equivariance.
GREEN + PUSHED (sorry-free, axiom-clean): the whole transport chain, projectiveDivisorOf_galois_weilFunction, weilPairing_galois_core_of_algEquiv, galoisFunctionFieldEquiv_algebraMap, projectiveDivisorOf_div.
OBSTACLE HIT TWICE (both attempts reverted, file green): inside a `have` about ((<W baseChange L>.toAffine) : SmoothPlaneCurve L).projectiveDivisorOf, Lean unfolds the curve to a record literal and then fails to find WeierstrassCurve.IsElliptic / IsIntegrallyClosed for it (a folded `inferInstanceAs` haveI does not match).
FIX TO TRY NEXT: introduce `set C : HasseWeil.Curves.SmoothPlaneCurve L := ⟨(W.baseChange L).toAffine⟩ with hC` at the START of the proof (before any `have`), and add `haveI : C.toAffine.IsElliptic := ...; haveI : IsIntegrallyClosed C.CoordinateRing := ...` right after, so every later statement mentions the LOCAL C (no unfolding). Then: hratio := projectiveDivisorOf_div ... ; obtain c from const_unit_of_projectiveDivisorOf_eq_zero; finish with (div_eq_iff _).mp.
THEN: weilPairing_galois_of_conj (conditional on the conjugation), then the conjugation leaf itself (algHom_ext_x_y_gen + translateAlgEquivOfPoint_*_x_gen/_y_gen), then M1c.
