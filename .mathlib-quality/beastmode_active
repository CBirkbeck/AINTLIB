FOCUS: D-iv-3(ii-c beta tail) — from the glued candidate to the INVARIANT extension. HAVE (all pushed): exists_translateFam_glue (g with limitRestrict (yFunctor_translate_le W k) g = translateFam W s k for ALL k) + limitFrobHom_zero + map_yFrobTop_zero + the additivity cluster. REMAINING beta: (A) the s-collapse at k = 0: translateFam W s 0 = limitRestrict (le-composite) s — from limitFrobHom_zero + limitRestrict_comp (the composed le : yFunctor(translate-0) <= yFunctor W through map_yFrobTop_zero — proof-irrelevant composite) — gives limitRestrict-of-g-at-the-0-piece = restrict-of-s i.e. g extends s (modulo the translate-0-vs-W opens-eq — expose as: limitRestrict (le-of-eq-composed-with-hle0) g = limitRestrict (le_of_eq ..) s-shaped or cleaner: state gRestrictW : limitRestrict (W <= saturation як composite through translate-0-eq) g = s?? the eq (Opens.map (yFrobTop 0)).obj W = W lets us build hle0' : yFunctor W <= yFunctor(saturation) and claim limitRestrict hle0' g = s: from hg 0 + the collapses via limitRestrict_comp-chains meeting at proof-irrelevance (the frobFixed_restrict-pattern). (B) the INVARIANCE: g in frobFixed (xImage W): mem_frobFixed: limitFrobHom 1 (yFunctor-sat) g = limitRestrict (le_of_eq stability) g: prove by the GLUE-SEPARATION (isLimitSheafOn_Y.injective at the SAME translate cover!): both sides restrict to equal values on every piece: piece-k-restriction of LHS: limitRestrict (piece-le) (limitFrobHom 1 sat g) = [limitFrobHom_limitRestrict-naturality + the piece-shifts] limitFrobHom-1-of-(restriction to the shifted piece) = limitFrobHom 1 of (translateFam (k-shifted?)) — the SHIFT-coherence: limitFrobHom 1 (translateFam k) ~ translateFam (k+1)-modulo-composites: needs limitFrobHom-ADDITIVITY at (1, k) — the pv-COMPOSITE LAW AGAIN?! — HMM. ALTERNATIVE invariance-route avoiding composites: the DIRECT computation: both limitFrobHom-1-g and cast-g restrict on piece-(k+1)-vs-piece-k pairs... consider instead proving invariance via the UNIQUENESS/INJECTIVE of the glue against the SHIFTED family: h1 := limitFrobHom 1 sat g lives over frobOpens 1 sat = sat (stability); its piece-k-restriction = [naturality] limitFrobHom 1 (piece-(k)-of-g-at-the-frobOpens-1-preimage-piece...) — the preimage of piece-k under spaFrob-1 is piece-(k+1)?? (Opens.map (yFrobTop 1)).obj(translate-k) = translate-(k+1) (additivity!) — so restrict-of-h1-to-piece-(k+1)... = limitFrobHom-1-applied-to (restrict-g-to-piece-k under the eq) = limitFrobHom 1 (translateFam k) — and the TARGET family at piece-(k+1) is translateFam (k+1) = restrict(limitFrobHom (k+1) s): the coherence limitFrobHom 1 после limitFrobHom k = limitFrobHom (k+1)-modulo-eq-casts IS the pv-composite at (frobPow -1, frobPow -k) — UNAVOIDABLE for the invariance. => (C) the value-level composite law pvHuber-comp IS needed after all: prove it NOW by the dense-image engine (mirror refl_apply's proof — the same structure: presheafValueMapOfHom-comp on coe-images via IsLocalization.ringHom_ext of the composed locMaps): pvFwdHuber e2 (mapHuber-e1-D) COMP pvFwdHuber e1 D = restrictionMap-cast COMP pvFwdHuber (e1.trans e2) D — statement with les-as-args like refl_apply. THEN limitFrobHom-add componentwise, THEN invariance. ORDER (scratch r81): (C) pv-comp; then (A); then limitFrobHom_add; then (B). This completes beta; then gamma-stalks.
