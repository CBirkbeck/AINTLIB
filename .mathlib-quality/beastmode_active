FOCUS: T802 final — kwToTate via mathlib MvPowerSeries.renameEquiv (e : Unit ≃ Fin 1 manual ⟨fun _ => 0, fun _ => (), rfl-proofs⟩); decay-transfer: PowerSeries.IsRestricted (atTop) → subring-IsRestricted (cofinite) via Nat.cofinite_eq_atTop + index-equiv comap; then ev := (evalHomBounded (rho∘constA-composite... base g := (chartDatum).canonicalMap.comp (constA F) cont via canonicalMap_continuous.comp const-cont [norm_constA]) gen (coe divByS Wa tA) pb).comp kwToTate; rev x := ev x.fst + ev x.snd * rho(Qa); ring-fields via canonicalMap_Qa_sq; roundtrips; assembly; pinning; T804 sweep + banner + digest.
