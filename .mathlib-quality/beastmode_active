FOCUS: Gap A. UPSTREAM BLOCKER CLEARED — sectionToPicRel / picClass / kappa_zero are now
AXIOM-CLEAN (propext/choice/Quot.sound only). Two fixes:
 (1) officialAux_exists_finite_chart PROVED (fibrewise finiteness + basic-open chart).
 (2) picClass now TAKES the IsOfficialCartier proof instead of deriving it from hsm, and
     sectionToPicRel feeds it the axiom-clean sectionDivisor_isOfficial. The general
     isOfficial (finite-flat => Cartier) still carries sliceAux_exists_noetherianStage,
     but it is NO LONGER on the Gap A critical path.
REMAINING Gap A leaves, both in Picard/SelfAdjointN.lean:
 (i) kappa_add                  -- relative Abel / divisor-additivity
 (ii) picMap_mulByHom_kappa_pow -- relative theorem of the square
WORKING (i) NEXT. Field template: HasseWeil.Pic0.TheoremOfSquareDivisorForm
.kappaDivisor_add_linEquiv (any characteristic). Look FIRST for existing relative machinery:
grep for translation/mulByHom pullback of sectionDivisor, and for PoleSheaf.lean which already
uses sectionDivisor_isOfficial and may carry O(D) comparison lemmas.
