FOCUS: DS4 M1b-3b-x — the sigma-naturality constant (last blocker before the pairing equivariance).
FILE IS GREEN and everything below is pushed + axiom-clean: full transport chain, projectiveDivisorOf_galois_weilFunction, weilPairing_galois_core_of_algEquiv, galoisFunctionFieldEquiv_algebraMap, projectiveDivisorOf_div.
DIAGNOSIS (3 attempts, all reverted): applying projectiveDivisorOf_div (or const_unit_of_projectiveDivisorOf_eq_zero) at the curve <(W.baseChange L).toAffine> makes Lean unfold the curve to a RECORD LITERAL, after which instance search for WeierstrassCurve.IsElliptic / IsIntegrallyClosed fails. A `private abbrev curveL` does not help (abbrev is reducible, so it unfolds too).
NEXT FIX: use a non-reducible `private def curveL : SmoothPlaneCurve L := <(W.baseChange L).toAffine>` PLUS `private instance : curveL.toAffine.IsElliptic` and `private instance : IsIntegrallyClosed curveL.CoordinateRing`, state the helper lemmas at curveL, and bridge to the record form once with a `show`/`rfl` at the very end. Alternative: restate projectiveDivisorOf_div and the const-extraction taking the two instances as EXPLICIT hypotheses (not instance-implicit), so no search is needed.
THEN: weilPairing_galois_of_conj; then the conjugation leaf (algHom_ext_x_y_gen + translateAlgEquivOfPoint_*_x_gen/_y_gen); then M1c (EtaleDescent:440).
