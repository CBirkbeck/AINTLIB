FOCUS: [AXIOM-CLEAN MANDATE] 3-leaf worklist under yRho_representable. ★★★ L1 = 2 SORRIES ONLY (HEAD b2a063936): divH_affine_arm ZERO-arm ([N]P = 0 at an affine place) + hDIVW INFINITY-arm — BOTH = the [D]-rung. THE ENTIRE AFFINE ENGINE IS CLOSED: SEC-ORD; the L1-skeleton; DIV-H-i; N-CONJ; the transfer; T-DEC/U-RATIO/MUL-IDENT/ORD-LEDGER; PT-0a/PT-0/PT-1/PT-2/PT-INJ/CAST-SMUL/CAST-INJ; U-ORD0; the z0-indicator-collapse; the T-dictionary (both T=0 and T=some cases with the indicator-link and omega finales). [D]-RUNG PROGRAMME (the remaining work; cont.30ar + this): the two arms need ordAtInfty of the engine germs (Y := pE(germ tUOC), rr-parts, H) — the DEGREE-relation route FAILS (the affine-sums need full divisors, SEC-ORD only covers over-chart places); the REAL route = the INFINITY-LOCAL FAMILY mirroring the zChart machinery on the Y-CHART: (i) infinity-scheme-point = projModelZero.base default (z-infinity); the yChart := Proj.basicOpen at X1 (chartYRingEquiv PoleFiltration:1571: Away-X1 ≃+* AdjoinRoot (infChartCubic W)); projModelZero_eq_fromSpec (PoleFiltration:2267) = the fromSpec-coordinates of the zero-point; (ii) [INF-SEC-ORD]: ordAtInfty(pE(germ-V f)) = indicator [infinity-pt = section-image] for a section-kernel generator — mirror ord_P_germ_sectionKer_generator with ordAtInfty's DVR (ordAtInftyValuation Infinity:1120) — the ValuationTransport-∞-analogue (yChartMaximalIdeal etc. — the RP-∞-programme, session-scale); (iii) [INF-U-ORD0]: units over infinity-containing opens have ordAtInfty 0; (iv) then the ZERO-arm = the ∞-ledger (same shape as the some-arm with ordAtInfty-values: Y-ord = indicator-diffs at ∞; zQm-indicator-at-∞ = [T = 0]-cases; z0-indicator-at-∞ = TRUE (the zero-image IS the ∞-point!) — the kappa-(-1)-term matches) and the INFINITY-arm = same at w = ∞ with div-H(∞) = (ordAtInfty H).untopD. ALTERNATIVE-CHEAPER-CHECK before building the family: HasseWeil may ALREADY have ordAtInfty-of-pullback/section-machinery (inftyOrdTransport_mulByInt PROVED — read its PROOF for the technique + reusable lemmas: DivisorPullback:283 + OrdAtInftyBridge.lean + Isogeny/Frobenius/OrdAtInfty.lean 2392-region — GREP FIRST). PLAYBOOK: all session-11 lessons (see cont.30al/30am/30ap/30aq/30ar + the T-arm patterns: hz0ind-normalization, hinvinj-helper, mod_cast-ledger-omega).
