FOCUS: D-iv-3(ii-c) — the disjoint translate cover + invariant extension (the deep core of the curve stalks). DESIGN (scratch r73, build bottom-up): (A) the translate opens: for W : Opens yTop, tW k := (Opens.map-iterated?? NO — use the EXISTING frobOpens/yFrobTop machinery: translate k of W := (Opens.map (yFrobTop p F phi k)).obj W (preimage under the k-th map = the (+k or -k)-translate); pairwise disjointness for W inside a window: from exists_nhd_smul_disjoint (Curve.lean 90: every y in Y has an open S ∋ y with (g-smul S) disjoint S for g ≠ 1 — at the ↥Y-subtype level!) — BRIDGE it to yTop-level opens (the homeo yTop ≅ ↥Y? NOT constructed — yTop is the Spa-side subtype, ↥Y the Spv-side; build the translate-disjointness DIRECTLY at yTop: the action on yTop is yFrobTop; wandering: for y : yTop exists W ∋ y open with (yFrobTop k)⁻¹ W ∩ W = empty for k ≠ 0 — derive from the Spv-level lemma through the val-preimages OR re-prove by the same windowU-argument at the Spa-trace (windows are Spa-trace-opens already: isOpen_windowU_Y-style + zsmul_windowU + windowU_disjoint — check what Curve.lean's proof used and mirror at yTop). (B) the saturated union: satW := curvePreimage (image-open pi(W)) — pi open (isOpenQuotientMap_toCurve-open through the yTopToCurve-alignment — need: yTopToCurve is an open quotient map too? it factors as toCurve ∘ yTopToY with yTopToY a HOMEO?? yTopToY : yTop -> ↥Y — a continuous bijection between the two subtype-presentations; openness: both carry the induced-from-Spv-vs-induced-from-Spa topologies... the Spa-subtype topology = Spv-subtype topology on the SAME underlying set (Spa has the subspace topology of Spv ✓ SpaTop := TopCat.of ↥(Spa …) with instTopologicalSpaceSubtype and yTop double-subtype = Y-subtype-of-Spv-composite ✓ homeo by inducing-composition) — PROVE yTopToY is a homeomorphism (IsInducing-composition + bijective) FIRST); then satW-set = union of translates of W-intersected-with-fibers... (C) the extension lemma statement: for y, a section s over a W-inside-window-with-disjoint-translates, exists sInv in frobFixed(pi(W)-open) restricting to s on the y-translate-piece. Start r73 with (A)-yTop-wandering + (B)-yTopToY-homeo. Kernel doctrine throughout.
