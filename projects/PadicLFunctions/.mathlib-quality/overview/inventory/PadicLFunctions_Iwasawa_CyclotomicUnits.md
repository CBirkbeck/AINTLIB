# Inventory: PadicLFunctions/Iwasawa/CyclotomicUnits.lean

File namespace: `PadicLFunctions.Coleman`. Context: `variable (p : ℕ) [hp : Fact p.Prime]`.
Subject: the global cyclotomic units `𝒟_n ⊂ F_n = ℚ(μ_{p^n})` inside `ℂ_[p]`, their local p-adic
closures `𝒞`, and the milestone that the Coleman-map inputs `c_n(a)` are global cyclotomic units
lying in the tower `𝒞_{∞,1}` (RJW arXiv:2309.15692 §11.3 / §9).

---

### def Fglobal
- Type: `noncomputable def Fglobal (n : ℕ) : IntermediateField ℚ ℂ_[p] := ℚ⟮zetaSys p n⟯`
- What: The global cyclotomic field `F_n = ℚ(μ_{p^n}) = ℚ(ξ_{p^n})` realised as the simple `ℚ`-subextension of `ℂ_[p]` generated by `zetaSys p n`.
- How: Direct definition as `IntermediateField.adjoin` of a single element (`ℚ⟮·⟯` notation).
- Hypotheses: `n : ℕ`; ambient `p` prime.
- Uses from project: [`zetaSys`]
- Used by: `FglobalPlus_le_Fglobal`, `Fglobal_le_K`, `globalUnits`, `cycloUnit_mem_Fglobal`, `zetaSysU_pow_mem_cycloUnits`
- Visibility: public
- Lines: 41–43 (no proof)
- Notes: none

### def FglobalPlus
- Type: `noncomputable def FglobalPlus (n : ℕ) : IntermediateField ℚ ℂ_[p] := ℚ⟮zetaSys p n + (zetaSys p n)⁻¹⟯`
- What: The maximal totally real subfield `F_n⁺ = ℚ(ξ + ξ⁻¹)` of `F_n`, presented by its standard real generator.
- How: Direct definition as a simple adjoin of `ξ + ξ⁻¹`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`zetaSys`]
- Used by: `FglobalPlus_le_Fglobal`, `globalUnitsPlus`, `cycloUnitsPlus`
- Visibility: public
- Lines: 45–48 (no proof)
- Notes: none

### lemma FglobalPlus_le_Fglobal
- Type: `lemma FglobalPlus_le_Fglobal (n : ℕ) : FglobalPlus p n ≤ Fglobal p n`
- What: The real subfield is contained in the full cyclotomic field, `F_n⁺ ≤ F_n`.
- How: Rewrites to `adjoin_simple_le_iff`, then shows the generator `ξ + ξ⁻¹ ∈ F_n` via `add_mem` of `ξ` (a self-generator) and `ξ⁻¹` (using `Fglobal.inv_mem`).
- Hypotheses: `n : ℕ`.
- Uses from project: [`FglobalPlus`, `Fglobal`]
- Used by: unused in file
- Visibility: public
- Lines: 50–53 (proof ~3 lines)
- Notes: none

### theorem norm_le_one_of_isIntegral_int
- Type: `theorem norm_le_one_of_isIntegral_int {x : ℂ_[p]} (hx : IsIntegral ℤ x) : ‖x‖ ≤ 1`
- What: Any element of `ℂ_[p]` integral over `ℤ` has p-adic norm `≤ 1` (sits in the closed unit ball).
- How: By contradiction assuming `‖x‖ > 1`. Writes the monic integral relation as `x^N = −Σ_{i<N} aᵢ xⁱ`; bounds the sum ultrametrically by a single term via `IsUltrametricDist.exists_norm_finsetSum_le_of_nonempty`, each term `≤ ‖x‖^(N−1)` since integer coefficients have norm `≤ 1` (`IsUltrametricDist.norm_intCast_le_one`); concludes `‖x‖^N ≤ ‖x‖^(N−1)` contradicting `pow_lt_pow_right₀`. Handles `N=0` (relation `1=0`) separately.
- Hypotheses: `x ∈ ℂ_[p]` integral over `ℤ`.
- Uses from project: []
- Used by: `globalUnits_le_localUnits`
- Visibility: public
- Lines: 55–102 (proof ~44 lines)
- Notes: long(30-50)

### def globalUnits
- Type: `noncomputable def globalUnits (n : ℕ) : Subgroup ℂ_[p]ˣ` (carrier `{u | ↑u ∈ Fglobal p n ∧ IsIntegral ℤ ↑u ∧ IsIntegral ℤ ↑u⁻¹}`)
- What: The global units `𝒱_n = 𝒪_{F_n}^×`: units of `ℂ_[p]` lying in `F_n`, integral over `ℤ` together with their inverse.
- How: Subgroup built by hand; `mul_mem'`/`one_mem'`/`inv_mem'` discharged by `mul_mem`/membership of `1`/field inverse closure on the three predicates, using `IsIntegral.mul`, `isIntegral_one`, and `Fglobal.inv_mem`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`Fglobal`]
- Used by: `globalUnitsPlus`, `globalUnits_le_localUnits`, `cycloUnits`, `cycloUnits_le_globalUnits` (via `cycloUnits`), `cyclo_elems_mem_globalUnits`
- Visibility: public
- Lines: 104–122 (proof ~14 lines across fields)
- Notes: none

### def globalUnitsPlus
- Type: `noncomputable def globalUnitsPlus (n : ℕ) : Subgroup ℂ_[p]ˣ` (carrier `{u | u ∈ globalUnits p n ∧ ↑u ∈ FglobalPlus p n}`)
- What: The real global units `𝒱_n⁺ = 𝒪_{F_n⁺}^×`, i.e. global units lying in the real subfield.
- How: Hand-built subgroup; each closure obligation combines membership in `globalUnits` with membership in `FglobalPlus` via `mul_mem`/`one_mem`/`inv_mem` and `FglobalPlus.inv_mem`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`globalUnits`, `FglobalPlus`]
- Used by: unused in file
- Visibility: public
- Lines: 124–138 (proof ~12 lines)
- Notes: none

### lemma Fglobal_le_K
- Type: `lemma Fglobal_le_K {n : ℕ} {x : ℂ_[p]} (hx : x ∈ Fglobal p n) : x ∈ K p n`
- What: Every element of the global field `F_n` lies in the local field `K_n = ℚ_p(ξ_{p^n})` (same generator, and `K_n ⊇ ℚ_p ⊇ ℚ`).
- How: `IntermediateField.adjoin_induction` on membership in `ℚ⟮ξ⟯`: generator case via `zetaSys_mem_K`; `algebraMap` (rationals) case via `eq_ratCast` + `SubfieldClass.ratCast_mem`; add/mul/inv cases by the corresponding `K`-closure lemmas.
- Hypotheses: `x ∈ F_n`.
- Uses from project: [`Fglobal`, `zetaSys`, `K`, `zetaSys_mem_K`]
- Used by: `globalUnits_le_localUnits`
- Visibility: public
- Lines: 140–152 (proof ~9 lines)
- Notes: none

### theorem globalUnits_le_localUnits
- Type: `theorem globalUnits_le_localUnits (n : ℕ) : globalUnits p n ≤ localUnits p n`
- What: Global units are local units, `𝒱_n ≤ 𝒰_n` — a global unit and its inverse land in the local unit ball `O_n`.
- How: Unfolds `mem_localUnits_iff`; for both `u` and `u⁻¹` shows membership in `O = K ⊓ {‖·‖≤1}` via `Subring.mem_inf`, combining `Fglobal_le_K` and `norm_le_one_of_isIntegral_int` on the integrality hypotheses.
- Hypotheses: `n : ℕ`.
- Uses from project: [`globalUnits`, `localUnits`, `mem_localUnits_iff`, `O`, `Fglobal_le_K`, `norm_le_one_of_isIntegral_int`, `Fglobal`]
- Used by: `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 154–167 (proof ~10 lines)
- Notes: none

### def cycloGenSet
- Type: `def cycloGenSet (n : ℕ) : Set ℂ_[p]ˣ` (carrier `{u | ↑u = ξ ∨ ↑u = −ξ ∨ ∃ a, 1≤a ∧ a≤p^n−1 ∧ ↑u = ξ^a − 1}`)
- What: The generating set `{±ξ_{p^n}} ∪ {ξ^a − 1 : 1 ≤ a ≤ p^n−1}` of the cyclotomic-unit group, as a set of units specified through `Units.val`.
- How: Direct set-builder definition (disjunction over the three generator shapes).
- Hypotheses: `n : ℕ`.
- Uses from project: [`zetaSys`]
- Used by: `cycloUnits`, `cyclo_elems_mem_cycloUnits`, `zetaSysU_pow_mem_cycloUnits`
- Visibility: public
- Lines: 171–176 (no proof)
- Notes: none

### def cycloUnits
- Type: `noncomputable def cycloUnits (n : ℕ) : Subgroup ℂ_[p]ˣ := Subgroup.closure (cycloGenSet p n) ⊓ globalUnits p n`
- What: **RJW Definition** — the cyclotomic units `𝒟_n`: the subgroup generated by `±ξ` and `ξ^a−1`, intersected with the global units `𝒱_n`.
- How: Direct definition as `Subgroup.closure (cycloGenSet) ⊓ globalUnits`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloGenSet`, `globalUnits`]
- Used by: `cycloUnitsPlus`, `cycloUnits_le_globalUnits`, `cycloClosure`, `cyclo_elems_mem_cycloUnits`, `zetaSysU_pow_mem_cycloUnits`, `zpPow_zetaSys_mem_cycloClosureOne`, `zpPow_mem_cycloUnits_topologicalClosure`, `mem_cycloClosureOne_of_pow_mem`, `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 178–183 (no proof)
- Notes: none

### def cycloUnitsPlus
- Type: `noncomputable def cycloUnitsPlus (n : ℕ) : Subgroup ℂ_[p]ˣ` (carrier `{u | u ∈ cycloUnits p n ∧ ↑u ∈ FglobalPlus p n}`)
- What: The real cyclotomic units `𝒟_n⁺ = 𝒟_n ∩ F_n⁺`.
- How: Hand-built subgroup combining `cycloUnits` membership with `FglobalPlus` membership; closure obligations via `mul_mem`/`one_mem`/`inv_mem` and `FglobalPlus.inv_mem`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloUnits`, `FglobalPlus`]
- Used by: unused in file
- Visibility: public
- Lines: 185–199 (proof ~12 lines)
- Notes: none

### lemma cycloUnits_le_globalUnits
- Type: `lemma cycloUnits_le_globalUnits (n : ℕ) : cycloUnits p n ≤ globalUnits p n := inf_le_right`
- What: Cyclotomic units are global units, `𝒟_n ≤ 𝒱_n`.
- How: `inf_le_right` on the defining intersection.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloUnits`, `globalUnits`]
- Used by: `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 201–202 (term proof)
- Notes: none

### def cycloClosure
- Type: `noncomputable def cycloClosure (n : ℕ) : Subgroup ℂ_[p]ˣ := (cycloUnits p n).topologicalClosure ⊓ localUnits p n`
- What: **RJW Definition** — `𝒞_n`: the p-adic (topological) closure of `𝒟_n` intersected into the local units `𝒰_n`.
- How: Direct definition as `topologicalClosure ⊓ localUnits`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloUnits`, `localUnits`]
- Used by: `cycloClosurePlus`, `cycloClosureOne`, `cyclo_mem_cycloTower1`, `mem_cycloClosureOne_of_pow_mem`, `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: public
- Lines: 204–211 (no proof)
- Notes: none

### def cycloClosurePlus
- Type: `noncomputable def cycloClosurePlus (n : ℕ) : Subgroup ℂ_[p]ˣ := cycloClosure p n ⊓ localUnitsPlus p n`
- What: `𝒞_n⁺ = 𝒞_n ∩ 𝒰_n⁺`, the real part of the local closure.
- How: Direct intersection definition.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloClosure`, `localUnitsPlus`]
- Used by: `cycloClosureOnePlus`
- Visibility: public
- Lines: 213–215 (no proof)
- Notes: none

### def cycloClosureOne
- Type: `noncomputable def cycloClosureOne (n : ℕ) : Subgroup ℂ_[p]ˣ := cycloClosure p n ⊓ localUnitsOne p n`
- What: `𝒞_{n,1} = 𝒞_n ∩ 𝒰_{n,1}`, the principal-unit part of the local closure.
- How: Direct intersection definition.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloClosure`, `localUnitsOne`]
- Used by: `cycloClosureOnePlus`, `cycloTower1`, `zpPow_zetaSys_mem_cycloClosureOne`, `mem_cycloClosureOne_of_pow_mem`, `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 217–219 (no proof)
- Notes: none

### def cycloClosureOnePlus
- Type: `noncomputable def cycloClosureOnePlus (n : ℕ) : Subgroup ℂ_[p]ˣ := cycloClosurePlus p n ⊓ localUnitsOne p n`
- What: `𝒞⁺_{n,1} = 𝒞_n⁺ ∩ 𝒰_{n,1}`, the real principal-unit part.
- How: Direct intersection definition.
- Hypotheses: `n : ℕ`.
- Uses from project: [`cycloClosurePlus`, `localUnitsOne`]
- Used by: `cycloTower1Plus`
- Visibility: public
- Lines: 221–223 (no proof)
- Notes: none

### def cycloTower1
- Type: `noncomputable def cycloTower1 : Subgroup (NormCompatUnits p)` (carrier `{u | ∀ n, 1 ≤ n → u.elems n ∈ cycloClosureOne p n}`)
- What: The tower `𝒞_{∞,1} = lim←_{n≥1} 𝒞_{n,1}` as a subgroup of the norm-compatible units `𝒰_∞`.
- How: Hand-built subgroup; membership predicate "every level lies in `𝒞_{n,1}`", closures via pointwise `mul_mem`/`one_mem`/`inv_mem` of `cycloClosureOne`.
- Hypotheses: none beyond ambient `p`.
- Uses from project: [`NormCompatUnits`, `cycloClosureOne`]
- Used by: `cycloTower1_le_unitsTower1`, `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 225–230 (proof ~3 lines)
- Notes: none

### def cycloTower1Plus
- Type: `noncomputable def cycloTower1Plus : Subgroup (NormCompatUnits p)` (carrier `{u | ∀ n, 1 ≤ n → u.elems n ∈ cycloClosureOnePlus p n}`)
- What: The real tower `𝒞⁺_{∞,1} = lim←_{n≥1} 𝒞⁺_{n,1}`.
- How: Hand-built subgroup, identical shape to `cycloTower1` but using `cycloClosureOnePlus`.
- Hypotheses: none beyond ambient `p`.
- Uses from project: [`NormCompatUnits`, `cycloClosureOnePlus`]
- Used by: unused in file
- Visibility: public
- Lines: 232–237 (proof ~3 lines)
- Notes: none

### lemma cycloTower1_le_unitsTower1
- Type: `lemma cycloTower1_le_unitsTower1 : cycloTower1 p ≤ unitsTower1 p := fun _ hu n hn => (Subgroup.mem_inf.1 (hu n hn)).2`
- What: The cyclotomic tower sits inside the local-unit tower, `𝒞_{∞,1} ≤ 𝒰_{∞,1}`.
- How: Pointwise: at each level, the `cycloClosureOne` membership splits via `Subgroup.mem_inf` and the second component is `localUnitsOne` membership = `unitsTower1` condition.
- Hypotheses: none beyond ambient `p`.
- Uses from project: [`cycloTower1`, `unitsTower1`]
- Used by: `cyclo_mem_unitsTower1`
- Visibility: public
- Lines: 239–240 (term proof)
- Notes: none

### theorem zetaSys_isIntegral
- Type: `private theorem zetaSys_isIntegral (n : ℕ) : IsIntegral ℤ (zetaSys p n)`
- What: `ξ_{p^n}` is integral over `ℤ` (a root of unity, root of `X^{p^n} − 1`).
- How: Witnesses the monic polynomial `X^{p^n} − C 1`, monic by `monic_X_pow_sub_C`, with `ξ^{p^n} = 1` from `zetaSys_primitiveRoot ... .pow_eq_one`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`zetaSys`, `zetaSys_primitiveRoot`]
- Used by: `isIntegral_cycloUnit`, `isIntegral_inv_cycloUnit`, `zetaSysU_pow_mem_cycloUnits`
- Visibility: private
- Lines: 247–252 (proof ~3 lines)
- Notes: none

### theorem cycloUnit_eq_geomSum
- Type: `private theorem cycloUnit_eq_geomSum {a n : ℕ} (hn : 1 ≤ n) : cycloUnit p a n = ∑ i ∈ Finset.range a, zetaSys p n ^ i`
- What: For `n ≥ 1`, the cyclotomic unit `c_n(a) = (ξ^a−1)/(ξ−1)` equals the geometric sum `Σ_{i<a} ξⁱ`.
- How: Clears the denominator with `div_eq_iff hne` (using `ξ−1 ≠ 0` via `zetaSys_primitiveRoot.ne_one` and `one_lt_pow₀`) and applies `geom_sum_mul`.
- Hypotheses: `n ≥ 1`.
- Uses from project: [`cycloUnit`, `zetaSys`, `zetaSys_primitiveRoot`]
- Used by: `isIntegral_cycloUnit`, `norm_cycloUnit_sub_one_lt_one`, `cycloUnit_mem_Fglobal`
- Visibility: private
- Lines: 254–260 (proof ~3 lines)
- Notes: none

### theorem isIntegral_cycloUnit
- Type: `theorem isIntegral_cycloUnit (a n : ℕ) : IsIntegral ℤ (cycloUnit p a n)`
- What: `c_n(a)` is integral over `ℤ` (a `ℤ`-polynomial in the integral `ξ`); unconditional since `n=0` gives junk value `0`.
- How: Case split on `n`. For `n=0`, `ξ_{p^0}=1` (via `IsPrimitiveRoot.one_right_iff`) makes `cycloUnit = 0`, integral. For `n≥1`, rewrites as geometric sum and applies `IsIntegral.sum` over powers of the integral `ξ` (`zetaSys_isIntegral`).
- Hypotheses: `a n : ℕ` (none).
- Uses from project: [`cycloUnit`, `zetaSys`, `zetaSys_primitiveRoot`, `cycloUnit_eq_geomSum`, `zetaSys_isIntegral`]
- Used by: `cyclo_elems_mem_globalUnits`
- Visibility: public
- Lines: 262–272 (proof ~7 lines)
- Notes: none

### theorem orderOf_zetaSys
- Type: `private theorem orderOf_zetaSys (n : ℕ) : orderOf (zetaSys p n) = p ^ n`
- What: The multiplicative order of `ξ_{p^n}` is `p^n`.
- How: `(zetaSys_primitiveRoot p n).eq_orderOf).symm`.
- Hypotheses: `n : ℕ`.
- Uses from project: [`zetaSys`, `zetaSys_primitiveRoot`]
- Used by: `inv_cycloUnit_eq_geomSum`, `cyclo_elems_mem_cycloUnits`
- Visibility: private
- Lines: 274–276 (term proof)
- Notes: none

### theorem inv_cycloUnit_eq_geomSum
- Type: `private theorem inv_cycloUnit_eq_geomSum {a : ℕ} (ha : ¬ p ∣ a) {n : ℕ} (hn : 1 ≤ n) : ∃ a', (cycloUnit p a n)⁻¹ = ∑ i ∈ Finset.range a', (zetaSys p n ^ a) ^ i`
- What: The inverse `c_n(a)⁻¹ = (ξ−1)/(ξ^a−1)` equals a geometric sum `Σ_{i<a'} (ξ^a)ⁱ` where `a·a' ≡ 1 mod p^n`.
- How: Picks `a'` via `Nat.exists_mul_mod_eq_one_of_coprime` (`a` coprime to `p^n`); reduces `ξ^{a·a'}=ξ` via `pow_mod_orderOf` + `orderOf_zetaSys`; uses `geom_sum_mul` to get `(ξ^a−1)·Σ = ξ−1`; nonzero denominators via `zetaSys_primitiveRoot.ne_one`; concludes by `inv_div`/`div_eq_iff`.
- Hypotheses: `p ∤ a`; `n ≥ 1`.
- Uses from project: [`cycloUnit`, `zetaSys`, `zetaSys_primitiveRoot`, `orderOf_zetaSys`]
- Used by: `isIntegral_inv_cycloUnit`
- Visibility: private
- Lines: 278–306 (proof ~17 lines)
- Notes: none

### theorem isIntegral_inv_cycloUnit
- Type: `theorem isIntegral_inv_cycloUnit {a : ℕ} (ha : ¬ p ∣ a) {n : ℕ} (hn : 1 ≤ n) : IsIntegral ℤ (cycloUnit p a n)⁻¹`
- What: The inverse cyclotomic unit `c_n(a)⁻¹` is integral over `ℤ`.
- How: Obtains the geometric-sum form via `inv_cycloUnit_eq_geomSum`, then `IsIntegral.sum` over powers of powers of the integral `ξ` (`zetaSys_isIntegral`).
- Hypotheses: `p ∤ a`; `n ≥ 1`.
- Uses from project: [`cycloUnit`, `zetaSys`, `inv_cycloUnit_eq_geomSum`, `zetaSys_isIntegral`]
- Used by: `cyclo_elems_mem_globalUnits`
- Visibility: public
- Lines: 308–314 (proof ~3 lines)
- Notes: none

### theorem norm_primitiveRoot_eq_one'
- Type: `private theorem norm_primitiveRoot_eq_one' {n : ℕ} {ξ : ℂ_[p]} (hξ : IsPrimitiveRoot ξ (p ^ n)) : ‖ξ‖ = 1`
- What: A primitive `p^n`-th root of unity in `ℂ_[p]` has norm `1`.
- How: `‖ξ‖^{p^n} = ‖ξ^{p^n}‖ = ‖1‖ = 1`; antisymmetry via `one_lt_pow₀` (rules out `‖ξ‖>1`) and `pow_lt_one₀` (rules out `‖ξ‖<1`).
- Hypotheses: `ξ` a primitive `p^n`-th root of unity.
- Uses from project: []
- Used by: `norm_zetaSys_pow_sub_one_lt_one`, `norm_cycloUnit_sub_one_lt_one`, `zetaSys_ne_zero'`, `zetaSysU_mem_localUnitsOne`, `norm_zetaSys_sub_one_lt_one'` (indirectly), and the `‖ξ^i−1‖` estimates
- Visibility: private
- Lines: 316–325 (proof ~5 lines)
- Notes: none; comment flags dedupe-with-`Coleman/Map.lean` at CLEANUP-FINAL

### theorem norm_pow_sub_one_le'
- Type: `private theorem norm_pow_sub_one_le' {ξ : ℂ_[p]} (hξ1 : ‖ξ‖ = 1) (c : ℕ) : ‖ξ ^ c − 1‖ ≤ ‖ξ − 1‖`
- What: For a norm-one `ξ`, `‖ξ^c − 1‖ ≤ ‖ξ − 1‖`.
- How: Factors `ξ^c − 1 = (Σ_{i<c} ξⁱ)(ξ−1)` (`geom_sum_mul`), bounds `‖Σ‖ ≤ 1` ultrametrically (`IsUltrametricDist.norm_sum_le_of_forall_le_of_nonneg`, each `‖ξⁱ‖=1`), then `nlinarith`.
- Hypotheses: `‖ξ‖ = 1`.
- Uses from project: []
- Used by: `norm_zetaSys_pow_sub_one_lt_one`, `norm_cycloUnit_sub_one_lt_one`
- Visibility: private
- Lines: 327–336 (proof ~6 lines)
- Notes: none; comment flags dedupe at CLEANUP-FINAL

### theorem norm_zetaSys_pow_sub_one_lt_one
- Type: `private theorem norm_zetaSys_pow_sub_one_lt_one {n : ℕ} (hn : 1 ≤ n) (i : ℕ) : ‖zetaSys p n ^ i − 1‖ < 1`
- What: `‖ξ_{p^n}^i − 1‖ < 1` for every `i` (n≥1).
- How: `norm_pow_sub_one_le'` bounds it by `‖ξ − 1‖ = ‖π_n‖`, then `norm_pi_lt_one`; uses `norm_primitiveRoot_eq_one'` for `‖ξ‖=1`.
- Hypotheses: `n ≥ 1`; `i : ℕ`.
- Uses from project: [`zetaSys`, `norm_primitiveRoot_eq_one'`, `zetaSys_primitiveRoot`, `norm_pow_sub_one_le'`, `norm_pi_lt_one`, `pi`]
- Used by: unused in file
- Visibility: private
- Lines: 338–345 (proof ~4 lines)
- Notes: none

### theorem norm_natCast_sub_one_lt_one_of_modEq
- Type: `private theorem norm_natCast_sub_one_lt_one_of_modEq {a : ℕ} (ha1 : a ≡ 1 [MOD p]) : ‖(a : ℂ_[p]) − 1‖ < 1`
- What: If `a ≡ 1 (mod p)`, then `‖(a:ℂ_[p]) − 1‖ < 1`.
- How: Writes `a − 1 = p·k` (`Nat.modEq_iff_dvd'`), casts to `(p:ℂ_[p])·(k:ℂ_[p])`, uses `‖p‖ = p⁻¹` (`Padic.norm_p` via `norm_algebraMap'`) and `‖k‖ ≤ 1`; concludes `p⁻¹ < 1` by `inv_lt_one_iff₀`. Handles `a=0` contradiction (`p ∣ 1`).
- Hypotheses: `a ≡ 1 (mod p)`.
- Uses from project: []
- Used by: `norm_cycloUnit_sub_one_lt_one`
- Visibility: private
- Lines: 347–371 (proof ~21 lines)
- Notes: long(30-50) NO (proof is ~21 lines, under 30); none

### theorem norm_cycloUnit_sub_one_lt_one
- Type: `theorem norm_cycloUnit_sub_one_lt_one {a : ℕ} (_ha : ¬ p ∣ a) {n : ℕ} (hn : 1 ≤ n) (ha1 : a ≡ 1 [MOD p]) : ‖cycloUnit p a n − 1‖ < 1`
- What: For `a ≡ 1 (mod p)`, `c_n(a)` is a principal unit: `‖c_n(a) − 1‖ < 1`.
- How: Splits `c_n(a) − 1 = (c_n(a) − a) + (a − 1)`. First piece `= Σ_{i<a}(ξⁱ−1)` (geom sum) has norm `< 1` ultrametrically (`norm_pow_sub_one_le'`, `norm_pi_lt_one`); second piece `< 1` by `norm_natCast_sub_one_lt_one_of_modEq`; combine via `IsUltrametricDist.norm_add_le_max` and `max_lt`.
- Hypotheses: `p ∤ a` (unused, `_ha`); `n ≥ 1`; `a ≡ 1 (mod p)`.
- Uses from project: [`cycloUnit`, `cycloUnit_eq_geomSum`, `zetaSys`, `norm_primitiveRoot_eq_one'`, `zetaSys_primitiveRoot`, `norm_pow_sub_one_le'`, `norm_pi_lt_one`, `pi`, `norm_natCast_sub_one_lt_one_of_modEq`]
- Used by: `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 373–401 (proof ~17 lines)
- Notes: none; large docstring "Statement note (T1113)" records the added `a ≡ 1 (mod p)` hypothesis (RJW gloss)

### theorem cycloUnit_mem_Fglobal
- Type: `private theorem cycloUnit_mem_Fglobal {a n : ℕ} (hn : 1 ≤ n) : cycloUnit p a n ∈ Fglobal p n`
- What: `c_n(a) = Σ_{i<a} ξⁱ ∈ F_n` (a polynomial in the generator).
- How: Rewrites to geometric sum, then `sum_mem` of powers of the self-generator `ξ ∈ ℚ⟮ξ⟯` (`IntermediateField.mem_adjoin_simple_self`).
- Hypotheses: `n ≥ 1`.
- Uses from project: [`cycloUnit`, `cycloUnit_eq_geomSum`, `Fglobal`]
- Used by: `cyclo_elems_mem_globalUnits`
- Visibility: private
- Lines: 403–407 (proof ~2 lines)
- Notes: none

### theorem cyclo_elems_mem_globalUnits
- Type: `private theorem cyclo_elems_mem_globalUnits {a : ℕ} (ha : ¬ p ∣ a) (hp2 : p ≠ 2) {n : ℕ} (hn : 1 ≤ n) : (cyclo p ha hp2).elems n ∈ globalUnits p n`
- What: The `n`-th component of the norm-compatible cyclotomic system `cyclo` lies in `𝒱_n`.
- How: Establishes its underlying value is `cycloUnit p a n` (unfolding the `dif_pos hn` branch + `Units.val_mk0`), then verifies the three global-unit predicates via `cycloUnit_mem_Fglobal`, `isIntegral_cycloUnit`, `isIntegral_inv_cycloUnit`.
- Hypotheses: `p ∤ a`; `p ≠ 2`; `n ≥ 1`.
- Uses from project: [`cyclo`, `globalUnits`, `cycloUnit`, `cycloUnit_ne_zero`, `cycloUnit_mem_Fglobal`, `isIntegral_cycloUnit`, `isIntegral_inv_cycloUnit`]
- Used by: `cyclo_elems_mem_cycloUnits`
- Visibility: private
- Lines: 409–421 (proof ~8 lines)
- Notes: none

### theorem cyclo_elems_mem_cycloUnits
- Type: `theorem cyclo_elems_mem_cycloUnits {a : ℕ} (ha : ¬ p ∣ a) (hp2 : p ≠ 2) {n : ℕ} (hn : 1 ≤ n) : (cyclo p ha hp2).elems n ∈ cycloUnits p n`
- What: **RJW TeX 3084** — `c_n(a) ∈ 𝒟_n`: each Coleman input is a global cyclotomic unit.
- How: Splits `cycloUnits = closure ⊓ globalUnits`; global part from `cyclo_elems_mem_globalUnits`. For the closure part, exhibits the word `(ξ^{a%p^n}−1)·(ξ−1)⁻¹` with both factors generators of `cycloGenSet` (using `Units.mk0`, nonzero via `zetaSys_primitiveRoot.ne_one`/`orderOf` divisibility), reduces `ξ^a = ξ^{a%p^n}` (`pow_mod_orderOf` + `orderOf_zetaSys`), then `mul_mem` of `subset_closure` + `inv_mem`.
- Hypotheses: `p ∤ a`; `p ≠ 2`; `n ≥ 1`.
- Uses from project: [`cyclo`, `cycloUnits`, `cyclo_elems_mem_globalUnits`, `zetaSys`, `zetaSys_primitiveRoot`, `orderOf_zetaSys`, `cycloGenSet`, `cycloUnit`, `cycloUnit_ne_zero`]
- Used by: `cyclo_mem_cycloTower1`
- Visibility: public
- Lines: 423–465 (proof ~35 lines)
- Notes: long(30-50)

### theorem cyclo_mem_cycloTower1
- Type: `theorem cyclo_mem_cycloTower1 {a : ℕ} (ha : ¬ p ∣ a) (hp2 : p ≠ 2) (ha1 : a ≡ 1 [MOD p]) : cyclo p ha hp2 ∈ cycloTower1 p`
- What: **MILESTONE (RJW TeX 3084)** — the norm-compatible cyclotomic system (the Coleman-map input) lies in the tower `𝒞_{∞,1}`.
- How: Pointwise at each `n ≥ 1`: `cyclo.elems n ∈ 𝒟_n` (`cyclo_elems_mem_cycloUnits`), hence in `𝒰_n` (`globalUnits_le_localUnits ∘ cycloUnits_le_globalUnits`) and in the topological closure (`Subgroup.le_topologicalClosure`); the principal-unit condition `‖c_n(a)−1‖<1` from `norm_cycloUnit_sub_one_lt_one`; assemble via `mem_inf` of `cycloClosureOne`/`cycloClosure`/`mem_localUnitsOne_iff`.
- Hypotheses: `p ∤ a`; `p ≠ 2`; `a ≡ 1 (mod p)`.
- Uses from project: [`cyclo`, `cycloTower1`, `cycloUnits`, `cyclo_elems_mem_cycloUnits`, `localUnits`, `globalUnits_le_localUnits`, `cycloUnits_le_globalUnits`, `cycloUnit`, `cycloUnit_ne_zero`, `cycloClosureOne`, `cycloClosure`, `mem_localUnitsOne_iff`, `norm_cycloUnit_sub_one_lt_one`]
- Used by: `cyclo_mem_unitsTower1`
- Visibility: public
- Lines: 467–498 (proof ~19 lines)
- Notes: none; docstring carries the "Statement note (T1113)" about the `a ≡ 1` hypothesis

### theorem cyclo_mem_unitsTower1
- Type: `theorem cyclo_mem_unitsTower1 {a : ℕ} (ha : ¬ p ∣ a) (hp2 : p ≠ 2) (ha1 : a ≡ 1 [MOD p]) : cyclo p ha hp2 ∈ unitsTower1 p`
- What: The Coleman input also lies in the local-unit tower `𝒰_{∞,1}` (immediate corollary).
- How: `cycloTower1_le_unitsTower1` applied to `cyclo_mem_cycloTower1`.
- Hypotheses: `p ∤ a`; `p ≠ 2`; `a ≡ 1 (mod p)`.
- Uses from project: [`cyclo`, `unitsTower1`, `cycloTower1_le_unitsTower1`, `cyclo_mem_cycloTower1`]
- Used by: unused in file
- Visibility: public
- Lines: 500–503 (term proof)
- Notes: none

### theorem zetaSys_ne_zero'
- Type: `private theorem zetaSys_ne_zero' {n : ℕ} : zetaSys p n ≠ 0`
- What: `ξ_{p^n} ≠ 0` (a nonzero root of unity).
- How: `norm_ne_zero_iff` from `‖ξ‖ = 1` (`norm_primitiveRoot_eq_one'`).
- Hypotheses: none.
- Uses from project: [`zetaSys`, `norm_primitiveRoot_eq_one'`, `zetaSys_primitiveRoot`]
- Used by: `zetaSysU`
- Visibility: private
- Lines: 515–518 (proof ~3 lines)
- Notes: none

### def zetaSysU
- Type: `private noncomputable def zetaSysU {n : ℕ} (_hn : 1 ≤ n) : ℂ_[p]ˣ := Units.mk0 (zetaSys p n) (zetaSys_ne_zero' p)`
- What: `ξ_{p^n}` packaged as a unit of `ℂ_[p]ˣ`.
- How: `Units.mk0` with nonzeroness `zetaSys_ne_zero'`.
- Hypotheses: `n ≥ 1` (only to index; `_hn` unused in body).
- Uses from project: [`zetaSys`, `zetaSys_ne_zero'`]
- Used by: `zetaSysU_val`, `norm_zetaSys_sub_one_lt_one'` (no), `zetaSysU_pow_mem_cycloUnits`, `zetaSysU_mem_localUnitsOne`, `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: private
- Lines: 520–522 (no proof)
- Notes: none

### theorem zetaSysU_val
- Type: `private theorem zetaSysU_val {n : ℕ} (hn : 1 ≤ n) : (zetaSysU p hn : ℂ_[p]) = zetaSys p n := Units.val_mk0 _`
- What: The underlying value of the unit `zetaSysU` is `ξ_{p^n}`.
- How: `Units.val_mk0`.
- Hypotheses: `n ≥ 1`.
- Uses from project: [`zetaSysU`, `zetaSys`]
- Used by: `zetaSysU_pow_mem_cycloUnits`, `zetaSysU_mem_localUnitsOne`, `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: private
- Lines: 524–525 (term proof)
- Notes: none

### theorem norm_zetaSys_sub_one_lt_one'
- Type: `private theorem norm_zetaSys_sub_one_lt_one' {n : ℕ} (hn : 1 ≤ n) : ‖zetaSys p n − 1‖ < 1`
- What: `‖ξ_{p^n} − 1‖ < 1`: the root of unity is principal (`= ‖π_n‖ < 1`).
- How: `norm_pi_lt_one` rewritten through `pi`.
- Hypotheses: `n ≥ 1`.
- Uses from project: [`zetaSys`, `norm_pi_lt_one`, `pi`]
- Used by: `zetaSysU_mem_localUnitsOne`, `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: private
- Lines: 527–530 (proof ~1 line)
- Notes: none

### theorem zetaSysU_pow_mem_cycloUnits
- Type: `private theorem zetaSysU_pow_mem_cycloUnits {n : ℕ} (hn : 1 ≤ n) (k : ℕ) : zetaSysU p hn ^ k ∈ cycloUnits p n`
- What: The integral power `ξ_{p^n}^k` is a cyclotomic unit (`∈ 𝒟_n`).
- How: Splits `cycloUnits = closure ⊓ globalUnits`. Closure: `ξ` is a generator (`Or.inl`), `pow_mem` of `subset_closure`. Global: `ξ^k ∈ F_n`, integral (`zetaSys_isIntegral.pow`), and inverse integral via `ξ⁻¹ = ξ^{p^n−1}` (`inv_eq_of_mul_eq_one_left` + `zetaSys_primitiveRoot.pow_eq_one`).
- Hypotheses: `n ≥ 1`; `k : ℕ`.
- Uses from project: [`zetaSysU`, `cycloUnits`, `cycloGenSet`, `zetaSysU_val`, `zetaSys`, `zetaSys_isIntegral`, `zetaSys_primitiveRoot`, `Fglobal` (via `mem_adjoin_simple_self`)]
- Used by: `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: private
- Lines: 532–555 (proof ~18 lines)
- Notes: none

### theorem zetaSysU_mem_localUnitsOne
- Type: `private theorem zetaSysU_mem_localUnitsOne {n : ℕ} (hn : 1 ≤ n) : zetaSysU p hn ∈ localUnitsOne p n`
- What: `ξ_{p^n}` is a principal local unit (`∈ 𝒰_{n,1}`): norm `1`, `≡ 1 mod 𝔭_n`.
- How: Shows `ξ ∈ O_n` and `ξ⁻¹ ∈ O_n` (via `zetaSys_mem_K`, `K.inv_mem`, and `‖ξ‖=‖ξ⁻¹‖=1` from `norm_primitiveRoot_eq_one'`), and the principal condition `‖ξ−1‖<1` from `norm_zetaSys_sub_one_lt_one'`; assembles via `mem_localUnitsOne_iff`.
- Hypotheses: `n ≥ 1`.
- Uses from project: [`zetaSysU`, `localUnitsOne`, `zetaSys`, `norm_primitiveRoot_eq_one'`, `zetaSys_primitiveRoot`, `zetaSysU_val`, `O`, `K`, `zetaSys_mem_K`, `mem_localUnitsOne_iff`, `norm_zetaSys_sub_one_lt_one'`]
- Used by: `zpPow_zetaSys_mem_cycloClosureOne`
- Visibility: private
- Lines: 557–568 (proof ~9 lines)
- Notes: none

### theorem zpPow_zetaSys_mem_cycloClosureOne
- Type: `theorem zpPow_zetaSys_mem_cycloClosureOne {n : ℕ} (hn : 1 ≤ n) (a : ℤ_[p]) {x : ℂ_[p]ˣ} (hx : (x : ℂ_[p]) = zpPow p (zetaSys p n) a) : x ∈ cycloClosureOne p n`
- What: The `ℤ_p`-power `ξ_{p^n}^a = zpPow ξ a` lands in the local closure `𝒞_{n,1}` (the inclusion `ℤ_p(1) ⊆ 𝒞_{∞,1}` of RJW §12.5).
- How: Builds the continuous unit-power map `F c = ⟨ξ^c, ξ^{−c}⟩` (continuity of `zpPow` via `PadicInt.addChar_of_value_at_one` + `Units.continuous_iff`); `F` agrees with `ξu^k` on naturals (`zpPow_natCast`). By density of `ℕ ↪ ℤ_p` (`PadicInt.denseRange_natCast`) and `image_closure_subset_closure_image`, `range F ⊆ closure ↑𝒟_n`, so `x = F a ∈ (𝒟_n)⁻`. Principal-unit part from `zpPow_mem_localUnitsOne` at `zetaSysU_mem_localUnitsOne`.
- Hypotheses: `n ≥ 1`; `a : ℤ_[p]`; `x`'s value is `zpPow ξ a`.
- Uses from project: [`zetaSys`, `zpPow`, `cycloClosureOne`, `cycloClosure`, `cycloUnits`, `norm_zetaSys_sub_one_lt_one'`, `zpPow_add`, `zpPow_natCast`, `zetaSysU`, `zetaSysU_val`, `zetaSysU_pow_mem_cycloUnits`, `zpPow_mem_localUnitsOne`, `zetaSysU_mem_localUnitsOne`]
- Used by: unused in file
- Visibility: public
- Lines: 570–617 (proof ~44 lines)
- Notes: long(30-50)

### theorem zpPow_mem_cycloUnits_topologicalClosure
- Type: `theorem zpPow_mem_cycloUnits_topologicalClosure {n : ℕ} {y : ℂ_[p]ˣ} (hyc : ‖↑y − 1‖ < 1) (hyclos : y ∈ (cycloUnits p n).topologicalClosure) (a : ℤ_[p]) {x : ℂ_[p]ˣ} (hx : ↑x = zpPow p ↑y a) : x ∈ (cycloUnits p n).topologicalClosure`
- What: **RJW lem:closure (TeX 3503)** — the closure `(𝒟_n)⁻` is `zpPow`-closed: if a principal unit `y` is in the closure, so is every `ℤ_p`-power `y^a`.
- How: Same density argument as `zpPow_zetaSys_mem_cycloClosureOne`: continuous `F c = ⟨y^c, y^{−c}⟩`, `F(k) = y^k ∈ (𝒟_n)⁻` (a subgroup, `pow_mem hyclos`), so `range F ⊆ closure ↑(𝒟_n)⁻`; since `(𝒟_n)⁻` is closed (`Subgroup.isClosed_topologicalClosure`), `closure = self`, giving `x = F a ∈ (𝒟_n)⁻`.
- Hypotheses: `‖y−1‖<1` (principal); `y ∈ (𝒟_n)⁻`; `a : ℤ_[p]`; `x`'s value is `zpPow y a`.
- Uses from project: [`cycloUnits`, `zpPow`, `zpPow_add`, `zpPow_natCast`]
- Used by: `mem_cycloClosureOne_of_pow_mem`
- Visibility: public
- Lines: 636–670 (proof ~31 lines)
- Notes: long(30-50)

### theorem mem_cycloClosureOne_of_pow_mem
- Type: `theorem mem_cycloClosureOne_of_pow_mem {n : ℕ} {g : ℂ_[p]ˣ} (hg : g ∈ localUnitsOne p n) (hgpow : g ^ (p − 1) ∈ cycloClosureOne p n) : g ∈ cycloClosureOne p n`
- What: **RJW lem:global generators 2** — `𝒞_{n,1}` is uniquely `(p−1)`-rooted: a principal unit `g` whose `(p−1)`-th power lies in `𝒞_{n,1}` lies in `𝒞_{n,1}` itself.
- How: `p−1 ∈ ℤ_p^×` (`PadicInt.norm_natCast_p_sub_one` → `isUnit_iff`), with left-inverse `c`; then `g = zpPow (g^{p−1}) c` (via `zpPow_natCast`/`zpPow_mul`/`hc`). The closure component follows from `zpPow_mem_cycloUnits_topologicalClosure` applied to `g^{p−1} ∈ (𝒟_n)⁻` (extracted from `hgpow`), `‖g^{p−1}−1‖<1` via `norm_zpPow_sub_one_lt_one`; local/principal components inherited from `hg`.
- Hypotheses: `g ∈ 𝒰_{n,1}`; `g^{p−1} ∈ 𝒞_{n,1}`.
- Uses from project: [`localUnitsOne`, `cycloClosureOne`, `cycloClosure`, `cycloUnits`, `mem_localUnitsOne_iff`, `zpPow`, `zpPow_natCast`, `zpPow_mul`, `norm_zpPow_sub_one_lt_one`, `zpPow_mem_cycloUnits_topologicalClosure`]
- Used by: unused in file
- Visibility: public
- Lines: 672–700 (proof ~22 lines)
- Notes: none

---

## File Summary

**Total declarations: 33** — defs 13 (`Fglobal`, `FglobalPlus`, `globalUnits`, `globalUnitsPlus`, `cycloGenSet`, `cycloUnits`, `cycloUnitsPlus`, `cycloClosure`, `cycloClosurePlus`, `cycloClosureOne`, `cycloClosureOnePlus`, `cycloTower1`, `cycloTower1Plus`, `zetaSysU`) — note this is 14 if `zetaSysU` counted; **defs = 14**, lemmas+theorems = 19, instances = 0.

Recount: defs 14, lemmas/theorems 19, instances 0 → **33 total**.

**Key API (used by ≥3 in this file):**
- `cycloUnits` — used by 9 decls (the central object `𝒟_n`).
- `zetaSys` (project import) — referenced throughout.
- `norm_primitiveRoot_eq_one'` — used by ≥5 decls (the `‖ξ‖=1` workhorse).
- `cycloClosure`, `cycloClosureOne` — each used by ≥4 decls.
- `zetaSys_primitiveRoot`, `zetaSys_isIntegral`, `cycloUnit_eq_geomSum`, `orderOf_zetaSys` — each used by 3.

**Unused within file (terminal/leaf or API for other files):** `FglobalPlus_le_Fglobal`, `globalUnitsPlus`, `cycloUnitsPlus`, `cycloClosureOnePlus` (only via `cycloTower1Plus`), `cycloTower1Plus`, `norm_zetaSys_pow_sub_one_lt_one`, `cyclo_mem_unitsTower1`, `zpPow_zetaSys_mem_cycloClosureOne`, `mem_cycloClosureOne_of_pow_mem`. (Public milestone/API theorems consumed downstream by Coleman/§12.)

**Decls with sorry: NONE.**

**set_option: NONE.**

**Proofs > 50 lines (need /decompose-proof): NONE** (count 0). Closest are `norm_le_one_of_isIntegral_int` (~44), `zpPow_zetaSys_mem_cycloClosureOne` (~44) — both under 50.

**Proofs 30–50 lines (count 4):**
- `norm_le_one_of_isIntegral_int` (lines 55–102, ~44)
- `cyclo_elems_mem_cycloUnits` (lines 423–465, ~35)
- `zpPow_zetaSys_mem_cycloClosureOne` (lines 570–617, ~44)
- `zpPow_mem_cycloUnits_topologicalClosure` (lines 636–670, ~31)

**Other notes:** `norm_natCast_sub_one_lt_one_of_modEq` (~21 lines, under 30). Comments flag dedupe of `norm_primitiveRoot_eq_one'` and `norm_pow_sub_one_le'` with `Coleman/Map.lean` at CLEANUP-FINAL. Docstrings of `norm_cycloUnit_sub_one_lt_one` and `cyclo_mem_cycloTower1` record statement-note T1113 (added `a ≡ 1 (mod p)` hypothesis, an RJW gloss).
