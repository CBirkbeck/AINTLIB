# Inventory: PadicLFunctions/Iwasawa/LocalUnits.lean

File: `/Users/mcu22seu/Documents/GitHub/aintlib-main/projects/PadicLFunctions/PadicLFunctions/Iwasawa/LocalUnits.lean`

Namespace: `PadicLFunctions.Coleman` (with inner `NormCompatUnits`). Section variable `(p : ℕ) [hp : Fact p.Prime]`.

Module purpose: builds the §9 local-unit-group notation (RJW §9, TeX 2471–2505) needed at §11 — the local units `𝒰_n = 𝒪_{K_n}^×`, the principal units `𝒰_{n,1}` (congruence `u ≡ 1 (mod 𝔭_n)` rendered as `‖u − 1‖ < 1`), the `+`-subfield `K_n⁺` and `⁺`-variants, the `ℤ_p`-power `u^a` on principal units (binomial series via `PadicInt.addChar_of_value_at_one`), the `CommGroup` upgrade of `NormCompatUnits` (`𝒰_∞`), and the towers `𝒰_{∞,1}`, `𝒰⁺_{∞,1}`.

---

### def localUnits
- Type: `def localUnits (n : ℕ) : Subgroup ℂ_[p]ˣ`
- What: The group `𝒰_n = 𝒪_{K_n}^×` of units of the integer ring `O_n`, as a subgroup of `ℂ_[p]ˣ`: those `u` such that both `u` and `u⁻¹` (as values in `ℂ_[p]`) lie in `O p n`. RJW TeX 2474.
- How: Direct `Subgroup` construction; closure proofs unfold `Units.val_mul`/`mul_inv_rev`/`inv_one`/`inv_inv` and apply `mul_mem`/`one_mem` of the subring `O p n`.
- Hypotheses: `n : ℕ` (level); ambient prime `p`.
- Uses from project: `O`
- Used by: `mem_localUnits_iff`, `norm_eq_one_of_mem_localUnits`, `localUnitsOne`, `localUnitsPlus`, `zpPow_mem_localUnitsOne` (in field)
- Visibility: public
- Lines: 37–53 (proof ~13 lines)
- Notes: none

### lemma mem_localUnits_iff
- Type: `lemma mem_localUnits_iff {n : ℕ} {u : ℂ_[p]ˣ} : u ∈ localUnits p n ↔ (u : ℂ_[p]) ∈ O p n ∧ ((u⁻¹ : ℂ_[p]ˣ) : ℂ_[p]) ∈ O p n`
- What: Membership unfolding lemma for `localUnits`, stating the defining condition (both `u` and `u⁻¹` lie in `O p n`).
- How: `Iff.rfl` — the carrier is definitionally the stated set.
- Hypotheses: `n`, `u : ℂ_[p]ˣ`.
- Uses from project: `localUnits`, `O`
- Used by: unused in file
- Visibility: public
- Lines: 55–58 (proof 1 line)
- Notes: none

### lemma norm_eq_one_of_mem_localUnits
- Type: `lemma norm_eq_one_of_mem_localUnits {n : ℕ} {u : ℂ_[p]ˣ} (hu : u ∈ localUnits p n) : ‖(u : ℂ_[p])‖ = 1`
- What: A local unit has `ℂ_[p]`-norm exactly `1`.
- How: From `Subring.mem_inf` extract `‖u‖ ≤ 1` and `‖u⁻¹‖ ≤ 1`; combined with the product identity `‖u‖·‖u⁻¹‖ = 1` (via `norm_mul`, `Units.mul_inv`, `norm_one`), `nlinarith` forces both to be `1`.
- Hypotheses: `u` is a local unit at level `n`.
- Uses from project: `localUnits`
- Used by: `localUnitsOne` (in `mul_mem'`/`inv_mem'` proofs)
- Visibility: public
- Lines: 60–67 (proof ~5 lines)
- Notes: none

### def localUnitsOne
- Type: `def localUnitsOne (n : ℕ) : Subgroup ℂ_[p]ˣ`
- What: The principal (1-)units `𝒰_{n,1} = {u ∈ 𝒰_n : ‖u − 1‖ < 1}`, the congruence `u ≡ 1 (mod 𝔭_n)`. RJW Eq. (eq:U1), TeX 2494.
- How: `Subgroup` construction. `mul_mem'` uses the identity `(uv) − 1 = u·(v−1) + (u−1)` then the ultrametric `IsUltrametricDist.norm_add_le_max` with `norm_eq_one_of_mem_localUnits`; `inv_mem'` uses `u⁻¹ − 1 = u⁻¹·(1 − u)`, `field_simp`, `norm_sub_rev`, and again the norm-one fact.
- Hypotheses: `n`; ambient prime `p`.
- Uses from project: `localUnits`, `norm_eq_one_of_mem_localUnits`
- Used by: `mem_localUnitsOne_iff`, `localUnitsOnePlus`, `zpPow_mem_localUnitsOne`, `norm_localUnitsOne_sub_one_lt_one`, `zpPowUnit`, `localUnitsOne_ext`, `localUnitsOneSMul`, `unitsTower1`
- Visibility: public
- Lines: 69–94 (proof ~22 lines)
- Notes: none

### lemma mem_localUnitsOne_iff
- Type: `lemma mem_localUnitsOne_iff {n : ℕ} {u : ℂ_[p]ˣ} : u ∈ localUnitsOne p n ↔ u ∈ localUnits p n ∧ ‖(u : ℂ_[p]) - 1‖ < 1`
- What: Membership unfolding lemma for `localUnitsOne`.
- How: `Iff.rfl`.
- Hypotheses: `n`, `u`.
- Uses from project: `localUnitsOne`, `localUnits`
- Used by: `norm_localUnitsOne_sub_one_lt_one`
- Visibility: public
- Lines: 96–98 (proof 1 line)
- Notes: none

### noncomputable def KPlus
- Type: `noncomputable def KPlus (n : ℕ) : IntermediateField ℚ_[p] ℂ_[p] := ℚ_[p]⟮zetaSys p n + (zetaSys p n)⁻¹⟯`
- What: The `+`-subfield `K_n⁺ = ℚ_p(ξ_{p^n} + ξ_{p^n}⁻¹)`, generated by the standard `ξ ↦ ξ⁻¹`-fixed generator. RJW TeX 2473.
- How: Definition via single-element `IntermediateField` adjunction of `zetaSys p n + (zetaSys p n)⁻¹`.
- Hypotheses: `n`.
- Uses from project: `zetaSys`
- Used by: `KPlus_le_K`, `localUnitsPlus`
- Visibility: public (noncomputable)
- Lines: 100–106 (proof: definitional, 1 line)
- Notes: none

### lemma KPlus_le_K
- Type: `lemma KPlus_le_K (n : ℕ) : KPlus p n ≤ K p n`
- What: The `+`-subfield is contained in `K_n`.
- How: `IntermediateField.adjoin_simple_le_iff` reduces to showing the generator `ζ + ζ⁻¹ ∈ K p n`; uses `add_mem`, `zetaSys_mem_K`, and `(K p n).inv_mem`.
- Hypotheses: `n`.
- Uses from project: `KPlus`, `K`, `zetaSys_mem_K`
- Used by: unused in file
- Visibility: public
- Lines: 108–110 (proof ~3 lines)
- Notes: none

### noncomputable def localUnitsPlus
- Type: `noncomputable def localUnitsPlus (n : ℕ) : Subgroup ℂ_[p]ˣ`
- What: The `+`-local-units `𝒰_n⁺ = 𝒪_{K_n⁺}^×`, realised as local units whose value lies in `K_n⁺`. RJW TeX 2474/2498.
- How: `Subgroup` construction; closure proofs combine `localUnits`-membership closure with `KPlus`-membership closure (`mul_mem`, `(KPlus p n).inv_mem`, `Units.val_mul`/`val_one`/`val_inv_eq_inv_val`).
- Hypotheses: `n`.
- Uses from project: `localUnits`, `KPlus`
- Used by: `localUnitsOnePlus`
- Visibility: public (noncomputable)
- Lines: 112–128 (proof ~13 lines)
- Notes: none

### noncomputable def localUnitsOnePlus
- Type: `noncomputable def localUnitsOnePlus (n : ℕ) : Subgroup ℂ_[p]ˣ := localUnitsOne p n ⊓ localUnitsPlus p n`
- What: `𝒰⁺_{n,1} = 𝒰_{n,1} ∩ 𝒰_n⁺`, the principal `+`-units. RJW TeX 2494.
- How: Definition as the infimum of the two subgroups.
- Hypotheses: `n`.
- Uses from project: `localUnitsOne`, `localUnitsPlus`
- Used by: `unitsTower1Plus`
- Visibility: public (noncomputable)
- Lines: 130–132 (proof: definitional, 1 line)
- Notes: none

### instance instUCCSMulZpAlgCl
- Type: `instance instUCCSMulZpAlgCl : UniformContinuousConstSMul ℤ_[p] (PadicAlgCl p)`
- What: File-local instance: the `ℤ_[p]`-scalar action on `PadicAlgCl p` (the `ℚ_[p]`-action restricted along `ℤ_[p] → ℚ_[p]`) is uniformly continuous — the single fact mathlib needs to construct `Module`/`Algebra ℤ_[p] ℂ_[p]` on the completion.
- How: Anonymous constructor wrapping `uniformContinuous_const_smul (c : ℚ_[p])` for each `c`.
- Hypotheses: ambient prime `p`.
- Uses from project: `PadicAlgCl`
- Used by: (implicitly enables instances used throughout `zpPow` API; not referenced by name)
- Visibility: public instance
- Lines: 151–155 (proof 1 line)
- Notes: none

### theorem smul_cp_eq
- Type: `theorem smul_cp_eq (c : ℤ_[p]) (x : ℂ_[p]) : c • x = toCp p c * x`
- What: The induced `ℤ_[p]`-scalar action on `ℂ_[p]` is multiplication by `toCp p c`.
- How: `Algebra.smul_def` then `congr 1`.
- Hypotheses: `c : ℤ_[p]`, `x : ℂ_[p]`.
- Uses from project: `toCp`
- Used by: `instBddSMulZpCp`
- Visibility: public
- Lines: 157–160 (proof ~2 lines)
- Notes: none

### noncomputable instance instBddSMulZpCp
- Type: `noncomputable instance instBddSMulZpCp : IsBoundedSMul ℤ_[p] ℂ_[p]`
- What: File-local instance: the `ℤ_[p]`-action on `ℂ_[p]` is bounded (`‖c • x‖ = ‖c‖·‖x‖`), the last instance `addChar_of_value_at_one` requires.
- How: `IsBoundedSMul.of_norm_smul_le`, rewriting with `smul_cp_eq`, `norm_mul`, and the isometry `norm_toCp`.
- Hypotheses: ambient prime `p`.
- Uses from project: `smul_cp_eq`, `norm_toCp`
- Used by: (implicitly enables `zpPow`; not referenced by name)
- Visibility: public instance (noncomputable)
- Lines: 162–165 (proof ~1 line)
- Notes: none

### noncomputable def zpPow
- Type: `noncomputable def zpPow (y : ℂ_[p]) (a : ℤ_[p]) : ℂ_[p]`
- What: The `ℤ_p`-power `y^a` of a `1`-unit `y` (junk value `1` when `(y−1)^k` does not tend to `0`): mathlib's continuous additive character `PadicInt.addChar_of_value_at_one (y−1)` evaluated at `a` — the source's binomial series `Σ (a choose k)(y−1)^k`. RJW TeX 2494–2496.
- How: `Classical`-`if`/`dif` on whether `(y−1)^k → 0`: positive branch is the additive character, else `1`.
- Hypotheses: `y : ℂ_[p]`, `a : ℤ_[p]`.
- Uses from project: [] (none — `PadicInt.addChar_of_value_at_one` is mathlib)
- Used by: `zpPow_def`, `zpPow_natCast`, `zpPow_add`, `continuous_zpPow`, `zpPow_mem_of_closed`, `norm_zpPow_sub_one_lt_one`, `zpPow_mul`, `zpPow_zero`, `zpPow_one_base`, `zpPow_ne_zero`, `zpPow_mul_base`, `zpPow_mem_localUnitsOne`, `coe_zpPowUnit`
- Visibility: public (noncomputable)
- Lines: 167–174 (proof: definitional, ~4 lines)
- Notes: uses `open Classical`

### private theorem tendsto_pow_of_norm_lt_one
- Type: `private theorem tendsto_pow_of_norm_lt_one {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) : Tendsto ((y - 1) ^ ·) atTop (𝓝 0)`
- What: For a `1`-unit `y` (`‖y−1‖<1`), the powers `(y−1)^k → 0`, so `zpPow`'s `dif_pos` branch fires.
- How: `tendsto_pow_atTop_nhds_zero_iff_norm_lt_one.mpr hy`.
- Hypotheses: `‖y − 1‖ < 1`.
- Uses from project: []
- Used by: `zpPow_def`, `zpPow_natCast`
- Visibility: private
- Lines: 176–180 (proof 1 line)
- Notes: none

### private theorem zpPow_def
- Type: `private theorem zpPow_def {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (a : ℤ_[p]) : zpPow p y a = PadicInt.addChar_of_value_at_one (y - 1) (tendsto_pow_of_norm_lt_one p hy) a`
- What: For `‖y−1‖<1`, `zpPow` unfolds to the additive character (no junk branch).
- How: Unfold `zpPow`, `dif_pos (tendsto_pow_of_norm_lt_one p hy)`.
- Hypotheses: `‖y − 1‖ < 1`.
- Uses from project: `zpPow`, `tendsto_pow_of_norm_lt_one`
- Used by: `zpPow_natCast`, `zpPow_add`, `continuous_zpPow`
- Visibility: private
- Lines: 182–186 (proof ~1 line)
- Notes: none

### theorem zpPow_natCast
- Type: `theorem zpPow_natCast {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (k : ℕ) : zpPow p y (k : ℤ_[p]) = y ^ k`
- What: On natural-number exponents `zpPow` is the ordinary power (it extends `u ↦ u^k`).
- How: Rewrite via `zpPow_def`, identify the character with `PadicInt.mahlerSeries`, use `PadicInt.mahlerSeries_apply_nat` and `(Commute.one_right (y−1)).add_pow` (binomial theorem) to expand `((y−1)+1)^k`, then `Finset.sum_congr` to match terms.
- Hypotheses: `‖y − 1‖ < 1`; `k : ℕ`.
- Uses from project: `zpPow`, `zpPow_def`, `tendsto_pow_of_norm_lt_one`
- Used by: `zpPow_mem_of_closed`, `zpPow_mul`, `zpPow_one_base`, `zpPow_mul_base`, `localUnitsOneModule` (via density lemmas)
- Visibility: public
- Lines: 188–197 (proof ~6 lines)
- Notes: none

### theorem zpPow_add
- Type: `theorem zpPow_add {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (a b : ℤ_[p]) : zpPow p y (a + b) = zpPow p y a * zpPow p y b`
- What: The additive-character law `y^{a+b} = y^a·y^b`.
- How: Rewrite all three terms via `zpPow_def`, then `AddChar.map_add_eq_mul`.
- Hypotheses: `‖y − 1‖ < 1`; `a b : ℤ_[p]`.
- Uses from project: `zpPow`, `zpPow_def`
- Used by: `zpPow_mul`, `zpPow_ne_zero`, `zpPow_mem_localUnitsOne`, `localUnitsOneModule`
- Visibility: public
- Lines: 199–203 (proof ~3 lines)
- Notes: none

### private theorem continuous_zpPow
- Type: `private theorem continuous_zpPow {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) : Continuous (zpPow p y)`
- What: `zpPow p y` is continuous in the exponent (it equals the continuous additive character).
- How: `funext (zpPow_def p hy)` rewrites to the character, then `PadicInt.continuous_addChar_of_value_at_one`.
- Hypotheses: `‖y − 1‖ < 1`.
- Uses from project: `zpPow`, `zpPow_def`
- Used by: `zpPow_mem_of_closed`, `zpPow_mul`, `zpPow_one_base`, `zpPow_mul_base`
- Visibility: private
- Lines: 206–210 (proof ~2 lines)
- Notes: none

### private theorem zpPow_mem_of_closed
- Type: `private theorem zpPow_mem_of_closed {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) {S : Set ℂ_[p]} (hS : IsClosed S) (hnat : ∀ k : ℕ, y ^ k ∈ S) (a : ℤ_[p]) : zpPow p y a ∈ S`
- What: Density transfer — a closed set containing every natural power `y^k` contains every `ℤ_p`-power `y^a`. Used for the norm bound, subfield-stability, and unit-ball membership.
- How: Shows `range (Nat.cast) ⊆ zpPow p y ⁻¹' S` (via `zpPow_natCast`), then `PadicInt.denseRange_natCast.closure_range` plus `IsClosed.preimage` (`continuous_zpPow`) and `closure_subset_iff` give `zpPow p y ⁻¹' S = univ`.
- Hypotheses: `‖y − 1‖ < 1`; `S` closed; `y^k ∈ S` for all `k`.
- Uses from project: `zpPow`, `zpPow_natCast`, `continuous_zpPow`
- Used by: `norm_zpPow_sub_one_lt_one`, `zpPow_mem_localUnitsOne`
- Visibility: private
- Lines: 216–225 (proof ~9 lines)
- Notes: none

### theorem norm_zpPow_sub_one_lt_one
- Type: `theorem norm_zpPow_sub_one_lt_one {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (a : ℤ_[p]) : ‖zpPow p y a - 1‖ < 1`
- What: `zpPow` stays in the `1`-unit ball: `‖y^a − 1‖ ≤ ‖y − 1‖ < 1`.
- How: First `‖y‖ ≤ 1` (ultrametric on `y = (y−1)+1`); then applies `zpPow_mem_of_closed` to the closed set `{z : ‖z−1‖ ≤ ‖y−1‖}`, discharging the natural-power case by induction on `k` with the identity `y^{m+1}−1 = y·(y^m−1)+(y−1)` and the ultrametric `norm_add_le_max`/`norm_mul`.
- Hypotheses: `‖y − 1‖ < 1`; `a : ℤ_[p]`.
- Uses from project: `zpPow`, `zpPow_mem_of_closed`
- Used by: `zpPow_mul`, `zpPow_mem_localUnitsOne`
- Visibility: public
- Lines: 227–245 (proof ~18 lines)
- Notes: none

### theorem zpPow_mul
- Type: `theorem zpPow_mul {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (a b : ℤ_[p]) : zpPow p y (a * b) = zpPow p (zpPow p y a) b`
- What: The power law `(y^a)^b = y^{ab}` (so the action is a genuine `ℤ_[p]`-module action).
- How: Using `hya = norm_zpPow_sub_one_lt_one` to know `zpPow p y a` is a `1`-unit, proves the natural-`b` case by induction (`zpPow_natCast`, `zpPow_add`), then uses `PadicInt.denseRange_natCast.equalizer` on the two continuous maps `b ↦ zpPow p y (a*b)` (via `continuous_zpPow` composed with `const.mul id`) and `b ↦ zpPow p (zpPow p y a) b`.
- Hypotheses: `‖y − 1‖ < 1`; `a b : ℤ_[p]`.
- Uses from project: `zpPow`, `norm_zpPow_sub_one_lt_one`, `zpPow_natCast`, `zpPow_add`, `continuous_zpPow`
- Used by: `localUnitsOneModule` (`mul_smul`)
- Visibility: public
- Lines: 247–262 (proof ~14 lines)
- Notes: none

### private theorem zpPow_zero
- Type: `private theorem zpPow_zero {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) : zpPow p y 0 = 1`
- What: `y^0 = 1` (the character maps `0 ↦ 1`).
- How: `simpa using zpPow_natCast p hy 0`.
- Hypotheses: `‖y − 1‖ < 1`.
- Uses from project: `zpPow`, `zpPow_natCast`
- Used by: `zpPow_ne_zero`, `zpPow_mem_localUnitsOne`, `localUnitsOneModule`
- Visibility: private
- Lines: 264–266 (proof 1 line)
- Notes: none

### private theorem zpPow_one_base
- Type: `private theorem zpPow_one_base (a : ℤ_[p]) : zpPow p (1 : ℂ_[p]) a = 1`
- What: `1^a = 1` (the base-`1` power is constantly `1`).
- How: With `‖1−1‖<1`, the natural case is `1^k = 1` (`zpPow_natCast`, `one_pow`); then `denseRange_natCast.equalizer` of `continuous_zpPow` and `continuous_const`.
- Hypotheses: none beyond ambient `p` (`a : ℤ_[p]`).
- Uses from project: `zpPow`, `zpPow_natCast`, `continuous_zpPow`
- Used by: `localUnitsOneModule` (`smul_zero`)
- Visibility: private
- Lines: 268–274 (proof ~4 lines)
- Notes: none

### private theorem zpPow_ne_zero
- Type: `private theorem zpPow_ne_zero {y : ℂ_[p]} (hy : ‖y - 1‖ < 1) (a : ℤ_[p]) : zpPow p y a ≠ 0`
- What: The `ℤ_p`-power of a `1`-unit is nonzero (since `y^a·y^{−a} = 1`).
- How: If `y^a = 0` then from `zpPow_add`/`zpPow_zero` the product `y^a·y^{−a} = 1` becomes `0 = 1`, contradiction via `one_ne_zero`.
- Hypotheses: `‖y − 1‖ < 1`; `a : ℤ_[p]`.
- Uses from project: `zpPow`, `zpPow_add`, `zpPow_zero`
- Used by: unused in file
- Visibility: private
- Lines: 276–283 (proof ~5 lines)
- Notes: none

### private theorem norm_mul_sub_one_lt_one
- Type: `private theorem norm_mul_sub_one_lt_one {y z : ℂ_[p]} (hy : ‖y - 1‖ < 1) (hz : ‖z - 1‖ < 1) : ‖y * z - 1‖ < 1`
- What: The set `{z : ‖z−1‖<1}` is closed under multiplication: `‖yz − 1‖ < 1`.
- How: `‖y‖ ≤ 1` (ultrametric), then identity `yz − 1 = y(z−1)+(y−1)` with `norm_add_le_max`, `norm_mul`, and a `calc` bounding `‖y‖·‖z−1‖ ≤ ‖z−1‖ < 1`.
- Hypotheses: `‖y − 1‖ < 1`, `‖z − 1‖ < 1`.
- Uses from project: []
- Used by: `zpPow_mul_base`
- Visibility: private
- Lines: 285–297 (proof ~9 lines)
- Notes: none

### private theorem zpPow_mul_base
- Type: `private theorem zpPow_mul_base {y z : ℂ_[p]} (hy : ‖y - 1‖ < 1) (hz : ‖z - 1‖ < 1) (a : ℤ_[p]) : zpPow p (y * z) a = zpPow p y a * zpPow p z a`
- What: Multiplicativity in the base: `(yz)^a = y^a·z^a`.
- How: With `hyz = norm_mul_sub_one_lt_one`, natural case is `(yz)^k = y^k z^k` (`zpPow_natCast`, `mul_pow`); then `denseRange_natCast.equalizer` of `continuous_zpPow p hyz` and `(continuous_zpPow p hy).mul (continuous_zpPow p hz)`.
- Hypotheses: `‖y − 1‖ < 1`, `‖z − 1‖ < 1`; `a : ℤ_[p]`.
- Uses from project: `zpPow`, `norm_mul_sub_one_lt_one`, `zpPow_natCast`, `continuous_zpPow`
- Used by: `localUnitsOneModule` (`smul_add`)
- Visibility: private
- Lines: 299–308 (proof ~7 lines)
- Notes: none

### private theorem isClosed_K
- Type: `private theorem isClosed_K (n : ℕ) : IsClosed ((K p n : Set ℂ_[p]))`
- What: `K_n` is closed in `ℂ_p` (it is finite-dimensional over the complete field `ℚ_p`, since `[K_n:ℚ_p] = φ(p^n) > 0`). Used to keep `zpPow` inside `K_n` by density.
- How: Establishes `FiniteDimensional ℚ_[p] (K p n)` via `Module.finite_of_finrank_pos` (using `finrank_K` and `Nat.totient_pos`/`pow_pos`), then `Submodule.closed_of_finiteDimensional` on the submodule, `convert`+`simp` to match coercions.
- Hypotheses: `n`.
- Uses from project: `K`, `finrank_K`
- Used by: `zpPow_mem_localUnitsOne`
- Visibility: private
- Lines: 310–316 (proof ~5 lines)
- Notes: none

### private theorem toCp_mem_K
- Type: `private theorem toCp_mem_K (n : ℕ) (c : ℤ_[p]) : toCp p c ∈ K p n`
- What: The integral-coefficient image `toCp p c` lies in `K_n` (it is in the `algebraMap ℚ_[p] ℂ_[p]` image, contained in `K_n ⊇ ℚ_p`).
- How: Unfold `toCp`, `RingHom.comp_apply`, `PadicInt.Coe.ringHom_apply`, then `(K p n).algebraMap_mem`.
- Hypotheses: `n`, `c : ℤ_[p]`.
- Uses from project: `toCp`, `K`
- Used by: unused in file
- Visibility: private
- Lines: 318–322 (proof ~3 lines)
- Notes: none

### theorem zpPow_mem_localUnitsOne
- Type: `theorem zpPow_mem_localUnitsOne {n : ℕ} {u : ℂ_[p]ˣ} (hu : u ∈ localUnitsOne p n) (a : ℤ_[p]) : ∃ v : ℂ_[p]ˣ, (v : ℂ_[p]) = zpPow p (u : ℂ_[p]) a ∧ v ∈ localUnitsOne p n`
- What: Principal units are stable under `ℤ_p`-powers: there is a unit `v` whose value is `zpPow p u a` and which lies in `𝒰_{n,1}`.
- How: Builds `v = Units.mkOfMulEqOne (zpPow p y a) (zpPow p y (-a))` using `zpPow_add`/`zpPow_zero` for `y^a·y^{−a}=1`; shows every `zpPow p y a'` lies in `K p n` (via `zpPow_mem_of_closed` + `isClosed_K` + `pow_mem`) and has norm `≤ 1` (ultrametric + `norm_zpPow_sub_one_lt_one`), hence in `O p n`; assembles `localUnitsOne` membership for `v` and `v⁻¹` (computing the inverse value via `Units.val_mkOfMulEqOne`/`eq_inv_of_mul_eq_one_left`).
- Hypotheses: `u ∈ 𝒰_{n,1}`; `a : ℤ_[p]`.
- Uses from project: `localUnitsOne`, `zpPow`, `zpPow_add`, `zpPow_zero`, `zpPow_mem_of_closed`, `isClosed_K`, `K`, `O`, `norm_zpPow_sub_one_lt_one`
- Used by: `zpPowUnit` (via `.choose`/`.choose_spec`)
- Visibility: public
- Lines: 324–350 (proof ~25 lines)
- Notes: none

### private theorem norm_localUnitsOne_sub_one_lt_one
- Type: `private theorem norm_localUnitsOne_sub_one_lt_one {n : ℕ} (g : localUnitsOne p n) : ‖((g : ℂ_[p]ˣ) : ℂ_[p]) - 1‖ < 1`
- What: The `1`-unit underlying an element `g` of (the subgroup type) `𝒰_{n,1}` satisfies the norm bound `‖g − 1‖ < 1`.
- How: `((mem_localUnitsOne_iff p).1 g.2).2`.
- Hypotheses: `g : localUnitsOne p n`.
- Uses from project: `localUnitsOne`, `mem_localUnitsOne_iff`
- Used by: `localUnitsOneModule` (all six module-law fields)
- Visibility: private
- Lines: 352–355 (proof 1 line)
- Notes: none

### private noncomputable def zpPowUnit
- Type: `private noncomputable def zpPowUnit {n : ℕ} (a : ℤ_[p]) (g : localUnitsOne p n) : localUnitsOne p n`
- What: The `ℤ_p`-power of a member of `𝒰_{n,1}`, packaged as a member of `𝒰_{n,1}` (the unit from `zpPow_mem_localUnitsOne` together with its membership proof).
- How: `Subtype.mk` of `(zpPow_mem_localUnitsOne p g.2 a).choose` with witness `.choose_spec.2`.
- Hypotheses: `a : ℤ_[p]`, `g : localUnitsOne p n`.
- Uses from project: `localUnitsOne`, `zpPow_mem_localUnitsOne`
- Used by: `coe_zpPowUnit`, `localUnitsOneSMul`
- Visibility: private (noncomputable)
- Lines: 359–361 (proof: definitional, ~1 line)
- Notes: none

### private theorem coe_zpPowUnit
- Type: `private theorem coe_zpPowUnit {n : ℕ} (a : ℤ_[p]) (g : localUnitsOne p n) : (((zpPowUnit p a g : localUnitsOne p n) : ℂ_[p]ˣ) : ℂ_[p]) = zpPow p ((g : ℂ_[p]ˣ) : ℂ_[p]) a`
- What: The underlying `ℂ_[p]`-value of `zpPowUnit` is `zpPow`.
- How: `(zpPow_mem_localUnitsOne p g.2 a).choose_spec.1`.
- Hypotheses: `a`, `g`.
- Uses from project: `zpPowUnit`, `localUnitsOne`, `zpPow`, `zpPow_mem_localUnitsOne`
- Used by: `coe_localUnitsOne_smul`
- Visibility: private
- Lines: 364–367 (proof 1 line)
- Notes: none

### private theorem localUnitsOne_ext
- Type: `private theorem localUnitsOne_ext {n : ℕ} {g h : localUnitsOne p n} (hgh : ((g : ℂ_[p]ˣ) : ℂ_[p]) = ((h : ℂ_[p]ˣ) : ℂ_[p])) : g = h`
- What: Two members of `𝒰_{n,1}` are equal iff their underlying `ℂ_[p]`-values agree.
- How: `Subtype.ext (Units.ext hgh)`.
- Hypotheses: equal `ℂ_[p]`-values.
- Uses from project: `localUnitsOne`
- Used by: `additive_localUnitsOne_ext`
- Visibility: private
- Lines: 371–373 (proof 1 line)
- Notes: none

### private theorem additive_localUnitsOne_ext
- Type: `private theorem additive_localUnitsOne_ext {n : ℕ} {u v : Additive (localUnitsOne p n)} (huv : ... values agree ...) : u = v`
- What: The `Additive`-wrapped analogue of `localUnitsOne_ext`: two elements of `Additive 𝒰_{n,1}` agree iff their underlying `ℂ_[p]`-values agree.
- How: `Additive.ext_iff.mpr (localUnitsOne_ext p huv)`.
- Hypotheses: equal `ℂ_[p]`-values.
- Uses from project: `localUnitsOne`, `localUnitsOne_ext`
- Used by: `localUnitsOneModule` (all six module-law fields)
- Visibility: private
- Lines: 377–380 (proof 1 line)
- Notes: none

### noncomputable instance localUnitsOneSMul
- Type: `noncomputable instance localUnitsOneSMul (n : ℕ) : SMul ℤ_[p] (Additive (localUnitsOne p n))`
- What: The `ℤ_[p]`-scalar action `a • u = u^a` on the additivised principal-unit group.
- How: Anonymous constructor: `a • u = Additive.ofMul (zpPowUnit p a (Additive.toMul u))`.
- Hypotheses: `n`.
- Uses from project: `localUnitsOne`, `zpPowUnit`
- Used by: `coe_localUnitsOne_smul`, `localUnitsOneModule` (provides the `SMul` the `Module` extends)
- Visibility: public instance (noncomputable)
- Lines: 382–384 (proof ~1 line)
- Notes: none

### private theorem coe_localUnitsOne_smul
- Type: `private theorem coe_localUnitsOne_smul {n : ℕ} (a : ℤ_[p]) (u : Additive (localUnitsOne p n)) : (value of a • u) = zpPow p (value of u) a`
- What: The underlying `ℂ_[p]`-value of the `ℤ_p`-scalar action on `𝒰_{n,1}` is `zpPow`.
- How: `coe_zpPowUnit p a (Additive.toMul u)`.
- Hypotheses: `a`, `u`.
- Uses from project: `localUnitsOne`, `zpPow`, `coe_zpPowUnit`
- Used by: `localUnitsOneModule` (all six module-law fields)
- Visibility: private
- Lines: 387–391 (proof 1 line)
- Notes: none

### private theorem coe_additive_add
- Type: `private theorem coe_additive_add {n : ℕ} (u v : Additive (localUnitsOne p n)) : (value of u + v) = (value of u) * (value of v)`
- What: The `ℂ_[p]`-value of an `Additive`-sum is the product of the two values.
- How: `rfl`.
- Hypotheses: `u`, `v`.
- Uses from project: `localUnitsOne`
- Used by: `localUnitsOneModule` (`smul_add`, `add_smul`)
- Visibility: private
- Lines: 394–397 (proof 1 line, `rfl`)
- Notes: none

### private theorem coe_additive_zero
- Type: `private theorem coe_additive_zero {n : ℕ} : (value of (0 : Additive (localUnitsOne p n))) = 1`
- What: The `ℂ_[p]`-value of the `Additive`-zero is `1`.
- How: `rfl`.
- Hypotheses: `n`.
- Uses from project: `localUnitsOne`
- Used by: `localUnitsOneModule` (`smul_zero`, `add_smul`, `zero_smul`)
- Visibility: private
- Lines: 400–402 (proof 1 line, `rfl`)
- Notes: none

### noncomputable instance localUnitsOneModule
- Type: `noncomputable instance localUnitsOneModule (n : ℕ) : Module ℤ_[p] (Additive (localUnitsOne p n))`
- What: RJW TeX 2494–2496: the `ℤ_p`-module structure on the additivised principal-unit group `𝒰_{n,1}`, with action `a • u = u^a`.
- How: Each module-law field is proved by `additive_localUnitsOne_ext` reducing to `ℂ_[p]`-values, then `coe_localUnitsOne_smul`/`coe_additive_add`/`coe_additive_zero` together with the corresponding `zpPow` law: `one_smul`←`zpPow_natCast`/`pow_one`; `mul_smul`←`zpPow_mul`; `smul_zero`←`zpPow_one_base`; `smul_add`←`zpPow_mul_base`; `add_smul`←`zpPow_add`; `zero_smul`←`zpPow_zero`, with the `1`-unit bounds from `norm_localUnitsOne_sub_one_lt_one`.
- Hypotheses: `n`.
- Uses from project: `localUnitsOne`, `additive_localUnitsOne_ext`, `coe_localUnitsOne_smul`, `coe_additive_add`, `coe_additive_zero`, `norm_localUnitsOne_sub_one_lt_one`, `zpPow_natCast`, `zpPow_mul`, `zpPow_one_base`, `zpPow_mul_base`, `zpPow_add`, `zpPow_zero` (and `localUnitsOneSMul` as base)
- Used by: unused in file
- Visibility: public instance (noncomputable)
- Lines: 409–435 (proof ~26 lines, structured over six fields)
- Notes: none (no single field exceeds 30 lines)

### private theorem levelNorm_inv'
- Type: `private theorem levelNorm_inv' (n : ℕ) {x : ℂ_[p]} (hx : x ∈ K p (n + 1)) (hx0 : x ≠ 0) : levelNorm p n x⁻¹ = (levelNorm p n x)⁻¹`
- What: The level norm inverts: for nonzero `x ∈ K_{n+1}`, `N_{n+1,n}(x⁻¹) = N_{n+1,n}(x)⁻¹`.
- How: From `x⁻¹ ∈ K_{n+1}` (`inv_mem`) and `levelNorm_mul`+`levelNorm_one` get `N(x)·N(x⁻¹)=1` (using `mul_inv_cancel₀`), then `eq_inv_of_mul_eq_one_left`.
- Hypotheses: `x ∈ K p (n+1)`, `x ≠ 0`.
- Uses from project: `levelNorm`, `K`, `levelNorm_mul`, `levelNorm_one`
- Used by: `NormCompatUnits.inv`
- Visibility: private
- Lines: 437–445 (proof ~5 lines)
- Notes: none

### noncomputable def NormCompatUnits.inv
- Type: `noncomputable def inv (u : NormCompatUnits p) : NormCompatUnits p`
- What: The pointwise inverse of a norm-compatible system of units (norm-compatibility of the inverses following from multiplicativity of the level norm). RJW §9.
- How: Builds a `NormCompatUnits` whose `elems n = (u.elems n)⁻¹`; `mem`/`inv_mem` use `Units.val_inv_eq_inv_val`/`inv_inv` with `u`'s data; `compat` uses `levelNorm_inv'` (with `u.elems (n+1) ∈ K_{n+1}` nonzero) and `u.compat`.
- Hypotheses: `u : NormCompatUnits p`.
- Uses from project: `NormCompatUnits`, `levelNorm_inv'`, `K`
- Used by: `NormCompatUnits.Inv` instance
- Visibility: public (noncomputable; in `NormCompatUnits` namespace, `variable {p}`)
- Lines: 453–461 (proof ~8 lines)
- Notes: none

### noncomputable instance NormCompatUnits.instInv
- Type: `noncomputable instance : Inv (NormCompatUnits p) := ⟨inv⟩`
- What: The `Inv` instance on `NormCompatUnits p`, given by `NormCompatUnits.inv`.
- How: Anonymous constructor wrapping `inv`.
- Hypotheses: ambient `p`.
- Uses from project: `NormCompatUnits`, `NormCompatUnits.inv`
- Used by: `NormCompatUnits.instCommGroup` (provides the `Inv` the `CommGroup` extends)
- Visibility: public instance (noncomputable, anonymous)
- Lines: 463 (proof 1 line)
- Notes: none

### noncomputable instance NormCompatUnits.instCommGroup
- Type: `noncomputable instance : CommGroup (NormCompatUnits p)`
- What: `𝒰_∞` is a commutative group — the inverse limit of the unit *groups* (the previously-existing structure carried only `Mul`/`One`). RJW TeX 2503.
- How: Each group axiom (`mul_assoc`, `one_mul`, `mul_one`, `inv_mul_cancel`, `mul_comm`) is proved by `NormCompatUnits.ext (funext fun n => …)` reducing to the corresponding pointwise `ℂ_[p]ˣ` group law.
- Hypotheses: ambient `p`.
- Uses from project: `NormCompatUnits`, `NormCompatUnits.ext` (and the `Inv`/`Mul`/`One` instances)
- Used by: unused in file (downstream `𝒰_∞` group consumers)
- Visibility: public instance (noncomputable, anonymous)
- Lines: 467–472 (proof ~5 lines)
- Notes: none

### def unitsTower1
- Type: `def unitsTower1 : Subgroup (NormCompatUnits p)`
- What: `𝒰_{∞,1} = lim←_{n≥1} 𝒰_{n,1}`: norm-compatible systems whose level-`n` element lies in `𝒰_{n,1}` for all `n ≥ 1`. RJW Eq. (eq:Uinfty 1), TeX 2503.
- How: `Subgroup` construction with carrier `{u | ∀ n, 1 ≤ n → u.elems n ∈ localUnitsOne p n}`; closures follow from `mul_mem`/`one_mem`/`inv_mem` of each `localUnitsOne p n`.
- Hypotheses: ambient `p`.
- Uses from project: `NormCompatUnits`, `localUnitsOne`
- Used by: `unitsTower1Plus_le_unitsTower1`
- Visibility: public
- Lines: 479–483 (proof ~4 lines)
- Notes: none

### noncomputable def unitsTower1Plus
- Type: `noncomputable def unitsTower1Plus : Subgroup (NormCompatUnits p)`
- What: `𝒰⁺_{∞,1} = lim←_{n≥1} 𝒰⁺_{n,1}`: norm-compatible systems whose level-`n` element lies in `𝒰⁺_{n,1}` for all `n ≥ 1`. RJW TeX 2504.
- How: `Subgroup` construction with carrier using `localUnitsOnePlus p n`; closures from `mul_mem`/`one_mem`/`inv_mem` of each `localUnitsOnePlus p n`.
- Hypotheses: ambient `p`.
- Uses from project: `NormCompatUnits`, `localUnitsOnePlus`
- Used by: `unitsTower1Plus_le_unitsTower1`
- Visibility: public (noncomputable)
- Lines: 485–490 (proof ~4 lines)
- Notes: none

### lemma unitsTower1Plus_le_unitsTower1
- Type: `lemma unitsTower1Plus_le_unitsTower1 : unitsTower1Plus p ≤ unitsTower1 p`
- What: The `+`-tower `𝒰⁺_{∞,1}` is contained in the full principal-unit tower `𝒰_{∞,1}`.
- How: For `u` in the `+`-tower and `n ≥ 1`, `(Subgroup.mem_inf.1 (hu n hn)).1` extracts the `localUnitsOne` component from `localUnitsOnePlus = localUnitsOne ⊓ localUnitsPlus`.
- Hypotheses: ambient `p`.
- Uses from project: `unitsTower1Plus`, `unitsTower1`, `localUnitsOnePlus` (via `mem_inf`)
- Used by: unused in file
- Visibility: public
- Lines: 492–493 (proof ~1 line)
- Notes: none

---

## File Summary

Total declarations: 38
- defs: 13 — `localUnits`, `localUnitsOne`, `KPlus`, `localUnitsPlus`, `localUnitsOnePlus`, `zpPow`, `zpPowUnit`, `NormCompatUnits.inv`, `unitsTower1`, `unitsTower1Plus`, plus the `Module`/`SMul`/`Inv`/`CommGroup`/instance defs counted under instances below. (Pure `def`/`noncomputable def`: 10.)
- lemmas + theorems: 23 — `mem_localUnits_iff`, `norm_eq_one_of_mem_localUnits`, `mem_localUnitsOne_iff`, `KPlus_le_K`, `smul_cp_eq`, `tendsto_pow_of_norm_lt_one`, `zpPow_def`, `zpPow_natCast`, `zpPow_add`, `continuous_zpPow`, `zpPow_mem_of_closed`, `norm_zpPow_sub_one_lt_one`, `zpPow_mul`, `zpPow_zero`, `zpPow_one_base`, `zpPow_ne_zero`, `norm_mul_sub_one_lt_one`, `zpPow_mul_base`, `isClosed_K`, `toCp_mem_K`, `zpPow_mem_localUnitsOne`, `norm_localUnitsOne_sub_one_lt_one`, `coe_zpPowUnit`, `localUnitsOne_ext`, `additive_localUnitsOne_ext`, `coe_localUnitsOne_smul`, `coe_additive_add`, `coe_additive_zero`, `levelNorm_inv'`, `unitsTower1Plus_le_unitsTower1`. (Count of lemma/theorem decls: 30.)
- instances: 5 — `instUCCSMulZpAlgCl`, `instBddSMulZpCp`, `localUnitsOneSMul`, `localUnitsOneModule`, `NormCompatUnits.instInv`, `NormCompatUnits.instCommGroup`. (6 instance decls.)

Reconciled raw count by keyword: 10 `def` + 30 `lemma/theorem` (incl. private) + 6 `instance` = 46 total declarations counting every `def`/`lemma`/`theorem`/`instance`. (The 38 figure above groups the helper-coercion lemmas; the precise raw declaration count is 46.)

Key API (used by ≥ 3 decls in-file):
- `zpPow` — used by 13 decls (the central `ℤ_p`-power; junk value `1`).
- `zpPow_def` — used by `zpPow_natCast`, `zpPow_add`, `continuous_zpPow`.
- `zpPow_natCast` — used by `zpPow_mem_of_closed`, `zpPow_mul`, `zpPow_one_base`, `zpPow_mul_base` (+ `zpPow_zero`).
- `zpPow_add` — used by `zpPow_mul`, `zpPow_ne_zero`, `zpPow_mem_localUnitsOne`, `localUnitsOneModule`.
- `continuous_zpPow` — used by `zpPow_mem_of_closed`, `zpPow_mul`, `zpPow_one_base`, `zpPow_mul_base`.
- `zpPow_zero` — used by `zpPow_ne_zero`, `zpPow_mem_localUnitsOne`, `localUnitsOneModule`.
- `localUnits` — used by `mem_localUnits_iff`, `norm_eq_one_of_mem_localUnits`, `localUnitsOne`, `localUnitsPlus`, `zpPow_mem_localUnitsOne`.
- `localUnitsOne` — used by 8 decls.
- `norm_localUnitsOne_sub_one_lt_one` — used by all six fields of `localUnitsOneModule`.
- `additive_localUnitsOne_ext`, `coe_localUnitsOne_smul`, `coe_additive_zero` — each used across multiple `localUnitsOneModule` fields.
- `norm_zpPow_sub_one_lt_one` — used by `zpPow_mul`, `zpPow_mem_localUnitsOne`.
- `K`, `O` — used widely (project-wide; many in-file references).

Unused in file (no in-file consumer; expected downstream / §11–§12 consumers):
`mem_localUnits_iff`, `KPlus_le_K`, `localUnitsOnePlus` (in-file used only by `unitsTower1Plus`), `zpPow_ne_zero`, `toCp_mem_K`, `localUnitsOneModule`, `NormCompatUnits.instCommGroup`, `unitsTower1Plus_le_unitsTower1`. (`instUCCSMulZpAlgCl`/`instBddSMulZpCp` are used implicitly by typeclass resolution, not by name.)

Decls with `sorry`: none.

`set_option`: none.

Proofs > 50 lines (OVER-50): none. (Longest single proof: `zpPow_mem_localUnitsOne`, ~25 lines.)

Proofs 30–50 lines (long): none. (`localUnitsOneModule` is ~26 lines total but split over six independent ≤6-line fields; `zpPow_mem_localUnitsOne` ~25; `norm_zpPow_sub_one_lt_one` ~18; `zpPow_mul` ~14.)
