FOCUS: T905 next — the d_I function. Setting: H : Ideal (restrictedMvPowerSeriesSubring k Ar). dI H I : WithTop N := sInf of {degAr (leadCoeff x) | x in H, x /= 0, leadIdx x = I} (WithTop-sInf, top if empty). Then dI_antitone: I1 <= I2 -> dI I2 <= dI I1 via monomial shift (T^(I2-I1) * x in H by ideal-mul; leadIdx shifts to I2 by leadIdxRPS_monomialShift + tsub_add_cancel_of_le; leadCoeff UNCHANGED: coeff at J+leadIdx of shift = coeff at leadIdx of f). Then S via Dickson. 111 commits pushed.
