FOCUS: D-iii-4b — stalk-level val_compat for yFrobHom. HAVE (8460f29a2): comap_limitFrobHom_openValue (the section-level equivariance) + comap_germ_stalkValue (StructureSheafStalks 831: comap (germ U v) (stalkValue v) = openValue U hvU) + stalkVle_intro/elim (535/563). PLAN: (1) ambient stalkValue-equivariance: comap ((ambient stalk transport at spaFrob k x)) (stalkValue x) = stalkValue (spaFrob k x) — where the ambient stalk transport = the colimit-map of ambientFrobNat; prove via Spv-ext + stalkVle intro/elim reducing to germs + comap_limitFrobHom_openValue; CHECK first how stalkVle_elim presents stalk elements (germ-representatives over shrinking opens). (2) the restrict-side: conjugation of ringStalkMap (yFrobHom k) x by yRingStalkEquiv to the ambient transport (restrictStalkIso naturality — the stalkPushforward/stalkFunctor plumbing; look for existing mathlib lemmas TopCat.Presheaf.stalkPushforward-naturality + restrictStalkIso_hom_germ or the project's YStalks yRingStalkIso construction to mirror). (3) assemble val_compat : yVPreObj.val (yFrobTop k x) = (yVPreObj.val x).comap (ringStalkMap (yFrobHom k) x).hom'. (4) isLocalHom: via the iso-route (yFrobHom is iso-components — build the PresheafedSpace-iso from k/-k or prove stalk-map bijective directly from componentwise equivs + colimit) OR direct: local-hom of a ring equiv between local rings. (5) VPreHom both ways + VObj-iso; then X. GERMS FIRST: read stalkVle_intro/elim + germ-api of spaRingPresheaf. Scratch r65. PERF doctrine now includes: no inline tactic proofs in literals; build is the kernel gate; chain component-lemmas not componentwise-rfl.
