FOCUS: W24 stage 3c — tateExtToFlat lands in Amb (IsRestrictedGauss 1-weights): joint-cofinite: {u : eps le |coeff u|} injects into Union over the FINITE set T := {t : eps le norm (coeff t F.1)} (outer null from F.2, metric) of per-t finite K-coeff sets (Amb-restrictedness of each WPA-coeff val); u recovered from (inl-part, inr-part) via slotEquiv-injectivity. Check IsRestrictedGauss-def-shape first (gaussNorm-null?) + WPA-IsRestricted vs norm-null bridge. Then 3d: surjectivity (unflatten g: coeff-t-of-preimage := the WPA-elt with K-coeffs g-at-interleave; nested-restricted from g-restricted) + corestrict ofBijective => tateExtEquiv.
