FOCUS: D-iv-3(gamma ii/iii) — stalk surjectivity + injectivity of ringStalkMap piYHom. HAVE (all pushed through the gamma-i commit): ringStalkMap_piYHom_germ + exists_invariant_extension + exists_disjoint_translates + the FrobeniusValuation germ toolbox (exists_common_rep, germ_limitRestrict, TopCat.Presheaf.germ_eq/germ_res_apply, stalk-exists-germ: (spaRingPresheaf ..).exists_germ_eq used in exists_common_rep — CHECK the analog for the RESTRICTED presheaf: every element of (yPresheafedSpace).ringPresheaf.stalk y is a germ: TopCat.Presheaf.exists_germ_eq generic? grep; likewise for curveSpace-stalk). PLAN r90 (surjectivity): theorem ringStalkMap_piYHom_surjective (y) : Function.Surjective (ringStalkMap (piYHom p F phi) y).hom: intro a; obtain (U, hyU, f, germ-eq) := exists-germ for the yPresheaf-stalk (generic TopCat.Presheaf.exists_germ_eq at the restricted presheaf); obtain (W0, hyW0, hdis) := exists_disjoint_translates y; W := U ⊓ W0 (disjointness inherits DOWNWARD: translates of a SUBSET stay disjoint — Disjoint.mono with the preimage-mono!); restrict f to yFunctor W; extension := exists_invariant_extension W hdis' (f-restricted) => t : frobFixed (xImage W) + the zero-piece property; CLAIM: M (xGerm (xImage W) (pi-y) (mem: y in W -> pi y in xImage) t) = a: by ringStalkMap_piYHom_germ: = yGerm (curvePreimage(xImage W)) y (t.1); relate to a = yGerm U y f: both germs agree because t.1 restricted to the ZERO piece = f-restricted (the extension property) and y is in the zero-piece-related open: germ-chase: yGerm-sat-of-t.1 = [germ_limitRestrict at translate-0-le] yGerm-translate-0-of (restrict t.1) = [zero-piece property] yGerm-translate-0-of (restrict-of-f-restricted) = [germ_limitRestrict twice] yGerm U f = a — the MEMBERSHIP of y in translate-0: y in (Opens.map (yFrobTop 0)).obj W <- map_yFrobTop_zero + y in W (hyU, hyW0) ✓. PLAN r91 (injectivity): from M-germs-equal: the y-germs of t.1, t'.1 equal => equal on small W' <= their common domain (TopCat.Presheaf.germ_eq); shrink by wandering; then invariant_sections_agree: two frobFixed-sections agreeing on the zero-piece of W' agree on-the-nose after restriction to the saturation of W' — via the separation at the translate cover (per-piece: the k-th restrictions equal by the invariance-recurrence replay: piece-k-value = limitFrobHom-k-of-zero-piece-value modulo collapses — derive from mem_frobFixed ITERATED?? the k-th piece of an invariant section: hInv gives the 1-step; k-step by...: the per-piece values of an invariant g are DETERMINED by the 0-piece: prove invariant_piece_determined (k) : limitRestrict (piece-k-le) g = translateFam W (restrict-0-of-g) k?? — that's glue_piece-style INDUCTION on k (both directions ±) using the succ-recurrence — an ℤ-induction (Int.induction_on) with the succ-step = the glue_piece_eq-machinery and the neg-step the (-1)-analog (needs translateFam_pred or reuse succ at shifted m — the succ-form relates m and 1+m: for the downward direction use succ at m := k-1 backwards ✓ same lemma!). Substantial but mechanical. THEN x-stalk equiv + local/val transport + D-iv-4 sheaf + xVObj. Scratch r90 first (surjectivity).
