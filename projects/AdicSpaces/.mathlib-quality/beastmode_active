FOCUS: W21a (ii) cont — PushedHeadCover structure (M, base-model, piece-models, all at stage M — exists via exists_common_headModel_stage) then the RationalCoveringData (WPHead M)-instance: base := Cb.DH-cast, covers := image of piece-DHs; hsubset/hcover via comap_rhoHead_mem_iff + models .hopen + C.hsubset/hcover (pull head-point back, transfer memberships). CAST-NOTE: models are at .N = M propositional — either cast DH along the eq (motive-friction) or restate the structure with N fixed to M from the start (BETTER: build the structure with M-typed fields directly from the ∃-forms at construction time). Foundation DONE+committed: rhoHead continuity/pb-transfer/Spa-map/mem-iff.
