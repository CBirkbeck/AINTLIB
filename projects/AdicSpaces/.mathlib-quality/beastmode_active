FOCUS: implement sol-resolution step 1 — tailValueF/headBoundF defs + the A_N digit bound (scaling proof: prefix-pair-sum = [c]*(W(O_F)-image), coords scaled; M_N = 0 degenerate case separately) into WittF.lean.
