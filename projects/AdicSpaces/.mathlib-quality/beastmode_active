FOCUS: T903 c0-arch (C-limit) — Valued.v (PhiHatK b) = ⨆ n, ρⁿ|bₙ| for decaying b: (≤) each prefix-value = finite-max ≤ sup (wAloc_prefixAloc + valued_AlocToHatK); v-of-limit ≤ sup via closed-balls-in-hatK?? balls in hatK: {v ≤ r}-closed: SAME subgroup-argument as isClosed_ball but in hatK — OR via eventually_wAloc_eq-style isosceles: if v(Φb) > sup: the prefix-differences eventually < v(Φb) ⟹ v(prefix) = v(Φb) > sup — contra prefix-value ≤ sup ✓ ISOSCELES-ONLY no balls: (≥) sup-terms: each ρⁿ|bₙ| = prefix-value-attained-at-large-N?? term-n ≤ max-of-prefix-(n<N) = v(prefixN)-when-attained... term-n ≤ finite-max(N) = wAloc(prefixN) = v(image-prefixN) ≤ max(v(Φb), v(image-prefixN − Φb))-isosceles-again: for N-large the diff < term-n-target... assemble: both directions via the isosceles + prefix-formula. At 70 commits pushed.
