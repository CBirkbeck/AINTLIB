FOCUS: W24 stage 3a — wpWeight_shiftWeight_eq (w s u): wpWeight (shiftWeight w s) u = wpWeight w (slotInr s u) where slotInr s u := comapDomain inr (equivMapDomain (slotEquiv s) u) with slotInr_apply : = u (if m=0 then 0 else m+s). Route: unfold both wpWeight-sums (Sigma over t.support of if t n % 2 = 1 and n ne 0 then weight n else 0); LHS: split shiftWeight-if (n le s gives 0) so only n gt s survive with w (n-s); RHS: m=0-term dies (n ne 0 fails), m ge 1 has slotInr-value u (m+s); equate by Finset.sum_nbij (n mapsto n-s / m mapsto m+s) over filtered supports OR sum over the COMMON superset u.support union image — mind support-membership both ways (slotInr-support: m mem iff u(m+s) ne 0 for m ge 1, u 0 ne 0 for m=0). THEN 3b support-transport (WPMem-shift via this + slotInr_zero_apply), 3c restrictedness joint-cofinite, 3d surjectivity + ofBijective => tateExtEquiv. All committed through stage 2b; tateExtToFlat_coeff is the tool; w24f.py in scratchpad is an UNAPPLIED draft.
