FOCUS: T905 last piece — the Groebner set S: the FINITE set of <=-minimal elements of {I | dIdx H I < top}. Route: Dickson via WellQuasiOrderedLE (Fin k ->0 N) instance (mathlib Finsupp.wellQuasiOrderedLE); need "a WQO up-set has finitely many minimal elements" — search mathlib for Set.IsPWO/IsWF minimals-finite API (Set.IsPWO.exists_finite_antichain? Set.Finite of minimals?) with #check probes BEFORE writing; if absent, prove: minimals form an antichain; an infinite antichain contradicts WQO (extract an infinite sequence, apply wqo to get i<j with x_i <= x_j, contra). Then S := union over the finitely many d-levels; choose x_I. Then T906. 112 commits pushed.
