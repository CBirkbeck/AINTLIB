FOCUS: CRUX step 2 — the exact tail identity + the moving-prefix estimate (1): T_N(x+y) ≤ max(T_N x, T_N y, ρ^N·M_N) via prefix-of-sum = prefix-of-A_N (truncation congruence), p^N-cancellation (need coe_p_ne_zero over W(F): coeff-1=1 proof + NoZeroDivisors W(F) from F-domain instance), 3-way gaussValueF_add_le. Then H-decay split-max; +-closure; (9); w-closedness; mult; decaySubring; ArSub-application. All specs in decomposition-laneB CRUX-section + archived sol reply.
