FOCUS: W21a recon — nonempty_headModelData_family design: re-run W18 with a floor stage M (exists_head_approx_finset already takes arbitrary m-precision; the W18-proof is per-datum — parametrize the assembly by a REQUIRED minimum stage: N := max(floor, sup of approx stages) works since exists_head_approx_finset的 sup-N absorbs any floor via max). Check W18-proof-shape for the floor-insertion point, then state family-version.
