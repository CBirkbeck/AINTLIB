FOCUS: T906 next — the epsilon for a finite generator family, then the STEP lemma. eps design: given a Finset G of nonzero restricted generators, for each g pick eps_g < gaussNormRPS g from exists_tail_bound_lt, set r_g := eps_g / gaussNormRPS g < 1, eps := G.sup r_g (< 1 since finite; if G empty eps := 1/2 or anything). Property needed: for g in G and J degLex-succ leadIdx g: v(coeff J g) <= eps * v(leadCoeff g). Then the step lemma (subtract z * T^(J-I) * x_I; three conclusions: norm non-increase, coeff-difference <= eps*|y| at indices succ J, and the J-coefficient is the division remainder with smaller degAr). Uses: exact_division, coeff_monomialShift, degLex translation-invariance (toSyn AddEquiv + covariant add). 116 commits pushed.
