FOCUS: T905 remaining: (3) leadIdxRPS_monomialShift = J + leadIdx (attainSetRPS(shift) = image (J + .) of attainSetRPS f via coeff_monomialShift + norm-invariance; degLex translation-invariance: m.toSyn is AddEquiv, syn is ordered-cancel: add_le_add_iff_left; then max-of-translated-image); then dI : (Fin k ->0 N) -> WithTop N over an Ideal H of restrictedMvPowerSeriesSubring k Ar (sInf of degAr-of-leadCoeff image; note H-elements are SUBTYPE-pairs: series + restrictedness); antitone dI via the shift trio (x_I in H with leadIdx I => monomial-shift stays in H (ideal mul) with leadIdx I+D, same leadCoeff hence same degAr); S := union over d of <=-minimals of {dI = d} (Dickson: WellQuasiOrderedLE (Fin k ->0 N) instance exists; need finite-minimals-lemma — search Set.Finite minimal antichain API or prove via wqo no-infinite-antichain). Then T906 Lemma 3.8. 110 commits pushed, board has full design.
