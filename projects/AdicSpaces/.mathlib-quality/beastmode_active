FOCUS: D-iv-3(gamma) — the stalk bijectivity of ringStalkMap piYHom. HAVE: exists_invariant_extension (5b4c39f5c) + exists_disjoint_translates + all germ machinery (yRingStalkIso_hom_germ, germ_limitRestrict, ringStalkMap-germ-naturalities from FrobeniusValuation) + piYHom (the projection morphism, CurveObject ~piComponent). PLAN (scratch r89): the stalk map M := ringStalkMap (piYHom p F phi) y : (curveSpace-ringStalk at yTopToCurve y) ->+* (yPresheafedSpace-ringStalk at y). (i) germ-naturality of M: M (xPresheaf-germ V c hV t) = yPresheaf-germ (curvePreimage V) y hV' (t.1) — the c-component is the subtype-inclusion piComponent; mirror ringStalkMap_yFrob_germ's proof (hunfold-rfl + h1 + h2 + hsplit-trans-chain) at piYHom. (ii) SURJECTIVITY: a germ a of yPresheaf at y: represent on U ∋ y; SHRINK U to W := U ⊓ (wandering-nbhd of y) (exists_disjoint_translates); restrict the rep to W; the invariant extension gives t : frobFixed (xImage W) with the zero-piece restriction matching; then M (xGerm of t at pi y ∈ xImage W) = yGerm of t.1 restricted = [the zero-piece property + germ_limitRestrict-collapses + the mem-translate-0: y ∈ translate-0-piece via yFrobTop_zero] the original germ a ✓. (iii) INJECTIVITY: M t-germ = M t'-germ => the y-germs of t.1 t'.1 agree => agree on a small W' ∋ y => [invariance of both + separation over the translate cover of the saturation of W'] the x-germs agree — needs: two invariant sections agreeing on the zero piece agree on the whole saturation (the SEPARATION-half analog of the extension: per-piece agreement propagates by the invariance-recurrence — the same glue_piece_eq-style computation with the two sections) — write invariant_sections_eq_of_zero_piece lemma. THEN (3ii-e): isLocalRing + val on the curve stalks TRANSPORT through the bijective M (mirror yVPreObj's construction: xVPreObj with val := comap M.inv-of-stalkValue-y ... or directly: the stalk-equiv from bijectivity (RingEquiv.ofBijective) + isLocalRing/val_supp transported like isLocalRing_yStalk/maximalIdeal_comap_ringEquiv did); THEN the X sheaf-condition (D-iv-4: from the yVObj sheaf + the invariants-equalizer — possibly via the SAME IsSheafOfTopologicalRings-replay pattern as YSheaf, with the saturated covers) and xVObj (D-iv-5). Scratch r89: start with (i) the germ-naturality of piYHom.
