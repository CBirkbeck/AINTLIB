FOCUS: T905 next — the leading index: degLex-maximal element of the attainment set {s : v(coeff s f) = gaussNormRPS f} (nonempty by attainment, finite via the eps = norm-half superlevel set); use MonomialOrder.degLex (Fin k) — its syn-order; leadIdxRPS def + spec (attains + degLex-dominates attainers). Then leading coefficient, d_I, monomial-mult monotonicity. 107 commits pushed.
