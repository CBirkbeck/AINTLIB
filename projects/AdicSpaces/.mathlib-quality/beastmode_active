FOCUS: T904 Lemma 2.8 remainder: (S4) the one-step lemma — v(y - divStep*x) <= v y (easy from valued_divStep_le + map_mul); THEN the exact-tail identity y - z*x decomposition: z*p^m*[x_m]-part recombines to the m-tail of y EXACTLY (Phi-level identity needed: divStep * teichHatK-of-x_m * p^m = tail-part — design via prefix-level identities + limits); then the N_l-descent with the w-coefficient support bound (head < m, conv-part <= N_l - 1, DC+ transfer, rho <= eps); then the well-founded iteration + Lemma 2.8 statement; Prop 2.9; Cor 2.10. 98 commits pushed.
