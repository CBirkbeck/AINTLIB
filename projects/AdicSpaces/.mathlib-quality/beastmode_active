FOCUS: D-iii-4c — the restrict-side conjugation for val_compat. HAVE (c4d199fbd): comap_ringStalkMap_ambientFrob_stalkValue (ambient equivariance, axiom-clean) + yVPreObj.val = comap (yRingStalkEquiv x) (stalkValue (ySpaPoint x)) (YStalks ~323). GOAL: val_compat for yFrobHom: yVPreObj.val (yFrobTop k x) = (yVPreObj.val x).comap (ringStalkMap (yFrobHom k) x).hom'. REDUCTION: suffices the CONJUGATION SQUARE: (yRingStalkEquiv x).toRingHom COMP (ringStalkMap (yFrobHom k) x).hom' = (ringStalkMap (ambientFrobHom k) (ySpaPoint x)).hom COMP (yRingStalkEquiv (yFrobTop k x)).toRingHom — as ring homs on the restricted stalk of the image point (check direction/variance carefully against ringStalkMap's contravariance) — then val_compat = comap-comp-chains + the ambient equivariance + ySpaPoint-naturality (ySpaPoint (yFrobTop k x) = spaFrob k (ySpaPoint x) — should be defeq/subtype-ext). The square: both sides are stalk-maps of the RESTRICTED vs AMBIENT presheaves along yIncl; yRingStalkEquiv was built from AlgebraicGeometry restrictStalkIso (YStalks yRingStalkIso ~273); the square is the stalkMap-naturality of the restrict-inclusion morphism — prove via TopCat.Presheaf.stalk_hom_ext (germ-level: both composites on a germ = germ of the transported section — using ringStalkMap_ambientFrob_germ + the yFrobNat-component-def + restrictStalkIso-germ lemmas (mathlib: PresheafedSpace.restrictStalkIso_hom_eq_germ?)). READ yRingStalkIso/yRingStalkEquiv defs in YStalks first (~253-323) + mathlib restrictStalkIso germ-lemmas. Scratch r66. Doctrine: ConcreteCategory.hom-spelling; composite-presheaf spelling; lemma-chains only; the BUILD is the gate.
