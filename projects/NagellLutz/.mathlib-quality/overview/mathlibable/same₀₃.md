# /mathlibable report — `EllSequence.HaveSameParity₄.same₀₃`

_Generated by `/overview` Step-9 mathlibable assessment (full 10-phase workflow), NagellLutz project._

### Baseline (Phase 0)
- lake build:               ⚠ not re-run (local build stale per task note); decl reasoned from source. The decl elaborates in the integrated tree (it is referenced by two downstream proofs, lines 468/470, which type-check on `main`).
- decl `EllSequence.HaveSameParity₄.same₀₃`:  ✓ resolved at `projects/NagellLutz/LutzNagell/EllipticDivisibilitySequence.lean:235`
- kind:                      lemma
- has sorry:                 no
- module docstring summary:  Defines elliptic divisibility sequences (EDS) over a commutative ring and constructs normalised EDSs from initial terms; this file is a project fork/extension of `Mathlib.NumberTheory.EllipticDivisibilitySequence` plus the division-polynomial four-index relation machinery.

**Qualified name verified.** The base name in the prompt was `same₀₃`; the true fully-qualified name is `EllSequence.HaveSameParity₄.same₀₃` (namespace stack: `EllSequence` opened at line 90 → `HaveSameParity₄` opened at line 216; decl at line 235; both closed by `end HaveSameParity₄` at 297 / `end EllSequence` at 597). The prompt's guessed qualified name matches.

---

### Statement (Phase 1)

`EllSequence.HaveSameParity₄.same₀₃` states: given four integers `a b c d` that "have the same parity" (in the sense of the project-local predicate `HaveSameParity₄ a b c d`), the two *endpoints* `a` and `d` have equal `Int.negOnePow` (i.e. `(-1)^a = (-1)^d` in `ℤˣ`, equivalently `a ≡ d (mod 2)`).

It is an **accessor lemma**: `HaveSameParity₄ a b c d` is *defined* (line 210) as the conjunction of the three *consecutive* equalities
`a.negOnePow = b.negOnePow ∧ b.negOnePow = c.negOnePow ∧ c.negOnePow = d.negOnePow`,
and `same₀₃` derives the *endpoint* equality `a.negOnePow = d.negOnePow` from those three by transitivity.

Variables / typeclasses involved (Lean side):
- `{a b c d : ℤ}` — four integer indices (section variables, lines 204/219).
- `R`, `W : ℤ → R`, etc. are in scope but **unused** by this lemma (it is purely about the integers).

Hypotheses (Lean side):
- `(same : HaveSameParity₄ a b c d)` — the bundled "same parity" hypothesis (`include same`, line 220).

Conclusion (math): `a ≡ d (mod 2)`, expressed as `(-1)^a = (-1)^d`.
Conclusion (Lean): `a.negOnePow = d.negOnePow`.
Proof body: `by rw [same.1, same.2.1, same.2.2]` — rewrite by the three conjuncts in sequence (transitivity).

---

### Size classification (Phase 2a)

Verdict: **SMALL**
Reason: A one-line accessor lemma over a project-local `def`; not a named theorem, not a `## Main results` entry, not a new structure. (Literature width is EXHAUSTIVE regardless; recorded for framing.)

### One-line check (Phase 2b)

Body line count: 1 substantive line (`by rw [...]`).
One-liner verdict: **n/a** — kind is `lemma`, not `def`/`abbrev`/`structure`. The 2b def-exemption table does not apply to lemmas. (Noted: it *is* a one-line lemma, which is itself a weak negative signal — see Phase 6.)

---

### Literature search table — EXHAUSTIVE protocol (Phase 3)

| #  | Channel                          | Query                                                                                                  | Hit? | Standard form found              | Notes |
|----|----------------------------------|--------------------------------------------------------------------------------------------------------|------|----------------------------------|-------|
|  1 | WebSearch (specific form)        | EDS division polynomial "same parity" indices four-index relation transformation Angdinata             | no   | —                                | Returned Wikipedia EDS + standard EDS papers (Stange/Shipsey/Silverman); none names a "same-parity-of-four-indices" predicate or an endpoint-parity accessor. |
|  2 | WebSearch (general form)         | "negOnePow" / "(-1)^n transitivity" parity equivalence integers (math sense: `a≡b ∧ b≡c ⇒ a≡c`)        | no   | `Int.ModEq` is transitive (`Trans`/`Eq.trans`) | The general fact is just transitivity of an equivalence relation — universally "known", not a citable named result. |
|  3 | WebSearch (named-after / aliases)| "shift operator" / EDS recursion parity normalisation; "Ward sequence" parity of indices               | no   | —                                | The EDS literature (Ward 1948, Shipsey, Stange) discusses parity of *individual* terms `W_n` and the recursion, never a 4-tuple "same parity" predicate or its endpoint accessor. |
|  4 | ChatGPT MCP                      | (intended) "standard math form of an endpoint-parity accessor over a same-parity-of-four predicate?"   | n/a  | —                                | MCP down per task environment note; substituted by an extra WebSearch generality pass (#2) + nLab (#6). The mathematical content (transitivity of `≡ mod 2`) is elementary and well-determined without it. |
|  5 | Local references                 | grep `projects/NagellLutz/.mathlib-quality/references/`                                                 | n/a  | —                                | Directory absent (`.../references/` does not exist) — recorded n/a. |
|  6 | nLab                             | "parity" / equivalence-relation transitivity                                                           | n/a  | —                                | Not a categorical/structural concept; nLab has no page for "same parity of four integers". The underlying notion (transitivity of an equivalence relation) is foundational, not an nLab topic. |
|  7 | nCatLab                          | —                                                                                                      | n/a  | —                                | Not a categorical concept. |
|  8 | Stacks Project                   | —                                                                                                      | n/a  | —                                | Not an algebraic-geometry concept (elementary integer parity bookkeeping). |
|  9 | MathOverflow / Math.StackExchange| transitivity of "same parity" relation on integers                                                     | n/a  | —                                | The statement `a≡b, b≡c, c≡d ⇒ a≡d (mod 2)` is folklore-trivial; no MO discussion treats it as a result. |
| 10 | recent arXiv (last 5 years)      | elliptic divisibility sequence division polynomial formalisation parity indices                        | no   | —                                | No arXiv paper introduces this predicate; it is an artifact of *this Lean formalisation's* four-index-relation transformation (`HaveSameParity₄`, `avg₄`, `rel₄_transf`), authored by D. K. Angdinata for the EDS/division-polynomial development. |

### Literature summary (Phase 3)

Concept identified as: **transitivity of the "same parity" (≡ mod 2) relation across a chain**, specialised to extract the endpoint equality from three consecutive ones. There is **no named mathematical concept** corresponding to `same₀₃`; it is an accessor over the bespoke predicate `HaveSameParity₄`.
Sources agree on the standard form: **n/a** — there is no "standard form" because there is no named result. The only general fact in play is that congruence mod 2 (equivalently `Int.negOnePow` equality) is transitive — which mathlib encodes structurally (`Eq.trans`, and `Int.ModEq` is `Trans`).
Most general standard form: the endpoint equality is `Eq.trans (Eq.trans h₁ h₂) h₃` for any three composable equalities `h₁ h₂ h₃` in any type — nothing parity-specific.
Generality dimensions where the literature varies: none meaningful — the statement is a transitivity composition; its only "content" is which conjuncts of a local conjunction to chain.
Disagreement with the literature: none — the literature simply has no such micro-result; this is internal API for the four-index relation transformation.

---

### Generality analysis — `EllSequence.HaveSameParity₄.same₀₃` (Phase 4)

Literature-standard form (from Phase 3): there is none; the underlying fact is transitivity of equality (`Eq.trans` chain), which is maximally general already and lives in core.

| # | Parameter / hypothesis            | Current Lean form                         | Literature-standard form | Weaker form exists? | Reason it can/can't be weakened |
|---|-----------------------------------|-------------------------------------------|--------------------------|---------------------|----------------------------------|
| 1 | `{a b c d : ℤ}`                   | four integers                             | n/a                      | NO (meaningfully)   | The statement is *defined* in terms of `Int.negOnePow`, so `a..d : ℤ` is intrinsic. One *could* abstract to "any three transitively-composable equalities", but that is just `Eq.trans` and already in core. |
| 2 | `(same : HaveSameParity₄ a b c d)`| bundled 3-conjunction of `negOnePow` eqs  | n/a                      | n/a                 | The hypothesis *is* the project-local predicate; the lemma exists only to unbundle it. Cannot be "weakened" without ceasing to be this lemma. |

### Generality verdict (Phase 4b)

The current form is: **MAXIMALLY GENERAL within its remit** (the remit being "accessor for `HaveSameParity₄`"), but that remit is **inherently project-local** — the statement names a predicate (`HaveSameParity₄`) that does not and should not exist in mathlib.
Number of weakening opportunities found: 0 that keep it the same lemma. The only "generalisation" is to discard the predicate and use `Eq.trans` directly — which is not a generalisation of *this* lemma but its dissolution into a core primitive.
Proposed restatement: none (any restatement that removes `HaveSameParity₄` is just `Eq.trans`).
Cost of restatement: CHEAP — but it is inlining, not restating (see Phase 6).

### Modern-idiom check (Phase 4c)

| #  | Question                                                                                       | Applies? | Proposed reformulation | Mathlib downstream |
|----|------------------------------------------------------------------------------------------------|----------|------------------------|--------------------|
|  1 | "let X be a foo" preambles → typeclasses/instances?                                            | no       | —                      | The hypothesis is a one-off conjunction, not a structure deserving a class. |
|  2 | sequences/metric → filters/topology?                                                           | no       | —                      | No analytic content; pure integer parity. |
|  3 | construct object → universal-property class?                                                   | no       | —                      | Nothing constructed. |
|  4 | set-with-closure-predicate → bundled substructure?                                             | no       | —                      | `HaveSameParity₄` is a finite conjunction, not a closure predicate. |
|  5 | vector-space/metric/field-specific → weaken typeclasses?                                       | no       | —                      | No typeclass content (it is about `ℤ`). |
|  6 | 1-categorical → higher-categorical?                                                            | no       | —                      | Not categorical. |
|  7 | concrete index (ℕ/ℤ/ℝ) → arbitrary additive group/monoid?                                      | no       | —                      | `Int.negOnePow` is ℤ-specific by definition; the "index" here is not a summation index to generalise. |

### Modern-idiom verdict (Phase 4c)

Modern idiom available: **no**.
Reason: There is no modernisation move — the lemma is a transitivity composition over a bespoke conjunction. The contemporary-mathlib form of "transitivity of equality" already exists as `Eq.trans` in Lean core; there is nothing to reorganise.

---

### Diamond / defeq risk (Phase 4.5)

**n/a — declaration kind is `lemma`.** Lemmas introduce no definitional equalities or typeclass-search paths. (For the record, the *parent* `HaveSameParity₄` is a `def : Prop` and was risk-assessed separately in `HaveSameParity₄.md`; that does not bear on this lemma.)

---

### Mathlib search-status: `EllSequence.HaveSameParity₄.same₀₃` (Phase 5)

[A] Lean-Finder       "from a≡b, b≡c, c≡d derive a≡d for negOnePow / mod 2"   no hit — only the structural `Eq.trans` / `Trans.trans` and `Int.ModEq.trans` come up; none is this predicate-accessor.
[B] Loogle            `Int.negOnePow _ = Int.negOnePow _ → Int.negOnePow _ = Int.negOnePow _ → _`  no hit for a 3-step chained accessor; the relevant atoms are `Int.negOnePow_eq_iff`, `Int.negOnePow_neg`, `Int.negOnePow_sub` (see `Mathlib/Algebra/Ring/NegOnePow.lean:86,91,94,98`).
[C] LeanSearch        "same parity of four integers endpoints equal", "negOnePow transitivity"  no hit — mathlib has no "same parity of four" predicate (confirmed: `grep -rl "SameParity\|sameParity" Mathlib/` → 0 files).
[D] Grep mathlib src  `HaveSameParity` / `same₀₃` in `Mathlib/NumberTheory/EllipticDivisibilitySequence.lean` and tree-wide  **0 hits** (the upstream EDS file, 547 lines, has neither; the project's `HaveSameParity₄`/`avg₄`/`rel₄_transf` machinery is *not* upstreamed).
[E] Name pattern      `same₀₃`, `negOnePow.*trans`                          no hit in mathlib; `negOnePow_mul_self`, `negOnePow_eq_iff` exist but are unrelated.

Searched for both:
  - the user's current form (`HaveSameParity₄ … → a.negOnePow = d.negOnePow`) → not present; the predicate itself is absent from mathlib.
  - the literature-standard form → there is none beyond transitivity of `=`, which is `Eq.trans` (Lean core) and `Int.ModEq.trans` (`Mathlib/Data/Int/GCD.lean` / ModEq API).

Concluded: **not in mathlib** (all 5 methods exhausted, plus the upstream EDS file checked directly). The *building blocks* (`Eq.trans`, and the `negOnePow` API in `Mathlib/Algebra/Ring/NegOnePow.lean`) are present; the exact form is a project-local accessor over a project-local predicate.

---

### Call sites — `EllSequence.HaveSameParity₄.same₀₃` (Phase 6.0)

Internal use count (NagellLutz project, excluding declaring line 235): **2** (both in the declaring file).
External-to-file callers: **0 distinct files** within NagellLutz.

| Caller file:line                                                              | Usage pattern (one-line excerpt)                                  |
|-------------------------------------------------------------------------------|-------------------------------------------------------------------|
| projects/NagellLutz/LutzNagell/EllipticDivisibilitySequence.lean:468          | `obtain rfl : dMin a = d := (dMin_le same.same₀₃ anti.1).antisymm …` |
| projects/NagellLutz/LutzNagell/EllipticDivisibilitySequence.lean:470          | `rw [← negOnePow_eq_iff, negOnePow_cMin, same.same₀₃]`             |

Inline-derivation grep (was the equivalent re-derived without `same₀₃`?):
  - **Yes — line 463**, same file: `rw [negOnePow_dMin, same.1, same.2.1, same.2.2]` inlines the very same three-conjunct transitivity (here to reach `d`, i.e. exactly what `same₀₃` packages) without calling `same₀₃`. This is direct evidence the "lemma" is a trivially-inlinable convenience: a consumer in the *same proof block* open-codes it. (Also note: duplicated verbatim in the consolidation forks `EllipticDivisibilitySequenceOriginal.lean:227` and `HasseWeil/.../EllipticDivisibilitySequence.lean:190`, each with the identical 2 call sites — a dedup target on `main`, orthogonal to mathlib.)

Call-sites read (per the Phase 6 signal table): K = 2 internal uses, AND the same statement is open-coded inline at ≥1 nearby site (line 463). That pattern points to **NO-composable** — a thin convenience wrapper that consumers bypass.

### Composition check (Phase 6)

Can `same₀₃` be derived from mathlib (+ the local predicate's projections) in ≤3 chained calls?

Attempt 1: `same.1.trans (same.2.1.trans same.2.2)`
  - Decls used: `And.left`/`And.right` projections (`same.1`, `same.2.1`, `same.2.2`) + `Eq.trans` (Lean core).
  - Result: **succeeds** — this is a 2-`Eq.trans` chain over the three conjuncts; exactly equivalent to the `rw [same.1, same.2.1, same.2.2]` proof body. (Indeed the sibling lemma `rel₄_eq_net` at line 226 already writes `same.1.trans (same.2.1.trans same.2.2)` inline for this very endpoint equality.)
  - Notes: no new lemma needed; it is a one-term composition of a core primitive with the hypothesis's own projections.

Conclusion: **COMPOSABLE** (≤3 calls; in fact a single `Eq.trans`-chain term, already open-coded at lines 226 and 463).

---

## Verdict: `EllSequence.HaveSameParity₄.same₀₃`

**Category:** NO-composable-from-mathlib

**Evidence:**
- Literature search (Phase 3): no named concept; the only underlying fact is transitivity of `=` / `≡ mod 2`. EDS literature (Wikipedia, Ward, Stange, Shipsey) has no "same-parity-of-four-indices" predicate or endpoint accessor.
- Generality analysis (Phase 4): MAXIMALLY GENERAL within its remit, but the remit is inherently project-local (statement names `HaveSameParity₄`); no modern-idiom move (4c all "no").
- Mathlib search (Phase 5): not in mathlib; predicate `HaveSameParity₄` absent from the upstream EDS file and tree-wide; building blocks (`Eq.trans`, `negOnePow` API) present.
- Composition check (Phase 6): COMPOSABLE — `same.1.trans (same.2.1.trans same.2.2)`, a single `Eq.trans`-chain; already open-coded inline at lines 226 and 463.

**Rationale:**

`same₀₃` is an *accessor* lemma: it unbundles the project-local predicate `HaveSameParity₄ a b c d` — itself nothing but the conjunction `a.negOnePow = b.negOnePow ∧ b.negOnePow = c.negOnePow ∧ c.negOnePow = d.negOnePow` — to recover the endpoint equality `a.negOnePow = d.negOnePow`. Its entire mathematical content is "equality is transitive across three steps," realised by `rw [same.1, same.2.1, same.2.2]`, i.e. the core term `same.1.trans (same.2.1.trans same.2.2)`. There is no theorem here in the mathlib sense: the statement cannot live in mathlib because it is phrased in terms of `HaveSameParity₄`, a bespoke four-index parity predicate that mathlib does not have and (per the companion assessment `HaveSameParity₄.md`, verdict `NO-composable-from-mathlib`) should not have — it is internal scaffolding for the division-polynomial four-index relation transformation (`avg₄`, `rel₄_transf`, `addMulSub_transf`).

Stripped of the local predicate, the residual fact (transitivity of `negOnePow`-equality / congruence mod 2) is already provided by mathlib/core: `Eq.trans`, and the parity API in `Mathlib/Algebra/Ring/NegOnePow.lean` (`negOnePow_eq_iff`, etc.) together with `Int.ModEq.trans`. The composition check confirms a ≤1-call derivation, and the call-site evidence is decisive: of the project's uses, the *same* three-conjunct chain is open-coded inline at line 463 (and at sibling lemma `rel₄_eq_net`, line 226) without going through `same₀₃` at all — consumers already bypass the wrapper. This is a thin convenience accessor, not a mathlib contribution.

**WHY not (refactor-actionable):**
Mathlib has the building blocks; `same₀₃`'s body is a single `Eq.trans` chain over the three projections of the local `And`. No new lemma is warranted — inline at the (two) call sites, or keep it purely as project-local convenience API. It is *not* `NO-mathlib-has-it` (mathlib has no decl that *is* this — only the generic `Eq.trans` atom, not an endpoint-parity accessor), and *not* BORDERLINE (no judgment call: the statement mentions a non-mathlib predicate and its content is one `Eq.trans`).

Mathlib building blocks:
  - `Eq.trans` (Lean core)
  - `And.left` / `And.right` (projections of the local `HaveSameParity₄` conjunction)
  - (parity context, if the predicate is ever dissolved) `Int.negOnePow_eq_iff` — `Mathlib/Algebra/Ring/NegOnePow.lean:98`; `Int.ModEq.trans` — Int.ModEq API.

Composition sketch (≤3 lines):
```lean
-- in place of `same.same₀₃`:
example (same : HaveSameParity₄ a b c d) : a.negOnePow = d.negOnePow :=
  same.1.trans (same.2.1.trans same.2.2)
```

Call sites in our project (from Phase 6.0): **K = 2** (NagellLutz: lines 468, 470; both in the declaring file).
Refactor plan:
  - This is a *project-internal* convenience accessor; the actionable point is that it is **not a mathlib candidate**, so it should simply remain local (or be inlined). If inlined: at line 468 replace `same.same₀₃` with `(same.1.trans (same.2.1.trans same.2.2))`; likewise at line 470. Note line 463 already inlines the equivalent chain, so the file is internally inconsistent about using the wrapper — a `main`-side `/cleanup` could standardise (either always use `same₀₃` or always inline). This is a dedup/style nicety, **independent of the mathlib verdict**.
  - Cross-project: the lemma is duplicated verbatim in `EllipticDivisibilitySequenceOriginal.lean` and `HasseWeil/.../EllipticDivisibilitySequence.lean` — a consolidation/dedup ticket on `main`, again orthogonal to mathlib.

Next action: **do not propose `same₀₃` for mathlib.** Leave it as project-local API (or inline the `Eq.trans` chain at its two call sites). Any cleanup is a `main`-side dedup/style ticket, not a mathlib PR.

---

## Next step

Do not propose `same₀₃` for mathlib. It is a project-local accessor over the bespoke `HaveSameParity₄` predicate; its content is a single `Eq.trans` chain (`same.1.trans (same.2.1.trans same.2.2)`) already open-coded at call sites. Keep it local or inline it; handle the cross-fork duplication via a `main`-side dedup ticket.
