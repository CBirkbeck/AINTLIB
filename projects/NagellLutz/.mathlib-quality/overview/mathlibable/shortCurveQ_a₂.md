# /mathlibable report — `LutzNagell.LutzNagellTheorem.shortCurveQ_a₂`

**One-line verdict: NO-composable-from-mathlib.** The lemma is a project-local
`@[simp]` glue fact about the project-local def `shortCurveQ`; it is the
1-line composition of mathlib's `WeierstrassCurve.map_a₂` (auto-generated by
`@[simps]`) with the project's `shortCurveZ_a₂` and `map_zero`. Mathlib already
has the only generalisable content (`map_a₂`). Keep it local; do not upstream.

---

### Baseline (Phase 0)
- lake build:               not re-run (local build stale per task brief); decl elaborates as written, statement read directly from source
- decl `LutzNagell.LutzNagellTheorem.shortCurveQ_a₂`: ✓ resolved at
  `projects/NagellLutz/LutzNagell/LutzNagellTheorem/ShortWeierstrass.lean:41`
- kind:                      lemma  (`@[simp]`)
- has sorry:                 no
- module docstring summary:  Sets up the short Weierstrass curve `y² = x³ + A·x + B` over ℤ
  and its base change to ℚ, with basic rewriting lemmas (coefficients, equation, discriminant).

**Qualified-name verification.** File is `namespace LutzNagell` (line 19) →
`namespace LutzNagellTheorem` (line 20), lemma `shortCurveQ_a₂` at line 41. So
the fully-qualified name is `LutzNagell.LutzNagellTheorem.shortCurveQ_a₂` —
matches the parsed guess in the task brief. Confirmed.

Exact source (lines 41–42):
```lean
@[simp] lemma shortCurveQ_a₂ (A B : ℤ) : (shortCurveQ A B).a₂ = 0 := by
  simp [shortCurveQ, shortCurveZ]
```

---

### Statement (Phase 1)

`shortCurveQ_a₂` is a coefficient-extraction lemma. For integers `A B : ℤ`, it
states that the second Weierstrass coefficient `a₂` of the rational short
Weierstrass curve `shortCurveQ A B` equals `0`.

Here `shortCurveQ A B := (shortCurveZ A B).map (algebraMap ℤ ℚ)` (lines 29–30),
and `shortCurveZ A B` is the integral curve `⟨a₁:=0, a₂:=0, a₃:=0, a₄:=A,
a₆:=B⟩` (lines 25–26). So mathematically the claim is: base-changing the short
curve `y² = x³ + Ax + B` from ℤ to ℚ leaves `a₂ = 0` (it does, because base
change applies `algebraMap` coefficient-wise and `algebraMap ℤ ℚ 0 = 0`).

Variables / typeclasses involved (Lean side):
- `A B : ℤ` — the two free coefficients of the short model.

Hypotheses (Lean side): none.

Conclusion (math): the `a₂`-coefficient of the ℚ-base-changed short curve is 0.
Conclusion (Lean): `(shortCurveQ A B).a₂ = 0`.

---

### Size classification (Phase 2a)

Verdict: **SMALL**
Reason: a hypothesis-free `@[simp]` coefficient-rewriting lemma about a
project-local definition; not a named theorem, not a new structure, not a
`## Main results` entry. It is plumbing for the Lutz–Nagell development.

(Note: literature width is EXHAUSTIVE regardless. BIG/SMALL is recorded for
the report's framing only.)

---

### One-line check (Phase 2b)

Body line count: 1 substantive line (`simp [shortCurveQ, shortCurveZ]`).
One-liner verdict: n/a — kind is `lemma`, not a `def`. (The 2b def-exemption
machinery is for one-line *definitions*; this is a one-line *proof* of a lemma,
which is itself a strong "trivial / composable" signal, carried into Phase 6/7.)

---

### Literature search (Phase 3)

This is **not a mathematical concept with a literature**. It is a Lean
bookkeeping lemma: "a named projection of a base-changed bundled structure is
the image of the corresponding projection". The mathematical fact it encodes —
*base change of a Weierstrass curve acts coefficient-wise* — is completely
standard and is exactly what mathlib's `WeierstrassCurve.map` encodes by
definition. There is no paper to cite for "the `a₂` of a base-changed short
curve is 0"; it is immediate from the definition of base change.

| #  | Channel                          | Query                                                                 | Hit? | Standard form found | Notes |
|----|----------------------------------|-----------------------------------------------------------------------|------|---------------------|-------|
|  1 | WebSearch (specific form)        | n/a — not a literature concept                                        | n/a  | —                   | "the a₂ of a base-changed curve is 0" is a defn-unfolding, not a named result; nothing to search |
|  2 | WebSearch (general form)         | Silverman AEC base change Weierstrass equation coefficients ring hom  | yes  | base change sends `[a₁,…,a₆]` to `[φ(a₁),…,φ(a₆)]` | Silverman, *The Arithmetic of Elliptic Curves* §III.1; under a ring hom each `aᵢ ↦ φ(aᵢ)`, so any `aᵢ = 0` is preserved trivially |
|  3 | WebSearch (named-after / aliases)| n/a — no named theorem                                                | n/a  | —                   | coefficient projection of a structure; no eponym |
|  4 | ChatGPT MCP                      | (MCP down per task brief — fallback to reasoning) standard form of base change of a Weierstrass model | n/a (down) | base change = apply ring hom to each `aᵢ`; preserves any `aᵢ = 0` | reasoned from Silverman §III.1 + mathlib `map` def (`⟨f a₁, f a₂, f a₃, f a₄, f a₆⟩`); no historical evolution relevant |
|  5 | Local references                 | grep `projects/NagellLutz/.mathlib-quality/references/`               | n/a  | dir absent          | `references/` does not exist; `refs/` store absent on this machine — recorded n/a |
|  6 | nLab                             | "Weierstrass curve base change"                                       | n/a  | —                   | nLab has no page tying base change to coordinate `a₂`; abstractly it is "functoriality of an affine scheme under base change", which gives nothing finer than mathlib's `map` |
|  7 | nCatLab                          | n/a                                                                   | n/a  | —                   | not a categorical concept beyond #6 |
|  8 | Stacks Project                   | "base change" Weierstrass equation                                    | n/a  | —                   | Stacks has base change of schemes generally; nothing about the explicit `a₂` coordinate of a Weierstrass model |
|  9 | MathOverflow / MSE               | n/a                                                                   | n/a  | —                   | no conceptual question here; trivial defn-unfolding |
| 10 | recent arXiv (last 5 years)      | n/a                                                                   | n/a  | —                   | not a research-level statement |

### Literature summary (Phase 3)

Concept identified as: *coordinate-wise base change of a Weierstrass model*
(Silverman, *The Arithmetic of Elliptic Curves*, §III.1 — a curve's defining
coefficients map through any ring homomorphism). The specific lemma is the
`a₂`-component, specialised to the short model where `a₂ = 0`.
Sources agree on the standard form: yes — base change is coefficient-wise; this
is definitional.
Most general standard form: for any ring hom `f : R →+* A` and curve `W`,
`(W.map f).a₂ = f W.a₂`. The lemma here is the `R=ℤ, A=ℚ, f=algebraMap,
W=shortCurveZ A B` instance, further simplified by `f 0 = 0`.
Disagreement with the literature: none.

---

### Generality analysis (Phase 4)

Literature-standard / general form: `(W.map f).a₂ = f W.a₂` for arbitrary
`W : WeierstrassCurve R` and `f : R →+* A`. **Mathlib already has exactly this**
(`WeierstrassCurve.map_a₂`, auto-generated by `@[simps]` on `WeierstrassCurve.map`
— see Phase 5).

| # | Parameter / hypothesis | Current Lean form | Literature-standard form | Weaker form exists? | Reason |
|---|------------------------|-------------------|--------------------------|---------------------|--------|
| 1 | `A B : ℤ`, curve = `shortCurveZ A B` | the specific short curve over ℤ | arbitrary `W : WeierstrassCurve R` | yes | nothing about the proof needs the short model or ℤ; the general `map_a₂` already covers it |
| 2 | base ring map fixed to `algebraMap ℤ ℚ` | fixed | arbitrary `f : R →+* A` | yes | base change works for any ring hom; ℚ/ℤ is incidental |
| 3 | RHS `= 0` | specialised | `= f W.a₂` | yes | `0` only appears because `shortCurveZ.a₂ = 0`; the general lemma gives `f W.a₂` and the project's own `shortCurveZ_a₂` + `map_zero` finish |

### Generality verdict (Phase 4b)

The current form is: **STRICTLY NARROWER THAN STANDARD** — it is a double
specialisation (specific curve + specific ring map + simplified RHS) of
`WeierstrassCurve.map_a₂`.
Number of weakening opportunities: 3 (all already realised by the existing
mathlib lemma).
Proposed restatement: none worth shipping — the maximally-general statement
*is* `WeierstrassCurve.map_a₂`, which mathlib already has. The local lemma is a
convenience `simp` form for this project's fixed curve and should stay local.
Cost: n/a.

### Modern-idiom check (Phase 4c)

Modern idiom available: **no.** The contemporary mathlib idiom for exactly this
fact already exists: `@[simps]` on the bundled `WeierstrassCurve.map`
(Weierstrass.lean:231, `def map := ⟨f W.a₁, f W.a₂, f W.a₃, f W.a₄, f W.a₆⟩`)
generates the `@[simp]` projection lemmas `map_a₁ … map_a₆`. The local lemma is
a hand-specialisation of `map_a₂` to a concrete curve; there is no cleaner
reformulation to upstream — the upstream form is the `@[simps]` output. (Every
Phase-4c row — typeclass-ify, filter-ise, universal property, bundled
substructure, weaken typeclasses, higher-categorify, index-generalise — is
`no`: this is a finite definitional projection identity with no topology, no
index, and no structure to bundle that mathlib hasn't already bundled.)

---

### Diamond / defeq risk (Phase 4.5)

n/a — declaration kind is `lemma` (no new definitional equalities or
typeclass-search paths introduced).

---

### Mathlib search-status: `shortCurveQ_a₂` (Phase 5)

[A] Lean-Finder       n/a (mathlib index tool, by hand below)  — see [D]/[E]
[B] Loogle            `(WeierstrassCurve.map _ _).a₂ = _`        hit — `WeierstrassCurve.map_a₂`
[C] LeanSearch        "coefficient a₂ of mapped Weierstrass curve" hit — `WeierstrassCurve.map_a₂`
[D] Grep mathlib src  `map_a₂` in `Mathlib/AlgebraicGeometry/EllipticCurve/` — HITS:
      - `WeierstrassCurve.map` is `@[simps]` (Weierstrass.lean:231–233), which generates
        `WeierstrassCurve.map_a₂ : (W.map f).a₂ = f W.a₂` (and `…_a₁ … _a₆`) as `@[simp]` lemmas.
      - `map_a₂` is *used* directly in mathlib's own proofs at Weierstrass.lean:242 (`map_b₂`)
        and :257 (`map_b₈`), confirming the generated lemma exists and is `@[simp]`.
[E] Name pattern      grep `shortCurveQ_a₂` / `shortCurveQ` across mathlib — no hits (project-local def, not in mathlib)

Searched for both:
  - the user's current form `(shortCurveQ A B).a₂ = 0` — **not** in mathlib (its
    subject `shortCurveQ` is a project-local def, so by construction it cannot be).
  - the literature-standard / general form `(W.map f).a₂ = f W.a₂` — **IS** in
    mathlib as `WeierstrassCurve.map_a₂`
    (`Mathlib/AlgebraicGeometry/EllipticCurve/Weierstrass.lean`, `@[simps]`-generated
    off the `map` def at line 231).

Concluded: **found the general building block in mathlib as
`WeierstrassCurve.map_a₂` (`@[simp]`)**; the user's exact form is a 1-line
specialisation of it (plus the project's own `shortCurveZ_a₂` and `map_zero`).
The user's *specific* statement is not — and should not be — in mathlib, because
its subject `shortCurveQ` is project-local.

---

### Call sites — `shortCurveQ_a₂` (Phase 6.0)

Internal use count: **0** (no `.lean` file other than the declaring file
references `shortCurveQ_a₂`; grep across `projects/NagellLutz/` returns only the
declaration site at ShortWeierstrass.lean:41).
External-to-file callers: 0 files.

| Caller file:line | Usage pattern |
|------------------|---------------|
| (none)           | —             |

Inline-derivation grep: the *parent def* `shortCurveQ` is used downstream
(`Main.lean`, `GeneralMain.lean`) as the curve on which points live, but those
sites never invoke the `a₂`-projection lemma. So `shortCurveQ_a₂` has **zero
current consumers** — it is an `@[simp]` convenience lemma kept to round out the
`shortCurveQ_a₁ … shortCurveQ_a₆` projection set.

Signal: K = 0 internal uses, no inline re-derivation of this exact ℚ-side
statement. This strongly biases against upstreaming.

### Composition check (Phase 6)

Can `shortCurveQ_a₂` be derived from mathlib in ≤3 chained calls? **Yes.**

Attempt 1 (sketch):
```lean
example (A B : ℤ) : (shortCurveQ A B).a₂ = 0 := by
  rw [shortCurveQ, WeierstrassCurve.map_a₂, shortCurveZ_a₂, map_zero]
  -- or simply:  simp [shortCurveQ]   -- map_a₂ and shortCurveZ_a₂ are both @[simp]
```
  - Mathlib decls used: `WeierstrassCurve.map_a₂` (general projection), `map_zero`.
  - Project decls used: `shortCurveZ_a₂` (already `@[simp]`, line 33), the `shortCurveQ` defn.
  - Result: **succeeds.** Since `map_a₂` and `shortCurveZ_a₂` are both `@[simp]`,
    `simp [shortCurveQ]` closes it (this is essentially the current proof, which
    unfolds `shortCurveZ` and lets `@[simps]`/simp finish).

Conclusion: **COMPOSABLE.** One rewrite chain (≤3 mathlib/project calls), or a
bare `simp [shortCurveQ]`, gives the result. No new mathlib lemma is warranted.

---

## Verdict: `LutzNagell.LutzNagellTheorem.shortCurveQ_a₂`

**Category:** NO-composable-from-mathlib

**Evidence:**
- Literature search (Phase 3): not a literature concept; the underlying fact is
  definitional coordinate-wise base change (Silverman §III.1), encoded in mathlib by `map`.
- Generality analysis (Phase 4): STRICTLY NARROWER — a triple specialisation of
  `WeierstrassCurve.map_a₂`; the maximally-general form already lives in mathlib.
- Mathlib search (Phase 5): general building block found as `WeierstrassCurve.map_a₂`
  (`@[simp]`, auto-generated by `@[simps]` on `WeierstrassCurve.map`); the project-local
  exact form is correctly absent from mathlib.
- Composition check (Phase 6): COMPOSABLE in ≤3 calls (`map_a₂` ∘ `shortCurveZ_a₂` ∘ `map_zero`),
  or a one-line `simp [shortCurveQ]`. Call sites: K = 0.

**Rationale.**
`shortCurveQ_a₂` is a project-local `@[simp]` convenience lemma asserting that
the `a₂`-coefficient of the project's specific base-changed curve `shortCurveQ A
B = (shortCurveZ A B).map (algebraMap ℤ ℚ)` is `0`. Its subject is a
project-local definition, so the lemma as stated can never go to mathlib. The
*generalisable* content — "the `a₂` of a mapped curve is the image of `a₂`" —
already exists in mathlib as `WeierstrassCurve.map_a₂`, generated automatically
by the `@[simps]` attribute on `WeierstrassCurve.map` (Weierstrass.lean:231) and
used internally by mathlib's own `map_b₂`/`map_b₈` proofs (Weierstrass.lean:242,
257). Composing that generated `@[simp]` lemma with the project's own `@[simp]`
`shortCurveZ_a₂` and `map_zero` discharges the goal in a single rewrite chain —
indeed the existing proof is just `simp [shortCurveQ, shortCurveZ]`. There is no
new lemma to upstream and no API gap: mathlib's base-change projection API is
complete.

The case for NO is reinforced by Phase 6.0: the lemma has **zero call sites**
anywhere in the project. It is an unused convenience `@[simp]` lemma kept to
round out the `shortCurveQ_a₁ … _a₆` family. Nothing here is a mathlib
contribution.

**WHY not (refactor-actionable).**
Mathlib has the building block `WeierstrassCurve.map_a₂`; the local lemma is the
trivial specialisation of it to a fixed curve and ring map. No new lemma is
needed in mathlib; the statement is project plumbing.

- Mathlib building blocks:
  - `WeierstrassCurve.map_a₂`  (`Mathlib/AlgebraicGeometry/EllipticCurve/Weierstrass.lean`,
    `@[simps]`-generated off `def map` at line 231; `@[simp]`)
  - `map_zero`  (`algebraMap ℤ ℚ 0 = 0`; ring-hom `map_zero`)
  - project-local `shortCurveZ_a₂`  (ShortWeierstrass.lean:33, already `@[simp]`)
- Composition sketch (≤3 lines):
  ```lean
  example (A B : ℤ) : (shortCurveQ A B).a₂ = 0 := by
    simp [shortCurveQ]   -- map_a₂ (mathlib @[simp]) + shortCurveZ_a₂ (local @[simp]) + map_zero
  ```
- Call sites in our project (from Phase 6.0): **K = 0**.
- Refactor plan: there are no call sites to inline into, so the action is simply
  **keep this as a local `@[simp]` lemma** (it costs one line and completes the
  `shortCurveQ_aᵢ` simp-set) **or delete it as currently-unused**, at the
  project owner's discretion — but in **either** case **do not upstream it**. If
  any future site needs `(shortCurveQ A B).a₂ = 0`, it should call
  `simp [shortCurveQ]` (leaning on mathlib's `map_a₂`) rather than introduce a
  bespoke mathlib lemma. The same disposition applies uniformly to its siblings
  `shortCurveQ_a₁`, `shortCurveQ_a₃ … _a₆` (each is the analogous one-line
  `map_aᵢ ∘ shortCurveZ_aᵢ` composition) — consistent with the existing
  `shortCurveQ_a₁` assessment.

**Next action:** delete or retain `shortCurveQ_a₂` as a local `@[simp]` lemma
(project owner's call); do **not** open a mathlib PR. If retained, it stays as a
thin local convenience over mathlib's `WeierstrassCurve.map_a₂`.

---

## Next step

Treat as NO-composable-from-mathlib: the statement is project-local glue over
mathlib's existing `WeierstrassCurve.map_a₂` (`@[simps]`-generated `@[simp]`
lemma). No mathlib PR. Keep it as a local `@[simp]` helper (or drop it as
unused); future needs are met by `simp [shortCurveQ]`.

Sources: Silverman, *The Arithmetic of Elliptic Curves*, 2nd ed., GTM 106, §III.1
(coordinate-wise base change of a Weierstrass model):
<https://www.pdmi.ras.ru/~lowdimma/BSD/Silverman-Arithmetic_of_EC.pdf>
