# Triage: remaining code sorries OUTSIDE the OpenLemma trio (2026-06-11 audit)

> **FG-C4 update (2026-06-13, post-P)**: current census is **5 code sorries in 4
> files**: `FormalIsogenySeries.lean:364` (BRIDGE-001, restated),
> `GapQfKernel.lean:651/:663` (III.1.5 pair, FG-D1),
> `OmegaPullbackCoeff.lean:480` (Wronskian, consumer-free), `PencilComapWitnesses.lean:1943`
> (optional post-Hasse). P (`pullback_invariantDiff_coeff_zero`, now GapQfKernel:1567) is
> **CLOSED (FG-C4), axiom-clean** — see the updated card below. Items 1, 8, 9 were
> closed/deleted by intervening sessions; item 10 (Wall A) and the GapQfKernel `:49` item
> were closed earlier. The only remaining IV.1-program leaf is the III.1.5 pair (FG-D1,
> the divisor-of-ω / mem_F substrate) + BRIDGE-001 (which `exact`-closes once mem_F lands).
> Line numbers below are from the original audit and have drifted.

14 code sorries in 8 files. Prose-only false positives confirmed: `EC/IsogenyAG/Dual.lean:436`, `GapSpines.lean:1267,1334,1340` (docstrings), `DualIsogeny.lean:13`. Hasse chain verified independent of all 14 (per-sorry consumer walks; HasseBound imports only PencilComapWitnesses + FrobeniusGaloisScaling + OneSubProjOrdTransport + FrobMatrixData; pencil leaf = `pencilScaling_holds_coprime` :2366 → `pencilScalingComapDataCard_canonical` :2138 → uniform `pencil_two_residues` :2038, axiom-clean).

## Verdict cards

1. **`AdditionPullback.lean:161`** — sorry inside private `addPullback_x_ne_const` (:158), III.3.6 pole witness for general α. Dead private chain: → `addPullback_x_transcendental` (:484) → `addBaseHom_injective` (:507) → `addCoordAlgHom_injective` (:591, zero consumers). Live users go through witness-parametric `addPullback_x_ne_const_of_pole` (:176) + per-α proofs (AdditionPullback/Frobenius.lean). **DELETE the 4-decl chain** (:158–175 sorried part, :484–510, :591–594). Fix stale prose at `OrdAtInftyBridge.lean:23`. Do NOT touch `addPullback_x_ne_const_of_pole` (:176).

2–7. **`GapQfKernel.lean` (6 sorries)** — entire sorried chain has zero live consumers; the file's live exports are the char-p Kähler tail (:797–1090), independent.
   - `:49 formalIsogenySeries_FGL_additivity` — IV.2.3a substance, = BRIDGE-003 at ([k],[1]). **CLOSED (FG-C2, 2026-06-11), axiom-clean**: instantiates the proven `formalIsogenySeries_add` (ChordExpansion) at ([k],[1]) + the `[k] ⊞ [1] = [k+1]` pair identification (`addPullback_xy_pair_mulByInt_one_eq_succ`, `EC/MulByIntAddRecurrence.lean`) + `ofPowerSeries`-injective descent. The whole L-F1 family (`coeff_one_formalIsogenySeries_mulByInt_eq`/`_of_neg`) is now axiom-clean; the TOP `omegaPullbackCoeff_mulByInt_via_formalGroup` still waits on the III.1.5 pair + P.
   - `:56 coeff_one_formalIsogenySeries_mulByInt_nonpos` — IV.2.3a m ≤ 0. **KEEP-OPEN** (~150 LOC: m=0 + `formalInverse` compat).
   - `:360/:372 omegaPullbackCoeff_isIntegral_polynomialX` / `_ordAtInfty_nonneg` — the III.1.5 wall. **KEEP-OPEN, hard**; new plausible route via `EC/DifferentialOrd.lean` (`Dω`, `ord_P_Dω_nonneg` :257, `ord_P_nonneg_of_mem_Rimg` :239); 2-torsion fibre re-imports unramifiedness; fresh /develop pass recommended. Est 1–2 weeks.
   - `:716 invariantDiff_localExpand_coeff_zero (N)` — ω₀(T)=1+O(T) (IV.4.2). **N = P at α = id** — prove P, get N free; or direct Laurent (~150–300 LOC; char-2 leading-term cancellation in ratio).
   - `:727 pullback_invariantDiff_coeff_zero (P)` — IV.4.3 at T=0. **CLOSED (FG-C4, 2026-06-13), axiom-clean** (now GapQfKernel:1567). Restated with the genuineness hyp `0 < orderTop (localExpand (α.pullback (localParam W)))` (the restatement strike already did this). Proof: char-2/char≠2 split mirroring N; the *substituted univariate implicit-differentiation* identity (no chain rule, no dividing by `f′` — so inseparable α incl. Frobenius are covered) + abstract-CommRing `pullback_diff_rearrange` + a T²-cleared LaurentSeries `linear_combination`. (N at :716 was already proven earlier as `invariantDiff_localExpand_coeff_zero`.)

8. **`GapSpines.lean:2115`** — `genuineIsogSmulSub_degree_eq_signed` (:2110), unconditional III.6.3 signed degree identity (old framework). Consumers: `degree_quadratic_exists_skeleton_nonzero` (:2219) → `qf_nonneg_skeleton` (:2244) → `HasseWeilSkeleton.hasse_bound_skeleton` (:30–33) ONLY (the superseded old-route bound; the proven bound routes through `qf_nonneg_skeleton_of_weil_det_data` HasseAssembly:74). **KEEP-OPEN** if skeleton kept; **deletable if the skeleton chain is retired** (orchestrator decision; witness-parametric `_via_walls` :2021 is shipped and stays). CAUTION: everything live in GapSpines sits ABOVE :2110 (`mulByInt_q_pullback_qth_root` :45, `verschiebung_dual_exists` :71, `sepDegree_oneSub_eq_pointCount` :1268, `addIsog_isGenuineWith` :1603, Sinf B3 chain) — surgical removal of :2110–2247 only.

9. **`Hasse/QuadraticForm.lean:307`** — `genuineIsogSmulSub_pivot_witness` (:295) + sole consumer `genuineIsogSmulSub_degree_eq_quadratic_form_minimal` (:314–325, zero consumers). **DELETE the pair.** File's live decls (:104 → HasseWeilSkeleton; :124 → HoleE) independent.

10. **`Verschiebung/Genuine.lean:1356`** — `addPullback_x_pair_x_ord_neg` (:1350), the symmetric-pole Wall-A leaf (3-way −6 tie, needs IV.1.4/formal group). **CLOSED (FG-C1, 2026-06-11), axiom-clean** — Genuine.lean is now sorry-free. The proof did NOT need the full B5 identity: pure `ord` arithmetic on the `(z,w)`-chart line data (FG-B3/B4 legs `localExpand_zwSlopeLine_of_x_ne`/`_zwNuLine_of_x_ne` give `ord λ, ord ν > 0` ⟹ intercept pole `ord c ≤ −1`; assume `0 ≤ ord X₃`, then the monic quadratic forces `0 ≤ ord Y₃′` and the line `c = Y₃′ − ℓX₃` gives `ord c ≥ min(0, ord c + 1)` — contradiction). Downstream now axiom-clean: `addPullback_x_pair_sum_reduces_to_O`, `addPullback_x_pair_ord_neg_of_summands_reduce`, `genuineIsogSmulSubV_universal_unconditional`, `WallA.genuineIsogSmulSub_degree_eq_signed_closed`.

11. **`OmegaPullbackCoeff.lean:477`** — Wronskian m≥5 branch (`wronskian_Φ_ΨSq_nat` :457), blocked on mathlib-absent EDS addition formula. **De-Fintype DONE (2026-06-11): the Wronskian is now consumer-free.** The field-general Route-B chain (born in `WeilPairing/TorsionGeometric.lean`) was relocated to **`HasseWeil/RouteBGeneral.lean`** (below `Hasse/Separability.lean`); `Separability:194` and `PullbackCoeff:126` now route through the axiom-clean `omegaCoeff_mulByInt`, de-tainting `omegaPullbackCoeff_mulByNat_p_eq_zero` → `mulByInt_p_not_isSeparable` → the **TwistedFactorization general-base wrappers** (`Isogeny.mulByInt_p_not_isSeparable`, `relativeVerschiebung` + compose, `nonempty_hasDualWitness_of_twisted_separable_witnesses`) — all verified `sorryAx`-free. Remaining cone of the sorry: in-file only (`divPoly_wronskian_identity` :648, `omegaPullbackCoeff_mulByInt` :849 — zero external consumers; the orphaned FormalGroupBridge/BridgeMulByInt pair was deleted). Axiom-clean general statement of the polynomial identity itself: `wronskian_Φ_ΨSq_general` (`EC/WronskianGeneral.lean`). **KEEP-OPEN** (consumer-free; close only if the EDS addition formula ever lands in mathlib).

12. **`WeilPairing/PencilComapWitnesses.lean:2291`** — `pencilScalingComapDataCard_pDvdR` (:2288), the documented optional post-Hasse p∣r′ strengthening. Consumers: only `_sep` (:2296) → `pencilScaling_holds` (:2336), zero outside file (bound uses `_coprime` :2366). **KEEP-OPEN** (in-situ docstring is a complete plan; D4-inseparable bricks shipped AdditionPullback/Frobenius.lean:4465+; ~800–1500 LOC). **Bonus hygiene: the superseded branch quartet `pencil_two_residues_secant` (:963) / `_doubling` (:1429, Wronskian-tainted) / `_summand_infty` (:1947) / `_nonsecant` (:1988) is dead** (subsumed by uniform `pencil_two_residues` :2038) — deleting clears the file's sorryAx reachability.

## FormalIsogenySeries design verdict (Workstream-3 bridge)

- `omegaPullbackCoeff_eq_formalIsogenyLeading` (:327, BRIDGE-001, IV.4.3 "a_φ = φ′(0)"): **likely refutable as stated** (translation pullbacks). RESTATE with the orderTop genuineness hyp; per-α instances already shipped (`_id` :732; negFrobenius SilvermanIV14:377; isogOneSub char-2 SilvermanIV14:2286). Then ladder: P-leaf (substitution identity, ~300–600 LOC) ⟹ N free ⟹ with `omegaPullbackCoeff_mem_F` (:384) closes via shipped `_via_localization` (GapQfKernel:768). mem_F substrate = the III.1.5 leaf (hard).
- `formalIsogenySeries_add` (BRIDGE-003, IV.1.4+III.5.2): **PROVEN, axiom-clean (FG-B5, 2026-06-11)** — restated in the `h_iv14` shape, relocated to `ChordExpansion.lean`, and proven via the chord-expansion layer (FG-B1..B4a + the cleared chart-Vieta identity + Hensel identification + the FG-A5 inversion spec). Harvested this session: Wall A (`addPullback_x_pair_x_ord_neg`, FG-C1) and the GapQfKernel BRIDGE-003 instance (`formalIsogenySeries_FGL_additivity`, FG-C2) are both closed axiom-clean. The historical B2-refutability note applied to the pre-restatement point-map-hypothesis shape only.

## Summary order
(1) deletions: AdditionPullback chain, QuadraticForm pair, doc-fixes (GapSpines:43 `via_formalGroup` pointer stale → routeB; FIS:36-40 consumer list). (2) optional: pencil dead quartet. (3) restatements BRIDGE-001/003 (+b2 log for 003). (4) P/N restate+attack. (5+) keep-opens in increasing depth.
