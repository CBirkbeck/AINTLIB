# Triage: Hasse-era "open lemma" trio — 32 code sorries, verdict cards (2026-06-11 audit)

Read-only audit of `OpenLemmas.lean` (12 code sorries: L258, 370, 383, 398, 430, 460, 490, 544, 815, 1336, 1381, 1534), `OpenLemmaPrimitives.lean` (15: L134, 371, 435, 469, 530, 678, 1519, 1776, 1831, 1845, 1857, 1893, 1913, 1934, 2025), `L6Witnesses.lean` (5: L656, 676, 695, 706, 3450).

**Proven-chain verification.** All three files ARE in `hasse_bound_unconditional`'s transitive import closure, but the proof-term consumer cone of every one of the 32 sorried decls terminates in: nothing, in-file dead chains, or the superseded legacy milestone `HasseWeilSkeleton.hasse_bound_skeleton` (HasseWeilSkeleton.lean:28). Load-bearing exports from the trio on the proven chain are all PROVEN decls: `OpenLemmaPrimitives.addPullbackAlgHomPair_x_gen_eq`/`_y_gen_eq` (OLP:1287/1310 — used by PencilComapWitnesses.lean:126,135, RouteBInduction.lean:45-95, GapSpines.lean:1619-1622), `kernel_point_is_pole_of_gamma_pullback_x` (OLP:96, used L6Witnesses:226), L6Witnesses' `_v2` bridge lemmas + squeeze composer consumed by GapSpines.

## File 1: OpenLemmas.lean

- **S1 `omegaPullbackCoeff_add_genuine` (L252, sorry L258)** — universal ω-additivity (III.5.2). Consumers: none. **DELETE**; shipped as `omegaPullbackCoeff_addIsog_pair` (RouteBInduction.lean:82). The needed specialisation L1' is already proven in-file (L277). Optionally prune pack field `l1_omega_add`.
- **S2–S7 bridge block** `bridge_Bi_kernelToPrime` (L339/370), `bridge_Bi_isPrime` (L374/383), `bridge_Bi_liesOver` (L387/398), `bridge_Bii_bijective` (L413/430), `bridge_Biii_ord_eq_neg_two` (L447/460), `bridge_Biv_inertia_eq_one` (L476/490). Consumers: strictly each other. **DELETE all six**; superseded by proven `_v2` versions in L6Witnesses (:1260, :1319, :1387, :2612, :4443) + `Sinf_kernelToPrime_v2_injective` + GapSpines squeeze.
- **S8 `v_1_3_sepDegree_eq_pointCount` (L541, sorry L544)** — sepDeg(1−π)=#E. Consumers: `witness_pc_sepDeg` (L1496) → `witnessBundle` (L1769) → `hasse_bound_from_open_lemmas`/`hasse_bound_sq_from_open_lemmas` (L1852/1860, zero external consumers); plus OLP's `pi_plus_V_eq_isogTrace_addMonoidHom` (OLP:1615, zero consumers). **DELETE batch** (S8 + witness_pc_sepDeg + witnessBundle + both hasse_bound_from_* + OLP pi_plus_V). Proven as `GapSpines.sepDegree_oneSub_eq_pointCount` (GapSpines:1268). Note: `witnessBundle` also consumes `witness_qf_nonneg` (S12) — same batch. `HasseWitnesses`/`hasse_bound_of_witnesses` (Hasse/Final.lean) STAY (L6ViaPoleDivisor:383 still builds one witness-parametrically).
- **S9 `verschiebung_isDualOf_frobenius` (L810, sorry L815)** — ∃V, IsDualOf V π. Sole consumer: OLP's `trace_eq_pi_plus_dualFrobenius_unconditional` (OLP:1528, `.choose` form, itself dead). **DELETE after the L10 chain** (consumer-first or `Exists.choose` of a vanished decl breaks). Duplicated by proven `GapSpines.verschiebung_dual_exists` (GapSpines:71-74) + `verschiebung_isDualOf_frobenius_general` (Verschiebung/QthRootRouteB.lean:468).
- **S10 `Polynomial.scalarExtensionEquiv` (L1333, sorry L1336)** — B ⊗[A] A[X] ≃ₐ[B] B[X]. Consumers: none. **DISCHARGE (1 line)**: mathlib `polyEquivTensor'` (`Mathlib/RingTheory/PolynomialAlgebra.lean:191`): body `(polyEquivTensor' (R := A) (A := B)).symm` after `import Mathlib.RingTheory.PolynomialAlgebra`. Namespace `HasseWeil.OpenLemmas.Polynomial` — no clash.
- **S11 `AdjoinRoot_tensorAlgEquiv_exists` (L1368, sorry L1381)**. Consumers: none. **DELETE**; purpose achieved by `Curves/CurveMapBaseChange.lean` (`coordRingScalarExt` :502, `coordRingScalarExtPinned` :613, `functionField_baseChange_tensorEquiv` :645) + `CoordHom.module_finite` (021adda).
- **S12 `witness_qf_nonneg` (L1521, sorry L1534)** — III.6.3 QF nonneg. Consumers: `witnessBundle` only. **DELETE** with the S8 batch; content proven inside the unconditional bound (HasseAssembly's `qf_nonneg_skeleton_of_weil_det_data_both` fed by the 3 proven scalings).

## File 2: OpenLemmaPrimitives.lean

- **P1 `Sinf_ord_nonneg_at_kernel_point` (L122, sorry L134)**. No code consumers. **DELETE**; proven downstream as `Conditional.Sinf_ord_nonneg_at_kernel_point_unconditional` (L6Witnesses:577).
- **P2 `mulByP_factors_through_relativeFrobenius` (L356, sorry L371)**. None. **DELETE**; content = `GapSpines.mulByInt_q_factors_through_frobenius` (GapSpines:~62) + `twistedFrobeniusFactorization` (EC/IsogenyAG/TwistedFactorization.lean:430).
- **P3 `mulByPN_factors_through_iterated_pFrobenius` (L422, sorry L435)**. None; self-marked DEPRECATED. **DELETE**.
- **P4 `mulByPN_factors_unconditional` (T7, L464, sorry L469)**. Consumer: `verschiebung_isDualOf_frobenius_universal` (T9, L497; zero external). **DELETE T9 then T7**; superseded by `qth_root_witness_general` → `verschiebung_isDualOf_frobenius_general` (QthRootRouteB.lean:457/468). T8 (`qth_root_universal_of_factorisation`, L480, proven) loses its only consumer — optional delete.
- **P5 `qth_root_witness_universal` (L526, sorry L530)**. None. **DELETE**; verbatim the proven `qth_root_witness_general` (QthRootRouteB.lean:457).
- **P6 `dual_additivity_for_one_sub_pi` (L652, sorry L678)** + **P7 `trace_eq_pi_plus_dualFrobenius_unconditional_for_V` (L1497, inner sorry L1519)**. Consumers: in-file L10 chain only (`trace_eq_pi_plus_dualFrobenius_unconditional` L1528, `l10_trace_eq_witness` L1552 — zero external). **DELETE the whole L10 chain (4 decls), order: l10_trace_eq_witness → trace_eq_pi_plus_dualFrobenius_unconditional → P7 → P6.** Modern content: `oneSubCanonicalDual`(+compose,+degree) in WeilPairing/OneSubPullbackEvaluation.lean:426+. Afterwards `OpenLemmas.trace_eq_pi_plus_dualFrobenius_of_dual_additivity_witness` (OpenLemmas:891, proven scaffold) loses its last consumer — keep as dead-but-shipped or delete; and S9 becomes deletable.
- **P8 `isIntegrallyClosed_coordinateRing_allChar` (L1773, sorry L1776)**. **DISCHARGE NOW: `inferInstance`** — `HasseWeil.isIntegrallyClosed_coordinateRing` (Ramification.lean:1407) is the unconditional global instance, only `[Field F]`; Ramification IS in OLP's import closure; `SmoothPlaneCurve.CoordinateRing` is a reducible abbrev. Fallback spelling: `exact HasseWeil.isIntegrallyClosed_coordinateRing C.toAffine`.
- **P9–P14 line-constructor scaffolding** `lineThrough` (L1829/1831), `tangentLineAt` (L1843/1845), `verticalLineThrough` (L1855/1857), `div_lineThrough` (L1885/1893), `div_tangent` (L1906/1913), `div_vertical` (L1927/1934). Consumers: only each other. **DELETE all six**; the all-char Miller pipeline shipped without them (`miller_hypothesis_holds_allChar` MillerAllChar.lean:483, `divZeroReduce_holds_allChar` :606, `AFInputs_allChar` :634; OLP's Group-4 wrappers L1683/1702/1748 already delegate to it).
- **P15 `hasse_bound_universal` (L2020, sorry L2025)**. None. **DISCHARGE-BY-MOVE**: delete from OLP; add in HasseBound.lean (or tiny capstone) `theorem hasse_bound … := hasse_bound_unconditional W (Nat.succ_le_of_lt Fintype.one_lt_card)` (hq is automatic for finite fields).

## File 3: L6Witnesses.lean

- **W1–W2 `genuine_dual_comp_pullback_x_gen_eq_mulByInt_x_decomp` (L639, sorry L656) / `…_y_gen…` (L659, sorry L676)**. Consumers: none (strategy pivot recorded L623-632). **DELETE both** (stranded Wall-B double-Vieta stubs).
- **W3–W4 `degree_quadratic_exists_edge_s_char_divisible` (L687, sorry L695) / `…_r_char_divisible` (L698, sorry L706)**. Consumers: live-with-sorryAx via `GapSpines.degree_quadratic_exists_edge` (GapSpines:2205-2206) → `degree_quadratic_exists_skeleton_nonzero` (:2222) → `qf_nonneg_skeleton` (:2242) → `HasseWeilSkeleton.hasse_bound_skeleton` ONLY (not on proven chain). **REFUTE (B2)**: FALSE as universally quantified — supersingular E with t²=4q (E/F_p supersingular base-changed to F_{p⁴}: q=p⁴, t=2p², #E=(p²−1)²) makes QF=(p²r−s)², vanishing at null pairs s=p²r which lie in the char-divisible branch ((r,s)=(1,p²) for p∣s; (p,p³) for p∣r); a degree-0 Isogeny is impossible (pullback is an injective field hom between trans-deg-1 function fields ⟹ finrank ≥ 1). Formal Lean counterexample expensive (needs concrete supersingular curve + trace) — practical action: **b2_log entries + DELETE both + retire the GapSpines edge chain** (see cross-cutting).
- **W5 `Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount` (L3424, sorry L3450)**. Consumers: in-file dead "CORE residue" cone only (`Sinf_primeOver_eq_kernelPrime_place` L3661 → L3685, L3720, L3915, L4149, L4192, membership-char ending L4242 — zero external). GapSpines' V.1.3 closure bypasses it (GapSpines:1340). **DELETE with its dead cone, bottom-up** (membership-char → :4192 → :4149 → :3915 → :3720 → :3685 → :3661 → W5 itself → `_of_witness` L3460). Content proven axiom-clean as `GapSpines.Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount_via_tower` (GapSpines:~1335). **KEEP the composer `…_of_finrank_witness` (L6Witnesses:~4588)** — GapSpines consumes it. Do NOT touch `bridge_Biv_inertia_eq_one_v2` (L4443) — clean and consumed.

## Cross-cutting

- **No file deletes**: each hosts load-bearing proven content (OpenLemmas: `Isogeny_eq_of_components` used GapSpines:1486,1833,1873, `witness_pc_sep`/`witness_pc_fin` L1469/1482, `function_field_x_separable` L645, L11a-f/L13/L14 wrappers, HasseOpenLemmaPack; OLP: the gen_eq pair ON the proven chain + σ-V/T11-T13 + Group-4 wrappers; L6W: `_v2` bridges + injectivity + composer).
- **Safe order**: (1) in-place discharges P8, S10; (2) zero-consumer leaves S1, S2-S7, S11, P1, P2, P3, P5, P9-P14, P15, W1, W2; (3) L10 chain then S9 (+optionally OpenLemmas:891 scaffold); (4) T9 → P4 (T8 optional); (5) v_1_3 batch (OLP pi_plus_V first, then hasse_bound_sq_from_open_lemmas → hasse_bound_from_open_lemmas → witnessBundle → S12 → witness_pc_sepDeg → S8); (6) W5 cone bottom-up; (7) W3/W4 B2 + GapSpines edge-chain retirement.
- **Editor risks**: every edit triggers a near-full Hasse-world rebuild — batch edits, build once. `Sinf` decls carry `letI := data.commRing/algPoly` boilerplate — whole-decl deletions safe, partial edits not. HasseOpenLemmaPack fields are types, survive theorem deletions. Keep the `private` alias at OpenLemmas:225 (feeds proven L1').
