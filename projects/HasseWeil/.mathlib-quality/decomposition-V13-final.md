# V.1.3 final residual — decomposition (deep pass 2026-05-28)

**Target.** `HasseWeil/Hasse/L6Witnesses.lean` —
`Sinf_primeOver_eq_kernelPrime_of_algebraMap_X_mem` (the sole `sorry` flowing into
`sepDegree_oneSub_eq_pointCount` / `ker_deg_skeleton`). Given a prime `P` of
`data.carrier = integralClosure K[X] (LinfAt f)` with `P.IsPrime` and
`hX_mem : algebraMap K[X] carrier X ∈ P` (so `f = (1−π)*x` has a pole at `P`),
produce `∃ T : (1−π).kernel, P = bridge_Bi_kernelToPrime_v2 W hq data T`.

`X ↦ 1/f`, `f = (isogOneSub_negFrobenius W hq).pullback (x_gen W)`,
`LinfAt f = K(E)`. `(1−π).pullback = addPullbackAlgHom_negFrobenius` (the genuine
addition-formula pullback for `id + (−π)`), **not** a bare power.

---

## Decisive findings (adversarial)

### Finding 1 — `ord_∞(f) = -2`, so `f` has a pole at `O` (point at infinity)
`ordAtInfty_isogOneSub_negFrobenius_pullback_x_gen` (`PoleDivisorFallback.lean:95`)
gives `ord_∞ f = -2`. So `f⁻¹` (= image of `X`) vanishes at `O` and at every kernel
point. Consequence: `x_gen`, `y_gen` are **NOT** globally integral over `K[1/f]`
(they have a pole at `O`), so the carrier is *not* generated by their images — the
place→point cannot read coordinates off carrier generators directly.

### Finding 2 — Approach 1a (algebraic Frobenius `a^q ≡ a mod P`) is NOT viable
`frobeniusIsog_pullback_apply` (`HasseWeil/Frobenius.lean:58`) confirms
`π* g = g^q` on `K(E)` — the cleanest possible Frobenius wiring, and it IS shipped.
But the q-power map acts on the **ambient** field `K(E)`, and does **not** descend to
the residue field `κ(P) = carrier/P`:
* `π*` does not preserve the `K[1/f]`-algebra structure of the carrier
  (`π*(1/f) = 1/f^q ≠ 1/f`), so it is not a ring endomorphism of `carrier` fixing `P`;
  there is no "P is `π*`-stable" handle.
* Deriving `a^q ≡ a (mod P)` for `a : carrier` is *equivalent* to `κ(P) ⊆ F_q`, i.e.
  to the inertia-degree-1 claim itself — it is the **content**, not an input. The only
  shipped Frobenius-fixed criterion `frobenius_fixed_iff_mem_baseField`
  (`FrobeniusFixedLocus.lean:130`) is stated over `AlgebraicClosure K`, on elements of
  `K̄`, not on residue-field classes of a bare carrier prime.
* Geometric confirmation (Finding 1): over `K̄` there genuinely ARE poles of `f` off
  the `F_q`-rational kernel (geometric points `P̄` with `(1−π)P̄ = O` but `P̄` not
  `F_q`-rational); a bare carrier prime `P` over `(X)` can a priori be such a
  Galois orbit (inertia `> 1`). 1a would have to manufacture rationality it cannot.
**Verdict: 1a dead.** No structural `a^q ≡ a mod P` is available.

### Finding 3 — Approach 1b is the viable route; it bottoms out at ONE CORE
1b = "place→point→Frobenius-fix coordinates". The `IsAlgClosed` in the shipped
extraction chain (`NormValuation.lean`) enters in exactly ONE place:
* `algebraMap_bijective_quotient_of_maximal` (`NormValuation.lean:251`) uses
  `[IsAlgClosed F]` (Zariski) ONLY to get **surjectivity** of `algebraMap F (CR⧸M)`.
* `exists_coordinates_of_isMaximal` (`:268`) uses it ONLY through that surjectivity.
* `equation_of_coordinates` (`:293`) carries `[IsAlgClosed]` as a hypothesis but its
  **proof body uses only `RingHom.injective` + `ha/hb`** — works over any field.
So given residue **surjectivity** for the bare prime, the coordinate extraction +
Weierstrass equation + `[IsElliptic]` ⇒ Nonsingular all de-gate mechanically. The
single irreducible content is therefore exactly the **residue surjectivity of the
structure map `K[X]/(X) → carrier/P` for a bare prime `P` over `(X)`** — equivalently
`inertiaDeg (X) P = 1`, the `K`-rationality of `P`. This is the *forward twin* of the
shipped kernel-prime computation `Sinf_finrank_kappa_kernelPrime_eq_one`.

### Finding 4 — Route B (cardinality squeeze) remains CIRCULAR
The only handle on `[K(E):K(f)]` is `Σ_P e_P f_P = [K(E):K(f)]`
(`finrank_eq_sum_ramificationIdx_mul_inertiaDeg`), used *inside*
`finrank_gamma_pullback_x_eq_weightedPoleDegree`. No independent `[K(E):K(f)] = 2q`
exists; any Σ e·f count reproduces the V.1.3 goal. A center-map injection
`primesOverFinset ↪ support(divisor f)` lands in the `K`-rational support only after
proving each center is `K`-rational — same CORE. **Verdict: no non-circular squeeze.**

---

## Leaves

### CORE (genuinely deep — the sole irreducible residual)
**`Sinf_residue_surjective_of_primeOver`** (BUILT as a sharp named `sorry` leaf):
```
(data) (P) [P.IsPrime] [P.LiesOver (X)] →
  letI : Algebra (K[X]⧸(X)) (carrier⧸P) := algebraQuotientOfLEComap …
  Function.Surjective (algebraMap (K[X]⧸(X)) (carrier⧸P))
```
*Why true:* `P` over `(X)` = a place where `f⁻¹` vanishes = a pole of `f = (1−π)*x` =
a Frobenius-fixed point `Q = π(Q)`, whose coordinates `c` satisfy `c^q = c` hence
`c ∈ K` (`frobenius_fixed_iff_mem_baseField`), so `κ(P) = K(coords) = K`.
*What could go wrong / why deep:* manufacturing the rationality requires either
(i) the geometric point `Q` over `K̄` and a Galois-descent of `Q = π(Q) ⟹ Q` defined
over `K` (the project has no `K̄`-base-change of the carrier / no descent of the
fixed-point locus to the carrier), or (ii) a residue-field-Frobenius on `κ(P)` tied to
`π*` (absent, Finding 2). **Verdict: deep — needs new infrastructure** (a
`K̄`-base-change of the Sinf carrier, or a residue-field Frobenius descent). Isolated
as the single irreducible obligation.

### REDUCTION 1 (BUILT, axiom-clean): surjectivity ⟹ inertiaDeg = 1
**`Sinf_inertiaDeg_eq_one_of_primeOver`** — direct application of the shipped bridge
`Sinf.inertiaDeg_eq_one_of_algebraMap_surjective` (`RamificationAtInfinity.lean:575`)
to the CORE. Verdict: **shipped composition** (proved on top of CORE).

### REDUCTION 2 (place→point, given inertiaDeg = 1): de-`IsAlgClosed` extraction
Given `inertiaDeg (X) P = 1` (⟺ `carrier/P ≅ K` as `K`-algebras), produce the kernel
point `T` and prove `P = P_T`. Sub-pieces:
* **R2a** carrier↔`CoordinateRing` localisation bridge: the extraction lemmas are
  stated for `CoordinateRing` maximal ideals; the Sinf carrier is `integralClosure
  K[X] K(E)`. Verdict: **buildable but sizeable** (needs the localisation/center map
  carrier-prime → CoordinateRing-maximal-ideal at the affine chart).
* **R2b** residue-`K` variant of `algebraMap_bijective_quotient_of_maximal`: replace
  `[IsAlgClosed]` by the inertia-1 hypothesis. Verdict: **buildable** (residue field is
  literally `K`; surjectivity is the inertia-1 hypothesis, injectivity is
  `RingHom.injective`).
* **R2c** coordinates ⇒ `SmoothPoint`: `equation_of_coordinates` (de-gated; body is
  field-agnostic) + `[IsElliptic]`'s `equation_iff_nonsingular`. Verdict: **shipped**
  (body already de-gatable).
* **R2d** `SmoothPoint Q` is a pole of `f` ⟹ `Q ∈ ker(1−π)`: `L1` (`hX_mem`) gives
  `ord_Q(f) < 0`; `pole_gamma_pullback_x_imp_kernel` (`PoleDivisorFallback.lean:2803`)
  ⟹ `Q.toAffinePoint ∈ ker`. Verdict: **shipped**.
* **R2e** `P = P_T` value identity: both `= {a | 0 < ord_T(algebraMap a)}` via
  `Sinf_intValuation_eq_exp_neg_ordAtPoint_at_kernel` read at `Q` /
  `maximalIdealAt_injective`. Verdict: **buildable** (mirror of the shipped
  injectivity argument `Sinf_kernelToPrime_v2_injective`, run with `Q` in hand).

**REDUCTION 2 verdict overall: buildable but sizeable** (R2a + R2b + R2e are real
additions). Until R2a–R2e ship, factoring REDUCTION 2 into a separate hypothesis-lemma
relocates the `sorry` only if R2a is itself stubbed; the net irreducible content is the
CORE.

---

## Parts 2–4 of the docstring sketch (subsumed by REDUCTION 2)
Parts 2–4 ("K-rational P ⟹ unique affine SmoothPoint Q; f pole at Q; Q ∈ ker; P = P_T")
are exactly REDUCTION 2 (R2a–R2e). They are *mechanical given inertiaDeg = 1* modulo the
carrier↔CoordinateRing bridge (R2a) and the value-identity packaging (R2e).

---

## What this pass shipped (verified)

### L6Witnesses.lean
1. **`Sinf_liesOver_of_algebraMap_X_mem` (L1.5) — CLOSED, axiom-clean**
   `[propext, Classical.choice, Quot.sound]`. `algebraMap X ∈ P` (for `P` prime) ⟹
   `P.LiesOver (X)`. Pure maximality of `(X)`; lets the residuals be stated in the clean
   `LiesOver` form.
2. **`Sinf_residue_surjective_of_primeOver` (CORE) — the SOLE irreducible `sorry`**:
   residue map `K[X]/(X) → carrier/P` surjective for a bare prime over `(X)`. Decomposition
   note in the docstring (needs `K̄`-base-change + Galois descent, OR residue Frobenius —
   both absent; 1a/Route-B both dead).
3. **`Sinf_inertiaDeg_eq_one_of_primeOver` (REDUCTION 1) — axiom-clean modulo the CORE**:
   `inertiaDeg (X) P = 1` from the CORE via the shipped
   `Sinf.inertiaDeg_eq_one_of_algebraMap_surjective`. Realises Part 1 concretely.
4. **`Sinf_kernelPoint_of_inertiaDeg_one_of_primeOver` (REDUCTION 2) — sharp residual**:
   place→point GIVEN `inertiaDeg (X) P = 1`. Sizeable-but-non-deep (R2a carrier↔CR bridge,
   R2e value identity). Documents Parts 2–4.
5. **`Sinf_primeOver_eq_kernelPrime_of_algebraMap_X_mem` (TARGET) — now `sorry`-FREE**:
   pure composition L1.5 → REDUCTION 1 (CORE) → REDUCTION 2. The bundled gap is decomposed
   into two named sharp leaves (CORE deep + REDUCTION 2 buildable).

### NormValuation.lean (reusable de-`IsAlgClosed` infrastructure — VERIFIED axiom-clean)
6. **`exists_coordinates_of_isMaximal_of_surjective`** — `[IsAlgClosed]`-free
   `exists_coordinates_of_isMaximal`, takes residue surjectivity as hypothesis.
   `#print axioms` = `[propext, Classical.choice, Quot.sound]`.
7. **`equation_of_coordinates_of_field`** — `[IsAlgClosed]`-free `equation_of_coordinates`
   (body was already field-agnostic). Axiom-clean.
8. **`exists_smoothPoint_of_isMaximal_of_surjective`** — `[IsAlgClosed]`-free
   `exists_smoothPoint_of_isMaximal`, `[IsElliptic]` + residue surjectivity ⟹ `SmoothPoint`.
   Axiom-clean. **This concretely validates Finding 3 (R2c)**: given the residue
   surjectivity (= the CORE output), the geometric point extraction is mechanical and clean.
   It is the de-`IsAlgClosed` engine REDUCTION 2 will consume once R2a (carrier↔CR) ships.

## Axiom keystones (report)
`#print axioms HasseWeil.sepDegree_oneSub_eq_pointCount` and `HasseWeil.ker_deg_skeleton`:
both `[propext, sorryAx, Classical.choice, Quot.sound]` — the `sorryAx` now flows through
exactly the two named sharp residuals (CORE + REDUCTION 2) instead of the former opaque
bundled extraction `sorry`. Fully closing requires discharging the CORE (new `K̄`-base-change
/ residue-Frobenius infrastructure) and REDUCTION 2's R2a/R2e; at that point both keystones
become `[propext, Classical.choice, Quot.sound]` = **V.1.3 COMPLETE**.

---

## UNIFICATION PASS (deep pass 2026-05-28, `worker-tensor-isom`) — two leaves → ONE

**Verdict re-confirmed after a fresh route assessment (both routes + all cardinality
squeezes are circular or require the same descent).** The CORE and R2a were **both**
re-derived from a *single* sharply-stated deep leaf — the curve↔function-field place
correspondence over finite `K`:

### THE single irreducible leaf
**`Sinf_primeOver_eq_kernelPrime_place`** (`L6Witnesses.lean`, the SOLE `sorryAx` source now
flowing into `sepDegree_oneSub_eq_pointCount` / `ker_deg_skeleton`):
```
(data) (P) [P.IsPrime] [P.LiesOver (X)] → ∃ T : (1−π).kernel, P = bridge_Bi_kernelToPrime_v2 … T
```
*A bare carrier prime `P` over `(X)` (a pole of `f`) IS the place at an `F_q`-rational kernel
point.* `#print axioms` = `[propext, sorryAx, Classical.choice, Quot.sound]`.

### Everything else closed on top of it (verified `#print axioms`)
* **`Sinf_residue_in_base_of_primeOver`** (CORE residue-value): from the bridge `P = P_T`,
  this is the *shipped axiom-clean* `Sinf_kappa_kernelPrime_residue_in_base` (the point `T`
  is now in hand). `sorryAx` only via the bridge.
* **`Sinf_residue_surjective_of_primeOver`** (CORE) — pure composition over residue-in-base
  (mirrors `Sinf_kappa_kernelPrime_algebraMap_surjective`). `sorryAx` only via the bridge.
* **`Sinf_inertiaDeg_eq_one_of_primeOver`** (REDUCTION 1) — CORE → shipped abstract bridge.
* **`Sinf_kernelPoint_place_eq_of_inertiaDeg_one_of_primeOver`** (R2a) — now a pure
  composition: the bridge gives `P = P_T`, so the two `HeightOneSpectrum`s share an `asIdeal`
  and their valuations coincide (`HeightOneSpectrum.ext`). `sorryAx` only via the bridge.
* **`Sinf_primeOver_eq_kernelPrime_of_valuation_eq`** (R2e) — **axiom-clean**
  `[propext, Classical.choice, Quot.sound]` (independently verified).

Net: the *bundled two-leaf* state (CORE deep + REDUCTION-2 sizeable) collapses to **ONE** deep
leaf; R2a/R2e/REDUCTION-1 are now shipped or pure compositions. The two keystones'
`sorryAx` flows through `Sinf_primeOver_eq_kernelPrime_place` ALONE.

### Why the bridge is irreducible (route assessment, all dead)
1. **Route Direct (value identity in reverse)** cannot *manufacture* `K`-rationality — it is
   an identity between two already-known objects.
2. **Route B / cardinality squeeze** is circular three ways: (a) `Σ e·f = [K(E):K(1/f)]` is the
   only degree handle (`finrank_eq_weighted_poleDegree_of_nonconstant`), `=2·pointCount`
   reproduces the goal; (b) `#primesOverFinset ≤ pointCount` via the projective pole count
   `l6_lemma5` needs a place-by-place `primesOver → projectiveDivisorOf.support` map, which
   needs each prime `K`-rational = the bridge; (c) {geometric poles}≃{places `P`} is a
   bijection only because each `P` has degree 1 = the bridge.
3. **Residue Frobenius** (revisited): `Frob = (·)^q` *does* restrict to a ring endo of the
   carrier `integralClosure K[1/f] K(E)` (since `Frob(1/f)=(1/f)^q ∈ K[1/f]` and integrality
   is preserved) — Finding 2's "not an endo" is *too strong*; but `Frob` stabilises `P`
   (`Frob⁻¹ P = P`) **iff** `v_P` is Frobenius-fixed, which is the geometric content again.
   So the residue-Frobenius descent `a^q ≡ a (mod P)` remains circular without the
   poles-are-Frobenius-fixed input. Dead, but for a sharper reason.

### Concrete remaining sub-plan to CLOSE `Sinf_primeOver_eq_kernelPrime_place`
The genuinely-missing infrastructure (multi-file, est. several hundred LOC), in dependency
order:
* **(S1) `K̄`-base-change of the place `v_P`.** `K̄(E)/K(E)` is *infinite*-dimensional, so
  abstract place-extension theory does **not** apply; the extension must go through the curve
  geometry. Build: from the carrier prime `P` (= a `HeightOneSpectrum` of the carrier, a DVR
  `O_{v_P} ⊂ L=K(E)`), and Phase-G's `functionField_baseChange : K(E) →ₐ[K] K̄(E)`, produce
  the (finite, nonempty) set of geometric points of `E_K̄` lying on `v_P` — equivalently the
  `K̄`-valuation subrings of `K̄(E)` dominating `O_{v_P}·K̄`. The clean target: a `SmoothPoint`
  of `E_K̄` (= a `K̄`-rational point, via the `[IsAlgClosed K̄]` correspondence
  `smoothPointEquivHeightOneSpectrum`) whose place restricts to `v_P`.
* **(S2) the geometric points on `v_P` are `F_q`-rational** (L5 input, *available*): each such
  point is a pole of `f_K̄ = x∘(1−π)_K̄`, hence in `ker(oneSubGeomFrobHom)` =
  `fixedLocus_geomFrobenius` = `range includePointBC` (`Curves/FrobeniusFixedPoint.lean`), so
  its coordinates satisfy `c^q=c` ⟹ `c ∈ range(K→K̄)` (`frobenius_fixed_iff_mem_baseField`).
* **(S3) Galois descent: degree 1.** The geometric points on `v_P` form a single
  `Gal(K̄/K)`-orbit of size `[κ(P):K]` (`Curves/GaloisAction.lean` `mapPoint` / `IsDefinedOverF`
  give the action). Each is Frobenius-fixed (S2) ⟹ Galois-fixed ⟹ the orbit is a singleton
  ⟹ `[κ(P):K]=1`, and the unique geometric point descends to a `K`-rational kernel point `T`
  with `v_P = pointValuation T`. Then `Sinf_primeOver_eq_kernelPrime_of_valuation_eq` (R2e,
  axiom-clean) gives `P = P_T`.

The cleanest *single* mathlib-aligned sub-target is **(S1)**: a `[IsAlgClosed]`-free
"place of `K(E)` with `F_q`-rational geometric fibre ⟹ degree-1 place" descent, built on the
existing `GaloisAction` + `FrobeniusFixedPoint` assets and Phase-G `functionField_baseChange`.
Tracked: `/develop` `T-SINF-CLOSED-POINT-PRIME-BRIDGE` (now a single sub-leaf S1+S3, with S2
shipped).

---

## PHASE 1 UNIFICATION (deep pass 2026-05-28, `worker-tensor-isom`) — TWO leaves → ONE via combinator

**Verdict re-confirmed: `Sinf_primeOver_eq_kernelPrime_place` (the previously SOLE deep leaf)
is now itself a *pure composition* of:**
1. A PURE FINSET/CARDINALITY **combinator**
   `Sinf_primeOver_eq_kernelPrime_place_of_sum_inertia_eq_pointCount` (axiom-clean
   `[propext, Classical.choice, Quot.sound]`).
2. A SINGLE NEW SHARP LEAF
   `Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount`:
   `Σ_{P ∈ primesOverFinset (X)} inertiaDeg (X) P = pointCount W.toAffine`.

This is the **cardinality dual** of the per-prime statement. It moves the irreducible deep
content from a per-prime structural identity (`P = P_T`) to a *cardinality identity* (the
residue-degree-weighted prime count = the kernel point count). The combinator dispatches
the per-prime conclusion from the cardinality via:
* `image := Finset.univ.image (T ↦ P_T)` is `⊆ primesOverFinset` (backward direction
  shipped, `bridge_Bii_kernelToPrime_mem_primesOverFinset_v2`).
* `image.card = #ker = pointCount` (injectivity `Sinf_kernelToPrime_v2_injective` +
  `kernel_eq_top_of_hom_eq_id_sub_frobenius`).
* `pointCount = Σ f_P ≥ #primesOverFinset` (sum hypothesis + `Ideal.inertiaDeg_pos ≥ 1`).
* `#image ≤ #primesOverFinset` and `#image = pointCount ≥ #primesOverFinset` together force
  `image = primesOverFinset` (`Finset.eq_of_subset_of_card_le`).
* Therefore the arbitrary prime `P ∈ primesOverFinset = image`, i.e. `P = P_T` for some `T`.

### K1-K6 decomposition of the sum identity (the new sharp leaf's discharge plan)
The clean route to discharge `Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount` is the standard
K̄-count (each piece is the natural mathlib-aligned sub-target):

* **K1 — `K̄`-base-change of the at-infinity carrier / curve places.** Build the bijection
  between `data.carrier`-primes over `(X)` and `K̄`-base-changed carrier-primes over `(X)·K̄`,
  with multiplicity = inertia degree (the residue-degree of `P/(X)` is `f_P =
  [κ(P):K] = #{K̄-primes lying over P}` since `κ(P)/K` is finite-field-separable, so
  `κ(P) ⊗_K K̄ ≃ K̄^{f_P}`).
* **K2 — `IsAlgClosed` correspondence over `K̄`.** Primes of `D_K̄ := carrier_K̄` over `(X)` ↔
  `K̄`-points of `E_K̄` that are poles of `f_K̄ := x ∘ (1−π)_K̄` (the shipped
  `smoothPoint_fiber_eq_primesOver` over `K̄`).
* **K3 — poles of `f_K̄` over `K̄` = `ker((1−π)_K̄)`** (geometric kernel; the geometric
  Frobenius-fixed locus, via `pole_gamma_pullback_x_imp_kernel` over `K̄`).
* **K4 — `#ker((1−π)_K̄) = pointCount W.toAffine`** (L5's
  `ncard_ker_oneSubGeomFrobHom_eq_pointCount`, `Curves/FrobeniusFixedPoint.lean`).
* **K5 — splitting identity** `Σ_{P ∈ primesOverFinset (X)} f_P = #{primes of D_K̄ over (X)·K̄}`:
  residue-degree of `P/(X)` splits under K̄/K base extension into `f_P` `K̄`-primes (since
  `κ(P) ⊗_K K̄ ≃ K̄^{f_P}` — finite-separable extension splits completely over `K̄`).
* **K6 — compose K1-K5**: `Σ f_P = #D_K̄-primes-over-(X)` (K1+K5) `= #(K̄-points poles of f_K̄)`
  (K2) `= #ker((1−π)_K̄)` (K3) `= pointCount` (K4).

The genuinely-missing infrastructure (multi-file, est. several hundred LOC) sits in K1+K5
(the prime-level base change with the residue-degree splitting). Phase-G's
`functionField_baseChange : K(E) →ₐ[K] K̄(E)` is the field-level half (axiom-clean); the
prime-level half (`Sinf` carrier base-change + K5 splitting `κ(P) ⊗_K K̄ ≃ K̄^{f_P}`) is the
sharp remaining structural addition. K2 + K3 + K4 are shipped or near-shipped (the
`[IsAlgClosed]` engine + L5 geometry).

### What Phase 1 shipped (verified `#print axioms`)
* `Sinf_primeOver_eq_kernelPrime_place_of_sum_inertia_eq_pointCount`: `[propext,
  Classical.choice, Quot.sound]` — pure Finset/cardinality combinator, axiom-clean.
* `Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount`: `[propext, sorryAx, Classical.choice,
  Quot.sound]` — THE single remaining sorry of the V.1.3 surjectivity residual.
* `Sinf_primeOver_eq_kernelPrime_place`: `[propext, sorryAx, Classical.choice, Quot.sound]`
  — `sorryAx` traces *only* through `Sinf_sum_inertiaDeg_over_xIdeal_eq_pointCount`.
* `sepDegree_oneSub_eq_pointCount`, `ker_deg_skeleton` (the V.1.3 keystones): unchanged
  axiom profile `[propext, sorryAx, Classical.choice, Quot.sound]` — `sorryAx` now flows
  exclusively through the new sharp leaf.

### Statement-shape adjustment (propagated)
The new sharp leaf and combinator mention `pointCount`, so they need
`[Fintype W.toAffine.Point]`. This instance binder was added to the affected downstream
theorems in `L6Witnesses.lean` (`Sinf_residue_in_base_of_primeOver`,
`Sinf_residue_surjective_of_primeOver`, `Sinf_inertiaDeg_eq_one_of_primeOver`,
`Sinf_kernelPoint_place_eq_of_inertiaDeg_one_of_primeOver`,
`Sinf_kernelPoint_of_inertiaDeg_one_of_primeOver`,
`Sinf_primeOver_eq_kernelPrime_of_algebraMap_X_mem`, `Sinf_exists_kernelPoint_of_primeOver`,
`Sinf_primeOver_xIdeal_eq_kernelPrime`, `bridge_Bii_surjective_v2`,
`bridge_Bii_mem_primesOverFinset_v2`). The upstream caller `GapSpines.lean` already has
`[Fintype W.toAffine.Point]` in its `variable` block.

### Why Phase 1 is non-circular (route assessment)
The combinator's circularity-trap is the sum hypothesis `Σ f_P = pointCount`. Two things
matter:
1. **The combinator does not derive the sum from `f_P = 1` for each P over (X)** (which
   would be the *content* the original `Sinf_primeOver_eq_kernelPrime_place` produces, hence
   circular). It uses only `f_P ≥ 1` (mathlib `Ideal.inertiaDeg_pos`, axiom-clean) plus the
   sum.
2. **The sum identity has a non-circular route via K1-K6** (above): K2-K4 do NOT invoke any
   `K`-rationality of bare primes — they work over `K̄` where `IsAlgClosed` makes places ↔
   points trivial; the K1+K5 base-change splitting is a *separable algebra* identity, not a
   `K`-rationality claim about specific primes. The K̄-count produces `Σ f_P = pointCount`
   without first knowing each `f_P = 1`.

After the K1-K6 leaf ships, *both* the bridge `Sinf_primeOver_eq_kernelPrime_place` AND the
per-prime `inertiaDeg = 1` (`Sinf_inertiaDeg_eq_one_of_primeOver`) become axiom-clean
consequences (the latter via the bridge + `bridge_Biv_inertia_eq_one_v2`).
