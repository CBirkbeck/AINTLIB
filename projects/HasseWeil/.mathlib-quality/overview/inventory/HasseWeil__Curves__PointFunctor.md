# Inventory: ./HasseWeil/Curves/PointFunctor.lean

**Total declarations**: 24  
**Defs**: 6 (noncomputable), **Lemmas/theorems**: 18, **Instances**: 0  
**Sorries**: none  
**set_option maxHeartbeats**: none  
**Long proofs (>30 lines)**: 1 (`evalAtPullback_mk`, ~48 lines)

---

## Namespace `HasseWeil.Curves.SmoothPlaneCurve`

---

### `noncomputable def evalAt`

- **Type**: `(C : SmoothPlaneCurve F) → C.SmoothPoint → C.CoordinateRing →+* F`
- **What**: The residue ring homomorphism at a smooth affine point `P`; concretely, it factors bivariate polynomial evaluation `g ↦ g.evalEval P.x P.y` through the quotient `F[C]`. Corresponds to the residue field at the maximal ideal at `P` (Silverman II.1).
- **How**: Direct term: `AdjoinRoot.evalEval P.nonsingular.1`. The `AdjoinRoot` API delivers the ring hom into `F` from a root of the Weierstrass polynomial evaluated at `P.x, P.y`.
- **Hypotheses**: `F` a field, `C : SmoothPlaneCurve F`, `P : C.SmoothPoint` (carries the nonsingularity proof).
- **Uses from project**: `SmoothPlaneCurve` (from `HasseWeil.Curves.Basic`)
- **Used by**: `evalAt_mk`, `evalAt_x`, `evalAt_y`, `evalAt_algebraMap`, `ker_evalAt`, `evalAtPullback` (def), `evalAtPullback_apply`, `evalAt_toPointMap` — all within this file; also used heavily in `AdditionPullback/SamePlace.lean`, `EC/TranslateValuation.lean`, `Hasse/L6Witnesses.lean`.
- **Visibility**: public
- **Lines**: 51–52 (1-line term)
- **Notes**: none

---

### `@[simp] theorem evalAt_mk`

- **Type**: `C.evalAt P (CoordinateRing.mk C.toAffine g) = g.evalEval P.x P.y`
- **What**: Evaluating the `mk g` class in the coordinate ring at `P` equals the bivariate evaluation `g.evalEval P.x P.y`.
- **How**: `AdjoinRoot.evalEval_mk`, the defining computation lemma for `AdjoinRoot.evalEval`.
- **Hypotheses**: `g : Polynomial (Polynomial F)`.
- **Uses from project**: `evalAt`
- **Used by**: `evalAt_x`, `evalAt_y`, `evalAt_algebraMap`, `ker_evalAt`, `evalAt_toPointMap`
- **Visibility**: public, `@[simp]`
- **Lines**: 54–57 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem evalAt_x`

- **Type**: `C.evalAt P (CoordinateRing.mk C.toAffine (C Polynomial.X)) = P.x`
- **What**: Evaluation sends the x-coordinate function to the x-coordinate of `P`.
- **How**: Rewrites via `evalAt_mk` then `Polynomial.evalEval_C`.
- **Hypotheses**: none beyond context.
- **Uses from project**: `evalAt_mk`
- **Used by**: `toPointMap_id`
- **Visibility**: public, `@[simp]`
- **Lines**: 60–63 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem evalAt_y`

- **Type**: `C.evalAt P (CoordinateRing.mk C.toAffine Y) = P.y`
- **What**: Evaluation sends the y-coordinate class `Y` (= the adjoin-root variable) to `P.y`.
- **How**: Rewrites via `evalAt_mk` then `Polynomial.evalEval_X`.
- **Hypotheses**: none beyond context.
- **Uses from project**: `evalAt_mk`
- **Used by**: `toPointMap_id`
- **Visibility**: public, `@[simp]`
- **Lines**: 66–68 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem evalAt_algebraMap`

- **Type**: `C.evalAt P (algebraMap F C.CoordinateRing c) = c`
- **What**: Evaluation at `P` is the identity on scalars embedded via `algebraMap`.
- **How**: Converts the `algebraMap` input to `mk (C (C c))` and uses `evalAt_mk` + `evalEval_C` simp lemmas.
- **Hypotheses**: `c : F`.
- **Uses from project**: `evalAt_mk`
- **Used by**: `evalAtPullback_algebraMap`
- **Visibility**: public, `@[simp]`
- **Lines**: 72–77 (proof ~4 lines)
- **Notes**: none

---

### `theorem ker_evalAt`

- **Type**: `RingHom.ker (C.evalAt P) = C.maximalIdealAt P`
- **What**: The kernel of the evaluation ring homomorphism at `P` equals the maximal ideal at `P` (generated by the two coordinate classes `XClass` and `YClass`). This is the scheme-theoretic identification: a function vanishes at `P` iff it lies in the maximal ideal.
- **How**: Uses `Ideal.IsMaximal.eq_of_le` from the maximality of `maximalIdealAt P` (project lemma `SmoothPlaneCurve.maximalIdealAt_isMaximal`). The kernel is shown proper (map of 1 is nonzero) and to contain the generating set `{XClass, YClass}` via `evalAt_mk` and `simp`.
- **Hypotheses**: none beyond context.
- **Uses from project**: `maximalIdealAt_isMaximal` (Basic.lean), `evalAt_mk`, `evalAt` (implicit)
- **Used by**: `maximalIdealAt_toPointMap`; heavily used in `SamePlace.lean`, `MulByIntSamePlace.lean`, etc.
- **Visibility**: public
- **Lines**: 86–100 (proof ~14 lines)
- **Notes**: none

---

## Namespace `HasseWeil.Curves.CurveMap`

---

### `noncomputable def evalAtPullback`

- **Type**: `{φ : CurveMap C₁ C₂} → φ.CoordHom → C₁.SmoothPoint → C₂.CoordinateRing →+* F`
- **What**: The ring homomorphism `C₂.CoordinateRing →+* F` obtained by composing the evaluation at `P ∈ C₁.SmoothPoint` with the coordinate-ring pullback `coordHom.toAlgHom`. This reads off the value of any `C₂`-function along the pullback at `P`.
- **How**: Direct term: `(C₁.evalAt P).comp coordHom.toAlgHom.toRingHom`.
- **Hypotheses**: A coordinate-ring witness `coordHom : φ.CoordHom`.
- **Uses from project**: `evalAt`
- **Used by**: `evalAtPullback_apply`, `evalAtPullback_algebraMap`, `evalAtPullback_mk`, `imagePoint_equation`, `toPointMap`, `toPointMap_x`, `toPointMap_y`, `evalAt_toPointMap`, `toPointMap_id`, `toPointMap_comp`, `maximalIdealAt_toPointMap`
- **Visibility**: public
- **Lines**: 113–115 (1-line term)
- **Notes**: none

---

### `@[simp] theorem evalAtPullback_apply`

- **Type**: `evalAtPullback coordHom P u = C₁.evalAt P (coordHom.toAlgHom u)`
- **What**: Definitional unfolding of `evalAtPullback`: application equals `evalAt` applied to the pullback of `u`.
- **How**: `rfl`
- **Hypotheses**: `u : C₂.CoordinateRing`.
- **Uses from project**: `evalAtPullback`, `evalAt`
- **Used by**: `evalAtPullback_algebraMap`, `toPointMap_id`, `toPointMap_comp`
- **Visibility**: public, `@[simp]`
- **Lines**: 117–119 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem evalAtPullback_algebraMap`

- **Type**: `evalAtPullback coordHom P (algebraMap F C₂.CoordinateRing c) = c`
- **What**: Pulling back a scalar through `coordHom` and evaluating at `P` returns the scalar unchanged; the pullback is `F`-linear.
- **How**: Uses `evalAtPullback_apply`, `AlgHom.commutes` (AlgHom fixes `algebraMap`), and `evalAt_algebraMap`.
- **Hypotheses**: `c : F`.
- **Uses from project**: `evalAtPullback_apply`, `evalAt_algebraMap`
- **Used by**: `evalAtPullback_mk` (line 170)
- **Visibility**: public, `@[simp]`
- **Lines**: 121–124 (1-line proof)
- **Notes**: none

---

### `theorem evalAtPullback_mk`

- **Type**: `evalAtPullback coordHom P (CoordinateRing.mk C₂.toAffine g) = g.evalEval (evalAtPullback coordHom P (mk C₂ (C X))) (evalAtPullback coordHom P (mk C₂ Y))`
- **What**: Universal bivariate evaluation property of `evalAtPullback`: for any bivariate polynomial `g`, the pullback-evaluation of `mk g` equals `g` evaluated at the image x- and y-coordinates defined by `evalAtPullback`.
- **How**: Introduces local abbreviations `x'` and `y'`, then proves equality of two ring homomorphisms `(F[X])[Y] →+* F` by double application of `Polynomial.ringHom_ext` — first on the outer `Y` variable, then on the inner `X` variable, checking agreement on constants, `X`, and `Y`. The two ring homs are: `ρ = evalAtPullback ∘ mk C₂` (factors through `F[C₂]`) and `σ = Polynomial.evalRingHom x' ∘ Polynomial.evalRingHom (C y')`. Agreement on constants uses `evalAtPullback_algebraMap`.
- **Hypotheses**: `g : Polynomial (Polynomial F)`.
- **Uses from project**: `evalAtPullback`, `evalAtPullback_algebraMap`
- **Used by**: `imagePoint_equation`, `evalAt_toPointMap`
- **Visibility**: public
- **Lines**: 133–189 (proof lines 142–189, ~48 lines)
- **Notes**: Proof >30 lines. The double `Polynomial.ringHom_ext` structure is verbose; could potentially be shortened by appealing to a general "ring hom determined by generators" result.

---

### `theorem imagePoint_equation`

- **Type**: `C₂.toAffine.Equation (evalAtPullback coordHom P (mk C₂ (C X))) (evalAtPullback coordHom P (mk C₂ Y))`
- **What**: The image coordinates `(x', y')` defined by `evalAtPullback` satisfy the Weierstrass equation of `C₂`. This certifies that the pair is indeed a point on `C₂`.
- **How**: Applies `evalAtPullback_mk` to `C₂.toAffine.polynomial`, then uses `AdjoinRoot.mk_self` (the Weierstrass polynomial is zero in the coordinate ring) and `map_zero` to conclude `0 = W₂.polynomial.evalEval x' y'`.
- **Hypotheses**: `coordHom : φ.CoordHom`.
- **Uses from project**: `evalAtPullback_mk`
- **Used by**: `toPointMap` (def)
- **Visibility**: public
- **Lines**: 194–205 (proof ~6 lines)
- **Notes**: none

---

### `noncomputable def toPointMap`

- **Type**: `[C₂.toAffine.IsElliptic] → φ.CoordHom → C₁.SmoothPoint → C₂.SmoothPoint`
- **What**: The induced map on smooth points: given a coordinate-ring witness for a curve map `φ : C₁ → C₂`, maps an affine smooth point `P ∈ C₁` to a smooth point of `C₂`. The x- and y-coordinates of the image are given by `evalAtPullback`; nonsingularity uses `equation_iff_nonsingular` (Silverman III.1.4), which holds for elliptic curves.
- **How**: `where`-structure: `x` and `y` are `evalAtPullback` applied to the coordinate classes; `nonsingular` is discharged by `WeierstrassCurve.Affine.equation_iff_nonsingular.mp` applied to `imagePoint_equation coordHom P`.
- **Hypotheses**: `[C₂.toAffine.IsElliptic]` (needed for `equation_iff_nonsingular` to apply; elliptic curves are nonsingular everywhere).
- **Uses from project**: `evalAtPullback`, `imagePoint_equation`
- **Used by**: `toPointMap_x`, `toPointMap_y`, `evalAt_toPointMap`, `toPointMap_id`, `toPointMap_comp`, `maximalIdealAt_toPointMap`; also used in `IsogenyAG.lean`.
- **Visibility**: public
- **Lines**: 212–221 (term/where body)
- **Notes**: The `[IsElliptic]` hypothesis is used only to invoke `equation_iff_nonsingular`; the definition would break for non-elliptic curves only because nonsingularity would not be automatic from the equation.

---

### `@[simp] theorem toPointMap_x`

- **Type**: `(toPointMap coordHom P).x = evalAtPullback coordHom P (mk C₂ (C X))`
- **What**: The x-coordinate of the image point equals the pullback-evaluation of the x-coordinate class.
- **How**: `rfl`
- **Hypotheses**: `[C₂.toAffine.IsElliptic]`.
- **Uses from project**: `toPointMap`, `evalAtPullback`
- **Used by**: `toPointMap_id`, `toPointMap_comp`
- **Visibility**: public, `@[simp]`
- **Lines**: 223–228 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem toPointMap_y`

- **Type**: `(toPointMap coordHom P).y = evalAtPullback coordHom P (mk C₂ Y)`
- **What**: The y-coordinate of the image point equals the pullback-evaluation of the y-coordinate class.
- **How**: `rfl`
- **Hypotheses**: `[C₂.toAffine.IsElliptic]`.
- **Uses from project**: `toPointMap`, `evalAtPullback`
- **Used by**: `toPointMap_id`, `toPointMap_comp`
- **Visibility**: public, `@[simp]`
- **Lines**: 230–234 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem evalAt_toPointMap`

- **Type**: `C₂.evalAt (toPointMap coordHom P) u = evalAtPullback coordHom P u`
- **What**: Coherence/defining property of the points functor: evaluating any coordinate-ring element `u` at the image point `toPointMap coordHom P` equals the pullback evaluation of `u` at `P`. This is the functoriality statement `u(φ(P)) = (φ* u)(P)`.
- **How**: Surjects `u` onto a `mk g` via `AdjoinRoot.mk_surjective`, then reduces both sides to the same bivariate evaluation using `evalAt_mk` and `evalAtPullback_mk`.
- **Hypotheses**: `[C₂.toAffine.IsElliptic]`, `u : C₂.CoordinateRing`.
- **Uses from project**: `toPointMap`, `evalAt_mk`, `evalAtPullback_mk`
- **Used by**: `toPointMap_comp`
- **Visibility**: public, `@[simp]`
- **Lines**: 244–250 (proof ~4 lines)
- **Notes**: none

---

## Namespace `HasseWeil.Curves.CurveMap.CoordHom`

---

### `noncomputable def CoordHom.id`

- **Type**: `(C : SmoothPlaneCurve F) → (CurveMap.id C).CoordHom`
- **What**: The coordinate-ring witness for the identity curve map: the pullback is the identity algebra homomorphism on `C.CoordinateRing`.
- **How**: `where`-structure with `toAlgHom := AlgHom.id F C.CoordinateRing` and `compat _ := rfl`.
- **Hypotheses**: none.
- **Uses from project**: `CurveMap.id` (CurveMap.lean)
- **Used by**: `id_toAlgHom`, `toPointMap_id`
- **Visibility**: public
- **Lines**: 257–259 (term/where, 2-line body)
- **Notes**: none

---

### `@[simp] theorem id_toAlgHom`

- **Type**: `(CoordHom.id C).toAlgHom = AlgHom.id F C.CoordinateRing`
- **What**: The underlying algebra homomorphism of the identity coordinate-ring witness is `AlgHom.id`.
- **How**: `rfl`
- **Hypotheses**: none.
- **Uses from project**: `CoordHom.id`
- **Used by**: `toPointMap_id`
- **Visibility**: public, `@[simp]`
- **Lines**: 261–262 (1-line proof)
- **Notes**: none

---

### `noncomputable def CoordHom.comp`

- **Type**: `ψ.CoordHom → φ.CoordHom → (ψ.comp φ).CoordHom`
- **What**: Composition of coordinate-ring witnesses: the pullback for the composed map `ψ ∘ φ` is `φ* ∘ ψ*` (pullbacks compose contravariantly).
- **How**: `where`-structure. `toAlgHom := φ_ch.toAlgHom.comp ψ_ch.toAlgHom`. The `compat` field is checked by unfolding the composition and applying `ψ_ch.compat` then `φ_ch.compat` in sequence.
- **Hypotheses**: `ψ_ch : ψ.CoordHom`, `φ_ch : φ.CoordHom`.
- **Uses from project**: `CurveMap.comp` (CurveMap.lean)
- **Used by**: `comp_toAlgHom`, `toPointMap_comp`
- **Visibility**: public
- **Lines**: 266–274 (proof 6 lines)
- **Notes**: none

---

### `@[simp] theorem comp_toAlgHom`

- **Type**: `(ψ_ch.comp φ_ch).toAlgHom = φ_ch.toAlgHom.comp ψ_ch.toAlgHom`
- **What**: The underlying algebra homomorphism of the composed witness is the composition of the individual homomorphisms (in contravariant order).
- **How**: `rfl`
- **Hypotheses**: none.
- **Uses from project**: `CoordHom.comp`
- **Used by**: `toPointMap_comp`
- **Visibility**: public, `@[simp]`
- **Lines**: 276–278 (1-line proof)
- **Notes**: none

---

### `@[simp] theorem toPointMap_id`

- **Type**: `toPointMap (CoordHom.id C) P = P`
- **What**: Functoriality: the identity curve map induces the identity on smooth points.
- **How**: Extends by x/y component using `toPointMap_x`/`y`, `evalAtPullback_apply`, `id_toAlgHom`, `AlgHom.coe_id`, `evalAt_x`/`evalAt_y`.
- **Hypotheses**: `[C.toAffine.IsElliptic]`.
- **Uses from project**: `toPointMap_x`, `toPointMap_y`, `evalAtPullback_apply`, `CoordHom.id_toAlgHom`, `evalAt_x`, `evalAt_y`
- **Used by**: unused within this file (terminal; used externally, e.g. `IsogenyAG.lean`)
- **Visibility**: public, `@[simp]`
- **Lines**: 285–291 (proof ~6 lines)
- **Notes**: unused in file

---

### `@[simp] theorem toPointMap_comp`

- **Type**: `toPointMap (ψ_ch.comp φ_ch) P = toPointMap ψ_ch (toPointMap φ_ch P)`
- **What**: Functoriality: composition of curve maps induces composition of point maps.
- **How**: Extends by x/y via `simp` with `toPointMap_x`, `toPointMap_y`, `evalAtPullback_apply`, `comp_toAlgHom`, `AlgHom.comp_apply`, `evalAt_toPointMap`.
- **Hypotheses**: `[C₂.toAffine.IsElliptic]`, `[C₃.toAffine.IsElliptic]`.
- **Uses from project**: `toPointMap_x`, `toPointMap_y`, `evalAtPullback_apply`, `comp_toAlgHom`, `evalAt_toPointMap`
- **Used by**: unused within this file (terminal; likely used externally)
- **Visibility**: public, `@[simp]`
- **Lines**: 294–300 (proof ~4 lines)
- **Notes**: unused in file

---

### `theorem maximalIdealAt_toPointMap`

- **Type**: `C₂.maximalIdealAt (toPointMap coordHom P) = Ideal.comap coordHom.toAlgHom.toRingHom (C₁.maximalIdealAt P)`
- **What**: Scheme-theoretic coherence: the maximal ideal at the image point is the comap of the maximal ideal at the source along the coordinate-ring pullback. This is the ideal-theoretic shadow of the points functor (Silverman II.2.4(c)).
- **How**: Reduces via `ker_evalAt` on both sides and `evalAt_toPointMap`/`evalAtPullback_apply` to equality of kernels, which holds by `rfl`.
- **Hypotheses**: `[C₂.toAffine.IsElliptic]`, `coordHom : φ.CoordHom`.
- **Uses from project**: `ker_evalAt`, `evalAt_toPointMap`, `evalAtPullback_apply`, `toPointMap`
- **Used by**: unused within this file (terminal)
- **Visibility**: public
- **Lines**: 308–316 (proof ~6 lines)
- **Notes**: unused in file

---

## Namespace `HasseWeil.Curves.SmoothPlaneCurve`

---

### `def SmoothPoint.toAffinePoint`

- **Type**: `C.SmoothPoint → C.toAffine.Point`
- **What**: Promotes an affine smooth point to the mathlib inductive `WeierstrassCurve.Affine.Point` type via the `Point.some` constructor, carrying the nonsingularity proof. This is the bridge between the project's `SmoothPoint` record and the mathlib group-law `Point` type.
- **How**: Direct term: `WeierstrassCurve.Affine.Point.some P.x P.y P.nonsingular`.
- **Hypotheses**: none.
- **Uses from project**: `SmoothPoint` (Basic.lean)
- **Used by**: `SmoothPoint.toAffinePoint_def`; used externally in `TranslateOrdInfty.lean`, `SamePlace.lean`, `IsogenyAG.lean`.
- **Visibility**: public
- **Lines**: 330–331 (1-line term)
- **Notes**: none

---

### `@[simp] theorem SmoothPoint.toAffinePoint_def`

- **Type**: `P.toAffinePoint = WeierstrassCurve.Affine.Point.some P.x P.y P.nonsingular`
- **What**: Definitional unfolding of `toAffinePoint`.
- **How**: `rfl`
- **Hypotheses**: none.
- **Uses from project**: `SmoothPoint.toAffinePoint`
- **Used by**: unused within this file (terminal; used externally in `TranslateOrdInfty.lean` line 517)
- **Visibility**: public, `@[simp]`
- **Lines**: 333–334 (1-line proof)
- **Notes**: unused in file
