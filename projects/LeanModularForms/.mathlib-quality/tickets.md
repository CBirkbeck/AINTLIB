# Ticket Board: Hecke-eigenvalue arithmetic (Group A)

*`/develop` Phase 1g, 2026-06-20, branch `dev/leanmodularforms`. Supersedes the completed Miyake
4.6.12 board (backed up to `tickets-PRE-groupA-backup-2026-06-20.md`). Plan: `.mathlib-quality/plan.md`.
Maths: `plan-coefficient-field-arithmetic.md`. New work lands in
`LeanModularForms/Labels/HeckeFieldArithmetic.lean` (new file).*

## Summary
- **Tranche 1 (executable now)**: 5 proof/def + 1 cleanup. Closes both `NewformOrbit` sorries
  **modulo the single isolated input FIH (T002)**. Net: 2 vague sorries → 1 precise foundational one.
- **Tranche 2 (IHR, research roadmap)**: 5 blocked tickets — a multi-month modular-symbol /
  Eichler–Shimura build that discharges T002. NOT session-executable; documented, not for immediate
  `/beastmode`.
- Open: 4 (T001, T003, T004, T005) | Blocked: 1 (T002) + 5 (IHR) | Parallel: T001 then {T003}; T004,T005 after T002+T003.

## Dependency order (Tranche 1)
```
T001 (def 𝕋ℤ) ── T003 (eigenHom) ──┬─ T004 (closes coeffSeq_isIntegral)
                                    └─ T005 (closes coeffField finite-dim)
T002 (FIH input; blocked on IHR) ───┘   →  CLEANUP-1
```

---

## Tranche 1 — L1 + FIH interface

### [T001] Define the integral Hecke algebra `heckeAlgℤ` + ℂ-linear Hecke endomorphisms
- **Status**: done (2026-06-20) · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: none · **Parallel**: yes · **Type**: def + API
- **Progress**: DONE. New file `Labels/HeckeFieldArithmetic.lean` builds green (3920 jobs). `heckeEnd N k (n : ℕ+)` packages `heckeT_n_cusp` as a `Module.End ℂ` via `heckeT_n_cusp_add`/`_smul`; `heckeAlgℤ N k := Algebra.adjoin ℤ (Set.range (heckeEnd N k) ∪ Set.range (diamondOpCusp k))`; `heckeEnd_mem`, `diamondOpCusp_mem` via `Algebra.subset_adjoin`. No sorry.
- **Mathlib check**: `Algebra.adjoin`, `Subalgebra ℤ (Module.End ℂ M)`, `Module.End` ring structure — all present.

#### Statement
```lean
/-- The Hecke operators `T_n` (n coprime to N), `U_p` (p ∣ N) and diamonds ⟨d⟩ as ℂ-linear
endomorphisms of the cusp-form space. -/
noncomputable def heckeEnd (N : ℕ) [NeZero N] (k : ℤ) (n : ℕ) :
    Module.End ℂ (CuspForm ((Gamma1 N).map (mapGL ℝ)) k) := -- from heckeT_n_cusp + its ℂ-linearity
  sorry  -- (def, not a proof obligation: package heckeT_n_cusp as a Module.End)

/-- The **integral Hecke algebra**: the ℤ-subalgebra of `End_ℂ(S_k(Γ₁N))` generated by all the
Hecke endomorphisms. -/
noncomputable def heckeAlgℤ (N : ℕ) [NeZero N] (k : ℤ) :
    Subalgebra ℤ (Module.End ℂ (CuspForm ((Gamma1 N).map (mapGL ℝ)) k)) :=
  Algebra.adjoin ℤ (Set.range (heckeEnd N k))
```
plus API: `heckeEnd_mem : heckeEnd N k n ∈ heckeAlgℤ N k`; `CommRing ↥(heckeAlgℤ N k)` (the Hecke
endomorphisms commute — reuse the codebase commutativity of `heckeT_n_cusp`).

#### Proof sketch
1. Package `heckeT_n_cusp k n` as a `Module.End ℂ` (it is ℂ-linear — reuse `heckeOperatorLinear` /
   the linear structure already in the Hecke files; if only the bare map exists, build the
   `LinearMap` from the additivity + `map_smul` lemmas in the Hecke files).  *(Decide here whether to
   include `U_p`/diamonds explicitly or note that `heckeT_n_cusp` over all n already generates them
   — `T_p² − ⟨p⟩p^{k-1} = T_{p²}` gives diamonds; `U_p = heckeT_n` at bad p in the codebase's
   convention. Pin the generating set so it includes the bad-prime operators.)*
2. `heckeAlgℤ` is `Algebra.adjoin ℤ` of the range; `heckeEnd_mem` is `Algebra.subset_adjoin`.
3. Commutativity: the generators pairwise commute (codebase: Hecke operators commute), so the
   adjoined subalgebra is commutative — provide the `CommRing` instance via `Algebra.adjoin` of a
   commuting set (or `Subalgebra` of a comm ring once commutativity of the generators is in hand).

#### Mathlib lemmas needed
`Algebra.subset_adjoin`, `Algebra.adjoin_le`, `Module.End` semiring/algebra instances. *(verify the
codebase exposes a ℂ-linear form of `heckeT_n_cusp`; if not, T001 includes building it.)*

#### Generality decision
Keep `N, k` general; the subalgebra over the base ring `ℤ` (we need the *integral* algebra for (A)).

---

### [T002] FIH input — the integral Hecke algebra is module-finite over ℤ
- **Status**: ENDGAME DONE / DEEP INPUT RE-ISOLATED (2026-06-22, via [T002-L1]) — `heckeAlgℤ_finite` is now **PROVEN** (the `End_ℤ(Λ)` endgame) from the single deep input **`exists_HeckeStableLattice`** (`Nonempty (HeckeStableLattice N k)`, the source-faithful full-rank Hecke-stable ℤ-lattice = Shimura (3.5.20)/Thm 3.52). That `sorry` is the sole remaining deep input; it is what **Tranche-2 / IHR-asm** now discharges. · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: T001 (done), IHR-asm (discharges `exists_HeckeStableLattice`) · **Type**: instance (endgame proven; input isolated)
- **Source-faithfulness note (2026-06-21)**: DS's algebraic-integer theorem (Thm 6.5.1, `ds.txt:18066`) is **weight-2-only**, via the homology lattice `H₁(X₁N,ℤ)` + integer Hecke matrix + Cayley–Hamilton; module-finiteness is then `End_ℤ(free)≅M_r(ℤ)` (Ex 6.5.1). **The q-expansion-lattice route was REJECTED** — DS has no q-expansion principle / Sturm bound, and the rational-coeff basis (Cor 6.5.6) is *downstream* of the homology (Galois descent), so that route only relocates the deep input. The weight-`k` generalization needs the `Sym^{k-2}` integral modular-symbol module (IHR-a) — beyond DS; cite Shimura/Hida. The genuine irreducible content is the **existence of the Hecke-stable full-rank free ℤ-lattice Λ**; `IHR-asm` (Λ ⟹ `Module.Finite`) is itself BOUNDED.

#### Statement
```lean
/-- **(FIH)** The integral Hecke algebra is a finite ℤ-module.  This is the single deep classical
input (justified by the integral modular-symbol realization, IHR); it replaces the two ad-hoc deep
sorries in `NewformOrbit.lean`. -/
instance heckeAlgℤ_finite (N : ℕ) [NeZero N] (k : ℤ) : Module.Finite ℤ (heckeAlgℤ N k) := sorry
```

#### Proof sketch
Discharged by Tranche 2: `𝕋ℤ` embeds (Hecke-equivariantly, via the injective period map `ι`) into
`End_ℤ(H_ℤ)` for the finite free modular-symbol module `H_ℤ`; a sub-ℤ-module of a finite free
ℤ-module is finite. **Until IHR is built, this `sorry` is the sole, documented deep input.**

#### Sources
Shimura, *Intro Arith Theory*, Thm 3.48–3.52; Eichler–Shimura; Manin (modular symbols). See
`plan-coefficient-field-arithmetic.md` Layer 2.

---

### [T002-L1] Tier-1 endgame — `heckeAlgℤ_finite` from a full-rank Hecke-stable ℤ-lattice
- **Status**: ✅ **DONE (2026-06-22)** — `HeckeStableLattice` struct + `heckeAlgℤ_finite` **PROVEN** (sorry-free body) from the isolated `exists_HeckeStableLattice`; green (Labels.Label 3958 jobs). Endgame: faithful DERIVED from `spanning` (`LinearMap.ext_on H.spanning`), `Φ : heckeAlgℤ ↪ End_ℤ Λ` (`restrictScalars`+`restrict`), `Module.Finite.linearMap` + `Module.Finite.of_injective`. `instNumberFieldCoeffField` axioms = `{propext, sorryAx, Classical.choice, Quot.sound}`, the `sorryAx` now tracing to `exists_HeckeStableLattice` (the deep, source-faithful full-rank-lattice statement — Shimura (3.5.20)/Thm 3.52) instead of the opaque `heckeAlgℤ_finite`. PHASE-6.5 cleanup deferred (AINTLIB producer rule). · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: T002 (re-isolated its `sorry` into `exists_HeckeStableLattice`) · **Parent**: T002 · **Type**: def + theorem (gate-PASSING; bounded)
- **Approved**: 2026-06-21 (user chose Tier 1), **hardened per the source-faithfulness audit** (agent ab638096): the lattice input field is **full-rank** (`span ℂ Λ = ⊤`, = Shimura's "maximal rank" (3.5.20)), and **faithfulness is DERIVED, never a stubbed field** — this is the anti-"empty-structure" guarantee. Decomp: `decomposition-T002-IHR.md`.

#### Statement
```lean
/-- A Hecke-stable, **full-rank** (Shimura (3.5.20) "maximal rank"), finite free ℤ-lattice in S_k(Γ₁N). -/
structure HeckeStableLattice (N : ℕ) [NeZero N] (k : ℤ) where
  toSubmodule : Submodule ℤ (CuspForm ((Gamma1 N).map (mapGL ℝ)) k)
  free        : Module.Free ℤ toSubmodule
  finite      : Module.Finite ℤ toSubmodule
  spanning    : Submodule.span ℂ (toSubmodule : Set (CuspForm ((Gamma1 N).map (mapGL ℝ)) k)) = ⊤
  stable      : ∀ T ∈ heckeAlgℤ N k, ∀ x ∈ toSubmodule, (T : Module.End ℂ _) x ∈ toSubmodule

/-- **(Tier-2 isolated input — the one remaining `sorry`)** Such a lattice exists: the integral
Eichler–Shimura / integral-q-expansion basis (Shimura Thm 3.52, Miyake 4.5.19(2)). -/
theorem exists_HeckeStableLattice (N : ℕ) [NeZero N] (k : ℤ) :
    Nonempty (HeckeStableLattice N k) := sorry

-- replaces the current `heckeAlgℤ_finite := sorry` with a PROOF from `exists_HeckeStableLattice`:
instance heckeAlgℤ_finite : Module.Finite ℤ (heckeAlgℤ N k) := by
  obtain ⟨H⟩ := exists_HeckeStableLattice N k; ...
```

#### Proof sketch (the endgame — bounded; Shimura (3.5.20)+ρ₀ / DS Ex 6.5.1)
1. `obtain ⟨H⟩ := exists_HeckeStableLattice N k`.
2. **Faithful DERIVED from `spanning`**: `hfaith : ∀ T ∈ heckeAlgℤ N k, (∀ x ∈ H.toSubmodule, (T:End ℂ _) x = 0) → T = 0`. Proof: a ℂ-linear `T` vanishing on `H.toSubmodule` vanishes on `Submodule.span ℂ ↑H.toSubmodule = ⊤` (`H.spanning`) — `LinearMap.ext`, with `x ∈ ⊤ = span` and `Submodule.span_induction` (or `LinearMap.ext_on H.spanning`).
3. **Restriction hom** `Φ : ↥(heckeAlgℤ N k) →ₗ[ℤ] (H.toSubmodule →ₗ[ℤ] H.toSubmodule)`, `T ↦ ((T:End ℂ _).restrictScalars ℤ).restrict (fun x hx => H.stable T T.2 x hx)`. ℤ-linear (`restrict`/`restrictScalars` linear).
4. **Φ injective**: `Φ T = 0 ⟹ ∀ x ∈ Λ, T.val x = 0 ⟹ T.val = 0` (step 2) `⟹ T = 0` (`Subtype.ext`).
5. **`H.toSubmodule →ₗ[ℤ] H.toSubmodule` is `Module.Finite ℤ`**: from `H.free` + `H.finite` (`Module.Finite.linearMap`).
6. `Module.Finite.of_injective Φ Φ_injective` ⟹ `Module.Finite ℤ ↥(heckeAlgℤ N k)`.

#### Mathlib lemmas needed
`Module.Finite.linearMap` (`LinearAlgebra/FreeModule/Finite/Matrix.lean:47`, needs `Module.Free`+`Module.Finite`), `Module.Finite.of_injective` (`RingTheory/Finiteness/Basic.lean:252`), `LinearMap.restrict` + `LinearMap.restrict_apply`, `LinearMap.restrictScalars`, `Submodule.span_induction` / `LinearMap.ext` / `LinearMap.ext_on`, `Submodule.mem_top`. (All verified to exist.)

#### Sources
Shimura, *Intro. Arith. Theory*, **(3.5.20)+ρ₀ p.84** (faithful integral representation from a maximal-rank stable lattice), **Thm 3.51(1) p.85** (`[D₀:ℚ]=r<∞`); Diamond–Shurman **Ex 6.5.1** (`End_ℤ(free rank r) ≅ M_r(ℤ)`, subring f.g.); Miyake Thm 4.5.19(1). Audit: `decomposition-T002-IHR.md`.

#### Generality decision
Per `N, k`. Lattice in `S_k(Γ₁N)` itself (matches Shimura/Miyake; cleaner than the homology dual). `spanning` (full-rank) is the honest hard field; **faithfulness is derived, never stubbed** (the audit's anti-empty-structure requirement). After this, the Tranche-2 IHR roadmap discharges `exists_HeckeStableLattice` (not `heckeAlgℤ_finite` directly).

---

### [T006] `Newform` ⟹ `IsFullEigenform` — bad-prime eigen-property  [4/5 DONE; T006-b is a major bad-prime adjoint development]
- **Status**: ✅ **DONE (2026-06-21) — `Newform.isFullEigenform` PROVEN + axiom-clean** (`#print axioms` = `{propext, Classical.choice, Quot.sound}`). All 5 sub-tickets T006-a..e closed; T006-b (the bad-prime adjoint, the hard one) closed via the Fricke route + bounded FD-tiling. · **File**: `Newforms/FullEigenform.lean` · **Depends on**: mainLemma (done) · **Parent**: T003 · **Type**: theorem
- **SCALE CORRECTION (beastmode execution, 2026-06-20)**: the decompose pass called T006-b ("U_p preserves new-extended") a "small residual lemma". **Executing it shows that was an under-estimate.** T006-a (U_p Fourier), T006-c (single-bad-prime `U_p f = a_p f` via the `g_p` argument), T006-d (assembly), and the **T006 main theorem `Newform.isFullEigenform`** are all PROVEN — i.e. all of DS 5.8.2(a)'s elementary content. The ONE residual, T006-b, is the **bad-prime Petersson-adjoint / Atkin–Lehner new-space stability** (DS Prop 5.6.2 at bad primes): it requires re-deriving the `petN` fundamental-domain-tiling + measure-theory machinery (the coprime analogue spans `AdjointTheory/DeltaBSystem.lean` + `ConcreteFamily.lean` + `SummandAdjoint.lean` + `AdjointTheoryPetersson.lean`, a clear multi-week formalization) for the bad-prime coset family `{[1,b;0,p]•FD}` — and there the per-summand adjoints `[p,-b;0,1]` carry **per-b shifts that do NOT collapse** to a common operator (the coprime case collapses via `T_p_lower_triple_product_matrix`, which needs `Coprime p N`). The bad-prime adjoint `V'_p` is genuinely NOT a level-N Hecke operator. **This is research-scale, absent from mathlib.** A background agent is attempting it in a new `AdjointTheoryBadPrime.lean`; if it confirms the multi-week scale, T006-b is the project's SECOND deep input (alongside T002), and should be its own `/develop` sub-project.
- **CORRECTION (source decompose, decomposition.md)**: The codebase docstring (Basic.lean:116–120) and my earlier note called this the unformalised **Atkin–Lehner–Li** bad-prime *sign* theorem ("DS 5.8.6"). Reading the source: **"5.8.6" is an Exercise**; the real result is **DS Theorem 5.8.2(a)** (`/tmp/ds.txt:15370`), which proves it **elementarily in ~10 lines** with NO sign computation: for any `m`, `g_m = T_m f − a_m(f)·f ∈ S_k^new` is a good-eigenform with `a₁(g_m)=0`, so its coprime coefficients vanish ⟹ `g_m ∈ old` **by the Main Lemma** (PROVEN this session) ⟹ `g_m ∈ new ⊓ old = {0}` ⟹ `T_m f = a_m(f)·f`. So this is **reachable now**, not a multi-week development.
- **Statement**: `theorem Newform.isFullEigenform (f : Newform N k) : IsFullEigenform f.toCuspForm`.
- **Proof sketch (DS 5.8.2(a))**: for coprime `m`, immediate (good-eigenform). For `m | N`: form `g_m`; `a₁(g_m)=0` via `aₘ_eq_eigenvalue_mul_aₒₙₑ`; `g_m` good-eigenform (coprime `T_n` commutes with `T_m`) ⟹ coprime coeffs `= c_n·a₁(g_m) = 0`; `mainLemma` ⟹ `g_m ∈ cuspFormsOld`; `cuspFormsNew ⊓ cuspFormsOld = ⊥` (Basic.lean:207) ⟹ `g_m = 0`.
- **Residual small lemmas (not blocks)**: `U_p` preserves `cuspFormsNew` and coprime-`T_n`/`U_p` commute (bad-prime analogues of `heckeT_n_preserves_cuspFormsNew`/`heckeT_n_cusp_comm`, both present for coprime; AdjointTheoryPetersson.lean has the adjoint machinery).
- **Sources**: DS Theorem 5.8.2(a) (`/tmp/ds.txt:15370`, proof `:15355–15369`); cf. Shimura Prop 3.53.

- **Progress** (2026-06-20, beastmode source-verified decomposition): T006 decomposes into bad-prime
  operator lemmas (all building blocks located in the codebase). The `g_m` argument runs **per bad
  prime** with a coefficient trick that AVOIDS good–bad commutation: for `l` coprime to `N`,
  `a_l(U_p f) = a_{pl}(f)` (`qExpansion_one_heckeT_p_divN_coeff`), and `a_{pl}(f) = a_p(T_l f) = a_l·a_p`
  via the good Fourier formula at coefficient index `p` (`gcd(p,l)=1` as `p∣N`, `l⊥N`); so the coprime
  coeffs of `g_p = U_p f − a_p f` vanish ⟹ `g_p ∈ old` (mainLemma) ⟹ `g_p ∈ oldExt ⊓ newExt = 0`.
  For `IsFullEigenform` the eigenvalue function is **existential**, so no need to identify the scalar as
  `a_n` — assemble all `n` from prime powers via `heckeT_n_unfold`. Sub-tickets T006-a/b/c/d below.

#### [T006-a] Bad-prime `U_p` Fourier relation (cusp level)
- **Status**: DONE (2026-06-20) · **File**: Newforms/FullEigenform.lean · **Depends on**: none · **Parent**: T006 · **Type**: lemma
- **Progress**: `heckeT_n_cusp_divN_coeff` proven via `qExpansion_one_heckeT_p_divN_coeff` + `heckeT_p_all = heckeT_p_divN` (dif_neg) + `heckeT_n_prime`.
- **Statement**: for a prime `p` with `¬Coprime p N` and `f : CuspForm (Gamma1 N) k`,
  `(qExpansion 1 (heckeT_n_cusp k p f)).coeff l = (qExpansion 1 f).coeff (p * l)`.
- **Sketch**: `heckeT_n_cusp k p f` coerces to `heckeT_p_all k p hp ⇑f.toModularForm'`; at bad `p`,
  `heckeT_p_all = heckeT_p_divN` (`heckeT_p_all_not_coprime_apply`); apply `qExpansion_one_heckeT_p_divN_coeff`.
- **Mathlib/codebase**: `qExpansion_one_heckeT_p_divN_coeff` (QExpansionSlash.lean:326), `heckeT_p_all_not_coprime_apply` (HeckeT_n.lean:647), `heckeT_n_prime`+`heckeT_n_cusp_toModularForm'`.

#### [T006-b] `U_p` preserves `cuspFormsNewExtended` — bad-prime Petersson adjoint  [REPLANNED 2026-06-20: DS Fricke route]
- **Status**: ✅ **DONE (2026-06-21)** — `heckeT_n_cusp_preserves_cuspFormsNewExtended_bad'` green + axiom-clean via the Fricke route (L1–L5 all closed; L4 via the bounded FD-tiling FD-a..d + trace↔Fricke FD-e.1 + gate FD-e.2). · **File**: `Newforms/AdjointTheoryBadPrime.lean` (+ `BadPrimeFDTiling.lean`, `BadPrimeTraceFricke.lean`, Fricke.lean) · **Depends on**: T006-b-L1..L5 (done) · **Parent**: T006 · **Type**: theorem
- **Decomposition**: see `.mathlib-quality/decomposition-T006b-fricke.md`. DS Prop 5.6.2 (`ds.txt:14688`): `T_p(new) ⊆ new ⟺ T_p*(old) ⊆ old`; for bad `p`, `T_p* = w_N T_p w_N⁻¹` (Ex 5.5.1, `ds.txt:14599`); so it suffices that `w_N` (L2) and `T_p` (L3) preserve `oldExt`. Assemble via L4 (the adjoint identity) + L5 (orthogonal-complement abstraction).
- **Supersedes** the agent's `Γ⁰(p)`-FD-tiling route (the lone sorry `peterssonInner_aggregate_eq_zero_of_new_old` @ AdjointTheoryBadPrime.lean:201). That route is correct-but-stuck (invented, not DS's; hits absent FD-tiling). KEEP the agent's sorry-free `petN→aggregate` reduction as a documented alternative; the Fricke route does NOT use it.
- **Deep core is L4.1 only** (DS Prop 5.5.2(b), the *generic* double-coset adjoint, ~1.5 source pages, reusable — it also re-derives the coprime `heckeT_n_adjoint`). Its per-summand step L4.2 = `peterssonInner_slash_adjoint` **already exists**. So this is a bounded multi-ticket sub-development, NOT the open-ended tiling morass.
- **Skeleton laid (2026-06-20)**: `AdjointTheoryBadPrime.lean` now has the Fricke-leaf declarations as `:= sorry` (`frickeOperatorCusp` L1, `frickeOperatorCusp_mem_cuspFormsOldExtended` L2, `heckeT_n_cusp_mem_cuspFormsOldExtended` L3, `badUpAdjoint` def, `petN_badUp_eq_petN_badUpAdjoint` L4, `badUpAdjoint_mem_cuspFormsOldExtended` = L2∘L3), and `heckeT_n_cusp_preserves_cuspFormsNewExtended_bad'` is **re-routed through them** (L5 folded into the final `hf _ (...)`). `lake build` GREEN (sorries only); `Newform.isFullEigenform` rides on these. The orphaned tiling sorry `peterssonInner_aggregate_eq_zero_of_new_old` (l.201) is kept as a documented alternative.
- **Execution progress (2026-06-20/21 /beastmode) — 6 of 7 leaves DONE; T006-b reduced to the single wall**:
  - **L1 DONE** (`frickeOperatorCusp`, Fricke.lean). **L2 DONE+wired** (`FrickeOldStable.lean`, axiom-clean — Fricke swaps degeneracy). **L3 DONE+wired** (`BadPrimeOldStable.lean`, axiom-clean — bad `U_p` old-stability + the new Regime-B collapse). **L4.1 DONE** (`AdjointTheory/DoubleCosetAdjoint.lean`, axiom-clean — the **reusable generic DS Prop 5.5.2(b)**). **L4.4 DONE** (Fricke.lean:95 existing). **L5 DONE** (subsumed in the main theorem). **`badUpAdjoint` + `heckeEndCusp` + `badUpAdjoint_mem` + W² API DONE.** `badUpAdjoint_mem` is **axiom-clean** (`[propext, Classical.choice, Quot.sound]`, no sorryAx) — the entire old-stability half is real.
  - **★ ONLY L4 REMAINS = the bad-prime `Γ_p(diag(1,p))` FD-tiling wall ★** (DS Lemma 5.5.1(c) for `p∣N`). CONFIRMED research-scale by TWO independent 200k–330k-token agents + structural analysis: the Fricke route ISOLATES the wall (into the generic theorem's `hFD` hypothesis) but cannot remove it. `Newform.isFullEigenform` + the main theorem are axiom-clean **modulo this one sorry**. The generic Prop 5.5.2(b) scaffold stands ready to plug into once the tiling is formalized.
  - **Net**: T006-b went from an undecomposed research-scale block → ONE precisely-named, fully-scaffolded research input (the bad-prime FD-tiling), with 3 new axiom-clean files + a reusable generic double-coset adjoint. `lake build` green throughout.

##### [T006-b-L1] `frickeOperatorCusp` — `w_N` as a `ℂ`-linear endo of cusp forms
- **Status**: DONE (2026-06-20) · **File**: Fricke.lean · **Depends on**: none · **Parent**: T006-b · **Type**: def + API
- **Progress**: `frickeOperatorCusp k : CuspForm →ₗ[ℂ] CuspForm` defined in Fricke.lean (cusp-vanishing transports via private `frickeGL_isCusp_smul` + `OnePoint.IsZeroAt.smul_iff`, mirroring `frickeGL_bdd_at_cusps`); `frickeOperatorCusp_coe`; bridge `frickeOperatorCusp_toModularForm'` in AdjointTheoryBadPrime.lean. `lake build` green, no sorry. (Cleanup deferred to AINTLIB `main` per producer rules.)
- **Statement**: `noncomputable def frickeOperatorCusp (k : ℤ) : Module.End ℂ (CuspForm ((Gamma1 N).map (mapGL ℝ)) k)` with `(frickeOperatorCusp k f).toModularForm' = frickeOperator k f.toModularForm'`; plus `frickeOperatorCusp_frickeOperatorCusp` (W² = scalar) and `frickeOperatorCusp_diamondOp`.
- **Sketch**: package `frickeOperator k` (Fricke.lean:262, ℂ-linear endo of `M_k(Γ₁N)`) as a cusp endo — `frickeGL` normalizes Γ₁N so `w_N` maps cusp forms to cusp forms (cusp-vanishing transports, same pattern as `heckeT_n_cusp` from `heckeT_n` @ AdjointTheory.lean:199). API from `frickeOperator_coe`, `frickeOperator_frickeOperator`, `frickeOperator_diamondOp`.
- **Mathlib/codebase**: `frickeOperator`, `frickeOperator_frickeOperator`, `frickeScalar` (Fricke.lean); `CuspForm.mk`/cusp-preservation pattern.
- **Sources**: DS Ex 5.5.1(a) (`ds.txt:14599`): `w_N = [Γ₁(N)γΓ₁(N)]_k`, `γ⁻¹Γ₁(N)γ = Γ₁(N)`.
- **Generality**: per `N,k`; over `ℂ`.

##### [T006-b-L2] `frickeOperatorCusp` preserves `cuspFormsOldExtended`
- **Status**: DONE (2026-06-20, agent ae357f24) · **File**: `Newforms/FrickeOldStable.lean` (NEW, green, axiom-clean, 0 sorries) · **Depends on**: T006-b-L1 · **Parent**: T006-b · **Type**: theorem
- **Progress**: `frickeOperatorCusp_mem_cuspFormsOldExtended` proven via the "Fricke swaps degeneracy" architecture: two cross-level matrix identities (`frickeGL N = frickeGL M · diag(d,1)`, `diag(d,1)·frickeGL N = frickeGL M · diag(d,d)`) ⟹ `w_N(levelRaise) = d⁻¹·levelInclude(w_M g)` and `w_N(levelInclude) = levelRaise(w_M g)`, both landing in oldExt via span_induction. **Wired into AdjointTheoryBadPrime.lean** (import + stub deleted; `badUpAdjoint_mem` uses it). Build green.
- **Statement**: `g ∈ cuspFormsOldExtended N k ⟹ frickeOperatorCusp k g ∈ cuspFormsOldExtended N k`.
- **Sketch**: `span_induction` over the `cuspFormsOldExtended` generators (level-raise `i_d(h)` from `M<N`, and level-inclusions). DS Ex 5.6.3(e) (`ds.txt:14747–14781`): `w_N ∘ i_d = (p^{k-2}-scalar)·i_{d'} ∘ w_{N/d}` — the commuting diagram intertwining `w` with the degeneracy maps across levels `N` and `N/d`. So `w_N(i_d h) = scalar·i_{d'}(w_{N/d} h)` is again old. Linearity of `frickeOperatorCusp` for the span closure.
- **Mathlib/codebase**: `cuspFormsOldExtended`/generators (LevelRaiseComm.lean:360); `levelRaise`/`levelInclude_cusp` + their `frickeGL`-conjugation matrix identities (some in Fricke.lean); `Submodule.span_induction`.
- **Sources**: DS Ex 5.6.3(e), `ds.txt:14747` ("it suffices to show that the oldforms are preserved under the injective linear map `w`") + diagram `:14748–14781`.
- **Generality**: per `N,k`.

##### [T006-b-L3] `heckeT_n_cusp` preserves `cuspFormsOldExtended` (narrowed to bad `p`)
- **Status**: DONE (2026-06-20, agent a794bec8) · **File**: `Newforms/BadPrimeOldStable.lean` (NEW, 485 L, green, axiom-clean, 0 sorries) · **Depends on**: none · **Parent**: T006-b · **Type**: theorem
- **Progress**: `heckeT_n_cusp_divN_mem_cuspFormsOldExtended` (bad `p`, all `badUpAdjoint` needs) proven. Bridge `⇑(U_p f) = heckeT_p_ut ⇑f` (level-independent upper sum); span_induction with both generator cases split on `p ∣ level`. New content: the Regime-B `U_p`-collapse (`d=p·e`: `heckeT_p_ut(levelRaise M (p·e)) = levelRaiseFun e` via scalar-matrix slash `p·I ↦ p^{k-2}`) → `levelInclude(levelRaise M e)` at level `N/p < N`. `heckeT_p_ut_levelRaise` was private so re-derived (~130 L). **Wired into AdjointTheoryBadPrime.lean** (`badUpAdjoint_mem` → bad-prime version). Build green.
- **Statement**: `(n : ℕ) [NeZero n] → g ∈ cuspFormsOldExtended N k → heckeT_n_cusp k n g ∈ cuspFormsOldExtended N k` (no coprimality).
- **Sketch**: DS Ex 5.6.3(a)(b)(c) (`ds.txt:14689–14738`): the two degeneracy-map diagrams (`[T,0;0,T]` for good `T`, and `[T_p, −p^{k-1}⟨p⟩; 0]` for `T_p`, `p∣N`) commute with `i_p`, so `T_n(old) ⊆ old`. Codebase: `heckeT_n_preserves_cuspFormsOld` (LevelRaiseComm.lean:614) is the coprime, NON-extended version; (i) extend to `cuspFormsOldExtended` (add the level-inclusion generators), (ii) drop coprimality via the prime-power `heckeT_n_unfold` + the bad-prime `U_p ∘ levelRaise` commutation. `span_induction`.
- **Mathlib/codebase**: `heckeT_n_preserves_cuspFormsOld` (template), `heckeT_n_levelRaise_comm` (coprime; needs bad-prime analogue), `heckeT_n_unfold` (HeckeT_n.lean:554).
- **Sources**: DS Ex 5.6.3(c), `ds.txt:14738`.
- **Generality**: all `n`; per `N,k`.

##### [T006-b-L4] Bad-prime Petersson adjoint via Fricke: `petN (U_p f) g = petN f (w_N U_p w_N⁻¹ g)`  [DONE — WALL OVERTURNED]
- **Status**: ✅ **DONE (2026-06-21) — `petN_badUp_eq_petN_badUpAdjoint` green + axiom-clean.** The "research-scale wall" was overturned exactly as the decompose pass predicted: bounded FD-tiling board (FD-a..d) + the trace↔Fricke identity (FD-e.1) + generic-double-coset gate wiring (FD-e.2), ~3 new files, all axiom-clean. · **File**: AdjointTheoryBadPrime.lean (+ `BadPrimeFDTiling.lean`, `BadPrimeTraceFricke.lean`) · **Depends on**: T006-b-L4.1 (done), FD-{a..e} (done), T006-b-L4.4 (done) · **Parent**: T006-b · **Type**: theorem (API GAP, bounded)
- **Reduced via L4.1**: instantiate `petN_doubleCoset_adjoint` (DoubleCosetAdjoint.lean) at `α = diag(1,p)`, `β` = the `p` upper-tri reps. Remaining: (i) **`hFD` = the bad-prime `Γ_p(diag(1,p)) = Γ₁(N)∩Γ⁰(p)` FD-tiling** — **NOW DECOMPOSE-VERIFIED BOUNDED**, see `decomposition-T006b-L4-fdtiling.md`; (ii) trace-output ↔ `badUpAdjoint` Fricke identification (L4.4 algebra); (iii) routine measure hyps.
- **★ WALL OVERTURNED (decompose pass, agent a955a8e6 + my verification) ★**: the `hFD` tiling is NOT research-scale. DS Lemma 5.5.1 is GENERIC; the codebase has generic FD engines (`iUnion_smul_of_transversal`/`smul_of_eq_conjAct` PeterssonLevelN:289/322, `Gamma_p_α_FD_finite_index_decomp` FDTransport:132 — all spot-checked α-generic, no Coprime). The bad case is **strictly simpler** than the coprime `DeltaBSystem` (1524 L): it **deletes** M_∞/Bézout/`Option`-branch (the bad obstruction `p∣a` is VACUOUS per DS `ds.txt:13247`). Decomposes into **L2 (Γ⁰(p) identify ~60 L) → L3 (index=p ~120 L, distinctness reuses coprimality-free `T_p_lower_tile_some_some_notMem_Gamma_up`) → L4 (Fin-p transversal ~80 L) → L5 (det-p conj ~50 L)**, the rest discharged by existing code. **~350–500 LOC total — a bounded `/beastmode` board.**
- **Statement**: for `p ∣ N`, `petN (heckeT_n_cusp k p f) g = petN f (frickeOperatorCusp k (heckeT_n_cusp k p (frickeOperatorCusp_inv k g)))` (the `w_N`-conjugate adjoint), where `frickeOperatorCusp_inv` is the W²-scalar inverse.
- **Sketch**: DS Ex 5.5.1(b) (`ds.txt:14599`): `T_p = [Γ diag(1,p) Γ]`, so by Prop 5.5.2(b) (L4.1) `T_p* = [Γ diag(p,1) Γ]`; and `diag(p,1) = γ⁻¹ diag(1,p) γ` (L4.4) gives `[Γ diag(p,1) Γ] = w_N [Γ diag(1,p) Γ] w_N⁻¹ = w_N T_p w_N⁻¹`. Combine with the abstract adjoint `petN (T_p f) g = petN f (T_p* g)` (uniqueness on the `petN` inner-product space).
- **Mathlib/codebase**: L4.1 (T006-b-L4.1), L4.4 (T006-b-L4.4), `petN_innerProductCore` (AdjointTheoryPetersson.lean:427), `LinearMap.adjoint`.
- **Sources**: DS Ex 5.5.1(b) (`ds.txt:14599`); Prop 5.5.2 (`ds.txt:14446`).
- **Generality**: bad `p`; the identity is special-cased to `U_p` but rides on the generic L4.1.

###### [T006-b-L4-FD-a] `Gamma_p_α(diag(1,p)) = Γ₁(N) ⊓ Γ⁰(p)`  (Leaf L2)
- **Status**: DONE (2026-06-21, agent ae7b361e) — green, axiom-clean · **File**: `Newforms/BadPrimeFDTiling.lean` (NEW) · **Depends on**: none · **Parent**: T006-b-L4 · **Type**: lemma
- **Statement**: `lemma Gamma_p_α_T_p_upper_zero_eq_inf (p : ℕ) (hp : Nat.Prime p) (hpN : ¬ Nat.Coprime p N) : Gamma_p_α (N := N) (T_p_upper p hp.pos 0) = Gamma1 N ⊓ Gamma_up p`. (`T_p_upper p _ 0 = [1,0;0,p] = diag(1,p)`; `Gamma_up p` = Γ⁰(p), FDTransport:1550.)
- **Proof sketch**: mirror `Gamma_p_α_T_p_lower_eq_inf` (FDTransport:1389), which does `Gamma_p_α(T_p_lower) = Γ₁(N)⊓Γ₀(p)` via `mem_Gamma_p_α_T_p_lower`. Here the conjugation `diag(1,p)·[a,b;Nc,d]·diag(1,p)⁻¹ = [a,b/p;pNc,d]` is integral ⟺ `p∣b` (upper-right) ⟺ `γ ∈ Γ⁰(p)`. The lower-left `pNc ≡ 0 (mod N)` is **automatic** (c≡0 mod N for Γ₁(N)) — so **NO coprimality needed** (simpler than the coprime `mpr` which needed Bézout). `ext γ; rw [Gamma_p_α, Subgroup.mem_inf, Gamma_up_mem, ...]` + the conjugation computation.
- **Mathlib/codebase**: `Gamma_p_α` (FDTransport:41), `Gamma_up`/`Gamma_up_mem` (FDTransport:1550), `mem_Gamma_p_α_T_p_lower` (template, FDTransport:~1370), `T_p_upper` (HeckeT_p.lean:48).
- **Sources**: DS `/tmp/ds.txt:13222` ("Γ₃ = Γ₁⁰(N,p) = Γ₁(N) ∩ Γ⁰(p) … b ≡ 0 (mod p)").
- **Generality**: per `N,k,p`; the diag(1,p) instance.

###### [T006-b-L4-FD-b] index `[Γ₁(N) : Γ₁(N) ⊓ Γ⁰(p)] = p` for `p ∣ N`  (Leaf L3 — the hardest, ~120 L)
- **Status**: DONE (2026-06-21, agent ae7b361e) — green, axiom-clean (`Gamma_up_relIndex_Gamma1_of_dvd`, `Fin p` shear transversal, `j := γ₀₁ mod p` via `γ₁₁≡1`) · **File**: `Newforms/BadPrimeFDTiling.lean` · **Depends on**: T006-b-L4-FD-a · **Parent**: T006-b-L4 · **Type**: theorem
- **Statement**: `theorem Gamma_up_relIndex_Gamma1_of_dvd (p : ℕ) (hp : Nat.Prime p) (hpN : ¬ Nat.Coprime p N) : (Gamma_up p).relIndex (Gamma1 N) = p`.
- **Proof sketch**: the coprime `Gamma_up_relIndex_Gamma1` (FDTransport:1722, `= p+1`) is INAPPLICABLE (its surjectivity `Γ⁰(p)·Γ₁(N)=SL₂ℤ` uses Bézout, FALSE for `p∣N`). Direct proof via the transversal `{[1,j;0,1] : j ∈ Fin p}`: (1) **distinctness** — `[1,i;0,1]·[1,j;0,1]⁻¹ ∈ Γ⁰(p) ⟺ i=j`: REUSE `T_p_lower_tile_some_some_notMem_Gamma_up` (DeltaBSystem:841, **verified `(p)(hp:Prime p){b₁ b₂:Fin p}`, NO Coprime**) AS-IS; (2) **covering** — for `γ ∈ Γ₁(N)`, since `p∣N` ⟹ `γ₀₀ ≡ 1 (mod p)` is a unit, `j := γ₀₁·γ₀₀⁻¹ mod p` gives `γ·[1,−j;0,1] ∈ Γ⁰(p)` (no M_∞, no Bézout); (3) `relIndex = card (Fin p) = p` via `Nat.card_congr` on the transversal bijection. The bad `p∣a` obstruction is **vacuous** (DS).
- **Mathlib/codebase**: `T_p_lower_tile_some_some_notMem_Gamma_up` (DeltaBSystem:841), `Gamma_up`/`Gamma_up_mem`, `Subgroup.relIndex`, `Nat.card_congr`, `ZMod` unit (`γ₀₀` mod p).
- **Sources**: DS `/tmp/ds.txt:13247` ("Instances of γ₂ with p|a occur iff p∤N … Thus γ₂,0,…,γ₂,p−1 are a complete set of coset representatives when p|N").
- **Generality**: `p∣N`.

###### [T006-b-L4-FD-c] the `Fin p` Hecke-tile transversal is bijective  (Leaf L4, ~80 L)
- **Status**: DONE (2026-06-21, agent ae7b361e) — green, axiom-clean (`shiftSL_loc_tile_transversal_bijective`, `Fin p` at PSL level) · **File**: `Newforms/BadPrimeFDTiling.lean` · **Depends on**: T006-b-L4-FD-a, T006-b-L4-FD-b · **Parent**: T006-b-L4 · **Type**: lemma
- **Statement**: the family `(fun b : Fin p ↦ ⟦([1,b;0,1] : SL₂ℤ)⁻¹⟧ : SL₂ℤ ⧸ (Gamma_p_α(diag(1,p)).subgroupOf ...))` is a bijection `Fin p ≃ (the coset space)` — i.e. `e : Fin p ≃ Γ₁(N) ⧸ (Γ₁(N)⊓Γ⁰(p))` with `e b = ⟦[1,b;0,1]⁻¹⟧`. (Exact shape mirrors `T_p_lower_tile_transversal_bijective`.)
- **Proof sketch**: mirror `T_p_lower_tile_transversal_bijective` (DeltaBSystem:953) but over `Fin p` (NOT `Option (Fin p)` — no `none`/M_∞ branch). Card = `p` (FD-b); injectivity = FD-b distinctness. **Delete** all `some/none`, `M_infty_Gamma1_factor`, `gcdA` branches.
- **Mathlib/codebase**: `T_p_lower_tile_transversal_bijective` (template, DeltaBSystem:953), FD-a, FD-b, `Equiv.ofBijective`, `Finset.card`/`Fintype.card_fin`.
- **Sources**: DS Lemma 5.1.2 (`/tmp/ds.txt:12833`); the `p` reps `/tmp/ds.txt:13631`.
- **Generality**: `p∣N`.

###### [T006-b-L4-FD-d] `isFundamentalDomain_BadHecke_tiles` — discharge `hFD`  (Leaf L5 + assembly, ~50 L)
- **Status**: DONE (2026-06-21, agent ae7b361e) — green, axiom-clean. **★ CORRECTION**: the target group is `Gamma_p_α (T_p_lower p hp.pos)` (=diag(p,1), the DS *adjoint* matrix α'=det(α)α⁻¹), NOT `T_p_upper..0` (=diag(1,p)) as originally sketched — verified 3 ways (the det-p tiles linearize under diag(p,1) not diag(1,p), and `g'=g∣glMap(T_p_lower)` is invariant precisely under `Gamma_p_α(T_p_lower)`, matching `petN_doubleCoset_adjoint`'s `hFD`). FD-a (about diag(1,p)) is a correct standalone lemma but not the group the assembly needs. · **File**: `Newforms/BadPrimeFDTiling.lean` · **Depends on**: T006-b-L4-FD-c · **Parent**: T006-b-L4 · **Type**: theorem
- **Statement**: `theorem isFundamentalDomain_BadHecke_tiles (p) (hp : Nat.Prime p) (hpN : ¬ Nat.Coprime p N) : IsFundamentalDomain ((Gamma_p_α (N:=N) (T_p_lower p hp.pos)).map SL2Z_to_PSL2R) (⋃ b ∈ (Finset.univ : Finset (Fin p)), (glMap (T_p_upper p hp.pos b.val) : GL (Fin 2) ℝ) • (Gamma1_fundDomain_PSL N : Set ℍ)) μ_hyp` — i.e. exactly the `hFD` hypothesis of `petN_doubleCoset_adjoint` at `α = T_p_lower`.
- **Proof sketch**: 2-step conjugation (mirror `isFundamentalDomain_Hecke_tiles_Gamma_p_α`, DeltaBSystem:1094): (1) the det-p Hecke reps `[1,b;0,p]•FD` conjugate (via `diag(1,p)`, mirror `T_p_lower_smul_Hecke_FD_eq_iUnion_tile` DeltaBSystem:331) to det-1 tiles `[1,b;0,1]•FD`; (2) apply the GENERIC det-1 engine `IsFundamentalDomain.iUnion_smul_of_transversal` (PeterssonLevelN:289) with the FD-c transversal, giving an FD for `Γ₁(N)⊓Γ⁰(p)`; (3) conjugate back via `IsFundamentalDomain.smul_of_eq_conjAct` (PeterssonLevelN:322) + FD-a. Map to PSL via `Gamma_p_α_fundDomain_PSL` (FDTransport:198, α-generic). AE-disjointness = `aedisjoint_glMap_T_p_upper_pair` (SummandAdjoint:402, no Coprime).
- **Mathlib/codebase**: `iUnion_smul_of_transversal`/`smul_of_eq_conjAct` (PeterssonLevelN:289/322), `Gamma_p_α_fundDomain_PSL`/`Gamma_p_α_FD_finite_index_decomp` (FDTransport:198/132), `isFundamentalDomain_Hecke_tiles_Gamma_p_α`/`T_p_lower_smul_Hecke_FD_eq_iUnion_tile` (templates, DeltaBSystem:1094/331), `aedisjoint_glMap_T_p_upper_pair`.
- **Sources**: DS Lemma 5.5.1(a)(c) (`/tmp/ds.txt:14380`).
- **Generality**: `p∣N`.

###### [T006-b-L4-FD-e] close `petN_badUp_eq_petN_badUpAdjoint` (the L4 gate) — SPLIT into e.1 + e.2
- **Status**: ✅ **DONE (2026-06-21)** — both halves green + axiom-clean; `Newform.isFullEigenform` verified `#print axioms` = `{propext, Classical.choice, Quot.sound}` (no `sorryAx`). · **Parent**: T006-b-L4 · **Type**: theorem (closed the sorry @ AdjointTheoryBadPrime:269)
- **Discovery**: the FD-e core is the **trace↔Fricke identity** — `petN_doubleCoset_adjoint` outputs `c_N·∫_{Γ₁-FD} pet f (traceSlash_Gamma_p_α(T_p_lower)(g∣diag(p,1)) q₀)`, and DS Ex 5.5.1(b) says this `Γ_p(diag(p,1))`-trace = `badUpAdjoint g`. The crux MATRIX identity (verified): `diag(p,1)·[1,0;jN,1] = adj(δ_{-j}) = w_N·[1,-j;0,p]·w_N⁻¹` (`frickeGL_mul_adj_lunipRep_mul_frickeGL_inv`, Fricke:95). Summing slashes over the `Fin p` transversal `{[1,0;jN,1]}` of `Γ_p(diag(p,1))\Γ₁` gives `(U_p(g∣w_N))∣w_N⁻¹ = (frickeScalar)⁻¹·(U_p(g∣w_N))∣w_N = badUpAdjoint g` (the `(frickeScalar)⁻¹` from the W↔W⁻¹ conversion via `frickeGL_sq_slash`).
- **[FD-e.1] DONE** (2026-06-21, agent a3e81a4c) — green, axiom-clean. trace↔Fricke identity `traceSlash_badPrime_eq_badUpAdjoint` · **File**: `Newforms/BadPrimeTraceFricke.lean` (NEW) · mirrors coprime `ds_traceSlash_Gamma_p_α_T_p_lower_eq_diamond_inv_heckeT_p` (DeltaBSystem:1505) but lands on Fricke not `⟨p⟩⁻¹T_p`. **Verified orientation**: transversal `[1,0;-bN,1]` indexed *directly* by the `U_p` output `b` (NO reindex `j↦-j`); `diag(p,1)·[1,0;-bN,1] = adj(δ_b)` *exactly* (no `Γ₀(N)` correction, unlike coprime); `adj(lunipRep b) = W⁻¹·T_p_upper(b)·W`. **Refinement**: bad membership is the *stronger* `pN ∣ γ₁₀` (index `p`), not coprime `p ∣ γ₁₀` — the conjugate's `γ₁₀/p` must itself be `≡0 mod N`. Helpers: `lowerUniSL`, `mem_Gamma_p_α_T_p_lower_pN(_mpr)`, `relIndex_Gamma_p_α_T_p_lower_bad = p`, `badTraceSlash_eq`, `adj_lunipRep_eq_frickeConj`, `badUp_coe_eq_sum`.
- **[FD-e.2] DONE** (2026-06-21, agent a064be5b) — green, sorry-free in its own body (only `sorryAx` is the black-box e.1 dependency). gate wiring `petN_badUp_eq_petN_badUpAdjoint` · **File**: `Newforms/AdjointTheoryBadPrime.lean` · applies `petN_doubleCoset_adjoint` at **`α = T_p_lower` (=diag(p,1), the adjoint matrix — corrected from the original diag(1,p) sketch)**, `β b = glMap(T_p_upper p hp.pos b.val)`, discharging all 15 hyps (most reuse this file's existing reduction + mirror ConcreteFamily's coprime `h_int_*_FD`, coprimality-free; `hFD` = FD-d), then `rw [traceSlash_badPrime_eq_badUpAdjoint, petN_eq_setIntegral_Gamma1_fundDomain_PSL]; congr 1` (final collapse is defeq via `badUpAdjoint_apply`/`CuspForm.coe_smul`). **bad `hcp` re-derived directly** (center-based, coprimality-free): `±I` is a scalar matrix (`mem_center_iff`) that `Commute`s with `diag(p,1)` (`Matrix.scalar_commute`), so conjugation fixes it ⟹ `-I ∈ Γ_p(A) ↔ -I ∈ Γ₁(N)` with NO Bézout (the coprime original routed through `mem_Gamma_p_α_T_p_lower_mpr_k_mod_N`, which genuinely needs Coprime). Added private mirrors `integrableOn_petersson_slash_T_p_lower_slash_SL'`/`h_int_{FD,trace_FD,tr_FD}'`/the 3 fiber sub-lemmas + center-iff.
- **Sources**: DS Prop 5.5.2(b) + Ex 5.5.1(b) (`/tmp/ds.txt:14446`/`:14599`). Closes T006-b-L4 → T006-b → `Newform.isFullEigenform` axiom-clean → unblocks T003.
- **Generality**: bad `p`.

##### [T006-b-L4.1] DS Prop 5.5.2(b): generic double-coset Petersson adjoint `[ΓαΓ]* = [Γα'Γ]`
- **Status**: DONE (2026-06-20, agent a78e2189) — but it ISOLATES rather than removes the wall · **File**: `AdjointTheory/DoubleCosetAdjoint.lean` (NEW, green, axiom-clean, 0 sorries) · **Depends on**: none · **Parent**: T006-b-L4 · **Type**: theorem (reusable)
- **Delivered**: `petN_doubleCoset_adjoint` (+ `petN_doubleCoset_sum_slashes_eq_aggregate`, `aggregate_petersson_eq_Gamma_p_α_fundDomain`) — the GENERIC Prop 5.5.2(b) for any α, reusing `peterssonInner_slash_adjoint` + the existing trace engine. Output is the `Γ_p(α)`-**trace** form. Sorry-free, but takes the **Lemma 5.5.1(c) FD-tiling `hFD` as an explicit hypothesis** (`⋃ⱼ βⱼ•Γ₁-FD` is a `Γ_p(α)`-fundamental domain) + `hcp` fiber-count + measure/integrability hyps.
- **★ DECISIVE FINDING ★**: `hFD` for `α = diag(1,p)`, `p∣N` **IS the bad-prime FD-tiling** — the SAME `Γ⁰(p)`/`Γ_p(diag(1,p))` wall the original tiling route hit (codebase has `isFundamentalDomain_Hecke_tiles_Gamma_p_α` ONLY for coprime). The Fricke route relocates the wall into this single `hFD` hypothesis (it doesn't bypass the tiling). **UPDATE (decompose pass 2026-06-21): `hFD` is NOT research-scale after all — it decompose-verified to a BOUNDED ~350–500 LOC board** (the earlier "research-scale" reads were over-pessimistic; they predated recognizing the α-generic FD engines `iUnion_smul_of_transversal`/`Gamma_p_α_FD_finite_index_decomp`, and conflated "no ready-made lemma" with "hard"). See T006-b-L4 + `decomposition-T006b-L4-fdtiling.md`. The generic theorem here is reusable (it also re-derives the coprime adjoint).
- **Statement**: for a congruence subgroup `Γ` and `α ∈ GL₂⁺(ℚ)` with `α' = det(α)·α⁻¹`, `petΓ (f[ΓαΓ]_k) g = petΓ (f) (g[Γα'Γ]_k)` for `f,g ∈ S_k(Γ)`. (Instantiate at `Γ = Γ₁(N)`, `α = diag(1,p)`.)
- **Sketch**: DS Prop 5.5.2(b) proof (`ds.txt:14482–14512`). (a) Per-summand change-of-variables = `peterssonInner_slash_adjoint` (AdjointTheory.lean:399, **EXISTS, generic**). (b) Lemma 5.5.1(c) (`ds.txt:14380–14443`): the double coset `ΓαΓ = ⊔Γβ_j = ⊔β_jΓ` admits common reps `β_j ∈ Γαγ_j ∩ γ̃_jαΓ`, so the same `β_j` define both `[ΓαΓ]` and (via `β_j'`) `[Γα'Γ]`. (c) Assemble (a) over the `β_j` cosets (`Γ ∩ β_jΓβ_j⁻¹`). NO fundamental-domain tiling — group-theoretic coset assembly.
- **Mathlib/codebase**: `peterssonInner_slash_adjoint` (✓ verified, AdjointTheory.lean:399), `peterssonAdj`/`peterssonAdj_det`/`peterssonAdj_mul` (AdjointTheory.lean:309), `petN` coset-sum def (PeterssonLevelN.lean:63).
- **Sources**: DS Prop 5.5.2 (`ds.txt:14446–14512`), Lemma 5.5.1 (`ds.txt:14380`). NOTE this generalizes/re-derives the coprime `heckeT_n_adjoint`.
- **Generality**: ANY `Γ`, ANY `α ∈ GL₂⁺(ℚ)` — maximal (the reusable form).
- **Note**: this may sub-decompose into Lemma 5.5.1(a)(b)(c) leaves; spawn during `/beastmode` if needed.

##### [T006-b-L4.4] Algebraic Fricke conjugation `diag(p,1) = γ⁻¹ diag(1,p) γ` ⟹ `[Γ diag(p,1) Γ] = w_N T_p w_N⁻¹`
- **Status**: DONE (the matrix core already exists) · **File**: Fricke.lean · **Depends on**: none · **Parent**: T006-b-L4 · **Type**: lemma (algebraic)
- **Progress**: `frickeGL_mul_adj_lunipRep_mul_frickeGL_inv` (Fricke.lean:95, machine-verified) is exactly the conjugation `W·adj(δ_r)·W⁻¹ = T_p_upper(r)` ⟺ `W⁻¹·[1,r;0,p]·W = adj(δ_r) = [p,0;−Nr,1]` (the lower rep). Confirmed by hand-computation `γ⁻¹[1,b;0,p]γ = [p,0;−bN,1]`. The remaining *operator*-level identity (`[Γ diag(p,1) Γ] = w_N U_p w_N⁻¹` as endos) is part of L4's wiring (walled on the tiling).
- **Statement**: the double-coset identity `[Γ₁(N) diag(p,1) Γ₁(N)]_k = w_N · [Γ₁(N) diag(1,p) Γ₁(N)]_k · w_N⁻¹` (operator form of `diag(p,1) = γ⁻¹ diag(1,p) γ` with `γ` the Fricke matrix).
- **Sketch**: DS Ex 5.5.1(b) "use the formula `[p,0;0,1] = γ⁻¹[1,0;0,p]γ`". Matrix identity (machine-checkable) + transport to the double-coset operator via `frickeGL` normalizing Γ₁(N). Fricke.lean:95 `frickeGL_mul_adj_lunipRep_mul_frickeGL_inv` is the analogous machine-verified identity for the bad rep; adapt.
- **Mathlib/codebase**: `frickeGL`, `frickeGL_inv_coe`, `frickeGL_mul_adj_lunipRep_mul_frickeGL_inv` (Fricke.lean), `T_p_upper`/`T_p_lower` reps.
- **Sources**: DS Ex 5.5.1(b), `ds.txt:14599`.
- **Generality**: per `N,k,p`.

##### [T006-b-L5] Orthogonal-complement: operator preserves `oldExtᗮ = newExt` ⟺ adjoint preserves `oldExt`
- **Status**: DONE (subsumed) · **File**: AdjointTheoryBadPrime.lean · **Depends on**: none · **Parent**: T006-b · **Type**: lemma (abstract)
- **Progress**: folded directly into `heckeT_n_cusp_preserves_cuspFormsNewExtended_bad'` — the `⟸` direction is the single step `intro g hg; rw [petN_badUp_eq_petN_badUpAdjoint]; exact hf _ (badUpAdjoint_mem_cuspFormsOldExtended ...)` (`f ⊥ oldExt` applied to `badUpAdjoint g ∈ oldExt`). No standalone lemma needed; this routing is sorry-free (modulo L4).
- **Statement**: on the finite-dim `petN`-inner-product space, `T(cuspFormsNewExtended) ⊆ cuspFormsNewExtended ⟺ T*(cuspFormsOldExtended) ⊆ cuspFormsOldExtended` — used in the `⟸` direction with `T = U_p`.
- **Sketch**: DS Ex 5.6.3(d). `cuspFormsNewExtended = (cuspFormsOldExtended)ᗮ` w.r.t. `petN` (the EXTENDED orthogonality is the definition, LevelRaiseComm.lean:380). For `f ∈ newExt`, `g ∈ oldExt`: `petN (T f) g = petN f (T* g) = 0` since `T* g ∈ oldExt` and `f ⊥ oldExt`. Direct (no mathlib `orthogonal` lemma strictly needed; just the adjoint identity + the def of newExt).
- **Mathlib/codebase**: `cuspFormsNewExtended` def (LevelRaiseComm.lean:380), `petN_innerProductCore` (AdjointTheoryPetersson.lean:427), `cuspForm_finiteDimensional` (Basic.lean:231).
- **Sources**: DS Ex 5.6.3(d), `ds.txt:14739`.
- **Generality**: abstract in `T`; specialized to `U_p` at the use site.

#### [T006-c] Single bad prime: `U_p f = a_p • f`
- **Status**: DONE (2026-06-20, modulo T006-b) · **File**: Newforms/FullEigenform.lean · **Depends on**: T006-a, T006-b · **Parent**: T006 · **Type**: theorem
- **Progress**: `Newform.heckeT_n_cusp_bad_prime_eq` proven. The `a_{pl}=a_l·a_p` step uses good Fourier `fourierCoeff_heckeT_n_period_one` at coeff `p` (NOT a bad-prime multiplicativity — clean). Local coeff-linearity helpers `qExpansion_sub_coeff`/`qExpansion_smul_coeff` added. Uses T006-b (sorry).
- **Statement**: for a prime `p ∣ N` and `f : Newform N k`, `heckeT_n_cusp k p f.toCuspForm = a_p • f.toCuspForm` where `a_p = (qExpansion 1 f).coeff p`.
- **Sketch**: `g := heckeT_n_cusp k p f − a_p • f`. (1) coprime-`l` coeffs vanish: `a_l(g) = a_{pl}(f) − a_p·a_l(f)`
  (T006-a) and `a_{pl}(f) = a_l·a_p` (good Fourier `fourierCoeff_heckeT_n_period_one` at index `p`, op `l`,
  `gcd(p,l)=1`, with `T_l f = a_l f` from `Eigenform.isEigen`+`Newform.eigenvalue_eq_coeff`) ⟹ `= 0`.
  (2) `mainLemma` ⟹ `g ∈ cuspFormsOld ⊆ cuspFormsOldExtended`. (3) `g ∈ cuspFormsNewExtended`
  (T006-b for `U_p f` + `f.isNew` + submodule closure). (4) `cuspFormsOldExtended_disjoint_cuspFormsNewExtended` ⟹ `g = 0`.
- **Sources**: DS Theorem 5.8.2(a) (`/tmp/ds.txt:15370`).

#### [T006-d] Prime-power / coprime assembly to all `n`
- **Status**: DONE (2026-06-20, modulo T006-b) · **File**: Newforms/FullEigenform.lean · **Depends on**: T006-c · **Parent**: T006 · **Type**: theorem
- **Progress**: `Newform.exists_heckeT_n_cusp_smul` + `Newform.isFullEigenform` (T006 main) proven. Strong induction via `heckeT_n_mul_ppow_quot`; prime-power good case = `isEigen` (p^v coprime), bad case = new helper `Newform.heckeT_ppow_bad_smul` (induction on v using T006-c). Local `heckeT_n_cusp_decomp`/`heckeT_n_cusp_one'` helpers added (originals private). **T006 main `Newform.isFullEigenform` compiles.**

  **Bad-prime `U_p` adjoint route located**: `AdjointTheory/SummandAdjoint.lean:117` `peterssonAdj_T_p_upper_eq_shift_mul_lower` gives `adj([1,b;0,p]) = shift_{-b}·[p,0;0,1]`, so `petN (U_p f) g = petN f (V'_p g)` with `V'_p g = Σ_b g|[p,-b;0,1] = p^k Σ_m a_{pm}(g) q^{p²m}` (lower-tri/`V_p`-type sum; NOT a level-N Hecke op since the coprime triple-product `T_p_lower_triple_product_matrix` needs `Coprime p N`). T006-b ⇒ need `V'_p` preserves `cuspFormsOldExtended`.
- **Statement**: for `f : Newform N k`, `∀ n : ℕ+, ∃ c : ℂ, heckeT_n_cusp k n f.toCuspForm = c • f.toCuspForm` (⟹ `IsFullEigenform`).
- **Sketch**: strong induction via `heckeT_n_unfold` (`T_n = T_{p^v}·T_{n/p^v}`, `p = minFac n`).
  Each prime-power factor acts as a scalar on `f`: good `p` via the `heckeT_ppow` recurrence
  (`heckeT_n_cusp_ppow_succ_succ`, `Eigenform.isEigen`, diamond scalar on χ-space); bad `p` via
  `heckeT_ppow_eq_pow_of_not_coprime` ⟹ `U_p^v` ⟹ `a_p^v •` from T006-c. Compose scalars through
  `heckeT_n_cusp_decomp_of_mul`. Closes `Newform.isFullEigenform`.
- **Mathlib/codebase**: `heckeT_n_unfold` (HeckeT_n.lean:554), `heckeT_n_cusp_decomp_of_mul`/`heckeT_n_cusp_ppow_succ_succ` (LevelRaiseComm.lean), `heckeT_ppow_eq_pow_of_not_coprime` (HeckeT_n.lean:472).

**★ The ONE true deep block is T002 (FIH = `heckeAlgℤ` finite over ℤ) ← IHR (Eichler–Shimura integral structure) ★** — see `decomposition.md`. Per source (Shimura Thm 3.48(3)/3.51/3.52 rest on the lattice (3.5.20), proven via Shimura Chapter 8 = the Eichler–Shimura cohomology isomorphism). Both (A) and (B) reduce to this single classical input.

### [T003] The newform eigensystem ring hom `newformEigenHom`
- **Status**: ✅ **DONE (2026-06-21) — `newformEigenHom` + `newformEigenHom_heckeEnd` proven + axiom-clean** (`{propext, Classical.choice, Quot.sound}`). Construction: `heckeAlgℤ` elements preserve the eigenline `ℂ∙f` (via `Algebra.adjoin_induction` — Hecke gens by `isFullEigenform`=T006, diamonds by `diamondOpCusp_apply_charSpace`), giving a unique scalar per element (f≠0 via `isNorm`), assembled into the ring hom; coprime agreement via `Eigenform.isEigen`. · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: T001 (done), T006 (done) · **Type**: def + lemmas
- **Progress** (2026-06-20): T001 done. T003 needs every generator of `heckeAlgℤ` to act on `f` by a scalar: coprime `T_n` via `Eigenform.isEigen` ✓, diamonds via the χ-character space ✓, but bad-prime `U_p` needs `Newform ⟹ IsFullEigenform` = T006 (Atkin–Lehner–Li, unformalised). Blocked on T006.

#### Statement
```lean
/-- A `Newform N k` is a simultaneous eigenvector for the whole integral Hecke algebra; the
eigenvalue map is a ring homomorphism `𝕋ℤ → ℂ`. -/
noncomputable def newformEigenHom (f : Newform N k) : heckeAlgℤ N k →+* ℂ := sorry

theorem newformEigenHom_heckeEnd (f : Newform N k) (n : ℕ) (hn : Nat.Coprime n N) :
    newformEigenHom f ⟨heckeEnd N k n, heckeEnd_mem ..⟩ = f.eigenvalue ⟨n, _⟩ := sorry
```

#### Proof sketch
1. `f.toCuspForm ≠ 0`, and for every generator `heckeEnd N k n`, `heckeEnd N k n f = c • f` for a
   scalar `c` (`f` is a common eigenvector: for coprime `n` this is `Eigenform.isEigen`; for `U_p`
   (p ∣ N) and diamonds it is the newform's full eigen-property — establish the `U_p`/diamond
   eigen-equations for a newform, or import them from the codebase's eigenform structure).
2. Define `newformEigenHom` on the subalgebra by `T ↦ (the scalar by which T acts on f)`: well-
   defined because the action `𝕋ℤ → End_ℂ` followed by "evaluate at f / divide by f" lands in
   scalars (every element of `𝕋ℤ` acts as a scalar on the eigenline `ℂ·f`). Concretely: the map
   `𝕋ℤ → ℂ`, `T ↦ φ(T f)` where `φ` is a fixed ℂ-linear functional with `φ(f)=1`, restricted to the
   eigenline is a ring hom (uses that `T f = (scalar) • f`).
3. Ring-hom laws: `(T₁T₂)f = T₁(T₂ f) = c₂ T₁ f = c₁c₂ f`; additivity similar; `1 ↦ 1`. Cancel the
   nonzero `f` (reuse `smul_eq_smul_cancel`-style cancellation).

#### Mathlib lemmas needed
`RingHom.mk`, scalar-cancellation on the eigenline. Codebase: `Eigenform.isEigen`, cusp-form
scalar-cancellation.

#### Generality decision
Per `Newform`; the U_p/diamond eigen-equations are the newform-specific input.

---

## Tranche 1 — bounded, formalizable NOW: close T004/T005 modulo isolated `heckeAlgℤ_finite`

*Approved 2026-06-21 (isolate `heckeAlgℤ_finite`). Decomposition: `decomposition-group-a-finish.md`.
Source-faithful (DS weight-k Fourier formulas + Thm 6.4.5/Ex 6.5.1; the q-expansion-lattice route was
REJECTED — not in DS). T006 + T003 DONE + axiom-clean. **`NewformOrbit.lean` must add
`import LeanModularForms.Labels.HeckeFieldArithmetic`** (acyclic) so L1/T004/T005 see `newformEigenHom`.
Per AINTLIB producer rules: NO cleanup tickets (central main owns cleanup).*

> **The isolated FIH input is [T002]** (`instance heckeAlgℤ_finite … := sorry`, stated above at line 70).
> Tranche-1 step: lay that `sorry` instance in `HeckeFieldArithmetic.lean` so T004b/T005 compile over it
> (the gate is sorry-free-in-body over a black-box input, the FD-e.2 pattern). Its PROOF is Tranche 2
> (research). **DO NOT attempt T002's proof in Tranche 1.**

### [T004b] `O_f := (newformEigenHom f).range` is module-finite over ℤ
- **Status**: ✅ **DONE (2026-06-21)** — `newformEigenHom_range_finite`, green (`Module.Finite.of_surjective` on `rangeRestrict.toIntAlgHom`). T002's `sorry` instance laid in HeckeFieldArithmetic. · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: T002 (statement laid), T003 · **Type**: lemma

#### Statement
```lean
theorem newformEigenHom_range_finite (f : Newform N k) :
    Module.Finite ℤ ↥(newformEigenHom f).range := sorry
```

#### Proof sketch
`(newformEigenHom f).range` is the image of `heckeAlgℤ N k` (which is `Module.Finite ℤ` by T002-iface)
under the surjective ring hom `(newformEigenHom f).rangeRestrict`. A ℤ-algebra hom is ℤ-linear, and the
range-restriction is surjective; apply `Module.Finite.of_surjective`.
1. `Module.Finite ℤ (heckeAlgℤ N k)` — `inferInstance` (T002-iface).
2. `(newformEigenHom f).rangeRestrict : heckeAlgℤ N k →+* (newformEigenHom f).range` is surjective
   (`RingHom.rangeRestrict_surjective`); view as ℤ-linear (`.toIntAlgHom.toLinearMap` or `AddMonoidHom`).
3. `Module.Finite.of_surjective <that ℤ-linear map> <surjectivity>`.

#### Mathlib lemmas needed
`Module.Finite.of_surjective` (`RingTheory/Finiteness/Basic.lean:252`),
`RingHom.rangeRestrict_surjective`, the ℤ-linearity of a `RingHom` between ℤ-algebras.

#### Sources
DS Definition 6.5.2 / eq (6.12) — `T_ℤ/I_f ≅ ℤ[{aₙ(f)}]` (`ds.txt:18111`), the eigenvalue ring is a
quotient/image of the f.g. `T_ℤ`.

#### Generality decision
Per `f : Newform N k`; ℤ-module-finiteness of the eigenvalue subring `O_f ⊆ ℂ`.

### [T004a] Newform coefficients are eigenvalues for ALL n
- **Status**: ✅ **DONE (2026-06-21)** — `coeffSeq_eq_newformEigenHom_heckeEnd`, green. Sub-leaf `coeff_n_eq_coeff_one_heckeT_n_cusp` (eq-5.21 for ALL n) PROVEN + **fully axiom-clean** (`{propext, Classical.choice, Quot.sound}`, no `sorryAx`). The agent corrected the decomposition (cofactor coprime to `p`, not `N`) and proved the stronger `a_m(Tₙg)=a_{mn}(g)` for `gcd(m,n)=1` by strong induction (`coeff_heckeT_n_cusp_coprime_mul`), instantiated at `m=1`. Import added. · **File**: Labels/NewformOrbit.lean · **Depends on**: T003, T006 · **Type**: lemma

#### Statement
```lean
theorem coeffSeq_eq_newformEigenHom_heckeEnd (f : Newform N k) (n : ℕ+) :
    coeffSeq f n = newformEigenHom f ⟨heckeEnd N k n, heckeEnd_mem n⟩ := sorry
```

#### Proof sketch
1. T006 `Newform.isFullEigenform` + T003: `heckeT_n_cusp k n.val f.toCuspForm = λₙ • f.toCuspForm`, with
   `λₙ = newformEigenHom f ⟨heckeEnd N k n, _⟩` (`heckeEnd N k n` applied = `heckeT_n_cusp k n.val`;
   `newformEigenHom f T = eigenScalar`, and `eigenScalar_smul` gives `λₙ • f = (heckeEnd N k n) f`).
2. Apply the `a₁`-coefficient functional (`coeffSeqLin`/`qExpansionLin`, ℂ-linear): `a₁(λₙ • f) = λₙ · a₁(f)`;
   `a₁(f) = 1` (`f.isNorm`). So `a₁(heckeT_n_cusp k n.val f) = λₙ`.
3. `a₁(heckeT_n_cusp k n.val f) = aₙ(f) = coeffSeq f n` (DS eq 5.21, **all n**). Compose ⟹ goal.
   - **Coprime n**: `coeffSeq_coprime_eq_eigenvalue` (NewformOrbit:127) + `newformEigenHom_heckeEnd` (T003) — done.
   - **Bad n (p ∣ N)**: the sub-leaf `a₁(heckeT_n_cusp k n.val f) = aₙ(f)`. `fourierCoeff_heckeT_n_period_one`
     (FourierHecke:702) is coprime-only; prove the bad-prime analogue at `m=1` from the bad-prime Fourier
     action — `a₁(Uₚ f) = aₚ(f)` (DS eq 5.4 with `χ(p)=0`) + `heckeT_n_cusp_divN_coeff` (FullEigenform:36)
     + the prime-power/composite recursion already used in `FourierHecke.lean`.

#### Mathlib lemmas needed
`ModularForm.qExpansion_smul` / `qExpansionLin` (NewformOrbit:387) for `a₁` linearity; `PowerSeries.coeff`
linearity; the existing coprime link; bad-prime Fourier-Hecke lemmas in `FourierHecke.lean`.

#### Sources
DS eq (5.21) `ds.txt:15332` (*"a₁(Tₙf)=aₙ(f) for all n ∈ ℤ⁺"*); Thm 5.8.2 `ds.txt:15376-15378`
(*"its Fourier coefficients are its Tₙ-eigenvalues … for all n"*); Prop 5.2.2 eq (5.4) `ds.txt:13421`
(bad-prime `aₙ(Tₚf) = a_{np}(f) + χ(p)p^{k-1}a_{n/p}(f)`, with `χ(p)=0` for `p|N`).

#### Generality decision
Per `f : Newform N k`, ALL `n : ℕ+` (unifies coprime + bad; the bad case is the genuine new content,
gated on T006 which already supplies the all-n eigen-equation).

### [T004] Close `coeffSeq_isIntegral` — `a_n(f)` is an algebraic integer
- **Status**: ✅ **DONE (2026-06-21)** — `coeffSeq_isIntegral` rewired + green (`IsIntegral.of_finite ℤ` on `⟨coeffSeq f n, _⟩ : O_f`, `.map …toIntAlgHom`, `.tower_top`). Sorry-free in body (transitive dep on the isolated `heckeAlgℤ_finite` only). · **File**: Labels/NewformOrbit.lean:442 (rewired) · **Depends on**: T004a, T004b · **Type**: theorem (closes a sorry)

#### Statement
```lean
theorem newform_coeff_isIntegral (f : Newform N k) (n : ℕ+) : IsIntegral ℚ (coeffSeq f n)
```
then replace the `sorry` body of `coeffSeq_isIntegral` (NewformOrbit.lean) with this.

#### Proof sketch
1. `Module.Finite ℤ ↥(newformEigenHom f).range` — directly **T004b**.
2. `coeffSeq f n = newformEigenHom f ⟨heckeEnd N k n, _⟩ ∈ (newformEigenHom f).range = O_f` — **T004a**
   (the all-n eigenvalue link; both coprime and bad n). So `⟨coeffSeq f n, mem⟩ : O_f`.
3. `IsIntegral ℤ (⟨coeffSeq f n, _⟩ : O_f)` by `IsIntegral.of_finite ℤ` (1); push along `O_f ↪ ℂ`
   (`IsIntegral.map` of the subalgebra inclusion) to `IsIntegral ℤ (coeffSeq f n)`; then `IsIntegral ℚ`
   by base change ℤ→ℚ (`IsIntegral.tower_top`/`map_isIntegral` along `ℤ → ℚ` in the scalar tower
   `ℤ → ℚ → ℂ`).

#### Mathlib lemmas needed
`IsIntegral.of_finite` (`RingTheory/Algebraic/Integral.lean:102` — verified), `IsIntegral.map`
(integrality along an algebra map), `IsIntegral.tower_top` / `IsIntegral.of_isScalarTower` for ℤ→ℚ.

#### Sources
DS Theorem 6.5.1 (`ds.txt:18081`, *"the eigenvalues aₙ(f) are algebraic integers"*) via the
algebraic-integer criterion Thm 6.4.5 (`ds.txt:17945`) — realised in Lean as `IsIntegral ℤ` of an
element of a module-finite ℤ-algebra. Also Shimura, *Intro. Arithmetic Theory*, Thm 3.52.

---

### [T005] Close `instFiniteDimensionalCoeffField` — `K_f` is a number field
- **Status**: ✅ **DONE (2026-06-21)** — `instFiniteDimensionalCoeffField` + `instNumberFieldCoeffField` green. `#print axioms instNumberFieldCoeffField` = `{propext, sorryAx, Classical.choice, Quot.sound}`, the `sorryAx` tracing SOLELY to the isolated `heckeAlgℤ_finite` (the only sorry in the whole Labels tree). Proof: finite ℤ-generators of `O_f` (`Submodule.fg_def`) → `IntermediateField.finiteDimensional_adjoin` of their integral ℂ-images → `coeffField ≤ adjoin ℚ Tc` → `Module.Finite.of_injective` on `IntermediateField.inclusion`. · **File**: Labels/NewformOrbit.lean:452 (rewired) · **Depends on**: T004a, T004b · **Type**: instance (closed the sorry)

#### Statement
```lean
instance : FiniteDimensional ℚ (coeffField f)
```
replacing the `sorry`.

#### Proof sketch
1. `Module.Finite ℤ O_f` (**T004b**, `O_f = (newformEigenHom f).range`). Let `A := Algebra.adjoin ℚ
   (O_f : Set ℂ) ⊆ ℂ`. Then `Module.Finite ℚ A` (base change of the f.g. ℤ-module `O_f` to ℚ; the
   ℚ-adjoin of a module-finite-over-ℤ subring is finite-dim over ℚ).
2. `A` is a domain (subring of `ℂ`) and finite-dimensional over the field `ℚ` ⟹ `A` is a field (a
   finite-dimensional domain over a field is a field — Artinian-domain argument).
3. `coeffField f = IntermediateField.adjoin ℚ (Set.range (coeffSeq f)) ⊆ A` — each `coeffSeq f n ∈ O_f
   ⊆ A` (**T004a**). A sub-ℚ-space of the finite-dim ℚ-space `A` is finite-dim ⟹
   `FiniteDimensional ℚ (coeffField f)`.

#### Mathlib lemmas needed
`Module.Finite` base change ℤ→ℚ (`Module.Finite.of_restrictScalars_finite` / `Algebra.adjoin` finite of
integral set), finite-dim domain over a field is a field (`Algebra.IsIntegral.isField_iff_isField` /
`isField_of_isIntegral_of_isField`, or Artinian-domain), `Submodule.finiteDimensional` (sub of finite-dim),
`IntermediateField.adjoin_le_iff` (to land `coeffField ⊆ A`).

#### Sources
DS §6.5 (`ds.txt:18116-18122`): the eigenvalue ring has *"finite rank as a ℤ-module … inside some
finite-degree extension field of ℚ, i.e., a number field"*; Defn 6.5.3 (the Hecke field `K_f`).

---

### [CLEANUP-1] Run /cleanup on HeckeFieldArithmetic.lean
- **Status**: DEFERRED (AINTLIB producer rule — cleaning/golf/dedup is owned by central main, not run on `dev/leanmodularforms`). Deps (T005) met, but not a dev `/beastmode` target. · **File**: Labels/HeckeFieldArithmetic.lean · **Depends on**: T005 · **Type**: cleanup
- Final per-file cleanup (Tranche 1 has < 3 proof tickets per the cadence's per-file rule + a final cleanup).

---

## Tranche 2 — IHR (integral modular-symbol realization) — RESEARCH ROADMAP

*These discharge T002. They require modular-symbol / Eichler–Shimura infrastructure absent from
mathlib; the `/develop` confidence gate does NOT pass for them (research-scale API gaps). Tracked as
blocked; NOT for immediate `/beastmode`. Each would itself be a `/develop` sub-project.*

*Refinement (2026-06-21): this IS DS's actual route (Thm 6.5.1 homological). **IHR-asm is BOUNDED and
now PROVEN** as `heckeAlgℤ_finite` from `exists_HeckeStableLattice` ([T002-L1], Tranche-1): `End_ℤ(free
r)≅M_r(ℤ)`, sub-module of finite-free is finite. So the irreducible content is IHR-a + IHR-c + IHR-d.*

*★ EXPERT-ANALYSIS CONFIRMATION (2026-06-22, `expert-review/2026-06-22/REVIEW_REPLY.md`): route A is
the cheapest, and **only INJECTIVITY of the period map is needed — NOT the full Eichler–Shimura
isomorphism** (no surjectivity/Hodge half). Faithfulness of `𝕋ℤ` on `𝕄_k(ℤ)` follows from IHR-a+IHR-b
+IHR-d alone (`T=0` on `𝕄_k(ℤ)` ⟹ `T=0` on `𝕄_k^∨⊗ℂ` ⟹ `T·ι=ι(T·)=0` ⟹ `T=0` on `S_k` by injectivity).
**The shortcuts were ruled out**: (Q4) the Sturm bound gives `𝕋ℤ` f.g.-as-ℤ-ALGEBRA but NOT integrality
of `T_p` (the q-expansion image isn't a full-rank ℤ-lattice without rationality); (Q5) even
algebraicity/number-field needs the ℚ-rational structure of `S_k` (eigenvalues of ℂ-operators could a
priori be transcendental) — no bypass. So the modular-symbol substrate (IHR-a + period injectivity
IHR-d) is genuinely irreducible. Frontier to BUILD: IHR-a (Manin finite-free, combinatorial) + IHR-d
(period injectivity, the analytic core); IHR-asm reduces to the proven [T002-L1] endgame.*

*▶ BUILD STARTED (2026-06-22, `/develop --decompose` + marathon). `decomposition-IHR.md` (the full ES-1..asm
decomposition with verbatim Shimura Ch.8 quotes + mathlib-foundation classification) + the green 12-sorry
typecheck skeleton `HeckeRIngs/GL2/ModularSymbols/Skeleton.lean` are written; the route is confirmed founded
(ES-4 injectivity rides the project's proven `petN_definite`/`CuspForm.pet_definite`; module via
`Representation.Coinvariants` / `MvPolynomial.homogeneousSubmodule` / `Projectivization.Action`). Build order:
**[ES-0] coefficient system → ES-1 module → ES-2 Hecke → ES-3 period → ES-4 injectivity → ES-asm**. The leaf
labels below (IHR-a..asm) map onto ES-1..asm. ≈3000–5000 LOC new; risk concentrated in ES-3/ES-4 engineering,
not absent prerequisites.*

### [ES build tracker] modular-symbol substrate leaf status (2026-06-22, live marathon)
Real files under `HeckeRIngs/GL2/ModularSymbols/` (NOT the planning `Skeleton.lean`; none imported by the
aggregator yet — downstream leaves import siblings directly). FINITENESS PLAN: see the "ES-1 finiteness —
REFINED PLAN" block in `decomposition-IHR.md` ((i)+(ii)+(iii) elementary route; `Module.Free 𝕄` NOT needed —
endgame uses `End_ℤ(𝕄)`/dual; `𝕄` may have torsion).
- **[ES-0] ✅ DONE** `CoefficientSystem.lean` — `symRep` (Sym^{k-2} = `homogeneousSubmodule`, linear-subst action, `g⁻¹`+row-index convention ⟹ genuine MonoidHom) + `div0Rep` (Div⁰ permutation rep on `Projectivization ℚ (Fin 2→ℚ)`, routed `SL₂ℤ→SL₂ℚ`). Axiom-clean, green (1922 jobs).
- **[ES-1b] ✅ DONE** `SL2Generation.lean` — `instance : Group.FG SL(2,ℤ)` (one-liner off mathlib's `SpecialLinearGroup.SL2Z_generators : closure {S,T}=⊤`); ⟹ `Group.FG (Gamma1 N)` auto-resolves via Schreier `fg_of_index_ne_zero`. Axiom-clean, green.
- **[ES-1a] ✅ DONE** `ModuleM.lean` — `𝕄 N k := Coinvariants (modSymRep …)` via a MANUAL `TensorProduct.map` rep `fullModSymRep` (CONFIRMED: sidesteps the `Representation.tprod`↔`Coinvariants` AddCommGroup instance-diamond — `Coinvariants` elaborated cleanly, no bundled-`Rep`/`letI` needed) + `noncomputable` ℤ-module instances + `𝕄.mk`/`𝕄.mk_surjective`. Axiom-clean, green (2513 jobs). (Finite/Free NOT here — separate.)
- **[ES-1d] ✅ DONE** `CoinvariantsFinite.lean` — general reusable lemma `Representation.Coinvariants.finite_of_span_orbit_top`: if `G`-translates of a finite `S` `k`-span `V` then `Module.Finite k (Coinvariants ρ)` (the (iii) step; uses mathlib's ready-made `Coinvariants.mk_self_apply`). Axiom-clean, green (1962 jobs); mathlib candidate.
- **[ES-1c-cusps] ✅ DONE** `FinitelyManyCusps.lean` — `instFiniteCuspsGamma1 : Finite (Γ₁N-orbits on ℙ¹ℚ)` + reusable `isPretransitive_SL2Z_projQQ`, via mathlib's `OnePoint.exists_mem_SL2` (transitivity) + `Subgroup.finite_quotient_of_pretransitive_of_index_ne_zero` (found ready-made) + `instFiniteIndexGamma1`. Action matches `div0Rep` (GL↔SL bridge is `rfl`). Axiom-clean, green (2262 jobs).
- **[ES-1c-main + asm] ✅ DONE** `ModuleMFinite.lean` — `instance instModuleFiniteModularSymbols : Module.Finite ℤ (𝕄 N k)`. Layer (i) `exists_div0_orbit_span` (`Div0` orbit-spanned over `ℤ[Γ₁N]`: `divDiff` differences API + augmentation-kernel-as-differences `span_divDiff_basepoint` [built in-file, no mathlib lemma] + orbit transversal `orbitReps` + telescoping `divDiff_mem_of_closure` via `Subgroup.closure_induction`, refined with `c ∈ orbitReps` to kill a spurious case) + Layer (ii) `untwist` + `instModuleFiniteSymPow` [built in-file via `homogeneousSubmodule_eq_finsupp_supported`], assembled through `finite_of_span_orbit_top`. Instance-diamond dodged via `TensorProduct.induction_on`. Axiom-clean (NO sorryAx), green (2600 jobs). **★ ES-1 CLOSED — `𝕄 N k` is `Module.Finite ℤ`. The entire algebraic/combinatorial foundation (6 files) is done, axiom-clean.**

### ES back half — ES-2 / ES-3 / ES-4 / ES-asm (the analytic heart + endgame) — ⬜ NEXT
Both ES-2 and ES-3 need only `𝕄` (done); ES-4 needs ES-3; ES-asm needs ES-1✅+ES-2+ES-3+ES-4.
- **[ES-2] ✅ DONE** `HeckeSymbol.lean` (1053 lines) — `heckeSymb n, diamondSymb d : 𝕄 N k →ₗ[ℤ] 𝕄 N k`. `actMat M = TensorProduct.map (GL action on ℙ¹ℚ, =`rfl`-compat w/ div0Rep) (substAlgHom (adjugate M) on Sym)`, `actMat_mul` covariant via `adjugate_mul_distrib`; coset reps EXACTLY match `heckeT_n` (`upperMat=[[1,b],[0,p]]`, `lowerDiamondMat=γ_p·[[p,0],[0,1]]`, same `γ_p` as `diamondOpCusp`). Descent (ker-preservation) lemmas: diamonds via `Gamma0_normalizes_Gamma1`+`mk_self_apply`; `U_p` (p∣N) via `upperMat_mul_sl`+`moebiusFin'` bijection; `T_p` (p∤N) via the b₀-upper↔lower swap `hb_sum_ite_swap_eq`. Instance-diamond dodged via term-mode `congr` (no `rw` across it). Axiom-clean (NO sorryAx), green (3305 jobs). Relations + equivariance NOT done (deferred to ES-3/ES-asm, per plan). **The entire algebraic+combinatorial half (ES-0,1,2) is DONE.**
- **[ES-3] period map** `ι : S_k →ₗ[ℂ] (𝕄 N k →ₗ[ℤ] ℂ)`, `⟨f,((α)-(β))⊗P⟩=∫_β^α f·P(·,1) dz`:
  - **[ES-3a] ✅ DONE** `PeriodIntegral.lean` (562 lines) — `evalSym1` (eval P at (z,1)), `periodIntegral` (the canonical `{0,∞}` imaginary-axis integral `∫_{Ioi 0} f(it)·evalSym1 P (it)·I dt`), + **convergence at BOTH cusps** (`integrableOn_imagAxisPeriodIntegrand`): `∞` via `CuspFormClass.exp_decay_atImInfty'` beating poly growth, `0` via `S`-slash to the conjugate arithmetic group (`isArithmetic_conj_S`). Route G (Route P rejected: mathlib primitives are disk/ℂ-only). + linearity in f,P. Axiom-clean (NO sorryAx), green (2996 jobs).
  - **[ES-3b] ✅ DONE (modulo `IsPeriodInvariant`)** `PeriodMap.lean` (~820 lines) — `periodMap (hinv : ∀ f, IsPeriodInvariant f) : S_k →ₗ[ℂ] (𝕄 →ₗ[ℤ] ℂ)` fully assembled via `Coinvariants.lift`, ℂ-linear in f, axiom-clean (NO sorryAx), green (3309 jobs). Built: polymorphic `genPeriodIntegral`+`genIntegrableOn` (reusable, any `[CuspFormClass F Γ k][Γ.IsArithmetic]`), `cuspToInftyIntegral`/`cuspValue` (finite cusp `q` via `transMat q`), `rawPairing` (the ℤ-bilinear ℂ-valued pairing). **★ symRep `g⁻¹`-convention CONFIRMED correct** — `evalSym1_symRep_smul`: automorphy `(cz+d)^{-(k-2)}`, powers cancel `(cz+d)^{k-(k-2)-2}=1`, NO `γ⁻¹P` needed.
  - **[ES-3b-inv] ⏳ ENGINE BUILT, reduced to 1 deep lemma** `PeriodInvariant.lean` — `isPeriodInvariant_all` + `periodMap'` (unconditional period map) PROVEN modulo ONE isolated `sorry` `cuspValue_symRep_gamma`. The **contour-deformation engine is axiom-clean + reusable** (ES-3c/ES-4 inherit): `differentiableOn_periodForm`, `vertical_segment_cauchy` (rectangle Cauchy via mathlib `integral_boundary_rect_eq_zero_of_differentiableOn`), `periodForm_norm_le` (uniform decay), `tendsto_horizontal_cap`, `horizontal_slide` (improper path-independence, horizontal), `cuspToInftyIntegral_eq_periodForm`. The full algebraic reduction (`rawPairing_modSymRep_sub`, `sum_cuspDiff_eq_zero_of_const`, `castSymPow_symRep`) also axiom-clean.
  - **[cuspValue_symRep_gamma] ⏳ ENGINE BUILT, reduced to `μ≡0`** — primitive + Möbius CoV done (axiom-clean, reusable): **`Complex.isExactOn_upperHalf`** (holomorphic PRIMITIVE on all of ℍ via ball-exhaustion+Morera+gluing `DifferentiableOn.isExactOn_ball`+`eqOn_of_deriv_eq` — FTC makes path-independence automatic, NO curved-region Cauchy needed) + **`periodForm_mob`** (Möbius CoV, automorphy cancels `k-(k-2)-2=0`) + `hasDerivAt_mob`/`automorphy`/`mob_eq_smul`. ES-3c/ES-4 reuse both. **Residual = `μ≡0`**: the primitive extends continuously to each finite cusp ⟺ **`periodForm f Q → 0` along `𝓝[{0<im}] (q:ℂ)` for every rational cusp `q`** (2-D cusp-form decay at finite cusps). Route: `σ_q∈SL₂ℤ`, `σ_q•∞=q`, `f∣σ_q` decays at `i∞` (`Im(σ_q⁻¹z)→∞` as `z→q` in ℍ; project has `q=0`/`S` 1-D case `tendsto_genIntegrand_nhdsGT_zero` + `IsArithmetic.conj`/`IsCusp.smul`) ⟹ exp beats poly ⟹ →0; FTC ⟹ primitive limit exists path-indep ⟹ `μ=0`. **Continuation dispatched.**
    - **UPDATE (2026-06-22):** 1st μ≡0 agent CRASHED (API 500) mid-edit, leaving the file broken (`HasDerivAt.scomp` arg-reversal + `•`vs`*`). **REPAIRED by hand** (`h1.scomp Y hpath`; `rw [smul_eq_mul, mul_comm Complex.I]; exact h2` — instances were defeq, `simpa` had over-normalized) → green, 1 sorry. The crash agent's GROUNDWORK is PRESERVED + axiom-clean: `tendsto_periodForm_vertical_nhdsGT_zero`, `tendsto_im_div_normSq_denom_vertical` (the `Im(σ_q⁻¹z)→∞` geometric core), `hasDerivAt_periodForm_primitive_vertical` (FTC-along-vertical, fixed) + a REFINED reduction docstring. **Sharper residual:** `μ≡0` is NOT 2-D continuity (false) but equality of vertical-ray vs geodesic-arc boundary limits = a **σ-conjugate horizontal-cap of `f∣σ`** vanishing by cap-decay (the `SL₂ℤ`-twisted analogue of the built `horizontal_slide`/`tendsto_horizontal_cap`). **Re-dispatched on the repaired base.**
    - **★★ CLOSED (2026-06-22):** `cuspBoundary_mob_naturality` PROVEN OUTRIGHT, axiom-clean (NO sorryAx), via the σ-conjugation cap (4-way case split on `o`/`γ•o`; `σ•∞=cusp` → `i∞`-frame → cap of conjugate `f∣σ` vanishes). New reusable layer: `periodForm_mob_general`/`hasDerivAt_primitive_mob_general` (Möbius CoV for ANY `g∈SL₂ℤ`), `tendsto_uniform_horizontal_cap`, `tendsto_primitive_curve_atInfty`, `tendsto_F_curve`, the pole geometry (`mob_pole_eq`/`tendsto_im_mob_pole`/`mob_upper_eq`). **⟹ `cuspValue_symRep_gamma` (k≥2), `isPeriodInvariant_all`, `periodMap'` axiom-clean for k≥2** (sorryAx now traces SOLELY to the intended `cuspValue_symRep_gamma_of_lt_two` k<2 branch). **THE ANALYTIC BACKBONE IS DONE.** `periodMap'` is the unconditional period map for k≥2; ES-3c/ES-4 reuse the engine.
  - **[ES-3c] ⏳ hD DONE, hT reduced to det-p CoV** `PeriodHecke.lean` (409 lines) — `periodMap'_diamond` (`hD`) FULLY PROVEN (det-1 CoV, same `γ_d` as `diamondOpCusp`). Built axiom-clean reusable: `cuspBoundary_mob_naturality_sl` + `cuspValue_symRep_sl` (general `SL₂ℤ` covariance), `rawPairing_fullModSymRep_sl` (det-1 CoV helper), `PeriodEquiv` scaffolding. **`periodMap'_heckeEnd` (`hT`) reduced to ONE founded sorry: the det-`p` Heilbronn CoV** (`GL₂⁺(ℚ)` analogue of `cuspValue_symRep_sl`; integrand-level verified clean — `p^{1-k}` slash cancels adjugate scaling; remaining = generalize the σ-conjugate boundary-naturality engine det-1→det-`p`, then per-coset sum + `T_n` recurrence via `heckeSymb_commute`). **Dispatched.**
- **[ES-4] ✅ DONE modulo the Stokes identity** `PeriodInjective.lean` — `periodMap'_injective (hk : 2 ≤ k)` ASSEMBLED + axiom-clean APART from one isolated sorry. Proven axiom-clean: `periodPairingA f g := petN f g + (-1)ⁿ•petN g f` (Shimura 8.2.17), `periodPairingA_twist_self : A(f,iⁿf)=2iⁿ•petN f f` (8.2.18c, the non-degeneracy witness), `periodMap'_petN_self : periodMap' f=0 → petN f f=0`, then injectivity via `petN_definite`. **LONE RESIDUAL (the last deep analytic fact, k≥2):** `periodPairingA_eq_zero_of_periodMap'_zero` = **Shimura (8.2.22)** Stokes identity (`periodMap' f=0 ⟹ A(f,g)=0 ∀g`): needs a `Γ₁(N)` FD with PAIRED geodesic boundary edges (project has only the coset-tiling `Gamma1_fundDomain_PSL`) + mathlib `integral2_divergence_prod` on the period 2-form + edge-telescoping. Research-scale; isolated cleanly. **Dispatched.**
  - **UPDATE — reduced to the Green's identity (2026-06-22):** PROVEN axiom-clean: `rawPairing_eq_zero_of_periodMap'_zero` (periodMap' f=0 ⟹ all `rawPairing f x=0`) + the target `periodPairingA_eq_zero_of_periodMap'_zero` (modulo the isolated lemma). **LONE RESIDUAL = `periodPairingA_eq_boundary_period`** (Shimura 8.2.22): `A(f,g)=Σ coeffᵢ·rawPairing f (yᵢ)` — the Petersson pairing IS a finite ℂ-combo of PERIODS of `f` (the Green's-theorem output). Research-scale (~1000+ LOC: `Γ₁N` FD with PAIRED geodesic edges [project has only the coset-tiling] + mathlib `integral2_divergence_prod` is RECTANGLES-only, need curvilinear + the twisted 2-form + edge-telescoping; no foothold). The existential form is FAITHFUL (trivial witness would assert the false `A(f,g)=0`).
  - **★★ CRITICAL — SOUNDNESS DRIFT FOUND (2026-06-23 drift audit, user-requested):** `cuspValue_symRep_gamma` (PeriodInvariant.lean:1818) is stated ∀k (NO `hk`) and proven `by_cases hk:2≤k` with the else-branch `exact cuspValue_symRep_gamma_of_lt_two … (lt_of_not_ge hk)` (:1857). But `cuspValue_symRep_gamma_of_lt_two` (:1798, `sorry`) is **FALSE at k=1**: `(k-2).toNat=0` (Sym⁰) but the automorphy needs `(cz+d)^{k-2}=(cz+d)^{-1}` which does NOT cancel ⟹ the c-independence genuinely fails for weight-1 f≠0. So `cuspValue_symRep_gamma` is a `sorry`-proof of a GLOBALLY-FALSE ∀-statement (instantiable at k=1 to derive False) — taints `isPeriodInvariant_all`/`periodMap'` and everything mentioning them, **even the k≥2 results, are logically unsound while this stands.** This is the exact "bundle the hard part into an un-proven (here FALSE) hyp" failure mode. **MANDATORY FIX (elevated from cleanup to soundness, do FIRST once the running PeriodHecke/PeterssonStokes agents land): GATE `cuspValue_symRep_gamma`/`isPeriodInvariant_all`/`periodMap'` with `hk : 2 ≤ k`, DELETE `cuspValue_symRep_gamma_of_lt_two` entirely** (its k≥2 callers already carry `hk`). Then the chain is TRUE-for-k≥2-only, no false sorry. k<2 handled separately in wiring (k≤0 trivial S_k=0; k=1 Deligne–Serre, out of modular-symbol scope). MUST do before any "axiom-clean k≥2" claim.
- **★★ GATING DONE + INDEPENDENTLY VERIFIED (2026-06-23):** the false `cuspValue_symRep_gamma_of_lt_two` is DELETED (grep: 0 matches); `cuspValue_symRep_gamma`/`isPeriodInvariant_all`/`periodMap'`/`periodMap'_apply_mk`/`cuspDiff_const` all gated with `hk : 2 ≤ k`; `rawPairing_eq_zero_of_periodMap'_zero` also got `hk` (its sole caller had it). **`PeriodInvariant.lean` is now SORRY-FREE**; `cuspValue_symRep_gamma`/`isPeriodInvariant_all`/`periodMap'_diamond` are AXIOM-CLEAN (no sorryAx). `periodMap'_injective`/`periodMap'_heckeEnd` sorryAx now traces ONLY to the genuine residuals (Green's `periodPairingA_eq_boundary_period`; det-p `periodEquiv_heckeEnd_prime`/`cuspValueGen_symActC_heilbronn`). NO statement weakened, NO use-site failed to get `hk`. Full `lake build LeanModularForms` green (8784 jobs). **The soundness defect is removed.** Remaining for k≥2 = the 2 founded deep leaves (det-p σ-cap + Green's Type-I bookkeeping) + wiring.
- **DRIFT-AUDIT verdict on the OTHER sorries (all FAITHFUL/non-vacuous, verified 2026-06-23):** endgame `heckeAlgℤ_finite_of_period` conclusion = genuine `Module.Finite ℤ heckeAlgℤ`, `ι` a real linear-map var, `hComm`/`hT`/`hD`/`hι` the genuine ES facts (hComm via real `hComm_genM`) — NO bundling. `periodPairingA_eq_boundary_period` existential: LHS=fixed `periodPairingA f g`, cheat-witness coeff=0 forces `periodPairingA f g=0` (FALSE where used: when `periodMap' f=0`, RHS→0 IS Shimura 8.2.22) — non-vacuous, honestly carries the Stokes content. `periodMap'_injective` chain faithful (via `petN_definite`+`periodPairingA_twist_self` proven). Remaining sorries = genuine-hard-TRUE: `exists_pairedBoundary_periodPairingA_eq`, `tile_stokes_fd` (Green's), det-p CoV (PeriodHecke, closing).
  - **★★ NET: the whole substrate (k≥2) reduces to TWO leaves — ES-3c (equivariance, founded, running) + `periodPairingA_eq_boundary_period` (the Eichler–Shimura period↔Petersson Green's identity, the sole research-scale residual).** Everything else axiom-clean.
  - **GREEN'S IDENTITY DECOMPOSITION (scoped 2026-06-23): research-scale but FOUNDED, ~800-1200 LOC, every dep present.** Route = **Poincaré/closed-1-form** (CONFIRMED mathlib API `MeasureTheory.curveIntegral_add_curveIntegral_eq_of_hasFDerivWithinAt` + "closed 1-form on convex set is exact", `CurveIntegral/Poincare.lean`; mathlib-native, mirrors the project's own `cuspToInftyIntegral`). 6 leaves: **L1** 2-form/Petersson-as-exact `petN f g = ∫_FD d(Fp·…)` (FOUNDED — `isExactOn_upperHalf`+`petN`); **L2** `Γ₁(N)` FD with Γ-PAIRED geodesic boundary edges (API-GAP combinatorial — project has only `Gamma1_fundDomain_PSL` coset-tiling + SL₂ℤ `FDBoundary.lean`; **THE LONG POLE / biggest stall risk, ~200-400 LOC**); **L3/L4** Stokes-via-divergence OR Poincaré-closed-form on the non-rectangular FD (RESEARCH, pick Poincaré, ~300-600); **L5** FTC + Γ-edge telescoping → periods (FOUNDED — `hasDerivAt_periodForm_primitive_*`+`periodForm_mob_general`+`slash_Gamma1_eq`); **L6** assembly. Build order: L1∥L2 → L4(Poincaré) → L5 → L6. **L2 dispatched (parallel, independent new file).**
  - **L2 ✅ COMBINATORIAL HEART DONE** `FundamentalDomainBoundary.lean` (401 lines, axiom-clean except 1 isolated identification sorry): `OrientedCuspEdge`/`PairedBoundary` structure, `edgePotentialSum_cycle_eq_zero` (abstract telescoping), `rawPairing_edgeDivisor_eq_sub`, **`two_smul_rawPairing_boundaryDivisor`** (collapses to EXACTLY `∑ coeffᵢ·rawPairing f (yᵢ)`). Reuses the project's `divDiff`/`divDiff_add_divDiff`/`div0Rep_divDiff_gamma1` (ModuleMFinite). **Residual isolated = `exists_pairedBoundary_periodPairingA_eq`** (connect abstract paired-boundary to the concrete `petN` integral). **★ HARD KERNEL precisely diagnosed:** (i) region-Stokes over the non-rectangular FD (Poincaré route) + (ii) a GEOMETRIC MODEL-CHANGE — L2's edges are the Manin ideal-triangle tessellation (rational-cusp vertices) while `Gamma1_fundDomain_PSL` is the Siegel `fd`-coset tiling (cusp ∞, arc boundary); bridging the two boundary-integral models is the deep step. Genuinely research-scale, no mathlib foothold (consistent w/ scoping). **L1+Stokes dispatched.**
  - **L1 ✅ + rectangle-Stokes ✅ DONE** `PeterssonStokes.lean` (432 lines, axiom-clean except 1 isolated sorry): **L1** `holAntihol_eq_divergence` (Shimura's exact-2-form `d(H·conj(a)dz̄)=-2i·h·conj(a)dx∧dy` as a planar divergence) + `coordCLM`/`exists_primitive_periodForm`; **rectangle-Stokes** `rectangle_stokes_periodForm` (`integral2_divergence_prod` end-to-end on a box, full a.e. plumbing). **Wall precisely isolated = `tile_stokes_fd`** (region-Stokes over the `fd` tile), NARROWED: `fd` splits at y=1 into a cap-rectangle (DONE via `rectangle_stokes_periodForm`) + a lower **ARC-STRIP** `{−½≤x≤½, √(1−x²)≤y≤1}`. **Definitive evidence:** mathlib divergence thm is BOX-ONLY; Poincaré (`CurveIntegral`) is for CLOSED forms but the Petersson form is EXACT (`dω≠0`) ⟹ does NOT apply; `regionBetween` is volume-only. **★ UNTRIED FOUNDED ROUTE:** the arc-strip is a Type-I region between graphs `φ(x)=√(1−x²)`, `ψ(x)=1` ⟹ Green's there = FTC-in-y + Fubini + flattening-CoV (NO missing mathlib thm — just bookkeeping). Also flagged: the Sym-weight model-bridge `periodForm g P·conj(a) ↔ conj(f)·g·y^{k−2}/(−2i)` (binomial of `(z−z̄)^{k−2}`). **Type-I-region attack dispatched on `tile_stokes_fd`.**
  - **★ DECISIVE VERDICT (2026-06-23): the Green's identity is FOUNDED, NOT a mathlib gap.** The "no foothold" claim was true ONLY for box-divergence + closed-form-Poincaré; the **Type-I region route (FTC-in-y + Fubini) needs NO missing mathlib theorem.** PROVEN axiom-clean in `PeterssonStokes.lean`: `setIntegral_complex_eq_regionBetween` (ℂ↔ℝ² area bridge via `volume_preserving_equiv_real_prod`), `fdSet_image_eq_regionBetween` (the fd-set IS `regionBetween √(1−x²) H`), `regionBetween_integral_eq_iterated` (Fubini), `integral_regionBetween_partialY` (FTC-in-y = the `∂_yG` half of region-Stokes). The parts that LOOKED foundational are DONE. `tile_stokes_fd` residual = contour-over-`fdBoundaryFun` = area-over-Type-I-region; remaining: `∂_xF` half (transposed Type-II via `Prod.swap`), 4-piece→5-segment boundary assembly (arc reparam via `integral_comp_mul_deriv`, present), orientation sign, Manin↔Siegel bridge (downstream). **All mechanical analysis on the proven backbone — laborious, not research-scale.**
  - **tile_stokes_fd pass 2 (2026-06-23): backbone PROVEN axiom-clean + 2 STATEMENT DEFECTS FIXED (drift-vigilance).** Built sorry-free: `integral_regionBetweenX_partialX` (the `∂ₓF` Type-II half via `integral_prod_swap`), `hasDerivAt_conj_a_vertical/_horizontal`, `hasDerivAt_Phi_vertical/_horizontal`, `divergence_Phi_cancel` (L1 cross-term cancellation in explicit derivs). **DEFECT 1 FIXED:** `a` was a FREE function (integrals ill-defined, L1 inapplicable) → added `had : ∀z, 0<z.im → HasDerivAt a (a'der z) z` (a TRUE necessary well-definedness hyp the real consumer satisfies; NOT bundling — the hard part stays the sorry). **DEFECT 2 FIXED:** `fdBoundaryFun` is CLOCKWISE but mathlib divergence is CCW ⟹ `∮=−(area)`; negated the contour conjunct, verified concretely (`∮ z dz̄=−2i`). tile_stokes_fd NOT consumed by any .lean yet (only docs) ⟹ statement-correction safe. **Residual = curvilinear JOINT-divergence over the Type-I region** (3-region decomp `R_upper⊔R_LL⊔R_LR` + measure-zero gluing + arc reparam angle↔x + 5-segment matching); the split route needs `a` C¹ (true for holomorphic `a`; mathlib's joint divergence is rectangle-only). ~several hundred LOC, founded. **⚠ WIRING WATCH-ITEM: the consumer (`exists_pairedBoundary_periodPairingA_eq`) MUST provide honest `had` (the conjugate-form factor IS holomorphic/C¹) + the corrected −sign — do NOT let a future pass fudge `had` or the sign to compile (that would be drift).** Continuation dispatched.
  - **tile_stokes_fd pass 3 (2026-06-23): STALLED on a tactical rabbit-hole (watchdog), REPAIRED by hand, net progress.** Stalled (600s) fiddling the `Ioo`/`Ioc` a.e.-equality in its new per-segment lemma → file broken. FIXED by hand: `ae_restrict_congr_set Ioo_ae_eq_Ioc` (transfer a.e. across null `{b}`) + `(ae_restrict_iff' measurableSet_uIoc).mp` to convert `=ᵐ[restrict (Ι a b)]`→ the `∀ᵐ x, x∈Ι a b →` form `intervalIntegral.integral_congr_ae` wants. **GREEN again (3401 jobs); the new `contourSegment_integrable_and_eq` (per-segment contour = `∫ W(γt)·conj(d t)` when `deriv γ = d` on the open segment) is now PROVEN axiom-clean** — the boundary-assembly engine. Stall was TACTICAL not a math wall. Re-dispatched on the now-further base.
  - **tile_stokes_fd pass 4 (2026-06-23): `tile_stokes_fd` PROVEN modulo ONE isolated residual `region_stokes_eq_neg_contour`.** Built axiom-clean: `contour_eq_five_segments` + `segDeriv1..5` + `deriv_fdBoundaryFun_segᵢ` (the 5-segment contour split, DONE — uses `contourSegment_integrable_and_eq` + `integral_add_adjacent_intervals` at 1/5..4/5; arcs via circle-exp derivs), `fdArc x=√(1−x²)`+positivity, `gXField`/`gYField`/`PhiField` + `gXField_add_gYField` (=`divergence_Phi_cancel`), `region_yhalf` (the Type-I `∂_yG` reduction `∫∫_R g_y = ∫[−Φ(x+iH)+Φ(x+i·fdArc x)]`), `continuousOn_gField_aux`. **Residual `region_stokes_eq_neg_contour` (1 sorry, GREEN): `∫∫_R (g_x+g_y) = −Σ(5 segments)`** — contour side ALREADY fully expanded to the 5 segments; remaining = (i) integrability of g_x,g_y on the compact closure (needs `a'der` continuous = `a` C¹, honest strengthening), (ii) Type-II `∂_xF` arc-strip x-fibre split (region not x-simple below y=√3/2; reuse `integral_regionBetweenX_partialX`), (iii) edge↔segment matching via arc reparam (`integral_comp_mul_deriv`). All founded, no missing mathlib thm. Pass 5 dispatched.
  - **tile_stokes_fd pass 5 (2026-06-23): `region_stokes_eq_neg_contour` PROVEN; residual → ONE sub-lemma `region_xhalf_arc_segments`.** `had` STRENGTHENED to C¹ honestly (`hadc : ContinuousOn a'der {0<im}`, threaded to `tile_stokes_fd`; holomorphic consumer satisfies it; NOT faked). Built axiom-clean: `continuousOn_gXField_gYField`, `regionBetween_subset_box` (R ⊆ `[-½,½]×[√3/2,H]`), `integrable_indicator_gField` (both field indicators `Integrable` via `ContinuousOn.integrableOn_compact` on the box ⊂ ℍ). The g_x/g_y split + g_y reduction (`region_yhalf`) PROVEN inside the lemma; straight-edge reparam (Seg5) verified tractable. **Residual `region_xhalf_arc_segments` (1 sorry, GREEN, ~300-500 LOC, founded): `∫∫_R g_x + (region_yhalf RHS) = −(5-seg sum)`** = TWO sub-pieces: (A) the `∂_xF` Type-II `x=0` region split (two `regionBetweenX` set-IDs with the kinked `√((1−y²)⁺)` boundary), (B) the arc trig reparam `x=cosθ,y=sinθ` matching `∫_y[F(−φ_R,y)−F(φ_R,y)]+∫_x Φ(x,fdArc x) = −(Seg2+Seg3)`. Math worked out; ~300-500 LOC intricate formalization. **NO external consumers of tile_stokes_fd ⟹ the `hadc` addition is safe.** Pass 6 (2-way decomp) dispatched. **HONEST: this is the genuine laborious core — founded + converging across passes, but a large mechanical lift (the σ-cap closed in 4 passes; this may take a few more).**
  - **tile_stokes_fd pass 6 (2026-06-23): CRASHED (API 529) mid-edit, REPAIRED by hand, NET PROGRESS — `tile_stokes_fd` now PROVEN modulo 2 small residuals.** Crash left the file broken (orphaned docstring missing `/--`; duplicate `rw` line; missing `include k Γ in` on `region_typeII_piece`). FIXED by hand (3 mechanical fixes). PROVEN & preserved from pass 6: **`region_xhalf_typeII` (the FULL (A) `∂_xF` Type-II reduction — splits R at x=0 into two `regionBetweenX` pieces, applies `integral_regionBetweenX_partialX` per piece)**, `region_typeII_piece` (per-piece `∂_xF` via the proven half-lemma), the integrability lemmas (`continuousOn_gXField_gYField`, `regionBetween_subset_box`, `integrable_indicator_gField`, `integrable_PhiField_top/_arc`), `seg1_eq`+seg-match lemmas, AND **`region_xhalf_arc_segments` + `tile_stokes_fd` are WIRED/PROVEN modulo the 2 residuals** (not sorry-carriers). **Only 2 residuals left (GREEN): `setIntegral_fdRegion_split_at_zero` (1261, the x=0 area-split via null `{0}×univ` slice — `setIntegral_union`, FOUNDED tractable) + `arc_reparam_segments` (1420, the (B) arc trig reparam `x=cosθ,y=sinθ` → Seg2+Seg3, intricate-but-FOUNDED).** Pass 7 dispatched to close both.
  - **★★★ tile_stokes_fd CLOSED — pass 7 (2026-06-24): CRASHED (ConnectionRefused) but had closed BOTH residuals; only a `linear_combination` SIGN SLIP remained, FIXED by hand (`(Real.sin θ)→(-(Real.sin θ))*Complex.I_sq` in the `I·e^{−iθ}=I·cosθ+sinθ` identity). `setIntegral_fdRegion_split_at_zero` + `arc_reparam_segments` (the (B) `x=cosθ,y=sinθ` arc reparam) both PROVEN. `PeterssonStokes.lean` is now SORRY-FREE (3401 jobs); `#print axioms tile_stokes_fd = {propext, Classical.choice, Quot.sound}` — VERIFIED axiom-clean.** The curvilinear region-Stokes — the "research-scale long pole, no mathlib foothold, ~1-1.5 person-months" — is DONE (7 passes, several server-crash recoveries, all work preserved).
  - **★ NOW: only 2 sorries left for the whole k≥2 discharge** — `exists_pairedBoundary_periodPairingA_eq` (FundamentalDomainBoundary:399, the model bridge: `petN f g + (-1)ⁿ petN g f = c·rawPairing f (B.boundaryDivisor⊗P)`) + `periodPairingA_eq_boundary_period` (PeriodInjective:121, the existential, follows from the bridge + `two_smul_rawPairing_boundaryDivisor`). The bridge's region-Stokes step is now PROVIDED by `tile_stokes_fd`; remaining = petN-FD-tiling sum (`setIntegral_SL_tile_fd_eq_fdo`) + interior-edge telescoping + the Manin↔Siegel model-change + the Sym-weight binomial `periodForm g P·conj a ↔ conj f·g·y^{k-2}`. Founded (tile-Stokes done) but the model-change is the last conceptually-substantial piece. **Model-bridge pass dispatched.**
  - **Model-bridge pass 1 (2026-06-24): target (2) `periodPairingA_eq_boundary_period` CLOSED (faithful 1-term witness); target (1) relocated + isolated.** `exists_pairedBoundary_periodPairingA_eq` MOVED to `PeterssonStokes.lean:2230` (where `tile_stokes_fd` is importable); `PeriodInjective` now imports `PeterssonStokes`; **`FundamentalDomainBoundary.lean` is now FULLY SORRY-FREE**; `PeriodHecke.lean` also 0-sorry. `#print axioms periodMap'_injective`/`periodPairingA_eq_boundary_period` = sorryAx tracing SOLELY to `exists_pairedBoundary_periodPairingA_eq`. Full lib green (8784 jobs). **★ The agent WRONGLY concluded the bridge is "impossible / tile_stokes_fd can't apply" (Petersson 2-form `f·conj g·y^{k-2}` is mixed). CORRECTION: the BINOMIAL `y^{k-2}=((z−z̄)/2i)^{k-2}`, `(z−z̄)^{k-2}=Σⱼ C(k-2,j) zʲ(−z̄)^{k-2−j}` splits it into a FINITE SUM of terms `[f·zʲ]·conj[g·z^{k-2−j}] = periodForm f Pⱼ · conj(periodForm g P_{k-2-j})` — EACH holo×antiholo = tile_stokes_fd's shape (Pⱼ=XʲY^{k-2-j} monomial basis of Sym^{k-2}; `a=g·monomial` holo/C¹).** So `tile_stokes_fd` DOES apply per binomial term; the "Sym-weight bridge" is FOUNDED (algebra + finite sum of the proven lemma), NOT a research gap. Remaining model-bridge = binomial decomp + per-term per-tile `tile_stokes_fd` + coset telescoping (`petN_eq_nsmul_sum_PSL_tile`+`setIntegral_Gamma1_smul_eq`) + Manin↔Siegel edge-matching (`two_smul_rawPairing_boundaryDivisor`). **Pass 2 (binomial strategy) dispatched.**
  - **Model-bridge pass 2 (2026-06-24): BINOMIAL VINDICATED — steps 1-2 PROVEN axiom-clean, residual → final boundary assembly (steps 3-5).** PROVEN: `symMon`/`evalSym1_symMon`/`periodForm_symMon_apply` (monomial basis), `petersson_binomial_complex` (`Σⱼ C(n,j)(−1)^{n−j}(gz·zʲ)conj(fz·z^{n−j}) = (2i)ⁿ conj(fz)·gz·(Im z)ⁿ`), **`petersson_binomial_periodForm` (Lemma A: Petersson integrand = finite Σ of `tile_stokes_fd`-shaped holo×antiholo terms)**, `periodForm_hasDerivAt_and_continuousOn_deriv` (had/hadc from holomorphy), **`exists_tile_boundary_periodForm_term` (Lemma B: `tile_stokes_fd` PLUGS IN per binomial term)**. The "impossible/mixed" mis-classification REFUTED. Residual `exists_pairedBoundary_periodPairingA_eq` = steps 3-5: **(3) translated-tile glue** — `petN f g = (const)Σ_{cosets γᵢ} ∫_{fdo} (f∣γᵢ)·conj(g∣γᵢ)·y^{k-2}` (`petN_eq_nsmul_sum_PSL_tile`+slash; `f∣γᵢ` cusp forms via coset action), tile_stokes_fd per (term,coset); **(4) H→∞ cap** (cusp decay `periodForm_norm_le`); **(5) coset telescoping** (interior edges cancel, `setIntegral_Gamma1_smul_eq`) + **Manin↔Siegel model-change** (boundary contours → `rawPairing f (boundaryDivisor⊗P)` via FTC edge potentials + `two_smul_rawPairing_boundaryDivisor`). Founded (ingredients all present) but ~hundreds LOC. `#print axioms periodMap'_injective` still has sorryAx (solely this). Pass 3 (final assembly) dispatched.
  - **Model-bridge pass 3 (2026-06-24): `exists_pairedBoundary_periodPairingA_eq` now PROVEN sorry-free** (petN→FD collapse via `petN_eq_setIntegral_Gamma1_fundDomain_PSL` + fibre count `slToPslQuot_fiberCard`). Residual RE-ISOLATED to `exists_pairedBoundary_fundDomain_petersson_eq` (PeterssonStokes:2413): the symmetrised Petersson AREA integral over the single `Gamma1_fundDomain_PSL` = the paired-edge boundary period `c·rawPairing f (B.boundaryDivisor⊗P)`. FAITHFUL (non-vacuous: `g=iⁿf` twisted-diag = `2iⁿ(f,f)/c_N ≠ 0`). `#print axioms periodMap'_injective` sorryAx traces SOLELY to this. **★ STALL POINT (honest): THREE independent agents have now converged on the SAME irreducible wall = the Manin↔Siegel MODEL CHANGE** — matching the Siegel-coset-tiling FD boundary contour to the Manin cusp-triangle `rawPairing f (boundaryDivisor⊗P)`. The project's `Gamma1_fundDomain_PSL` is a measure-theoretic coset tiling with NO explicit paired-edge boundary; closing the residual needs **building the explicit Γ₁N side-paired fundamental-domain boundary** (which edges are interior/cancel vs outer/survive + the Γ₁N side-pairing) — genuine Eichler–Shimura "boundary of the modular curve" infrastructure, ABSENT from mathlib (no modular curves / modular symbols / ES map). This is research-scale (a major sub-development), NOT laborious bookkeeping like tile_stokes_fd was. **Everything around it is PROVEN: binomial bridge (A,B), tile_stokes_fd, petN→FD collapse, the period-injectivity chain. The whole k≥2 substrate rests on this ONE lemma.** Decision point surfaced to user (build the paired-edge-FD sub-development vs bank the near-complete state). **USER CHOSE: wire now, then keep attempting.**
  - **★★★ WIRING DONE (2026-06-24) — substrate INSTALLED as the live `heckeAlgℤ_finite`.** Rerouted: `instance heckeAlgℤ_finite` moved DOWNSTREAM to NEW `Labels/HeckeAlgFiniteFinal.lean`, proved by `rcases le_or_gt 2 k`: **k≥2 → `heckeAlgℤ_finite_of_period (hComm_genM) (periodMap' hk) (periodMap'_injective hk) (periodMap'_heckeEnd hk) (periodMap'_diamond hk)`** (all 5 hyps matched VERBATIM, no bridging — interface design vindicated); **k<2 → `heckeAlgℤ_finite_of_lattice`** (the demoted-to-theorem old proof from `exists_HeckeStableLattice`). `newformEigenHom_range_finite` (T004b) moved downstream; `NewformOrbit` imports `HeckeAlgFiniteFinal`. Fixed a latent `genM` DUP (HeckeCommute had its own copy ≠ HeckeFinite's — made HeckeCommute import HeckeFinite, deleted the dup). **T004/T005 statements UNCHANGED; full lib green (8801 jobs); NO new sorry, NO statement weakened (verified).** `#print axioms heckeAlgℤ_finite`/`coeffSeq_isIntegral`/`instNumberFieldCoeffField` → sorryAx traces to {model-change `exists_pairedBoundary_fundDomain_petersson_eq` (k≥2) + `exists_HeckeStableLattice` (k<2)} — the project's k≥2 Hecke-finiteness now rests on the PRECISE ES model-change, NOT the opaque lattice. **"Built ≠ wired" gap CLOSED.** Now: continue attempting the model change (k≥2's last sorry) + the k=1 lattice is the Deligne–Serre residual.
  - **Model-change pass 4 (2026-06-24, Manin-FD angle): the model-change HALF PROVEN; residual shrunk to the Manin-FD construction core.** The `IsFundamentalDomain.setIntegral_eq` angle SIDESTEPPED the prior 3 agents' Siegel-deformation wall (the period 1-form isn't closed ⟹ deformation wasn't free): proved axiom-clean `petersson_imageGamma1_PSL_invariant` + **`symmetrised_petersson_fundDomain_eq`** (the symmetrised Petersson area integral is EQUAL over ANY fundamental domain — the model change, NO deformation). `exists_pairedBoundary_fundDomain_petersson_eq` now PROVEN from these. **Residual re-isolated to `exists_manin_fundDomain_boundary_period` (PeterssonStokes:2465)** = the genuine irreducible core: (1) CONSTRUCT a Manin ideal-triangle (cusp-vertex) fundamental domain `D` for Γ₁N + `IsFundamentalDomain` + cusp-edge `PairedBoundary` for `∂D`; (2) region-Stokes over the ideal triangle (cusp cap via decay). Entangled (Stokes needs concrete D) ⟹ one faithful sorry. **This is genuine MODULAR-CURVE FD infrastructure (the geometric foundation of modular symbols), absent from mathlib — qualitatively a multi-week sub-development, NOT the analytic grinding that's been closing things.** `hι` k≥2 discharged modulo this one core; faithful (non-vacuous; D bound to be a real `IsFundamentalDomain`). Build green. **DECISION POINT: commit to the Manin-FD infra build vs bank the installed state.** → **USER CHOSE: commit to the Manin-FD build.**
  - **Manin-FD DECOMPOSED (2026-06-24, `decomposition-maninFD.md` + green skeleton `ManinFD.lean`). KEY: the build SHRINKS — the new-ideal-triangle-FD is NOT needed; use `D = Gamma1_fundDomain_PSL` (already proven `IsFundamentalDomain`), so the target ≡ the existing `exists_pairedBoundary_fundDomain_petersson_eq`.** mathlib has NO `IsFundamentalDomain` for the modular action + NO same-group FD-union, but the project has `isFundamentalDomain_Gamma1_PSL` + `IsFundamentalDomain.iUnion_smul_of_transversal` (tiling engine) + `tile_stokes_fd` (per-tile Stokes, applies verbatim to fd-translates) + `cuspValue`/`rawPairing_edgeDivisor_eq_sub` (geometry→period bridge) + `cuspBoundary_mob_naturality` (deep cusp-period naturality, PROVEN). **ONE TRUE GAP = M0** (`maninArea_eq_boundary_period`, ~600-1200 LOC, no foothold): glue per-tile capped contours + **interior-edge pairwise cancellation (side-pairing telescoping)** + `H→∞` cap + Manin↔Siegel FTC-potential=`cuspValue` matching (Shimura 8.2.22). Founded leaves: **M4** (PairedBoundary `B` from Γ₁N side-pairing, ~200-400) + **M5** (assembly plumbing). Verdict: multi-week but ONE wall (M0), not several; no geodesic-API/FD-union blocker (the "triangle=2×𝒟" shortcut would only help a route we don't take). Build order: **M4 first** (de-risk the `B` interface) → M0 (the push) → M5. **M4 + M0-interface dispatched.**
  - **★ M4 DONE + M0 sharpened (2026-06-24): the WHOLE k≥2 substrate's ONLY sorry is now `maninArea_eq_boundary_period` (M0).** `maninPairedBoundary N : PairedBoundary N` PROVEN sorry-free + axiom-clean + NON-VACUOUS (2-edge cycle: `edge false = {0,∞}`, `edge true = g₀·reverse`; `pair = swap`; side-pairing `g₀ = [[1,0],[N,1]] ∈ Γ₁N`, lower-left N≠0 ⟹ NOT the vacuous `γ=1`; `boundaryDivisor = (∞)−(1/N) ≠ 0`). `exists_manin_fundDomain_boundary_period` now PROVEN (D=`Gamma1_fundDomain_PSL`+`isFundamentalDomain_Gamma1_PSL`+`maninPairedBoundary`+M0). Structural: `PairedBoundary.ι : Type*`→`Type` (monomorphic, fixes `.{0}` unification). Full lib green (8801 jobs); M0 (PeterssonStokes:2504) is the LONE substrate sorry; `periodMap'_injective` sorryAx via it. **M0 decomposes into 5 substeps — 4 FOUNDED: (1) per-tile binomial (`petersson_binomial_periodForm`+`setIntegral_Gamma1_fundDomain_PSL_eq_sum`); (2) translated-tile Stokes (Möbius CoV `periodForm_mob_general`+`tile_stokes_fd`); (3) H→∞ cap (`tendsto_horizontal_cap`+`periodForm_norm_le`); (5) FTC=cuspValue (`hasDerivAt_periodForm_primitive_vertical`+`cuspValue_eq_top_sub_botVal`+`two_smul_rawPairing_boundaryDivisor`) — + ONE WALL: (4) INTERIOR-EDGE CANCELLATION (side-pairing telescoping / the modular-symbols boundary map; no mathlib analogue).** M0 sub-build dispatched (build 1,2,3,5; isolate 4).
  - **★ M0 reduced to JUST the wall (2026-06-24): `maninArea_eq_boundary_period` PROVEN modulo `maninFD_interior_edges_cancel`.** Step 1 `symmArea_eq_symm_stdTile_sum` (the symmetrised area = coset-tiling sum `Σ_{q : PSL⧸imageGamma1_PSL} ∫_{(q.out)⁻¹•fdo} petersson`) PROVEN axiom-clean (`setIntegral_Gamma1_fundDomain_PSL_eq_sum`+`integrableOn_petersson_*`). Steps 2/3/5 = already-proven reusable infra. **The WHOLE k≥2 substrate's LONE sorry is now `maninFD_interior_edges_cancel` (PeterssonStokes:2576)**: `Σ_q ∫_{tile} pet(f,g) + (-1)ⁿ•Σ_q ∫_{tile} pet(g,f) = c·rawPairing f (maninPairedBoundary.boundaryDivisor⊗P)`. Faithful (LHS provably = nonzero symm area via step 1; RHS = genuine nonzero `(1−g₀)·∂e₀`). The wall decomposes again: (a) per-tile CoV-to-`fdo`+binomial+`tile_stokes_fd`, (b) H→∞ cap, (d) FTC=`cuspValue`+`two_smul_rawPairing_boundaryDivisor` = FOUNDED; **(c) orientation-reversal INTERIOR-EDGE CANCELLATION across adjacent tiles = the modular-symbols boundary map, the genuine no-foothold core (abstract telescoping `edgePotentialSum_cycle_eq_zero` is in hand; the concrete tiling-adjacency↔abstract matching is the work).** Wall sub-build dispatched (a/b/d; isolate c).
  - **★ BOTTOMED OUT at Shimura 8.2.22 (2026-06-24): `maninFD_interior_edges_cancel` PROVEN modulo (c) `interior_edges_cancel_sum` (PeterssonStokes:2705, the LONE k≥2 substrate sorry).** Founded + axiom-clean: (a) `tile_area_eq_slashed_stdfdo` (per-tile CoV: `∫_{p⁻¹•fdo} pet f g = ∫_fdo pet (f∣σ⁻¹)(g∣σ⁻¹)` via `setIntegral_smul_eq`+`petersson_slash_SL`), (d) `two_smul_rawPairing_maninBoundary_eq_edgePotential_sum` (FTC/telescoping assembly), `slashed_stdfdo_sum_eq_symmArea` (faithfulness witness: LHS = the genuine nonzero symm area). **The residual (c) IS Shimura 8.2.22 restated:** `∫_{D_N} pet(f,g) + (-1)ⁿ•∫_{D_N} pet(g,f) = c·rawPairing f (∂F⊗P)` over `D_N=Gamma1_fundDomain_PSL` — the area→Manin-boundary-period identity = the modular-symbols boundary map. Faithful (lift hyp `hσ` load-bearing; nonzero LHS; nonzero Manin symbol `(1−g₀)∂e₀`). **THE IRREDUCIBLE CORE — but it has SOME foothold: classically it's the fd SIDE-PAIRING (arc folded by `S`, verticals paired by `T`, lifted to the Γ₁N coset tiling); project has `ForMathlib/FDBoundary.lean` (SL₂ℤ fd 5-segment boundary) + mathlib `ModularGroup` `S`/`T`/`𝒟`.** Full lib green (8801 jobs); `periodMap'_injective` sorryAx solely via (c). **(c) fd-side-pairing attempt dispatched** (the genuine core; reveals founded-laborious vs true research bottom).
  - **★★★ VERIFIED IRREDUCIBLE BOTTOM (2026-06-24, 10-probe investigation): `interior_edges_cancel_sum` IS Shimura 8.2.22, single-pass grinding EXHAUSTED.** Precise finding: the S/T fd-side-pairing does NOT cancel to zero — it RELOCATES content into nonzero tail-period boundary terms (provably via `cuspValue_symRep_gamma`: `cuspValue f (symRep γ P)(γc) = cuspValue f P c + T`, `T = F∞−G∞` = the Manin symbol value ≠ 0). So the residual = the area→Manin-boundary Stokes identity (`∫_{D_N} pet(f,g) + (-1)ⁿ•∫ pet(g,f) = c·rawPairing f (∂F⊗P)`), "100% of the content", NO further algebraic peeling. Cheats RULED OUT (LSP-verified): `c=0`/`P=0` needs `area=0` (false); division-witness `c:=area/rawPairing` circular via `periodMap'_injective`. Matches `decomposition-maninFD.md` M0 = "research-scale long pole, no mathlib foothold, unavoidable on both routes". **The remaining work = a MULTI-WEEK modular-symbols-geometry build: (step 1, founded ~hundreds LOC) `μ_hyp→planar` conversion over `fdo` (`μ_hyp=(comap coe vol).withDensity(im⁻²)`) + binomial assembly + per-tile Stokes + H→∞ cap → per-tile boundary contour; then (the core) the side-pairing telescoping = the area↔boundary Stokes / modular-symbols boundary map.** ALL scaffolding around it is PROVEN+axiom-clean+INSTALLED. **DECISION: commit to the multi-week area↔boundary-Stokes build vs bank the verified near-complete state.** → **USER CHOSE: commit to the FULL multi-week build.**
  - **AREA↔BOUNDARY-STOKES BUILD — committed (2026-06-24), leaf-by-leaf like the original substrate.** Two pieces: **(step-1, FOUNDED ~hundreds LOC) `tile_area_eq_boundary_contour`**: per-tile `∫_fdo pet(F,G) ∂μ_hyp` = its `fdBoundaryFun` boundary contour, via (i) `μ_hyp→planar` conversion (`μ_hyp = (comap UpperHalfPlane.coe vol).withDensity (im⁻²)` ⟹ integrand `F·conj G·im^{k-2}` against planar Lebesgue — the NEW first concrete lemma), (ii) binomial Lemma A `petersson_binomial_periodForm`, (iii) per-term `tile_stokes_fd`/`exists_tile_boundary_periodForm_term`, (iv) H→∞ cap `tendsto_horizontal_cap`. Closeable. After it, `interior_edges_cancel_sum` reduces to **(the CORE, no foothold) `Σ_q (per-tile boundary contours) = rawPairing f (boundaryDivisor⊗P)`** = the side-pairing telescoping / modular-symbols boundary map. **Step-1 dispatched first** (founded; shrinks the lone sorry to the pure telescoping core).
  - **★★★ `/develop --decompose` (user-requested, 2026-06-24, `decomposition-stokes.md`): SOURCE-FAITHFUL re-read of Shimura §8.2 OVERTURNS the route. The 4 prior agents INVENTED the coset-tiling+interior-edge-cancellation+Manin↔Siegel-model-change route — Shimura does NONE of that.** Shimura's ACTUAL (8.2.22) (book pp.222–236): a GROUP-1-COCYCLE pairing — ONE Stokes on a SINGLE FD `F`, then the standard side-pairing `∂F=Σ(S_λ−a_λ S_λ)`, collapsed by the cocycle identity (8.1.3) `u(a⁻¹)=−a⁻¹u(a)`. The research-wall was self-inflicted by leaving the source. **VERIFIED available:** mathlib v4.31 `groupCohomology.cocycles₁`+`cocycles₁_map_inv`(=8.1.3)+`mem_cocycles₁_iff`(=8.1.1) (`LowDegree.lean:276-311`); the project ALREADY models `Div⁰⊗Sym` as `Representation ℤ SL₂ℤ` (`fullModSymRep`/`modSymRep`) + the f-cocycle covariance is PROVEN (`cuspValue_symRep_gamma`/`cuspDiff_const`); the consumer's existential `periodPairingA_eq_boundary_period` is LOOSE (`yᵢ ∈ Div⁰⊗Sym`, NOT forced to `maninPairedBoundary`) and its sole consumer `periodPairingA_eq_zero_of_periodMap'_zero` only needs the loose form (all terms die under `periodMap' f=0`). **⟹ the concrete `maninPairedBoundary`/model-change is UNNEEDED (droppable); re-target the loose existential onto cocycle-defect divisors `yᵢ=divDiff(a•c)c⊗Q`.** Leaf verdict: L0/L1/L2 (area=combo, exact-2-form, single-Stokes+cap) FOUNDED; **L4 cocycle re-grouping FOUNDED (mathlib cocycles₁ + proven covariance)**; L5 injectivity FOUNDED; **L3 (`∂F=Σ(S_λ−a_λS_λ)` FD side-pairing) = the one geometric step, no turnkey mathlib foothold** — agent claims OFF-critical-path via the loose existential (★ MY SCRUTINY: this is the one claim to verify by building — does proving `A=Σ rawPairing f yᵢ` genuinely avoid the FD side-pairing, or does the Stokes step still need ∂F decomposed?). **NET: route changed from research-wall → cocycle route that's FOUNDED-or-nearly; build founded parts (L4 bridge + re-target) first, verify L3's necessity empirically.**
  - **COCYCLE-ROUTE BUILD started (2026-06-24, user approved). Plan: (1) `Cocycle822.lean` — bridge `modSymRep`→`Rep ℤ SL₂ℤ` (`Rep.of`), `u_f := cuspDiff f ∈ groupCohomology.cocycles₁` (cocycle id from `cuspValue_symRep_gamma`); (2) re-target `periodPairingA_eq_boundary_period` onto the LOOSE existential `Σ cᵢ rawPairing f(yᵢ)`, `yᵢ`=cocycle-defect divisors, bypassing `exists_pairedBoundary_periodPairingA_eq`/`maninPairedBoundary` (which become dead/droppable); (3) assemble A(f,g) = single-FD Stokes (L0/L1/L2 proven) + side-pairing (L3) + cocycle collapse (`edgePotentialSum_cycle_eq_zero`+`cocycles₁_map_inv`). KEEP GREEN; the existing `interior_edges_cancel_sum` route stays until the re-target lands. ★ EMPIRICAL L3 TEST: the build either closes A=Σ rawPairing OUTRIGHT (⟹ L3 founded ⟹ k≥2 DONE) or isolates L3 (the FD side-pairing `∂F=Σ(S_λ−a_λS_λ)`) precisely + reports if it's founded (via `FDBoundary` SL₂ℤ side-pairing) or the true residual.** Cocycle-route build dispatched.
- **[ES-asm] ✅ DONE (abstract/conditional)** `HeckeFinite.lean` — `heckeAlgℤ_finite_of_period` proves `Module.Finite ℤ (heckeAlgℤ N k)` FROM hypotheses `(hComm, ι, hι, hT, hD)`, axiom-clean (NO sorryAx), green (4023 jobs). Via **`FreeAlgebra ℤ Idx`** (NOT `MvPolynomial` — `End` is noncommutative): `evalS/evalM` (`FreeAlgebra.lift`), `range evalS = heckeAlgℤ`, `range evalM` finite (`End_ℤ 𝕄` finite by ES-1 + Noetherian), generator→poly equivariance by `FreeAlgebra.induction` ⟹ `ker evalM ≤ ker evalS` ⟹ `heckeAlgℤ` = ℤ-linear image of finite `range evalM` ⟹ finite. `dualPrecomp T = LinearMap.lcomp ℤ ℂ T` (the transpose, an anti-hom). **★ PINNED INTERFACE for ES-3b/3c/4 + the NEW leaf:** `ι : S_k →ₗ[ℂ] (𝕄→ₗ[ℤ]ℂ)`, `hι : Injective ι`, `hT : ι(heckeEnd n f)=dualPrecomp(heckeSymb n)(ι f)`, `hD : ι(diamondOpCusp d f)=dualPrecomp(diamondSymb d)(ι f)`, and **`hComm : Pairwise (Commute (genM i) (genM j))`** (symbol Hecke ops commute — discovered REQUIRED because `dualPrecomp` is an anti-hom).
- **[ES-2-commute] ✅ DONE** `HeckeCommute.lean` (1254 lines) — `hComm_genM`, `heckeSymb_commute`, `heckeSymb_diamondSymb_commute`, `diamondSymb_commute` ALL axiom-clean (NO sorryAx); full `LeanModularForms` builds (8784 jobs). The good-prime `T_pT_q=T_qT_p` (`heckeSymb_p_all_commute`, 3 cases) closed via 4-block decomp + shift identity `lowerMat q·upperMat p b = shiftSL(qb/p)·(upperMat p (qb%p)·lowerMat q)` (`shiftSL∈Γ₁N`, `Int.emod_add_mul_ediv`) + `c↦p·c%q` reindex bijection (`Finset.sum_nbij`) + diamond pull-out (`mk_tpOp_fullModSymRep_gamma0`); bad-bad via CRT. **★ `hComm` (endgame input 1/5) FULLY DISCHARGED.**
- **★ REMAINING for the discharge:** ES-3b (`ι`/`periodMap`, building) → ES-3c (`hT`/`hD` equivariance) + ES-4 (`hι` injectivity, deepest) + ES-2-commute (`hComm`) → instantiate `heckeAlgℤ_finite_of_period` → `heckeAlgℤ_finite` ⟹ discharge `exists_HeckeStableLattice` ⟹ **close Group A.**

#### hT UPDATE (2026-06-23): leaf 2 CLOSED, leaf 1 → det-p σ-cap
`PeriodHecke.lean`: **`heckeEnd_apply_eq_heckeEnd_aux` ✅ CLOSED axiom-clean** — built genuinely-UNRESTRICTED cusp-side commutativity from ModularForm-level primitives (`heckeT_p_all_comm_distinct`/`_diamondOp`, `Unified.heckeT_n_comm_diamondOp_all` — all cover bad/`U_p`); the agent correctly REJECTED the coprime-restricted `heckeT_n_cusp_comm` (drift-avoidance) + threaded `hk` (correctly noting the mirror scalar `(k-1).toNat` is false for k<2). Built axiom-clean: generic cusp-value machinery for ARBITRARY arithmetic cusp forms (`cuspValueGen`/`cuspToInftyIntegralGen`/`rawPairingGen`/`cuspValueGen_eq_top_sub_botValGen` + bridges to typed), `rawPairingGen_actMat` (the det-p per-coset CoV, modulo the deep input), `rawPairing_eq_sum_of_coe` (Heilbronn coset-sum). New global `instance isArithmetic_conj_glMap`. **`hT` reduced to ONE deep residual `cuspValueGen_symActC_heilbronn` (:1143)** = the det-p (GL₂⁺ℚ) analogue of the CLOSED det-1 σ-cap `cuspBoundary_mob_naturality`; needs the det-p OnePoint pole/upper geometry (GL₂ℚ analogues of `pole_of_smul_infty`/`OnePoint.smul_*_eq_ite`). The det-p integrand engine (`periodForm_mobM_general`, `tendsto_FM_pole/upper`) is purpose-built for it. `periodEquiv_heckeEnd_prime` (:1614) reduces to it + coset bookkeeping. **PeriodHecke now IDLE.**
- **★★ hT FULLY DISCHARGED (2026-06-23):** det-p σ-cap CLOSED — `cuspValueGen_symActC_heilbronn` + `periodEquiv_heckeEnd_prime` both axiom-clean (mirrored the det-1 σ-cap `cuspBoundary_mob_naturality_sl` + the built det-p engine; new det-p OnePoint geometry `pole_of_smul_infty_M`/`heilbronnGL_sl`/`tendsto_FM_upper_gen`/`cuspBoundary_mobM_naturality`; lower-coset matching via SAME `(Gamma0MapUnits_surjective).choose` as `diamondOp`). **`#print axioms periodMap'_heckeEnd` = {propext, Classical.choice, Quot.sound} — sorryAx GONE for k≥2.** PeriodHecke green (4029 jobs). **★ 4 of 5 endgame inputs now AXIOM-CLEAN for k≥2: hComm✅ ι✅ hD✅ hT✅. ONLY `hι` (injectivity) remains** — its sole dep is the Green's identity (`periodPairingA_eq_boundary_period` → `tile_stokes_fd` [running] → model bridge `exists_pairedBoundary_periodPairingA_eq`).

## ★ WIRING ARCHITECTURE (mapped 2026-06-23; do after all period inputs proven)
- **Consumer graph is CLEAN:** the only real consumer of the `heckeAlgℤ_finite` INSTANCE is `Labels/NewformOrbit.lean` (T004/T005 chain: `heckeAlgℤ_finite` → `newformEigenHom_range_finite` [T004b, in HeckeFieldArithmetic] → `coeffSeq_isIntegral` [T004] → `instNumberFieldCoeffField` [T005]). NO `ModularSymbols/*` imports `NewformOrbit` ⟹ inserting the substrate before NewformOrbit is acyclic. (`Skeleton.lean` refs are the dead planning skeleton — ignore.)
- **`exists_HeckeStableLattice` is a COROLLARY, not an input:** given `heckeAlgℤ_finite`, the lattice `Λ := Σᵢ heckeAlgℤ·vᵢ` (`vᵢ` a ℂ-basis of S_k) is f.g.(finite sum of f.g.) + free(f.g. torsion-free/PID) + spanning(contains basis, `1∈heckeAlgℤ`) + Hecke-stable(subalgebra). So the substrate proves `heckeAlgℤ_finite` DIRECTLY (no in-S_k lattice / no rationality needed — the expert's dual-lattice route), and `exists_HeckeStableLattice` becomes a downstream corollary or is dropped.
- **The move (import-cycle fix):** the sorry'd `heckeAlgℤ_finite` instance + `exists_HeckeStableLattice` are UPSTREAM (HeckeFieldArithmetic) but the real proof (`heckeAlgℤ_finite_of_period`) is DOWNSTREAM (after substrate, which imports HeckeFieldArithmetic). Fix: remove the sorry instance + `exists_HeckeStableLattice` from HeckeFieldArithmetic; create `ModularSymbols/HeckeFiniteFinal.lean` (downstream of all substrate files) proving the REAL `instance heckeAlgℤ_finite`; move `newformEigenHom_range_finite` (T004b, currently in HeckeFieldArithmetic, uses the instance) there too; have `NewformOrbit` import it. Bounded ripple (only T004b moves; T004/T005 stay in NewformOrbit + 1 import line).
- **WEIGHT CASES** (instance is stated `∀ k : ℤ`): **k≥2** = the substrate (`heckeAlgℤ_finite_of_period`). **k≤0** = `S_k=0` ⟹ `End ℂ S_k` subsingleton ⟹ `heckeAlgℤ` finite (need the `CuspForm ... k = 0` vanishing lemma for k≤0; `ModularForm.isZero_of_neg_weight`-style — find/prove). **★ k=1 = GENUINE GAP:** weight-1 Hecke-algebra finiteness is **Deligne–Serre** (Galois/Artin, NOT modular symbols — `cuspValue_symRep_gamma` is *false* at k=1, Sym^{k-2}=Sym^{-1} degenerate). No elementary/modular-symbol route exists. Options at wiring: (a) restrict downstream T004/T005 to `2≤k` (loses weight-1 newforms), or (b) isolate k=1 as a named deep input (Deligne–Serre). Decide + flag to user.

### [IHR-a] Integral modular-symbol module + Manin presentation  — blocked, research
`𝕄_k(Γ₁N,ℤ)` = `Γ₁N`-coinvariants of `Div⁰(ℙ¹ℚ) ⊗ Sym^{k-2}ℤ²`; finite free over ℤ via Manin symbols
(generators over `Γ₁N\SL₂ℤ × V_{k-2}`, 2-term `x+x|S` and 3-term `x+x|U+x|U²` relations + parabolic).
Combinatorial. *(mathlib: `groupCohomology`, `Representation`, symmetric powers; modular symbols
absent.)*

### [IHR-b] Integral Hecke (Heilbronn) action on `𝕄_k(Γ₁N,ℤ)`  — blocked, research
`T_n, U_p, ⟨d⟩` act by finite sums of integer (Heilbronn) matrices. Combinatorial. Depends: IHR-a.

### [IHR-c] Period pairing well-defined / Γ-invariant / Hecke-equivariant  — blocked, research (analytic)
`⟨f,{α,β}⊗P⟩ = ∫_α^β f(z)P(z,1)dz`: converges (cusp decay), descends to `𝕄_k(Γ₁N)`, Hecke-equivariant.
The genuine analytic input. Uses codebase cusp decay + mathlib contour integration. Depends: IHR-a.

### [IHR-d] Injectivity of the period map `ι : S_k ↪ 𝕄_k(Γ₁N,ℤ)^∨ ⊗ ℂ`  — **✅ DONE (2026-06-30) via the Eichler route (EICH-1..5)**
A cusp form with all periods zero is zero (the "easy half" of Eichler–Shimura). Depends: IHR-c.
**✅ DONE 2026-06-30**: `periodMap'_injective_eichler` PROVEN sorry-free + **axiom-clean** (`#print axioms` = {propext, Classical.choice, Quot.sound}; NO sorryAx, NO interior_edges_cancel_sum). Whole EICH cluster closed (EICH-1, EICH-2/2a/2b/2b-i, EICH-3/3a/3a-i, EICH-4, EICH-5). `heckeAlgℤ_finite` k≥2 re-pointed onto it; **interior_edges_cancel_sum eliminated from the critical path**. Sole remaining Group-A input: `exists_HeckeStableLattice` (k<2, Deligne–Serre).
**UPDATE (2026-06-30, expert-review round 2 + /develop --decompose):** the boundary-identity route
(`interior_edges_cancel_sum`, Shimura 8.2.22) is SUPERSEDED by the reviewer's **Eichler-integral + Δⁿ
weight-0** route — feasible + bounded (decomposition `.mathlib-quality/decomposition-eichler.md`;
compiling skeleton `ModularSymbols/EichlerInjective.lean`). Discharged by the executable cluster
**EICH-1 → {EICH-2, EICH-3} → EICH-4 → EICH-5** below. On completion, `periodMap'_injective` (hence
`heckeAlgℤ_finite` for k≥2) no longer consumes `interior_edges_cancel_sum`.

### [IHR-asm] Assemble FIH from IHR — discharges T002  — blocked, research
`𝕋ℤ ↪ End_ℤ(𝕄_k^∨_tf)` via Hecke-equivariance + injectivity (IHR-d); sub-ℤ-module of finite free is
finite ⟹ `Module.Finite ℤ (heckeAlgℤ N k)` (T002). Depends: IHR-a/b/c/d.
**Status note:** the assembly itself (`heckeAlgℤ_finite_of_period`) is PROVEN + INSTALLED
(`Labels/HeckeAlgFiniteFinal.lean`); only IHR-d (injectivity) remains — see the EICH cluster.

---

## Tranche 2′ — Eichler-integral route to IHR-d (EXECUTABLE NOW)

*Source: expert reviewer reply `expert-review/2026-06-24/reply.md` §1–§2; decomposition (adversarial,
source-faithful) `.mathlib-quality/decomposition-eichler.md`; compiling sorry-skeleton
`LeanModularForms/HeckeRIngs/GL2/ModularSymbols/EichlerInjective.lean` (9 sorries, 0 err). Each ticket
fills the named `sorry`(s) IN PLACE. Full per-leaf source quotes + adversarial attacks are in the
decomposition doc; tickets carry the Lean statement + sketch + lemmas + generality.*

Dependency order: `EICH-1 → {EICH-2 (= EICH-2a → EICH-2b (= EICH-2b-i → wrapper, wrapper DONE) → glue, glue DONE), EICH-3 (= EICH-3a)} → CLEANUP-EICH-1 → EICH-4 → EICH-5 → CLEANUP-EICH-2`.

### [EICH-1] G1 — the Eichler integral `E_f` + holomorphy + Bol `D^{k-1}E_f = f`
- **Status**: done (2026-06-30) · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: none · **Parallel**: yes · **Type**: def + 2 lemmas
- **Progress**: 2026-06-30: **DONE, axiom-clean** (build green 3716 jobs; `bol_iterated_eichler`/`differentiableOn_eichlerIntegral` `#print axioms` = {propext, Classical.choice, Quot.sound}, no sorryAx). `eichlerIntegral f := eichlerQSeries f 0 = ∑' m, (aₘ/mⁿ)·exp(2πi m z)`, n=(k-1).toNat (no m=0 guard: aₘ=0 at 0 via `CuspFormClass.qExpansion_coeff_zero`). Holomorphy via `hasDerivAt_tsum_of_isPreconnected` on half-planes (uniform geom bound, `qExpansionFormalMultilinearSeries_radius ≥ 1`). Bol via single-deriv lemma `hasDerivAt_eichlerQSeries` (degree-j → degree-(j+1) q-series) iterated by `iteratedDeriv_succ'` + `EventuallyEq.iteratedDeriv_eq`, scalar/mⁿ cancellation vs `hasSum_qExpansion`. Helpers (all proved, private): `eichlerCoeff, eichlerQSeries, hasSum_qExpansion_eichler, summable_norm_coeff_mul_geom, summable_norm_coeff_mul_pow_geom, hasDerivAt_eichlerQSeries, iteratedDeriv_eichlerQSeries, one_mem_strictPeriods_Gamma1`. KEY API for rest of cluster: mathlib `UpperHalfPlane.qExpansion`/`hasSum_qExpansion`/`qExpansionFormalMultilinearSeries_radius`/`CuspFormClass.qExpansion_coeff_zero`/`Function.Periodic.norm_qParam`; `(Gamma1 N).map (mapGL ℝ) = Gamma1 N` by rfl ⟹ width 1. NOTE: `eichlerIntegral` is defined as the **q-series**, NOT the integral ∫_z^{i∞} — EICH-2 must bridge to the integral form (or argue q-series-natively). Cleanup deferred to central main (producer rules). Post-proof cleanup: skipped per AINTLIB producer rules.

#### Statement (fill the `sorry`s as written in the skeleton)
```lean
def eichlerIntegral (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) : ℂ → ℂ := sorry

theorem differentiableOn_eichlerIntegral (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) :
    DifferentiableOn ℂ (eichlerIntegral f) {z : ℂ | 0 < z.im} := by sorry

theorem bol_iterated_eichler (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k) {z : ℂ}
    (hz : 0 < z.im) :
    ((2 * (π : ℂ) * Complex.I)⁻¹) ^ (k - 1).toNat *
        iteratedDeriv (k - 1).toNat (eichlerIntegral f) z = f (UpperHalfPlane.ofComplex z) := by sorry
```
#### Proof sketch
1. **Define** `eichlerIntegral f z := ∑' m : ℕ+, (qExpansion coeff a_m of f) / (m:ℂ)^(k-1) * Complex.exp (2πi m z)` (the `q`-series form; reply.md (2)). Use the project/mathlib `qExpansion`/Fourier coefficient of `f` at `∞`. (Alt: the integral `C_k ∫_z^{i∞} f(τ)(τ-z)^{k-2}dτ` via `isExactOn_upperHalf`, but the `q`-series is cleaner for Bol.)
2. **Holomorphy**: the series converges locally uniformly on ℍ (`|exp(2πimz)| = e^{-2πm·Im z}`, and `a_m = O(m^{k-1})` by the trivial Hecke bound ⟹ terms `O(1)·e^{-2πm·Im z}`); apply `differentiableOn_tsum`/`Summable`-of-geometric-decay + `DifferentiableOn` of each term.
3. **Bol**: `(2πi)⁻¹ d/dz exp(2πimz) = m·exp(2πimz)`, so `D^{k-1}(a_m/m^{k-1} exp) = a_m exp`; sum termwise (`iteratedDeriv_tsum` with uniform convergence) ⟹ `= ∑ a_m exp = f`. Identify `f(ofComplex z)` with its own `q`-series (mathlib `hasSum_qExpansion`-style).
#### Mathlib lemmas needed
`UpperHalfPlane.qExpansion` / Fourier coefficients; `Complex.exp`, `Complex.deriv_exp`; `iteratedDeriv`, `iteratedDeriv_const_smul`; `Summable`/`differentiableOn_tsum` for locally-uniform holomorphic series; the cusp-form `q`-expansion `HasSum`. Project: `periodForm`, `isExactOn_upperHalf` (alt route).
#### Sources
reply.md §1.1 (2)-(3) (verbatim in decomposition L-G1); Shimura §8.2; Diamond–Shurman §5.x.
#### Generality
Stated for `CuspForm ((Gamma1 N).map (mapGL ℝ)) k`, `k : ℤ` with `hk : 2 ≤ k` (needed for `(k-1).toNat ≥ 1` and `(τ-z)^{k-2}` polynomial). Carrier `ℂ → ℂ` (matches `periodForm`).

### [EICH-2] G2/L7 — weight-`(2-k)` slash invariance of `E_f` when `ι(f)=0`
- **Status**: **MAIN LEMMA CLOSED** (`eichler_slash_invariant` is sorry-free; build green 3716 jobs), routed through 2 isolated `private` sorry helpers spawned as **EICH-2a** (integral representation) + **EICH-2b** (defect formula as cusp-value differences) below. · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-1, EICH-2a, EICH-2b, **EICH-3a (`eichler_basepoint_indep`, already proven)** · **Parallel**: with EICH-3 · **Type**: lemma
- **Progress**: 2026-06-30: `eichler_slash_invariant` **fully proven** here. Gluing: `eichler_slash_sub_eq_cuspValue_diff` (EICH-2b) packages the pointwise defect `(E_f∘↑ ∣[2-k] γ) τ - (E_f∘↑) τ` as `∑_{j≤n} coeff τ j · (cuspValue f (symMon n j) c₁ - cuspValue f (symMon n j) c₂)` (n = (k-2).toNat); then `funext τ` + `sub_eq_zero.mp` + `Finset.sum_eq_zero`, and EACH cusp-value difference is `0` by `eichler_basepoint_indep f hk hf (symMon (k-2).toNat j) c₁ c₂` (`sub_self`, `mul_zero`). `#print axioms eichler_slash_invariant` = {propext, Classical.choice, Quot.sound, **sorryAx**}; the `sorryAx` traces SOLELY to EICH-2a + EICH-2b. Reuses mathlib `ModularForm.SL_slash_apply`/`SlashAction ℤ SL(2,ℤ) (ℍ→ℂ)` (negative weight `2-k` OK); project `cuspValue`, `symMon`, `eichler_basepoint_indep`. New private `def eichlerConst k := I^(k-1)·Γ(k-1)/(2π)^(k-1)` (the C_k of reply.md (4), used in EICH-2a/2b statements; consumes mathlib `Complex.Gamma`).

#### Statement
```lean
theorem eichler_slash_invariant (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) (γ : CongruenceSubgroup.Gamma1 N) :
    ((eichlerIntegral f) ∘ ((↑) : ℍ → ℂ)) ∣[2 - k] (γ : SL(2, ℤ))
      = (eichlerIntegral f) ∘ ((↑) : ℍ → ℂ) := by sorry
```
#### Proof sketch (reply.md §1.2 — the heart)
1. **Defect = integral over `[γ⁻¹∞, ∞]`**: `(E_f|_{2-k}γ - E_f)(z) = C_k ∫_{γ⁻¹∞}^∞ f(τ)(τ-z)^{k-2}dτ`. Prove by the change of variables `τ ↦ γτ` in `∫_{γz}^{i∞} f(τ)(τ-z)^{k-2}dτ` using `f`'s weight-`k` automorphy — reuse the project's Möbius CoV (`periodForm_mob_general`) and the cocycle computation pattern of `isPeriodInvariant_all` (`cuspDiff_const`), lifted to the function level.
2. **Coefficients are periods**: expand `(τ-z)^{k-2} = ∑_j C(k-2,j)(-z)^{k-2-j}τ^j` (binomial bridge `petersson_binomial_periodForm`); each coefficient is the period `∫_{γ⁻¹∞}^∞ f(τ)τ^j dτ = rawPairing f (((γ⁻¹∞)-(∞)) ⊗ P_j)`.
3. **Vanish**: `hf` (`periodMap' f = 0`) ⟹ `rawPairing f (D ⊗ P) = 0` for `D ∈ Div⁰` (`rawPairing_eq_zero_of_periodMap'_zero`); `((γ⁻¹∞)-(∞)) ∈ Div⁰`, so all coefficients are 0 ⟹ defect 0 ⟹ slash-invariance.
#### Mathlib lemmas needed
`SlashAction`/`ModularForm.slash_def` (weight `2-k` on `ℍ→ℂ`, negative OK); `Finset.sum`/binomial. Project: `rawPairing_eq_zero_of_periodMap'_zero`, `periodForm_mob_general`, `cuspDiff_const`, `petersson_binomial_periodForm`, `UpperHalfPlane.coe`.
#### Sources
reply.md §1.2 (5) (verbatim in decomposition G2); Shimura (8.2.15)/(8.2.22).
#### Generality
`k : ℤ`, `2 ≤ k` (so `2-k ≤ 0`). Bridge `((↑) : ℍ → ℂ)` to match the mathlib slash carrier — no math weakening (skeleton-verified to typecheck).

### [EICH-2a] G2a — integral representation `E_f(z) = C_k ∫_z^{i∞} f(τ)(τ-z)^{k-2} dτ`  [SPAWNED 2026-06-30 from EICH-2]
- **Status**: **DONE, axiom-clean** (2026-06-30; build green 3716 jobs; `#print axioms eichlerIntegral_eq_vertical` = {propext, Classical.choice, Quot.sound}, **no sorryAx**). · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-1 · **Parallel**: with EICH-2b · **Type**: theorem (FULLY CLOSED, no `sorry` in cone) · **Parent**: EICH-2 · **Blocks**: EICH-2b (consumed as a `have` in its proof body)
- **Progress**: 2026-06-30: `eichlerIntegral_eq_vertical` proven sorry-free. **FIXED `eichlerConst`** to the reciprocal of the per-term Gamma factor: `eichlerConst k := (2π)^{(k-1).toNat} / (I^{(k-1).toNat}·Γ((k:ℂ)-1))` (the old def was the reciprocal of what makes the lemma true — the per-term integral produces `I^{k-1}Γ(k-1)/(2π)^{k-1}`, so `eichlerConst` must invert it). EICH-2b's existential `coeff`/cusps make it witness-agnostic, so the change is consistent (the `have _hrep` consumer only uses the statement). Helpers added (all proved, private): `eichlerTerm` (per-m integrand `a_m·exp(2πim(z+it))·(it)^{k-2}·i`, coeff = `(qExpansion 1 f).coeff m` NOT eichlerCoeff — the `1/m^{k-1}` weight is produced BY the Gamma integral), `eichlerGammaFactor`, `eichlerConst_mul_gammaFactor` (=1), `integral_eichlerTerm` (per-term Gamma integral = `eichlerGammaFactor·eichlerCoeff·exp`), `norm_eichlerTerm`, `integrableOn_eichlerTerm`, `integral_norm_eichlerTerm`, `summable_integral_norm_eichlerTerm`, `hasSum_eichlerTerm` (pointwise `∑' m, term = f(z+it)·(it)^{k-2}·i`). KEY mathlib: Gamma integral = `Complex.integral_cpow_mul_exp_neg_mul_Ioi` (`∫ t^(a-1) e^{-(r t)} = (1/r)^a Γ a`, a=k-1, r=2πm); `∑'/∫` interchange = `MeasureTheory.integral_tsum_of_summable_integral_norm`; integrability = `integrableOn_rpow_mul_exp_neg_mul_rpow` (p=1); summability bound `∫‖term‖ ≤ ‖a_m‖·ρ^m·K` (ρ=e^{-2π·Im z}<1, K=m=1 Gamma integral) via `summable_norm_coeff_mul_geom` (EICH-1). NO new sub-ticket needed (no API gap surfaced).
#### Statement
```lean
-- currently in EichlerInjective.lean as an isolated `private` sorry:
private def eichlerConst (k : ℤ) : ℂ :=
  Complex.I ^ (k - 1).toNat * Complex.Gamma ((k : ℂ) - 1) / (2 * (π : ℂ)) ^ (k - 1).toNat

private theorem eichlerIntegral_eq_vertical (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    {z : ℂ} (hz : 0 < z.im) :
    eichlerIntegral f z =
      eichlerConst k *
        ∫ t in Set.Ioi (0 : ℝ),
          f (UpperHalfPlane.ofComplex (z + Complex.I * t)) *
            (Complex.I * (t : ℂ)) ^ (k - 2).toNat * Complex.I := by sorry
```
The vertical-ray parametrisation `τ = z + i t`, `dτ = i dt`, `t ∈ (0,∞)` of reply.md §1.1 (4). This is the integral side of `eichlerIntegral` (which EICH-1 defined as the **q-series** `∑' m, (a_m/m^{k-1}) exp(2πi m z)`), so EICH-2a is the bridge q-series ⟶ integral that EICH-1's note flagged as needed.
#### Proof sketch (reply.md §1.1 (4))
Integrate the absolutely convergent q-series term by term. For each `m ≥ 1`:
`∫_0^∞ exp(2πi m (z + i t))·(i t)^{k-2}·i dt = (C_k / m^{k-1}) · exp(2πi m z)`.
Proof of the per-term identity: factor `exp(2πi m z)` out (it is `t`-independent), leaving
`exp(2πi m z) · i^{k-1} ∫_0^∞ exp(-2π m t) t^{k-2} dt`; the substitution `s = 2π m t` gives
`∫_0^∞ exp(-2π m t) t^{k-2} dt = Γ(k-1)/(2π m)^{k-1}` — mathlib `Complex.GammaIntegral`/`Complex.Gamma_eq_integral` (for `0 < re(k-1)`, true since `k ≥ 2`) after `MeasureTheory.integral_comp_smul`/`integral_comp_mul_left`-style rescaling. The `m^{k-1}` denominator cancels exactly against `eichlerCoeff f m = a_m / m^{k-1}`, leaving `eichlerConst k`. Interchange `∑'` and `∫` by dominated convergence (`MeasureTheory.integral_tsum` / `hasSum_integral_of_dominated_convergence`), dominating with the EICH-1 summability bounds `summable_norm_coeff_mul_pow_geom` (the same `‖a_m‖·m^j·ρ^m` tail that powers EICH-1's term-by-term differentiation).
#### Mathlib lemmas needed
`Complex.GammaIntegral`, `Complex.Gamma_eq_integral`, `Complex.GammaIntegral_convergent`; `MeasureTheory.integral_tsum`/`hasSum_integral_of_dominated_convergence`, `MeasureTheory.integral_comp_mul_left`/`integral_smul`, `intervalIntegral`/`Set.Ioi` integral API, `Complex.exp_add`/`exp_nat_mul`. Project (EICH-1): `eichlerCoeff`, `eichlerQSeries`, `summable_norm_coeff_mul_pow_geom`, `hasSum_qExpansion_eichler`.
#### Sources
reply.md §1.1 (4) (verbatim): *"up to a fixed nonzero constant `C_k`: `E_f(z) = C_k ∫_z^{i∞} f(τ)(τ-z)^{k-2} dτ` (absolutely convergent by cusp decay)."* + §1.2 substitution note: *"`∫_z^{i∞} exp(2πimτ)(τ-z)^{k-2} dτ = (C_k/m^{k-1}) exp(2πimz)` via substitution `u=τ-z` then the Gamma integral `∫_0^∞ exp(-2πm t) t^{k-2} dt = Γ(k-1)/(2πm)^{k-1}`."*
#### Generality
`k : ℤ`, `2 ≤ k` (so `(k-2).toNat = k-2 ≥ 0` and `re(k-1) ≥ 1 > 0` for the Gamma integral). `C_k = i^{k-1}·Γ(k-1)/(2π)^{k-1}` (`eichlerConst`, real-positive up to `i^{k-1}`).

### [EICH-2b] G2a+G2b — modularity-defect formula as cusp-value differences  [SPAWNED 2026-06-30 from EICH-2]
- **Status**: **`eichler_slash_sub_eq_cuspValue_diff` FULLY CLOSED** (build green 3716 jobs; `#print axioms` = {propext, Classical.choice, Quot.sound, **sorryAx**}, the `sorryAx` tracing SOLELY to the new sub-ticket **EICH-2b-i** `eichler_defect_cuspValue`). · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-1, EICH-2a, **EICH-2b-i** · **Parallel**: with EICH-2a · **Type**: theorem (now sorry-free, routed through EICH-2b-i) · **Parent**: EICH-2 · **Blocks**: EICH-2 (`eichler_slash_invariant` — already routed through this statement)
- **Progress**: 2026-06-30: `eichler_slash_sub_eq_cuspValue_diff` proven as a **thin witness-supplying wrapper** over the new analytic-core helper `eichler_defect_cuspValue` (EICH-2b-i). Witnesses: `c₁ = cuspAction γ⁻¹ c∞` (= `γ⁻¹·∞`), `c₂ = c∞` where `c∞ = OnePoint.equivProjectivization ℚ OnePoint.infty`, `coeff τ j = eichlerConst k · C(n,j) · (-(τ:ℂ))^{n-j}` (`n = (k-2).toNat`). The wrapper discharges the `c₂ = ∞ ⟹ cuspValue f · c∞ = 0` simplification (`cuspValue` def + `Equiv.symm_apply_apply` ⟹ the `.infty` match arm = `0`), reducing each cusp-value *difference* to the single value at `γ⁻¹∞`, which is exactly the helper's RHS. The **moving-basepoint** analytic content (the function-level Möbius CoV under the integral) is re-isolated as the single `private sorry` `eichler_defect_cuspValue` (EICH-2b-i below). Exact slash formula used by the helper statement: mathlib `ModularForm.SL_slash_apply` — `(F ∣[w] g) τ = F (g•τ) · denom(toGL(map g), τ)^(-w)` (here `w = 2-k`, `det g = 1`).
#### Statement
```lean
-- currently in EichlerInjective.lean as an isolated `private` sorry:
private theorem eichler_slash_sub_eq_cuspValue_diff
    (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (γ : CongruenceSubgroup.Gamma1 N) :
    ∃ (c₁ c₂ : Projectivization ℚ (Fin 2 → ℚ)) (coeff : ℍ → ℕ → ℂ),
      ∀ τ : ℍ,
        ((eichlerIntegral f ∘ ((↑) : ℍ → ℂ)) ∣[2 - k] (γ : SL(2, ℤ))) τ -
            (eichlerIntegral f ∘ ((↑) : ℍ → ℂ)) τ =
          ∑ j ∈ Finset.range ((k - 2).toNat + 1),
            coeff τ j *
              (cuspValue f (symMon (k - 2).toNat j) c₁ -
                cuspValue f (symMon (k - 2).toNat j) c₂) := by sorry
```
Packages reply.md §1.2 (5)–(6) in the exact shape the EICH-2 gluing consumes: the intended witnesses are `c₁ = γ⁻¹·∞`, `c₂ = ∞`, `coeff τ j = C_k·C(n,j)·(-τ)^{n-j}` (binomial coefficients of `(τ-·)^n`, `n = (k-2).toNat`). The cusps and `coeff` are existential so the witnesses need not be pinned for the gluing (any pair works — `eichler_basepoint_indep` zeroes every cusp-value difference).
#### Proof sketch (reply.md §1.2 (5)-(6))
1. **Defect = path integral** (the function-level Möbius CoV): by `eichlerIntegral_eq_vertical` (EICH-2a, already consumed as a `have` in the in-tree proof body), `(E_f|_{2-k}γ - E_f)(z) = C_k ∫_{γ⁻¹∞}^∞ f(τ)(τ-z)^{k-2} dτ`. Derive by the change of variables `τ ↦ γτ` in `E_f(γz) = C_k ∫_{γz}^{i∞} f(τ')(τ'-γz)^{k-2} dτ'` (set `τ'=γτ`; `f`'s weight-`k` automorphy `f(γτ)=(cτ+d)^k f(τ)` supplies, together with the `(cτ+d)^{2-k}` and det factors of the slash `∣[2-k]γ`, the cancellation that leaves `(τ-z)^{k-2}`). REUSE the project's Möbius CoV `periodForm_mob_general` and the cocycle/contour pattern of `isPeriodInvariant_all`/`cuspDiff_const`, lifted from the pairing level to the function level (an extra "differentiate/contour under the integral" layer — the principal analytic gap of EICH-2).
2. **Binomial ⟹ per-monomial periods**: expand `(τ-z)^n = Σ_{j=0}^{n} C(n,j)(-z)^{n-j} τ^j` (binomial; `evalSym1 (symMon n j) τ = τ^j` by `evalSym1_symMon`); each coefficient is the period `∫_{γ⁻¹∞}^∞ f(τ)·τ^j dτ`. REUSE the binomial bookkeeping of `petersson_binomial_periodForm`.
3. **Contour split ⟹ cusp-value differences**: `∫_{γ⁻¹∞}^∞ f(τ)τ^j dτ = ∫_{γ⁻¹∞}^{i∞} - ∫_∞^{i∞} = cuspValue f (symMon n j) (γ⁻¹∞) - cuspValue f (symMon n j) (∞)` (`cuspValue` = the geodesic integral to `i∞` from a finite cusp / `0` at `∞`, via `cuspToInftyIntegral`). Set `coeff τ j = C_k·C(n,j)·(-τ)^{n-j}`. ∎
#### Mathlib lemmas needed
`ModularForm.SL_slash_apply`/`slash_def` (the `∣[2-k]γ` unfolding, `denom γ τ^(-(2-k))`), `UpperHalfPlane.coe_smul`/`denom`/automorphy; `Finset.sum`/`Nat.choose`/`add_pow`-style binomial; `intervalIntegral`/contour-split API. Project: `eichlerIntegral_eq_vertical` (EICH-2a), `periodForm_mob_general`, `hasDerivAt_primitive_mob_general`, `cuspDiff_const`/`isPeriodInvariant_all` (the CoV/cocycle pattern), `cuspValue`/`cuspToInftyIntegral`, `symMon`/`evalSym1_symMon`, `petersson_binomial_periodForm`, the contour lemmas (`vertical_segment_cauchy`, `tendsto_horizontal_cap`, `Complex.isExactOn_upperHalf`).
#### Sources
reply.md §1.2 (5) (verbatim): *"For γ = [[a,b],[c,d]] ∈ Γ: (E_f|_{2−k} γ)(z) − E_f(z) = C_k ∫_{γ⁻¹∞}^∞ f(τ)(τ−z)^{k−2} dτ. Expanding (τ−z)^n = Σ_j C(n,j)(−z)^{n−j} τ^j, every coefficient of the RHS is a period ∫ f τ^j dτ. So (1) ⟹ E_f|_{2−k} γ = E_f for all γ ∈ Γ."* (decomposition-eichler.md G2/L7.)
#### Generality
`k : ℤ`, `2 ≤ k` (so `(τ-z)^{k-2}` is a genuine polynomial of degree `n = k-2 ≥ 0`; the `k≥2` gate is necessary — `k<2` gives a negative power). `γ ∈ Γ₁(N)` (where the slash defect is a finite combination of periods that `ι(f)=0` kills; the general-`γ` slash, where the defect does NOT vanish, is the separate stronger EICH-3a). Existential cusps/`coeff` so the gluing in EICH-2 is witness-agnostic.

### [EICH-2b-i] modularity-defect analytic core — function-level Möbius CoV under the integral  [SPAWNED 2026-06-30 from EICH-2b]
- **Status**: **DONE — FULLY CLOSED, axiom-clean** (2026-06-30; build green 3716 jobs; `#print axioms eichler_slash_invariant` = {propext, Classical.choice, Quot.sound}, **no sorryAx** — the whole EICH-2 cone is now sorry-free). · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-1, EICH-2a · **Parent**: EICH-2b · **Blocks**: EICH-2b (`eichler_slash_sub_eq_cuspValue_diff`), hence EICH-2 (`eichler_slash_invariant`) — all unblocked. · **Type**: theorem (CLOSED)
- **Progress**: 2026-06-30: `eichler_defect_cuspValue` proven sorry-free, routed through the new fully-proven analytic core `eichler_defect_eq_cuspValueMoving`. **SIGN CORRECTION**: the honest slash+Möbius-CoV derivation gives the defect `= C_k·∫_{i∞}^{γ⁻¹∞} f(σ)(σ-τ)ⁿ dσ = -C_k·(cusp-value combination)` (reply.md (5)'s `∫_{γ⁻¹∞}^∞` has the opposite orientation), so the statement carries an overall `-`; verified independently three ways (general derivation, standard-convention re-derivation, k=2 concrete). Invisible to `eichler_slash_invariant` (all cusp-value differences vanish under ι(f)=0), but the FIXED-sign statement needed the `-`; matching sign-flip made in `eichler_slash_sub_eq_cuspValue_diff`'s existential `coeff` witness (still proven, EICH-2-main intact). **Method (NO new mathlib-absent input; did NOT need to generalise `cuspBoundary_mob_naturality`/`cuspValue_symRep_gamma`):** the moving polynomial `(·-τ)ⁿ` is realised as a genuine `SymPow ℂ n` element `eichlerMovingPoly n τ = Σ_j C(n,j)(-τ)^{n-j}·symMon n j` (so the whole polynomial-agnostic primitive engine applies). Then: build primitive `F` of `periodForm f (symRep γ Pτ)` (`isExactOn_upperHalf`) and `G=F∘mob γ` of `periodForm f Pτ` (`hasDerivAt_primitive_mob_general` + `f∣γ=f`); a NEW **interior-base vertical-ray FTC** `ray_integral_eq_top_sub` (bottom is an interior point, so value `= L - Φ(w₀)`, no `0⁺`-limit subtlety) with its **interior integrability** `integrableOn_periodForm_ray_interior` (via `periodForm_norm_le` decay + new dominator `integrableOn_shifted_pow_mul_exp_neg`) computes both `E_f(τ)=C(Ginf-G(τ))` and `E_f(γτ)·Dⁿ=C(Finf-F(γτ))` from EICH-2a; the interior endpoints cancel (`mob γ τ=γτ`, `G(τ)=F(γτ)`) ⟹ defect `=C(Finf-Ginf)`. The single cusp value is `cuspValue f Pτ(γ⁻¹∞)=Ginf-Finf` via `cuspValue_eq_top_sub_botVal` + `tendsto_F_pole` (finite-cusp pole geodesic, σ=1) / `tendsto_F_upper` (γ upper-tri case). New algebra: `evalSym1_symRep_eichlerMovingPoly` (`evalSym1(symRep γ Pτ)σ=(cτ+d)ⁿ(σ-γτ)ⁿ` for ALL σ, via `evalSym1_substAlgHom`+`eval_eichlerMovingPoly_hom`+`symMat` adjugate entries). Helpers `eichlerMovingPoly`/`evalSym1_eichlerMovingPoly`/`cuspValue_eichlerMovingPoly` (binomial ↔ cuspValue ℂ-linearity). NO maxHeartbeats/native_decide/custom axioms.
#### Statement (now PROVEN, sign-corrected)
- (former isolated-sorry statement preserved below for reference; the `-` sign is now explicit in the live decl.)
#### Statement
```lean
-- currently in EichlerInjective.lean as an isolated `private` sorry:
private theorem eichler_defect_cuspValue
    (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (γ : CongruenceSubgroup.Gamma1 N) (τ : ℍ) :
    ((eichlerIntegral f ∘ ((↑) : ℍ → ℂ)) ∣[2 - k] (γ : SL(2, ℤ))) τ -
        (eichlerIntegral f ∘ ((↑) : ℍ → ℂ)) τ =
      ∑ j ∈ Finset.range ((k - 2).toNat + 1),
        (eichlerConst k * ((k - 2).toNat).choose j * (-(τ : ℂ)) ^ ((k - 2).toNat - j)) *
          cuspValue f (symMon (k - 2).toNat j)
            (cuspAction γ⁻¹ (OnePoint.equivProjectivization ℚ (OnePoint.infty : OnePoint ℚ))) := by sorry
```
The pointwise weight-`(2-k)` modularity defect of `E_f` equals the explicit binomial combination of cusp values of `f` against the monomials `symMon n j` at the SINGLE cusp `γ⁻¹·∞` (`n = (k-2).toNat`, `C_k = eichlerConst k`). This is the deferred analytic content extracted from `eichler_slash_sub_eq_cuspValue_diff`: that wrapper supplies the existential witnesses (`c₁ = γ⁻¹∞`, `c₂ = ∞`) and discharges `cuspValue f · ∞ = 0`; ALL remaining content is here.
#### Why deep (genuinely self-contained)
This is the **function-level Möbius change of variables under the integral with a MOVING basepoint** — the principal analytic gap of EICH-2. By EICH-2a both `E_f(γτ)` and `E_f(τ)` are vertical-ray Eichler integrals; substituting `τ' = γ·w` in `E_f(γτ)·denom(γ,τ)^{k-2}` (on the *moving* polynomial `(τ'-τ)^{k-2}`, using `f(γτ')=(cτ'+d)^k f(τ')` + `γτ'-γτ = (τ'-τ)/((cτ'+d)(cτ+d))` + `dw = dτ'/(cτ'+d)^2`, the `(cτ'+d)`/`(cτ+d)` powers cancelling against the slash factor) collapses the defect to the geodesic period `-C_k ∫_{[γ⁻¹∞→i∞]} f(τ')(τ'-τ)^n dτ'`; the binomial expansion `(τ'-τ)^n = Σ_j C(n,j)(-τ)^{n-j} τ'^j` plus **straightening the curved geodesic to the vertical line** (path-independence via cusp decay killing the connecting horizontal cap) identifies the `j`-th monomial period with `cuspToInftyIntegral f (symMon n j) q₀ = cuspValue f (symMon n j) (γ⁻¹∞)` (`evalSym1_symMon`). **CRUCIALLY this is NOT reducible to the project's `cuspValue_symRep_gamma`**: that lemma is the Möbius covariance of *fixed-polynomial* modular-symbol periods (proven via primitives + boundary values + the cited `cuspBoundary_mob_naturality`), whereas here the integrand carries the τ-DEPENDENT polynomial `(τ'-τ)^{k-2}`, so the CoV must be done under the integral with the basepoint as a free parameter — a genuinely separate development. (Bol's identity `D^{k-1}(F|_{2-k}γ)=(D^{k-1}F)|_k γ` would show the defect is a degree-≤n polynomial in τ but NOT identify its coefficients as these cusp-value periods, and the slash-commutation of Bol is itself absent from the project — only `bol_iterated_eichler` = `D^{k-1}E_f=f` is present.)
#### Proof sketch (reply.md §1.2 (5)-(6))
1. **`E_f(γτ)·denom^{k-2}` ⟶ geodesic integral** via the Möbius CoV `τ'=γ⁻¹w` under the EICH-2a integral; cocycle algebra cancels all `(cτ'+d)`/`(cτ+d)` powers (mathlib `intervalIntegral.integral_comp_smul_deriv`/`MeasureTheory.integral_image_eq_integral_abs_deriv_smul`; project `mob`/`hasDerivAt_mob`/`denom_mob_ne`/`automorphy`).
2. **Subtract `E_f(τ)`** (the common `[τ→i∞]` legs cancel; residual = the `[γ⁻¹∞→i∞]` geodesic of `f(τ')(τ'-τ)^n`).
3. **Binomial** `(τ'-τ)^n = Σ_j C(n,j)(-τ)^{n-j} τ'^j` (`add_pow`/`Nat.choose`), then **straighten geodesic → vertical line** (`tendsto_horizontal_cap`/`vertical_segment_cauchy`/`Complex.isExactOn_upperHalf`) to hit `cuspToInftyIntegral f (symMon n j) q₀` = `cuspValue f (symMon n j) (γ⁻¹∞)`. Sign/normalisation folded into the (existential) `coeff` of the parent wrapper, so it does not affect the EICH-2 gluing. ∎
#### Mathlib lemmas needed
`ModularForm.SL_slash_apply` (exact slash, USED in the statement); change-of-variables in an integral (`intervalIntegral.integral_comp_smul_deriv`, `MeasureTheory.integral_image_eq_integral_abs_deriv_smul`); `add_pow`/`Nat.choose`/`Finset.sum` binomial. Project: `eichlerIntegral_eq_vertical` (EICH-2a, consumed as a `have` in-body), `mob`/`mob_eq_smul`/`hasDerivAt_mob`/`denom_mob_ne`, `automorphy`, `cuspToInftyIntegral`/`cuspValue`/`cuspToInftyIntegral_integrand`, `symMon`/`evalSym1_symMon`, the contour lemmas (`vertical_segment_cauchy`, `tendsto_horizontal_cap`, `Complex.isExactOn_upperHalf`), `periodForm_mob_general`/`hasDerivAt_primitive_mob_general` (the CoV pattern at the primitive level).
#### Sources
reply.md §1.2 (5) (verbatim): *"For γ = [[a,b],[c,d]] ∈ Γ: (E_f|_{2−k} γ)(z) − E_f(z) = C_k ∫_{γ⁻¹∞}^∞ f(τ)(τ−z)^{k−2} dτ. Expanding (τ−z)^n = Σ_j C(n,j)(−z)^{n−j} τ^j, every coefficient of the RHS is a period ∫ f τ^j dτ."* (decomposition-eichler.md G2/L7.)
#### Generality
`k : ℤ`, `2 ≤ k` (the `k≥2` gate is necessary — `(τ'-τ)^{k-2}` must be a genuine non-negative power); `γ ∈ Γ₁(N)` (so `f`'s weight-`k` automorphy applies). Single cusp `γ⁻¹∞` (the parent's `c₂=∞` term being `0`).

### [EICH-3] G3 — cusp behaviour: base-point independence + holomorphy/vanishing at every cusp
- **Status**: **2/2 skeleton lemmas DONE, axiom-clean** (build green 3716 jobs); residual all-cusps boundedness re-isolated as **EICH-3a** · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-1 · **Parallel**: with EICH-2 · **Type**: def + 2 lemmas — **the principal residual gap (G3)**
- **Progress**: 2026-06-30: both skeleton lemmas closed sorry-free + axiom-clean (`#print axioms` = {propext, Classical.choice, Quot.sound}).
  - **`eichler_basepoint_indep`**: integer case via `rawPairing_eq_zero_of_periodMap'_zero hk f hf (divDiff c₁ c₂ ⊗ₜ Q)` → `rawPairing_tmul`/`divDiff_val`/`map_sub`/`LinearMap.sub_apply`/`rawBilin_single` ⟹ `cuspValue f (castSymPow Q) c₁ - cuspValue f (castSymPow Q) c₂ = 0` (`eq_of_sub_eq_zero`). Extended from integer `Q : SymPow ℤ` to complex `P : SymPow ℂ` by `ℂ`-linearity (`cuspValue_add_right`/`cuspValue_smul_right`) + `Submodule.span_induction` over the spanning set, using new private helper **`span_range_castSymPow_eq_top m : Submodule.span ℂ (Set.range (castSymPow m)) = ⊤`** (via `Submodule.map_injective_of_injective (SymPow ℂ m).injective_subtype` + `map_subtype_top` + `homogeneousSubmodule_eq_finsupp_supported`/`Finsupp.supported_eq_span_single`; each degree-`m` monomial is `castSymPow` of the integer monomial).
  - **`eichler_cusp_holo`**: `IsBoundedAtImInfty` via `isBoundedAtImInfty_iff` with `M = Σ' ‖aₘ‖·ρ^m`, `A = 1`, `ρ = exp(-2π) < 1`. For `Im z ≥ 1`: `‖exp(2πi m z)‖ = exp(-2π m·Im z) ≤ ρ^m` (`Function.Periodic.norm_qParam`), `‖eichlerCoeff f m‖ ≤ ‖aₘ‖`, then `norm_tsum_le_tsum_norm` + `Summable.tsum_le_tsum`. **Does NOT use `hf`** (boundedness at i∞ is automatic from `a₀=0`); `hf` kept in signature for uniformity (`set_option linter.unusedVariables false in`). Summability from EICH-1 helper `summable_norm_coeff_mul_pow_geom f 0`.
- **NOTE on EICH-4 `bdd_at_cusps'`**: `eichler_cusp_holo` gives ONLY the cusp-`∞` (`γ=1`) shadow. `ModularForm`'s `bdd_at_cusps'` field needs boundedness at **every** cusp ⟹ blocked on EICH-3a (below). `eichlerIntegralAt` (the literal G3a `E_{f,c}=E_f`) also remains undefined; the provable driver `eichler_basepoint_indep` is what `ι(f)=0` actually supplies.

#### Statement
```lean
-- (new def, the cusp-based Eichler integral — needed for the literal G3a; flagged sub-gap)
def eichlerIntegralAt (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k)
    (c : Projectivization ℚ (Fin 2 → ℚ)) : ℂ → ℂ := sorry

theorem eichler_basepoint_indep (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) (P : SymPow ℂ (k - 2).toNat)
    (c₁ c₂ : Projectivization ℚ (Fin 2 → ℚ)) :
    cuspValue f P c₁ = cuspValue f P c₂ := by sorry   -- skeleton's provable driver; strengthen to E_{f,c₁}=E_{f,c₂} once eichlerIntegralAt is in

theorem eichler_cusp_holo (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) :
    UpperHalfPlane.IsBoundedAtImInfty ((eichlerIntegral f) ∘ ((↑) : ℍ → ℂ)) := by sorry
```
#### Proof sketch (reply.md §1.3 (7)-(8))
1. **`eichler_basepoint_indep`** (the driver): `cuspValue f P c₁ - cuspValue f P c₂ = rawPairing f (((c₁)-(c₂)) ⊗ P) = 0` by `rawPairing_eq_zero_of_periodMap'_zero` + `hf`. (Direct from L0.)
2. **`eichlerIntegralAt c`**: define analogously to `eichlerIntegral` but integrating from the cusp `c` (slash to bring `c → ∞`, then the `∞`-based series of `f|σ`). The literal G3a `E_{f,c} = E_f` then follows because their difference is `C_k ∫_c^∞ f(τ)(τ-z)^n dτ`, a period = 0 by step 1.
3. **`eichler_cusp_holo`**: from (8), `E_f|_{2-k}σ` at each cusp has only positive `q`-powers (the `a_m/m^{k-1}` series has `a_0 = 0` for a cusp form, no constant/negative term) ⟹ bounded as `Im → ∞`. Use project cusp decay (`tendsto_horizontal_cap`, `periodForm_norm_le`) + the slash-σ reduction; package as `IsBoundedAtImInfty` (and the per-cusp version for `eichlerModularForm.bdd_at_cusps'`).
#### Mathlib lemmas needed
`UpperHalfPlane.IsBoundedAtImInfty`, `OnePoint.IsBoundedAt`; `Filter.Tendsto`/`Asymptotics`. Project: `rawPairing_eq_zero_of_periodMap'_zero`, `cuspValue`, `tendsto_horizontal_cap`, `periodForm_norm_le`, the slash machinery.
#### Sources
reply.md §1.3 (7)-(8) (verbatim in decomposition G3).
#### Generality
`k : ℤ`, `2 ≤ k`. NOTE (from decomposition): G3 is the largest residual — `eichlerIntegralAt` is a new def and the per-cusp content is non-trivial; if it balloons, spawn sub-tickets per `/develop` rules.

### [EICH-3a] G3b (all cusps) — boundedness of `E_f` at every cusp  [SPAWNED 2026-06-30 from EICH-3]
- **Status**: **`eichler_bdd_at_cusp` PROVEN, modulo the re-isolated finite-cusp sub-residual EICH-3a-i** (2026-06-30, updated). `eichler_bdd_at_cusp` is now a `by_cases` on `γ₁₀ = 0`: the **`γ • ∞ = ∞` (`γ₁₀ = 0`) case is CLOSED axiom-clean** here (upper-triangular: `denom = γ₁₁ = ±1`, `Im(γ•τ) = Im τ`, so the cusp-`∞` bound of `eichler_cusp_holo` transfers verbatim); the **`γ₁₀ ≠ 0` finite-cusp case is delegated to the new isolated `private` lemma `eichler_slashSL_bdd_finite`** (sub-ticket **EICH-3a-i** below). `#print axioms periodMap'_injective_eichler` (and `eichler_bdd_at_cusp`) now traces to `[propext, sorryAx, Classical.choice, Quot.sound]` — `sorryAx` solely from `eichler_slashSL_bdd_finite`, and **NOT `interior_edges_cancel_sum`** (Eichler route stays detached from the Stokes wall). Build green (3716 jobs); downstream `HeckeAlgFiniteFinal` green (4050 jobs). So closing EICH-3a-i makes the entire `k≥2` injectivity / `heckeAlgℤ_finite` (k≥2) chain axiom-clean. · **File**: ModularSymbols/EichlerInjective.lean (`eichler_bdd_at_cusp` ~line 648; residual `eichler_slashSL_bdd_finite` ~line 627) · **Depends on**: EICH-1, EICH-2 (`eichler_cusp_holo`, `eichler_slash_invariant`) · **Type**: theorem (`c=0` proven; `c≠0` = 1 isolated `private` sorry) · **Parent**: EICH-3 · **Blocks**: nothing now (EICH-4 packaged around it)
#### Statement
```lean
-- currently in EichlerInjective.lean as an isolated `private` sorry (the only NEW sorry from EICH-3):
private theorem eichler_bdd_at_cusp (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) (γ : SL(2, ℤ)) :
    UpperHalfPlane.IsBoundedAtImInfty
      (((eichlerIntegral f) ∘ ((↑) : ℍ → ℂ)) ∣[2 - k] γ) := by sorry
```
This is EXACTLY the reduction of `ModularForm.bdd_at_cusps'` for `ModularForm ((Gamma1 N).map (mapGL ℝ)) (2-k)`: the field is `{c : OnePoint ℝ} (hc : IsCusp c Γ) : c.IsBoundedAt (E_f ∘ ↑) (2-k)`, and since `Γ = (Gamma1 N).map (mapGL ℝ)` is arithmetic, `Subgroup.IsArithmetic.isCusp_iff_isCusp_SL2Z Γ` + `OnePoint.isBoundedAt_iff_forall_SL2Z` rewrite it to `∀ γ : SL(2,ℤ), mapGL ℝ γ • ∞ = c → IsBoundedAtImInfty ((E_f ∘ ↑) ∣[2-k] γ)`. Proving `eichler_bdd_at_cusp` for ALL `γ ∈ SL(2,ℤ)` therefore discharges `bdd_at_cusps'` for every cusp `c` (drop the `mapGL ℝ γ • ∞ = c` premise; we prove it unconditionally in `γ`). `eichler_cusp_holo` already gives the `γ = 1` case.
#### Proof sketch (reply.md §1.3 (8))
For `γ ∈ SL(2,ℤ)`, `(E_f ∘ ↑) ∣[2-k] γ` is — up to a fixed nonzero constant `C_k'` and the cusp width `h` — the Eichler `q`-series at `∞` of the **translated** cusp form `f ∣[k] γ`: `(E_f|_{2-k}γ)(z) = C_k' Σ_{m≥1} (b_m/(m/h)^{k-1}) e^{2πimz/h}`, where `(f|_k γ)(z) = Σ_{m≥1} b_m e^{2πimz/h}` is the `q`-expansion of `f|_k γ` at `∞` (width `h`). Since `f` is a cusp form, `f|_k γ` is too ⟹ `b_0 = 0` ⟹ only positive `q`-powers ⟹ the series → 0 as `Im → ∞` ⟹ `IsBoundedAtImInfty`. The bound is the SAME geometric-tail argument as `eichler_cusp_holo` but applied to the `q`-expansion of `f|_k γ` (cusp width `h = `the width of `Γ₁(N)` at the cusp `γ•∞`) instead of `f` at `∞`.
Two routes to formalize:
  (a) **Direct**: compute the `q`-expansion of `(E_f ∘ ↑) ∣[2-k] γ` as a `q^{1/h}`-series with `a₀ = 0` and bound its tail (reuse the `eichler_cusp_holo` machinery: `Function.Periodic.norm_qParam` + `summable_norm_coeff_mul_pow_geom`-analogue for `f|_k γ`). Needs: the slash-`γ` formula for the Eichler `q`-series (the modularity-defect/slash computation), i.e. that `E_f|_{2-k}γ` IS the Eichler integral of `f|_k γ` up to `C_k'`.
  (b) **Via `eichlerIntegralAt`**: define `eichlerIntegralAt c` (Eichler integral based at cusp `c = γ•∞`), show `E_{f,c} = E_f` (their difference is a period `= 0` by `eichler_basepoint_indep`/L0), and `E_{f,c}|_{2-k}γ = ` the `∞`-Eichler integral of `f|_k γ` ⟹ bounded.
The slash-formula on the Eichler `q`-series (general `γ`) is the shared hard core; it is STRONGER than `eichler_slash_invariant` (which only needs `γ ∈ Γ₁(N)`, where the defect vanishes), so EICH-3a is genuinely deeper than EICH-2 and is the principal residual analytic gap of the whole Eichler route's cusp behaviour.
#### Mathlib lemmas needed
`UpperHalfPlane.IsBoundedAtImInfty`, `UpperHalfPlane.isBoundedAtImInfty_iff`, `Function.Periodic.norm_qParam`, `Real.exp_le_exp`, `norm_tsum_le_tsum_norm`, `Summable.tsum_le_tsum`; for the `bdd_at_cusps'` reduction (consumed in EICH-4, not here): `Subgroup.IsArithmetic.isCusp_iff_isCusp_SL2Z`, `OnePoint.isBoundedAt_iff_forall_SL2Z`, `OnePoint.IsBoundedAt`. Project: `eichler_cusp_holo` (the `γ=1` template), the EICH-1 summability helpers (`summable_norm_coeff_mul_pow_geom`, `eichlerCoeff`, `eichlerQSeries`), `eichler_basepoint_indep`/`rawPairing_eq_zero_of_periodMap'_zero` (for route (b)), the project `q`-expansion-at-cusps + slash machinery (`UpperHalfPlane.qExpansion` of `f|_k γ`, cusp width), `tendsto_horizontal_cap`/`periodForm_norm_le` (cusp decay).
#### Sources
reply.md §1.3 (8) (verbatim): *"After slashing by σ, the LHS becomes the Eichler integral at ∞ of f|_k σ. If the cusp width is h and (f|_k σ)(z) = Σ_{m≥1} b_m e^{2πimz/h}, then (E_f|_{2−k} σ)(z) = C_k' Σ_{m≥1} (b_m / (m/h)^{k−1}) e^{2πimz/h}. Only positive Fourier powers ⟹ E_f holomorphic AND vanishing at every cusp."* (decomposition-eichler.md G3b).
#### Generality
`k : ℤ`, `2 ≤ k`. Holds for every `γ ∈ SL(2,ℤ)` (hence every cusp). NOTE: `hf` likely unused (boundedness is automatic from `b₀ = 0` for the cusp form `f|_k γ`, just like `eichler_cusp_holo`); kept for signature uniformity. Does NOT need `eichlerIntegralAt` if route (a) is taken, but the general-`γ` slash-on-`q`-series formula is required either way.

### [EICH-3a-i] G3b finite-cusp residual — boundedness of `(E_f∘↑)∣[2-k]γ` for `γ•∞ ≠ ∞` (`γ₁₀ ≠ 0`)  [SPAWNED 2026-06-30 from EICH-3a]
- **Status**: **OPEN — the SOLE remaining residual of the whole Eichler route** (2026-06-30). After EICH-3a's `eichler_bdd_at_cusp` was split into the `γ₁₀ = 0` case (PROVEN, axiom-clean: cusp-`∞` bound transfer) and the `γ₁₀ ≠ 0` finite-cusp case, only the latter remains. `#print axioms periodMap'_injective_eichler` traces `sorryAx` solely to this lemma and **NOT `interior_edges_cancel_sum`**. So closing EICH-3a-i makes the entire `k≥2` injectivity / `heckeAlgℤ_finite` (k≥2) chain axiom-clean. · **File**: ModularSymbols/EichlerInjective.lean (`eichler_slashSL_bdd_finite`, ~line 627) · **Depends on**: EICH-1, EICH-2 · **Type**: theorem (1 isolated `private` sorry, in-tree) · **Parent**: EICH-3a · **Blocks**: nothing (`eichler_bdd_at_cusp` packaged around it)
#### Statement
```lean
private theorem eichler_slashSL_bdd_finite (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) (γ : SL(2, ℤ)) (hc : ¬ ((γ 1 0 : ℝ) = 0)) :
    UpperHalfPlane.IsBoundedAtImInfty (((eichlerIntegral f) ∘ ((↑) : ℍ → ℂ)) ∣[2 - k] γ) := by sorry
```
#### Why this is the genuine, mathlib/project-absent gap (full analysis done 2026-06-30)
For `γ₁₀ ≠ 0`, `γ • ∞ = a/c` is a **finite** cusp. After slashing, `(E_f∘↑)∣[2-k]γ` is the `∞`-Eichler integral of the **conjugate** cusp form `g := f ∣[k] γ` (a `CuspForm` for the arithmetic group `γ⁻¹ • ((Gamma1 N).map (mapGL ℝ)) • γ`, cusp width `h ≠ 1`) **plus a degree-`≤ k-2` period polynomial** `P` in `τ`. Rigorously established (see `eichler_slashSL_bdd_finite`'s in-tree docstring): there is **no** single-conjugate-cusp-value formula for the general-`γ` defect — the proven `eichler_defect_eq_cuspValueMoving` collapses to one cusp value **only** for `γ ∈ Γ₁(N)` (where `f∣γ = f`); for general `γ` the honest defect carries the extra `C_k·(H(τ) − G(τ))` term, `H,G` primitives of the **distinct** period forms `periodForm f` and `periodForm (f∣γ)`, which IS the period polynomial `P`. Both `E_g`-boundedness and `P = 0` need conjugate-frame machinery:
  - **`P = 0` (period polynomial vanishing) IS dischargeable**: `ι(f)=0 ⟹ ι(f∣γ)=0` via the EXISTING `PeriodHecke.rawPairingGen_actMat` (`rawPairingGen (CuspForm.translate f (glMap (heilbronnGL M))) x = rawPairing f (actMat M x)`), specialised to `M = γ ∈ SL(2,ℤ)` (det 1, via `heilbronnGL_sl`), so `rawPairing f = 0 ⟹ rawPairingGen (f∣γ) = 0`. Combined with a generic `eichler_basepoint_indep_gen` (a near-verbatim port of `eichler_basepoint_indep` using `cuspValueGen`/`cuspFunctionalGen`/`rawPairingGen_tmul`, all present), the conjugate-form cusp-value differences vanish.
  - **`E_g`-boundedness (the analytic core) is GENUINELY ABSENT**: it needs the Eichler-integral construction for `g` at its own cusp width `h ≠ 1` — the width-`h` analogues of `eichlerCoeff`/`eichlerQSeries`/`eichlerIntegral`/`eichler_cusp_holo` — *and* the identification `(E_f∘↑)∣[2-k]γ = E_g + P`. Neither mathlib nor the project has a width-`h` Eichler integral or this slash-decomposition.
- **Alternative route (also absent)**: `h := (E_f∘↑)∣[2-k]γ` is width-`N` periodic (`eichler_slash_invariant` + `hf`, since `γ Tᴺ γ⁻¹ ∈ Γ(N) ⊆ Γ₁(N)`) and `D^{k-1}h = (f∣γ)∘↑` (**slashed Bol's identity (10)**, ABSENT from project/mathlib); then the Laurent expansion of `cuspFunction N h` has no negative powers (matching the holomorphic-at-`0` `cuspFunction N (f∣γ)` via the `q d/dq` correspondence and a **meromorphic-order-under-`q d/dq` removable-singularity** lemma, also ABSENT). Either route is a self-contained complex-analytic development beyond the present file.
- **Recommended**: build the width-`h` conjugate-form Eichler integral (generalise EICH-1 to an arbitrary arithmetic `CuspForm` + strict period `h`) and the `E_g + P` slash-decomposition; discharge `P = 0` via `rawPairingGen_actMat` (already proven) + `eichler_basepoint_indep_gen`. OR prove slashed-Bol (10) + the meromorphic-order Laurent lemma.
#### Mathlib/project lemmas
PROVEN & reusable: `PeriodHecke.rawPairingGen_actMat`, `heilbronnGL_sl`, `cuspValueGen`/`cuspFunctionalGen`/`cuspValueGen_eq_cuspValue`/`rawPairingGen_tmul`/`cuspValueGen_{add,smul}_right`, `CuspForm.translate` (+ `CuspFormClass.zero_at_infty_slash` = `b₀=0` for `f∣γ`), `rawPairing_eq_zero_of_periodMap'_zero`, `span_range_castSymPow_eq_top`, `eichler_cusp_holo`/`eichler_slash_invariant`, the EICH-1 summability helpers, `Function.Periodic.cuspFunction` + `Mathlib.Analysis.Complex.Periodic`, `MeromorphicAt`/`meromorphicOrderAt` + `tendsto_nhds_iff_meromorphicOrderAt_nonneg`. ABSENT: width-`h` Eichler integral; the `(E_f∘↑)∣[2-k]γ = E_g + P` slash-decomposition; slashed Bol's identity (10); `meromorphicOrderAt (q d/dq F) = meromorphicOrderAt F`.
#### Sources
reply.md §1.3 (8) (verbatim, see EICH-3a Sources); reply.md §2 (9)–(10) for the Bol-identity alternative.

### [CLEANUP-EICH-1] Run /cleanup on EichlerInjective.lean (after 3 proof tickets)
- **Status**: deferred (AINTLIB producer rules — cleaning/golf done on central main, not on dev producer branch) · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-3 · **Parallel**: no · **Type**: cleanup
- Cadence rule: 3 proof tickets (EICH-1,2,3) on the file → cleanup before building further. NOT run here per producer rules.

### [EICH-4] G4 — package `E_f` as `ModularForm Γ (2-k)`, then `E_f = 0` via the negative-weight / weight-0 vanishing
- **Status**: **DONE (2026-06-30)** — both `eichlerModularForm` and `eichler_eq_zero` are PROVEN, modulo the single `eichler_bdd_at_cusp` (EICH-3a) `sorry` consumed by `eichlerModularForm.bdd_at_cusps'`. **Implementation note (route taken ≠ the Δⁿ sketch below):** instead of the `E_f^{12}·Δⁿ` trick, used the cleaner two-case negative/zero-weight argument — `k>2`: `ModularForm.isZero_of_neg_weight` (weight `2-k<0`) gives `eichlerModularForm = 0` directly; `k=2`: `ModularForm.eq_const_of_weight_zero` (weight `2-k=0`, `Γ` arithmetic) gives `E_f≡c`, and the new helper `eichler_isZeroAtImInfty` (`E_f→0` at `i∞`, the zero-at-∞ strengthening of `eichler_cusp_holo`, hf-free) forces `c=0`. The OFF-ℍ extension to the full `eichlerIntegral f = 0 : ℂ→ℂ` is via `q`-expansion-coefficient uniqueness (`ModularFormClass.qExpansion_coeff_unique`): the form is `0` so its cuspFunction/qExpansion vanish ⟹ every `eichlerCoeff f m = 0` ⟹ the `q`-series is `0` everywhere. · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-2, EICH-3a · **Type**: def + lemma

#### Statement
```lean
def eichlerModularForm (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) : ModularForm ((Gamma1 N).map (mapGL ℝ)) (2 - k) := sorry

theorem eichler_eq_zero (f : CuspForm ((Gamma1 N).map (mapGL ℝ)) k) (hk : 2 ≤ k)
    (hf : periodMap' N k hk f = 0) : eichlerIntegral f = 0 := by sorry
```
#### Proof sketch (reply.md §1.4)
1. **`eichlerModularForm`**: assemble the structure — `holo'` from `differentiableOn_eichlerIntegral` (EICH-1), `slash_action_eq'` from `eichler_slash_invariant` (EICH-2), `bdd_at_cusps'` from **`eichler_bdd_at_cusp` (EICH-3a, all cusps)** — NOT `eichler_cusp_holo`, which only gives the cusp-`∞` shadow. Weight `2-k ≤ 0` typechecks (`ModularForm Γ w` any `w : ℤ`). **The exact field requirement** (mathlib `ModularForm.bdd_at_cusps' {c : OnePoint ℝ} (hc : IsCusp c Γ) : c.IsBoundedAt toFun k`) reduces, since `(Gamma1 N).map (mapGL ℝ)` is arithmetic, via `Subgroup.IsArithmetic.isCusp_iff_isCusp_SL2Z` + `OnePoint.isBoundedAt_iff_forall_SL2Z`, to: `∀ γ : SL(2,ℤ), IsBoundedAtImInfty ((E_f ∘ ↑) ∣[2-k] γ)` — exactly `eichler_bdd_at_cusp` (EICH-3a). · **Depends on**: EICH-2, EICH-3, **EICH-3a**, CLEANUP-EICH-1.
2. **`eichler_eq_zero`**: form `g := (eichlerModularForm f hk hf)^12 * (ModularForm.discriminant)^((k-2).toNat)` — weight `12(2-k) + 12(k-2) = 0`, a `ModularForm Γ 0`. It vanishes at a cusp (Δ does). `ModularForm.eq_const_of_weight_zero` (Γ arithmetic) ⟹ `g` constant; vanishing at a cusp ⟹ `g = 0`. Since `ModularForm.discriminant_ne_zero` (Δ ≠ 0 on ℍ), `(eichlerModularForm …)^12 = 0` pointwise on ℍ ⟹ `eichlerIntegral f = 0` on ℍ (and off ℍ by the `ofComplex` convention / the def).
#### Mathlib lemmas needed
`ModularForm.discriminant`, `ModularForm.discriminant_ne_zero`, `ModularForm.eq_const_of_weight_zero` (`[𝒢.IsArithmetic]`); `ModularForm` mul/pow instances; `pow_eq_zero_iff`. Project: `IsArithmetic` instance for `(Gamma1 N).map (mapGL ℝ)` (via `.conj`/`.map`).
#### Sources
reply.md §1.4 (verbatim in decomposition G4); the Δⁿ-trick routes around level-1-only negative-weight vanishing.
#### Generality
`k : ℤ`, `2 ≤ k` (so `n = (k-2).toNat`, `2-k ≤ 0`). k=2 ⟹ n=0 ⟹ `Δ^0=1`, `E_f` already weight 0 (handle the degenerate exponent).

### [EICH-5] R — `periodMap'_injective` via the Eichler route (replaces the boundary route)
- **Status**: **DONE (2026-06-30)** — `periodMap'_injective_eichler` PROVEN (modulo EICH-3a). **Re-point done in `HeckeAlgFiniteFinal.lean`** (NOT `PeriodInjective.lean`: that would be a circular import, since `EichlerInjective` imports `PeriodInjective` for `periodMap'`/`rawPairing_eq_zero_of_periodMap'_zero`): the `k≥2` branch of `heckeAlgℤ_finite` now passes `ModularSymbols.periodMap'_injective_eichler hk` (import of `EichlerInjective` added). **Axiom check (verified):** `periodMap'_injective_eichler` traces to `{propext, Classical.choice, Quot.sound, eichler_bdd_at_cusp}` ONLY — no `interior_edges_cancel_sum`, no other `sorryAx`. So `heckeAlgℤ_finite` (k≥2) is fully detached from the Stokes route. The old `periodMap'_injective` (Stokes) is left untouched/unused on this branch. · **File**: ModularSymbols/EichlerInjective.lean (+ re-point HeckeAlgFiniteFinal.lean) · **Depends on**: EICH-1, EICH-4 · **Type**: theorem (milestone)

#### Statement
```lean
theorem periodMap'_injective_eichler (hk : 2 ≤ k) :
    Function.Injective (periodMap' (N := N) (k := k) hk) := by sorry
```
Then re-point `periodMap'_injective` (PeriodInjective.lean:164) to this (so `heckeAlgℤ_finite` k≥2 no
longer consumes `interior_edges_cancel_sum`).
#### Proof sketch
1. `rw [← LinearMap.ker_eq_bot, LinearMap.ker_eq_bot']; intro f hf`. 
2. From `hf : periodMap' N k hk f = 0`: `eichler_eq_zero` (EICH-4) ⟹ `eichlerIntegral f = 0`.
3. `bol_iterated_eichler` (EICH-1): `f (ofComplex z) = ((2πi)⁻¹)^(k-1) · iteratedDeriv (k-1) (eichlerIntegral f) z = ((2πi)⁻¹)^(k-1) · iteratedDeriv (k-1) 0 z = 0` for all `z ∈ ℍ` ⟹ `f = 0` (cusp form determined by values on ℍ; `CuspForm.ext` + `iteratedDeriv_zero_fun`).
4. **Re-point**: replace `periodMap'_injective`'s proof body with `exact periodMap'_injective_eichler hk` (or inline). VERIFY `#print axioms periodMap'_injective` no longer lists the `interior_edges_cancel_sum` sorry (only the EICH sorries until they close).
#### Mathlib lemmas needed
`LinearMap.ker_eq_bot'`, `iteratedDeriv_const` (of the zero function), `CuspForm.ext`. Project: `periodMap'`, `bol_iterated_eichler`, `eichler_eq_zero`.
#### Sources
reply.md §1 (the assembly); same STATEMENT as the existing `periodMap'_injective` (no drift).
#### Generality
`k : ℤ`, `2 ≤ k`. This is the milestone closing IHR-d for k≥2.

### [CLEANUP-EICH-2] Final /cleanup on EichlerInjective.lean + axiom re-check
- **Status**: deferred (AINTLIB producer rules — cleaning/golf on central main) · **File**: ModularSymbols/EichlerInjective.lean · **Depends on**: EICH-5 · **Parallel**: no · **Type**: cleanup
- Axiom re-check DONE (2026-06-30): `#print axioms periodMap'_injective_eichler` = {propext, Classical.choice, Quot.sound} (NO `interior_edges_cancel_sum`, NO sorryAx); `heckeAlgℤ_finite` k≥2 axiom-clean, lone sorryAx from k<2 `exists_HeckeStableLattice` only. The /cleanup pass itself is deferred to central main per producer rules.
