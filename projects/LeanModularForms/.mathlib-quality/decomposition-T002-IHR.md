# Decomposition — T002 `heckeAlgℤ_finite` (the integral Hecke-algebra finiteness)

*`/develop`, 2026-06-21. Sources read directly: Diamond–Shurman Ch. 6 (`/tmp/ds.txt`, agent a4834ad9);
mathlib recon (grep `.lake/packages/mathlib`). Goal: `instance heckeAlgℤ_finite : Module.Finite ℤ
(heckeAlgℤ N k)` — currently the single isolated `sorry` (HeckeFieldArithmetic.lean:143) on which all of
Tranche-1 (T004/T005, both PROVEN) rests.*

## Executive feasibility — RESEARCH-SCALE; the confidence gate does NOT pass

**T002's deep content is the existence of a Hecke-stable, full-rank, free ℤ-lattice `Λ` in
`S_k(Γ₁N)` (or its ℂ-dual).** Given `Λ`, finiteness is elementary (DS Ex 6.5.1: `heckeAlgℤ ↪
End_ℤ(Λ) ≅ M_r(ℤ)`, a sub-ℤ-module of a finite free module). **The lattice existence is the
irreducible import, and it is absent from mathlib at every layer** (`ModularSymbol`,
`EichlerShimura`, `ModularCurve` = 0 files; `FiniteDimensional (ModularForm …)` exists only for
level one). DS itself **cites it "without proof"** from Riemann-surface theory ([FK80]) and proves
only weight 2. **No bounded ticket board reaches it; it is a multi-month formalization.**

## What DS actually proves vs. black-boxes (weight 2, the source-faithful chain)

| Layer | Content | DS status | mathlib |
|---|---|---|---|
| **(a) deepest** | `H₁(X₁N,ℤ)` free f.g. rank `2g`, exists | **BLACK BOX** — "stated without proof … see [FK80]" (`ds.txt:16505-16552`) | absent (no modular curves / integral homology) |
| **(b)** | `H₁` is a **full-rank lattice** in `S₂^∨` (`Ω¹_hol ≅ S₂`, period non-degeneracy) | **MIXED** — `Ω¹≅S₂` proved (Ex 3.3.6 + Riemann–Roch, cited); **full-rank "stated without proof"** (`ds.txt:16569-16596`) | absent |
| **(c)** | Hecke acts on `H₁` by integer `2g×2g` matrices | **PROVED** (§6.2-6.3 push/pull functoriality, `ds.txt:18182-18194`) | n/a (needs the lattice) |
| **(d) endgame** | Cayley–Hamilton ⟹ alg-int eigenvalues (Thm 6.5.1 `ds.txt:18066-18082`); `End_ℤ(free r)≅M_r(ℤ)` ⟹ `T_ℤ` module-finite (Ex 6.5.1 `ds.txt:18385`) | **PROVED, elementary** | mathlib has it (`Module.Finite` of submodule of f.g. over Noetherian ℤ) |

Verbatim, the lattice black box (`ds.txt:16508-16541`): *"We state without proof that … the group of
integer sums of integrations over loops is the free Abelian group generated by … the Aᵢ and the Bᵢ.
(See a Riemann surface theory book such as [FK80] …)"*. The full-rank (`ds.txt:16569-16587`): *"We
also state without proof that `ℝ_{A₁}⊕…⊕ℝ_{Bg} = Ω¹_hol(X)^∧`."*

**Weight k (what `Newform N k` needs): DS offers NOTHING.** Exhaustive sweep — "Sym", "symmetric
power", "modular symbol", "Manin", "group cohomology", "parabolic", "Eichler–Shimura isomorphism" =
ZERO hits (only the unrelated mod-`p` Eichler–Shimura *relation*, Ch. 8). The weight-`k` integral
structure is `H¹(Γ₁N, Sym^{k-2}ℤ²)` (Manin modular symbols) — a different, absent apparatus.

## Three-tier isolation (this is the actionable structure)

- **Tier 0 (current state):** `heckeAlgℤ_finite` is the isolated `sorry`. Tranche-1 (T004/T005) proven on top. Clean.
- **Tier 1 — the lattice-endgame (BOUNDED, formalizable now):** isolate the *deeper, cleaner,
  SOURCE-MATCHING* input `exists_hecke_stable_lattice` and **PROVE `heckeAlgℤ_finite` from it**.
  This formalizes Shimura (3.5.20)+ρ₀ / DS Ex 6.5.1.

  **★ HARDENED per the source-faithfulness audit (agent ab638096): the input field is FULL-RANK
  (`span ℂ ↑Λ = ⊤`, = Shimura's "maximal rank" (3.5.20)), NOT the weaker "faithful". Faithfulness
  is DERIVED, never assumed — otherwise the structure could be satisfied by a tiny non-full-rank Λ
  with a stubbed faithfulness, hiding the real difficulty.** Shape:
  ```
  structure HeckeStableLattice (N k) where
    toSubmodule : Submodule ℤ (CuspForm ((Gamma1 N).map (mapGL ℝ)) k)
    free        : Module.Free ℤ toSubmodule
    finite      : Module.Finite ℤ toSubmodule
    spanning    : Submodule.span ℂ (toSubmodule : Set _) = ⊤        -- FULL RANK = the deep content (rationality)
    stable      : ∀ T ∈ heckeAlgℤ N k, ∀ x ∈ toSubmodule, (T : End ℂ _) x ∈ toSubmodule
  -- exists_HeckeStableLattice : Nonempty (HeckeStableLattice N k) := sorry   -- the Tier-2 deep import
  theorem heckeAlgℤ_finite (H : HeckeStableLattice N k) : Module.Finite ℤ (heckeAlgℤ N k) :=
    -- faithful DERIVED from `spanning`: T|_Λ = 0 → T = 0 on `span ℂ Λ = ⊤` → T = 0 (LinearMap ext on span)
    -- Φ : heckeAlgℤ →ₗ[ℤ] End_ℤ Λ, T ↦ ((T:End).restrictScalars ℤ).restrict H.stable; injective (faithful)
    -- End_ℤ Λ = (Λ →ₗ[ℤ] Λ) is Module.Finite ℤ (Λ finite free; `Module.Finite.linearMap`)
    -- Module.Finite.of_injective Φ Φ-injective
  ```
  Mathlib (verified): `Module.Finite.linearMap` (FreeModule/Finite/Matrix.lean:47, needs free+finite),
  `Module.Finite.of_injective` (Finiteness/Basic.lean:252), `LinearMap.restrict`/`restrictScalars`,
  `Submodule.span_induction`/`LinearMap.ext_on` (faithful from spanning). ~bounded (~100-150 LOC). This
  TIGHTENS the import from `heckeAlgℤ_finite` to `exists_HeckeStableLattice`: layer (c)+(d) PROVED, only
  the full-rank lattice (a)+(b) imported, matching Shimura (3.5.20)/Thm 3.52 exactly. **Gate PASSES for Tier 1.**
  ⚠ The faithfulness obligation must be a real derivation from `spanning`; never a `sorry`'d or `True`-like field.
- **Tier 2 — the lattice existence (RESEARCH-SCALE, gate does NOT pass):** prove
  `exists_hecke_stable_lattice`. Two routes:

  **Route A — modular-symbol / integral Eichler–Shimura (IHR-a..asm; DS's weight-2 route generalized).**
  Three research-scale leaves, EACH a multi-month sub-project on infrastructure absent from mathlib:
  - IHR-a: integral modular-symbol module `𝕄_k(Γ₁N,ℤ)` finite free via Manin presentation (Div⁰(ℙ¹ℚ)⊗Sym^{k-2}ℤ²,
    2-/3-term + parabolic relations). *Source: Manin 1972 / Shimura Ch.8 — NOT DS, NOT mathlib.*
  - IHR-c: period pairing `⟨f,{α,β}⊗P⟩=∫_α^β f(z)P(z,1)dz` — analytic (convergence via cusp decay, Hecke-equivariance).
  - IHR-d: injectivity `S_k ↪ 𝕄_k^∨⊗ℂ` (easy half of Eichler–Shimura).
  - IHR-asm = Tier-1 endgame (bounded).

  **Route B — integral q-expansion lattice (the CLEANER isolation).** `Λ := {f ∈ S_k(Γ₁N) : ∀n, aₙ(f) ∈ 𝒪}`
  (𝒪 = ℤ or ℤ[ζ_N] for the diamonds). Decomposes into:
  - **B1 (DEEP, the single research import):** `Λ` is FULL RANK, i.e. `Λ ⊗ ℂ = S_k(Γ₁N)` — the **rationality
    / q-expansion principle** (S_k has a ℂ-basis of integral-q forms). *Source: Shimura, Intro. Arith. Theory,
    Ch. 3; Katz, algebraic modular forms. Absent from DS (derived there FROM homology) and from mathlib.*
  - **B2 (BOUNDED):** `Λ` is Hecke-stable — via the project's EXISTING integer Fourier-Hecke formulas
    (`fourierCoeff_heckeT_n_period_one`, `heckeT_n_cusp_divN_coeff`, the eq-5.21 work just completed).
    Subtlety: the diamond `⟨d⟩` scales by `χ(d)` (root of unity) ⟹ work over `ℤ[ζ_N]`, or per-character.
  - B3 = Tier-1 endgame (bounded).

  **Route B isolates the deep input to ONE statement (B1, rationality)** + a bounded rest that mostly already
  exists in the project; Route A has THREE research-scale leaves. **Route B is the recommended isolation IF
  Tier-2 is ever attempted.** (Both B1 and Route-A leaves are genuinely research-scale; neither is a bounded
  ticket.) NB: this is NOT the same "q-expansion route" rejected in `decomposition-group-a-finish.md` — there it
  was rejected as *DS-faithful evidence*; here it is the cleanest *isolation of the irreducible import*, with its
  deep leaf (B1) carrying a real (non-DS) source.

## Attacks (adversarial)
1. *"Maybe finiteness follows from `dim_ℂ S_k < ∞` alone."* → FALSE: a ℤ-subalgebra of a fin-dim ℂ-algebra
   need not be module-finite (`ℤ[1/2] ⊆ ℂ`). Integrality of `T_p` over ℤ ⟺ alg-int eigenvalues = the deep content. SURVIVES.
2. *"Maybe the q-expansion lattice's Hecke-stability is the deep part."* → NO: the Fourier-Hecke formulas have
   integer coefficients (project has them); the deep part is FULL RANK (rationality, B1), not stability. SURVIVES.
3. *"Maybe mathlib's level-one `FiniteDimensional (ModularForm 𝒮ℒ k)` generalizes cheaply."* → It gives ℂ-dimension
   finiteness (needed, and the project's DimGenCongLevels has Γ₁N dimension), NOT an integral lattice. Orthogonal. SURVIVES.
4. *"Route B avoids modular symbols, so it's bounded."* → NO: B1 (rationality) is itself research-scale (Katz/Shimura,
   absent). Route B only WINS on isolation cleanliness (1 deep leaf vs 3), not on removing the deep import. SURVIVES.

## Recommendation
- **Default: stay at Tier 0** (`heckeAlgℤ_finite` isolated; Tranche-1 proven). Honest, clean, axiom-profile documents the single import.
- **Optional bounded step: Tier 1** — formalize `exists_hecke_stable_lattice ⟹ heckeAlgℤ_finite` (DS Ex 6.5.1 endgame, ~80-150 LOC, gate passes). Tightens the import to the mathematically-irreducible "lattice existence".
- **Tier 2 (lattice existence) is a multi-month research formalization** — Route B (q-expansion/rationality) for cleanest isolation, or Route A (modular symbols) for the DS-faithful weight-2 route generalized. Each would be its own multi-`/develop` sub-project building modular-curve / integral-homology / rationality infrastructure from scratch. The `/develop` confidence gate does NOT pass for Tier 2; tickets are NOT created for it here.
