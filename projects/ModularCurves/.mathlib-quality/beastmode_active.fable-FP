FOCUS: [KL-3] GRIND (coordinator override). Attack: 858 gap-truncation + 846 00MW-split (clean buchsbaumEisenbud_acyclic) → local-criterion Tier-A1 spawn → local_criterion_twoTerm → coker_flat → fibreExact_spreads → reduce_to_polynomial(ULift) → viaBE → wire.
