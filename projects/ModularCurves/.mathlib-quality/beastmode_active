FOCUS (2026-08-09): **AP-D7 IS CLOSED. The Katz–Mazur construction of the relative Weil pairing
is COMPLETE except for AP-D4 `⊇`.** Commit c6281f9d5, pushed. `lake build ModularCurves` green.

READ FIRST: `.mathlib-quality/HANDOVER-2026-08-09-ds4-weil-pairing.md` — the full state of the
line, written to be the only document needed. Kickoff message for a new worker:
`.mathlib-quality/HANDOVER-MESSAGE-2026-08-09.md`.

=== THE ONE REMAINING TARGET ===
`exists_torsionPoint_of_mem_kerMulByN` — `WeilPairing/KMPairing.lean:302` — AP-D4 `⊇`.
It is Abel's theorem in two halves (rigidity `κ(Q)=1 → Q=0`, and surjectivity onto `kerMulByN`),
and it additionally needs a **fibrewise degree function on `Pic`** that the tree does not have.
Escape hatch already proved: `exists_torsionPoint_of_mem_kerMulByN_of_surjective` carries both
halves as hypotheses and is axiom-clean.

=== NEXT ACTIONABLE STEP (handover §6.3 — the important one) ===
1. Fix the stale docstrings (handover §8). Worst offender: `torsionSplittingEval_add`'s docstring
   in `KMBilinear.lean` still says "OPEN, `sorry`" and lists four bricks that now all exist.
2. Then: `injective_of_lTensor_residueField_injective_sModule`
   (`ForMathlib/LocalFlatnessCriterion.lean:456`) + its private plumbing at `:473`. Their
   docstrings already contain the COMPLETE proof (Artin–Rees induction `K ⊆ 𝔪ᴿⁿ•N`, then Krull);
   the mathlib API is confirmed present. The `R`-substrate and `R`-free forms in that same file
   are already proved sorry-free — only the `S`-module variant is open.
   *** BUT VERIFY THE CHAIN FIRST. *** I established that `exists_section_of_degree_one` and
   `relEffCartierDiv_of_isIso_subschemeι` are mutually inverse, so Blocker 4's genuine content is
   flatness of the vanishing subscheme = Stacks 00ME. I did NOT verify end-to-end that closing
   :456/:473 discharges `evalGenerator_mem_nonZeroDivisors` (`AbelEquivalence.lean:848`) and thence
   Blocker 4 (`:971`). The board's étale-transversality route (Stacks §37.38/055S) is probably a
   detour, but that is a hypothesis, not a verified fact.
3. Then `AbelEquivalence.lean` :971 / :994 / :1013, then the two Abel halves, then Group E.

=== NEW AND REUSABLE (from the AP-D7 closure) ===
`WeilPairing/Translation.lean` — `unitPullback` / `unitPullback_congr` phrase unit pullback through
`Scheme.Hom.appLE`, putting the open-comparison in a `Prop` argument so `subst` + proof irrelevance
replaces dependent rewrites. **Reach for this before writing any dependent open transport.** Also
`translateByPoint` (+ `_comp_snd`, `comp_`, `_comp_mulByN`), `preimage_translateByPoint_mulByN`,
`eq_mul_globalTwist_of_translate`.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone; NEVER `git add -A` while a subagent is mid-run; run a full
`lake build ModularCurves` before adding any import (it is the clash detector). Some `ForMathlib/`
modules are orphans that `lake build ModularCurves` skips — build them explicitly.
