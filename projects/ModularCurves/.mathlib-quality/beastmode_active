FOCUS: route beta item (A) step 1 — asSection_pull_basisComb + basisComb_comp_mulByHom landed in WeilPairing/FullLevelBaseChange.lean (leaf importing GammaHRepresentability; import question SETTLED). Remaining: calibrate the last two steps of fullLevelHom_pullAlong against the actual goal (comp_pointToTorsion_of_eq arguments must be explicit) — recipe is in that file.
