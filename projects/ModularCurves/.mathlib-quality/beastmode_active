FOCUS (2026-08-09): **THE DS4 LEAF IS CLOSED.** `exists_invertible_tensor_idealModule_add`
(`Picard/SelfAdjointN.lean:267`) is PROVED, axiom-verified standard-three — the relative theorem of
the square, the single classical leaf under (★)/(★′) and hence under the Katz–Mazur construction of
the relative Weil pairing. Downstream `kappa_add`, `exists_pic_map_snd_sectionCls_add`,
`kappa_zsmul` have all lost their `sorryAx`.

Chain closed this session, every link axiom-clean:
  Stacks 0FWG (`ForMathlib/ReducedConstantRankFree.lean`, a mathlib gap) → Mumford §5
  (`ForMathlib/ConstantKernelRankProjective.lean`) → the rank-one seesaw (`ForMathlib/Seesaw.lean`,
  `SeesawGlobalBase.lean`) → the field-level theorem of the square
  (`Picard/PrincipalIdealModuleIso.lean`, `Picard/IdealModuleMul.lean`,
  `WeilPairing/TheoremOfSquareField.lean`) → transport (`TheoremOfSquareFibrewise.lean`,
  `FibreWeierstrassPresentation.lean`) → universal pair (`TheoremOfSquareUniversal.lean`) →
  base change + gluing (`TheoremOfSquareBaseChange.lean`, `ChartFromUniversalPair.lean`,
  `ChartGroupSum.lean`).

=== NEXT TARGET: `exists_pic_map_snd_picMap_mulByHom_kappa` (`Picard/SelfAdjointN.lean:503`) ===
The remaining sorry in that file, and the last one on the (★)/(★′) line. Its docstring (:465-482)
says it is **NOT an independent leaf** — it is a formal consequence of the now-PROVED leaf, by
symmetry of the normalized Poincaré bundle:
  put `𝒜 = 𝒪(D_0) ⊗ f^*(0^*𝒪(D_0))⁻¹` and `𝒫 = m^*𝒜 ⊗ p₁^*𝒜⁻¹ ⊗ p₂^*𝒜⁻¹` on `E_T ×_T E_T`;
  `𝒫` is trivial on both axes and SYMMETRIC (`τ^*𝒫 ≅ 𝒫`, because `m ∘ τ = m`);
  `(id, −Q)^*𝒫 ≅ κ(Q)`; the leaf base-changed to `E_T ×_T E_T` with the two universal sections says
  `𝒫` is additive in its second variable; symmetry upgrades that to the first; hence
  `([N] × id)^*[𝒫] = [𝒫]^N`, and restricting along `(id, −Q)` gives the statement.
The docstring calls this "bookkeeping, not new mathematics" — it needs `𝒫` and its symmetry built.
`picMap_mulByHom_kappa_pow` (:490) and `picMap_mulByHom_kappa_eq_one` (:497) consume it and are
otherwise proved, so closing it clears the whole file.

=== TWO COORDINATOR ITEMS (cross-cutting renames — NOT producer work, but they bite here) ===
1. `ModularCurves.idealModule` (`EllipticCurve/PoleSheaf.lean:154`, ideal module of a MORPHISM)
   SHADOWS `AlgebraicGeometry.Scheme.Modules.idealModule` (`Picard/IdealModule.lean:156`, of an
   IDEAL SHEAF) inside `namespace ModularCurves`. Importing `ChartGroupSum` made 518 modules newly
   reachable and broke every bare `idealModule` in `SelfAdjointN.lean`. Worked around by ONE
   semantics-preserving line after `namespace ModularCurves`:
   `local notation "idealModule" => AlgebraicGeometry.Scheme.Modules.idealModule`.
   Durable fix = rename `ModularCurves.idealModule`.
2. `sectionVanishingIdeal` was the same failure mode and was silently orphaning the ENTIRE Seesaw
   line; fixed this session (renamed to `sectionVanishingIdealSheaf`).
**Run a full `lake build ModularCurves` before adding any import — it is the clash detector, and a
conclusion-grep has twice missed what it caught.**

=== HOUSEKEEPING ===
* STALE DOCSTRING: `SelfAdjointN.lean:14` still says "One sorry left
  (`exists_invertible_tensor_idealModule_add`)". That leaf is closed; the remaining sorry is :503.
* Cleanup debt: four primed theorems in `RelPicLocal.lean` duplicate ~370 lines of the unprimed
  ones, which are now thin specialisations. `isIso_idealGenHom`'s proof block is copy-pasted in
  three places. `WeilPairing/TensorSection.lean` is a DEAD module whose `tensorSection` family
  full-name-collides with two other copies.
* `HasHighCechExactnessOpens` (arbitrary-base seesaw) is untouched — it quantifies over non-affine
  opens where the two-chart discharge does not reach. Reason recorded in its module docstring.

=== STANDARDS THAT PAID OFF — KEEP THEM ===
* **Verify fit by ELABORATION, not assertion.** Seven consecutive passes shipped a scratchpad probe
  applying the result as a black box from an independent file; it repeatedly caught real mismatches.
  The last two added `fail_if_success` checks proving a gap was real, and tests over a CONCRETE
  non-reduced base — that standard is now expected.
* **Full `lake build ModularCurves` as the duplicate/clash detector** before writing.
* **My boarded routes were wrong SEVEN times** (six over-engineered, once under-engineered), usually
  because I anchored on a nearby proved theorem's shape instead of reading what the statement needs.
  Tell the worker to trust the statement over the sketch and to report when it does.

DEAD ENDS — LOGGED, DO NOT REVISIT:
  * `ProjIsPrincipal` / `kappaDivisor_add_linEquiv` is UNSOUND over non-closed fields
    (`b2_log.jsonl`, T10-asm) — `projectiveDivisorOf` sees only `k`-rational points. It is no longer
    needed at all: the field theorem is now proved by the explicit chord-and-vertical route.
  * `WeilPairing/LineVertical.lean` is NOT about the chord and the vertical despite its header.
  * "cover, generators and ratio must be produced together" was a WRONG DIAGNOSIS.
  * Recovering the chord/vertical shape from the ideal identity alone is BLOCKED — it needs the
    coordinate ring to be Dedekind. Split the call site instead.

REUSABLE BY-PRODUCTS FROM THIS SESSION:
  * `OverlapRel W b a j` (`b = a·tʲ` on the `Y ⊓ Z` overlap) with `.mul/.sub/.add/.pow/.cancel` and
    base cases — the overlap dictionary this line has wanted for a long time. Plus `germY`/`germZ`
    with injectivity, `infChartOpen`, `projModelZero_ker_ideal_infChartOpen`.
  * `ker_ideal_of_fromSpec_factor` — replaces the whole `appLE` apparatus whose helpers are
    `private` in `PoleFiltration.lean`.
  * mathlib's `XYIdeal_mul_XYIdeal` IS the affine-chart theorem of the square.
  * `isIso_idealGenHom`, now named (copy-pasted in three other places — cleanup ticket).

BLOCKER 4 (independent, does NOT gate AP-D): `IsIso ((sectionVanishingIdeal M hM σ).subschemeι ≫ π)`
at `EllipticCurve/AbelEquivalence.lean`. Route REPLACED after review: étale transversality
(`dh̄ ≠ 0` on the smooth fibre ⟹ `h : U → 𝔸¹_S` étale ⟹ `Z` finite étale of degree one ⟹ iso;
Stacks §37.38 / 055S — prove the special case from a standard-smooth Jacobian presentation, since the
general slicing lemma routes through fibrewise flatness). The old nonzerodivisor route IS Stacks
00MF and does not dodge Artin-Rees; producing the section from fibre points is not viable.
Hypothesis audit: `h⁰ = 1` and `H¹ = 0` alone are NOT enough — also needs smooth geometrically
integral genus-one fibres, `deg M_s = 1`, and the base-change iso giving `σ_s ≠ 0`.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone. **NEVER `git add -A` while a subagent is mid-run** (it swept two
scratch files in this session). Some `ForMathlib/` modules are ORPHANS that `lake build ModularCurves`
skips — build them explicitly.
