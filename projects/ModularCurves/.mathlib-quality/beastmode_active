FOCUS: [G2/hnorm] — the normalisation: read KMDataset exists_normalized_dataset (KMDataset:220) signature + its consumers to determine whether the glue NEEDS a fresh hnorm-proof for the chart-family or the normalisation-replay rescales any family (in which case G2 = apply it to the G1-family and TRACK the rescalers through the dressed transitions — the a/b-absorption); then 3c-iv div-germ statement; then G3
