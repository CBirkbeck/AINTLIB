FOCUS: route beta — fullLevelSqIso_inv_baseChange LANDED (the square base-change). NEXT: identify the kernel-pair transition (combine it with fullLevelHom_eq_of_levelCoord over the piece) to get the reading relation det w = det g * det v; then only WP-D3d remains.
