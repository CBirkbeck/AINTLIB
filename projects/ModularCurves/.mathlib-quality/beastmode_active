FOCUS (2026-08-09): **THE FIELD-LEVEL THEOREM OF THE SQUARE IS PROVED, UNCONDITIONAL AND SORRY-FREE.**

`ModularCurves.nonempty_tensorObj_idealModule_add_field`
(`WeilPairing/TheoremOfSquareField.lean`) : `I(P) ⊗ I(Q) ≅ I(P+Q) ⊗ I(O)` on the projective
Weierstrass model over ANY field. Axiom-verified standard-three. Zero sorries in that file.

Closed this session, all axiom-clean:
  * BLOCKER 1 + BLOCKER 2 — the rank-one seesaw (`ForMathlib/Seesaw.lean`, `SeesawGlobalBase.lean`),
    proved WITHOUT cohomology-and-base-change, on the back of Stacks 0FWG
    (`ForMathlib/ReducedConstantRankFree.lean`) which mathlib lacks.
  * The field-level bridge: `Picard/PrincipalIdealModuleIso.lean` (principal divisor ⟹ ideal modules
    isomorphic, arbitrary scheme), `Picard/IdealModuleMul.lean` (divisor sum ⟹ tensor product), and
    `WeilPairing/TheoremOfSquareField.lean` (composition + both charts).

=== CURRENT TARGET: BLOCKER 3 — `exists_invertible_tensor_idealModule_add`
(`Picard/SelfAdjointN.lean:267`). What remains is TRANSPORT, not new mathematics. ===

**STEP 1 — DONE (2026-08-09), hfib IS NOW HYPOTHESIS-FREE.**
  * `WeilPairing/TheoremOfSquareFibrewise.lean` — `nonempty_pullback_discrepancy_iso_unitObj_of_field`,
    the discrepancy `Δ = (I(D_R) ⊗ I(D_Z)) ⊗ N` trivial on every field-valued fibre, in exactly the
    seesaw's `hfib` shape. Discrepancy form chosen because it is what
    `SelfAdjointN.exists_invertible_tensor_idealModule_add_of_discrepancy_trivial` consumes.
  * `WeilPairing/FibreWeierstrassPresentation.lean` — `nonempty_pullback_discrepancy_iso_unitObj_projModel`
    and `..._of_point`: the Weierstrass presentation of the fibre is DERIVED, so no presentation
    hypothesis remains, for the tautological family `projModelπ W₀` at arbitrary `x : Spec k ⟶ Spec A`.
  Both axiom-clean, zero sorries. **THE FIT IS VERIFIED BY ELABORATION** — three scratchpad probes
  call `exists_pullback_iso_of_fibrewise_trivial_of_isReduced`, its `_of_affineCover` variant, and
  the raw binder shape, and compile. KEEP THAT STANDARD for every remaining transport step.
  No `[DecidableEq k]` leaks into the binder (`classical` is internal).

  TWO ROUTE CORRECTIONS FOUND HERE, do not re-derive:
   * Going through `projModelPointsAddEquiv` / `chartAffinePointEquiv` for the additive matching is a
     TRAP — it forces comparing `projModelPointsEquiv` across TWO base rings (`A` and `k`), and that
     base-change compatibility does not exist in the tree. Use `mulModelHom_map`
     (`EllipticCurve/GroupLawConstruction.lean:853`) + `mulModelHom_specPoints`
     (`EllipticCurve/AdditionSpecPoints.lean:1776`) instead — they match additively entirely OVER `k`
     at the level of scheme morphisms, via the universal property of the base-change square. Zero leg:
     `projModelZero_baseChange`.
   * `WeierstrassAtlasBundle.lean:44`'s `e : … ≅ projModel (W i)` is a STRUCTURE FIELD (hypothesised
     data), not a proof of the presentation.

  STILL OPEN on this sub-line: transport to a family only ISOMORPHIC over the base to a `projModel`,
  and a non-`Spec` affine `S` (the `Scheme.SpecToEquivOfField` / `S.isoSpec` step).

DETECTION NOTE, earned twice: a conclusion-grep CAN miss a duplicate that spells its sections
differently (`Limits.pullback.lift (t ≫ z₁) (𝟙 T') _` vs `sectionBaseChange`). The detector that
worked was a full `lake build ModularCurves` — the environment NAME CLASH. Run it before writing.

(historical) STEP 1 — `hfib` from the field theorem. At `x : Spec k ⟶ pairBase π`, split with
`nonempty_pullback_tensorObj` (`Picard/PullbackTensorObj.lean:61`), rewrite each factor with
`nonempty_pullback_idealModule_ker_sectionBaseChange` (`Picard/IdealModulePullback.lean:387` —
stated along `Limits.pullback.fst π t`, SYNTACTICALLY the morphism in `hfib`), identify the fibre by
`isPullback_projModelBaseChange`, identify the four sections by `projModelPointsAddEquiv`
(`EllipticCurve/MulByHomDegree.lean:65`), then apply
`nonempty_tensorObj_idealModule_add_field`. ALL of these are proved.

STEP 2 — the universal pair. `WeilPairing/TautologicalPair.lean` (sorry-free) has `pairBase`,
`pairCurve`, `tautPoint₁/₂`, and `pairClassify` (:79) = "every pair of sections is a base change of
the universal pair", already proved. Reducedness/integrality of `pairBase` is
`EllipticCurve/GroupLawAxioms.lean:129/:132` (exact spelling match).
SPELLING TRAP: the fibre-cube instances at `:136/:147` and `:142/:152` are NOT literally
`pairCurve`'s spelling — they agree by `pullback.condition`, but instance search will not see it.

STEP 3 — apply the seesaw. Use `exists_pullback_iso_of_fibrewise_trivial_of_isReduced_of_affineCover`
(`ForMathlib/SeesawGlobalBase.lean`) — it takes an ARBITRARY base, which is why T8b was done: `B` is
integral but not affine. Then pull back along the zero section to kill `N`.

STEP 4 — discharge the seesaw's `hhigh` for the elliptic family. It is `H^{≥2}(X_s, M_s) = 0`,
automatic because the fibres are curves. Tools:
`subsingleton_H_add_two_of_two_affine_open_cover` +
`HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
over `q ≥ 1` (the model is `kernel_data_of_hasDegreeOneFibreCohomology`, but only for `q ≥ 1`).

STEP 5 — base-change down to an arbitrary, possibly NON-REDUCED base, stated and glued
ZARISKI-LOCALLY on `T` (moduli-stack issue globally; this is old ticket T9b, largely covered by
`pairClassify`). `RelEffCartierDiv.sectionDivisor_baseChange` (`EllipticCurve/PoleSheaf.lean`) is
part of the bridge.

DEAD ENDS — LOGGED, DO NOT REVISIT:
  * `ProjIsPrincipal` / `kappaDivisor_add_linEquiv` is UNSOUND over non-closed fields
    (`b2_log.jsonl`, T10-asm) — `projectiveDivisorOf` sees only `k`-rational points. It is no longer
    needed at all: the field theorem is now proved by the explicit chord-and-vertical route.
  * `WeilPairing/LineVertical.lean` is NOT about the chord and the vertical despite its header.
  * "cover, generators and ratio must be produced together" was a WRONG DIAGNOSIS.
  * Recovering the chord/vertical shape from the ideal identity alone is BLOCKED — it needs the
    coordinate ring to be Dedekind. Split the call site instead.

REUSABLE BY-PRODUCTS FROM THIS SESSION:
  * `OverlapRel W b a j` (`b = a·tʲ` on the `Y ⊓ Z` overlap) with `.mul/.sub/.add/.pow/.cancel` and
    base cases — the overlap dictionary this line has wanted for a long time. Plus `germY`/`germZ`
    with injectivity, `infChartOpen`, `projModelZero_ker_ideal_infChartOpen`.
  * `ker_ideal_of_fromSpec_factor` — replaces the whole `appLE` apparatus whose helpers are
    `private` in `PoleFiltration.lean`.
  * mathlib's `XYIdeal_mul_XYIdeal` IS the affine-chart theorem of the square.
  * `isIso_idealGenHom`, now named (copy-pasted in three other places — cleanup ticket).

BLOCKER 4 (independent, does NOT gate AP-D): `IsIso ((sectionVanishingIdeal M hM σ).subschemeι ≫ π)`
at `EllipticCurve/AbelEquivalence.lean`. Route REPLACED after review: étale transversality
(`dh̄ ≠ 0` on the smooth fibre ⟹ `h : U → 𝔸¹_S` étale ⟹ `Z` finite étale of degree one ⟹ iso;
Stacks §37.38 / 055S — prove the special case from a standard-smooth Jacobian presentation, since the
general slicing lemma routes through fibrewise flatness). The old nonzerodivisor route IS Stacks
00MF and does not dodge Artin-Rees; producing the section from fibre points is not viable.
Hypothesis audit: `h⁰ = 1` and `H¹ = 0` alone are NOT enough — also needs smooth geometrically
integral genus-one fibres, `deg M_s = 1`, and the base-change iso giving `σ_s ≠ 0`.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone. **NEVER `git add -A` while a subagent is mid-run** (it swept two
scratch files in this session). Some `ForMathlib/` modules are ORPHANS that `lake build ModularCurves`
skips — build them explicitly.
