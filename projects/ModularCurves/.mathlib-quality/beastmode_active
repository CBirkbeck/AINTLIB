FOCUS (2026-08-08): **BLOCKERS 1 AND 2 ARE BOTH CLOSED. THE RANK-ONE SEESAW IS PROVED.**

`ForMathlib/Seesaw.lean` is SORRY-FREE and axiom-verified standard-three:
`exists_pullback_iso_of_fibrewise_trivial_of_isReduced` (Stacks 0EX7 at rank 1) — an invertible
sheaf on a proper flat family of finite presentation over a REDUCED affine base, trivial on every
fibre, is pulled back from the base. Proved WITHOUT cohomology-and-base-change, which mathlib lacks.

The chain, all axiom-clean:
  KM-SEESAW-1′ (`ForMathlib/Seesaw.lean`) — fibrewise rank one
  → Stacks 0FWG, local + global (`ForMathlib/ReducedConstantRankFree.lean`) — THE MATHLIB GAP, filled
  → Mumford §5 constant-kernel-rank (`ForMathlib/ConstantKernelRankProjective.lean`)
  → Half B.1 (`ForMathlib/SeesawPushforwardInvertible.lean`) — `π_*M` invertible
  → Half B.2 (`ForMathlib/SeesawBaseLocallyTrivial.lean`) — `M` base-locally trivial
  → Half A (`WeilPairing/RelPicLocal.lean`, primed generalisations) — descent
  → KM-SEESAW-2″ and the top-level assembly.

=== CURRENT TARGET: BLOCKER 3 — `exists_invertible_tensor_idealModule_add`
(`Picard/SelfAdjointN.lean:267`), the relative theorem of the square. It is what the seesaw exists
to serve, and it unblocks AP-D4 / AP-D5-existence / AP-D6 / AP-D7. ===

Route (external review kept it ahead of the chart route and of a generic-vertical-divisor route):
prove the identity on the UNIVERSAL pair of points over the reduced — indeed integral — base
`B = C ×_U C`, where every residue fibre is trivial by the field theorem
`HasseWeil.Pic0.RouteCTheoremOfSquareDiv.kappaDivisor_add_linEquiv`; apply the seesaw; pull back
along the zero section to kill `N`; base-change down to an arbitrary, possibly NON-REDUCED, base.

**T8b — DONE (2026-08-08).** `ForMathlib/SeesawGlobalBase.lean`:
`exists_pullback_iso_of_fibrewise_trivial_of_isReduced_of_affineCover` — the seesaw over an
ARBITRARY base, axiom-verified standard-three, zero sorries. The `[IsAffine S]` restriction that
blocked the consumer is gone. `UniversallyOConnected` restricts cleanly (via `IsPullback.paste_horiz`
with `isPullback_morphismRestrict`), so the fallback was not needed. Also exported: the standalone
gluing lemma `exists_pullback_iso_of_restrict_pullback_cover`, and `UniversallyOConnected.restrict`.
INSTANCE BRIDGE worth remembering: `CompactSpace ↥U` exists on `↥(U : Set X)` but instance search
does not reach it through the `U.toScheme` carrier wrapper — use `inferInstanceAs`.
`hhigh` there is quantified over ALL opens `W ⊆ S`, because the cover of `S` is produced inside the
proof from `S.affineOpens`.

REMAINING OBLIGATION FROM THE REVIEW:

=== NEXT CONCRETE TICKET FOR BLOCKER 3 ===
The seesaw is now fully available (arbitrary base). Per `Picard/SelfAdjointN.lean`'s own module
docstring, the EXPECTED BOTTLENECK is NOT the seesaw but:

  **T10-cmp — the comparison between HasseWeil's PROJECTIVE-DIVISOR linear equivalence and the
  scheme-theoretic `picClass`, together with its BASE-CHANGE NATURALITY.**

That is what turns the field theorem
`HasseWeil.Pic0.RouteCTheoremOfSquareDiv.kappaDivisor_add_linEquiv` (proved unconditionally in any
characteristic, Silverman III.3.5) into the fibrewise-triviality input `hfib` the seesaw wants for
the rigidified discrepancy `Δ^rig_{P,Q} = Δ_{P,Q} ⊗ f_B^*(0^* Δ_{P,Q})⁻¹` on `C_B ⟶ B`,
`B = C ×_U C`. Start there — everything downstream of it is boarded.

*** SCOPING SURVEY DONE (2026-08-08) — read `plan-blockers-2026-08-08.md` §"BLOCKER 3 — scoping
survey" BEFORE starting. It CORRECTS this focus in two places: ***
  (i) the universal pair is `WeilPairing/TautologicalPair.lean` (293 lines, SORRY-FREE), NOT
      `EllipticCurve/AdditionBaseChange.lean`. And `pairClassify` (:79) — "every pair of sections is
      a base change of the universal pair" — is ALREADY PROVED, which is most of T9b.
  (ii) the bridge is NOT a Picard-group comparison. It is one field-level module triviality
      `nonempty_tensorObj_idealModule_add_field` (statement in the plan), whose ONLY new mathematics
      is "div(f) = 0 ⟹ multiplication by f is an isomorphism of ideal modules" — via `idealGenHom`
      (`Picard/IdealModule.lean:240`), `bijective_idealGenHom_app` (:307) and
      `isIso_of_bijective_app_on_cover` (`Picard/PicComparison.lean:838`), on the two Weierstrass
      charts already trivialised at `EllipticCurve/PoleSheafModel.lean:410-424`. mathlib does NOT
      have "principal divisor ⟹ trivial line bundle" for `Scheme.Modules`.
  Then `hfib` is PURE TRANSPORT: `nonempty_pullback_tensorObj`
  (`Picard/PullbackTensorObj.lean:61`) + `nonempty_pullback_idealModule_ker_sectionBaseChange`
  (`Picard/IdealModulePullback.lean:387` — stated along `Limits.pullback.fst π t`, SYNTACTICALLY the
  morphism in `hfib`) + `isPullback_projModelBaseChange` + `projModelPointsAddEquiv`
  (`EllipticCurve/MulByHomDegree.lean:65`, which identifies a scheme section with a mathlib
  `W.Point` — indispensable and already proved).
  PRICE FIRST: `kappaDivisor_add_linEquiv` is char-free but NOT instance-free — it routes through
  `miller_hypothesis_holds_allChar`, needing `[IsIntegrallyClosed W_k.CoordinateRing]` per fibre.
  AND `hhigh` remains open: even a finished T10-cmp does not by itself close `SelfAdjointN.lean:267`.

Sequence for Blocker 3:
  1. **T10-cmp** — the field-level bridge above (THE bottleneck; genuinely new, but short).
  2. build `Δ^rig` on the universal pair from `WeilPairing/TautologicalPair.lean`; reducedness /
     integrality of `pairBase` is `EllipticCurve/GroupLawAxioms.lean:129/:132` (exact spelling
     match). SPELLING TRAP: the fibre-cube instances at `:136/:147` and `:142/:152` are NOT literally
     `pairCurve`'s spelling — they agree by `pullback.condition`, but instance search will not see it.
  3. feed `hfib` from step 1, apply `exists_pullback_iso_of_fibrewise_trivial_of_isReduced_of_affineCover`,
     then pull back along the zero section to kill `N`.
  4. **T9b** — base-change down to an arbitrary, possibly NON-REDUCED, base, stated and glued
     ZARISKI-LOCALLY on `T` (moduli-stack issue globally).
     `RelEffCartierDiv.sectionDivisor_baseChange` (`EllipticCurve/PoleSheaf.lean`) is part of that bridge.
  5. discharge `hhigh` for the elliptic family (fibres are curves) with
     `subsingleton_H_add_two_of_two_affine_open_cover` +
     `HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
     over `q ≥ 1`.

  * **T9b** — the passage from an arbitrary elliptic curve to the universal Weierstrass parameter
    space is only ZARISKI-LOCAL (globally there is a moduli-stack issue); state and glue the
    base-change-down step locally on `T`.

TWO CARRY-OVERS FROM CLOSING BLOCKER 2:
  * `hhigh` on the top-level seesaw is quantified over ALL finite affine covers, because the cover is
    produced inside the proof by `exists_finiteAffineBaseCech_flat`. It cannot be discharged from the
    fibre side there — it is `H^{≥2}(X,M) = 0`, needing a CURVE hypothesis the general statement does
    not carry. In the elliptic application it IS available: kill it with
    `subsingleton_H_add_two_of_two_affine_open_cover` +
    `HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
    over `q ≥ 1`.
  * CLEANUP DEBT: the four primed theorems in `RelPicLocal.lean` duplicate ~370 lines of the unprimed
    ones, which are now thin specialisations (`hp.baseChange g |>.isIso_app`). Left byte-identical so
    no consumer moved.
  * LANDMINE: `WeilPairing/TensorSection.lean` is now a DEAD module. Its `ModularCurves.tensorSection`
    family FULL-NAME-COLLIDES with copies in `EllipticCurve/PoleSheaf.lean` and
    `EllipticCurve/PullbackTensorSection.lean`. Do not re-import it into anything in `Seesaw.lean`'s
    closure without deduplicating first.

BLOCKER 4 (independent, does NOT gate AP-D) — `IsIso ((sectionVanishingIdeal M hM σ).subschemeι ≫ π)`
at `EllipticCurve/AbelEquivalence.lean`. Route REPLACED after review; see below.

--- retained detail from the previous focus ---

HALF A (descent) — ALREADY PROVED, just needs re-shaping. `exists_pullback_twist_of_locally`
(`WeilPairing/RelPicLocal.lean:1157`, AP2-B1, zero sorries) with `M' = unitObj` says: base-locally
a pullback ⟹ globally `M ≅ π^*N₀` with `N₀ = π_*M` invertible. So Blocker 2 reduces to
"Zariski-locally on the base, M is trivial". Caveat: it is stated for `pullback.snd p g`; either
instantiate at `g = 𝟙 S` and transport along `pullback π (𝟙 S) ≅ X`, or (RECOMMENDED, ticket T7)
generalise the RelPicLocal chain to a bare `f : Y ⟶ T` with `UniversallyOConnected f`, which is
also the shape `exists_invertible_tensor_idealModule_add` consumes.

HALF B.1 (`π_*M` invertible) — the real work. `hpkg`/AP2-A2's engine is UNUSABLE (its `hexact`
is false: `H¹(E_s,𝒪) = κ(s)` on a genus-1 fibre). Use Mumford's argument on the tree's own
UNCONDITIONAL finite-projective replacement:
  1. `LowDegreeFiniteReplacement` (`ForMathlib/LowDegreeFiniteProjectiveReplacement.lean`)
     gives `u := kZeroToKOne f : K⁰ → K¹`, `K¹` finite free, `kZero_projective` (:522), and
     `baseChangeKernelEquiv A : ker(u ⊗ A) ≃ₗ ker(f ⊗ A)` (:799) for EVERY `A`, with no exactness.
  2. `coker u ⊗ κ(p) = coker(u ⊗ κ(p))` (right exactness), of dimension
     `rk K¹ − rk_{κ(p)} K⁰ + 1`, locally constant by `Module.isLocallyConstant_rankAtStalk`
     (mathlib, VERIFIED present) since `K⁰` is finite projective.
  3. Apply 0FWG (see IN FLIGHT below) to `coker u` ⟹ finite locally free.
  4. Both `0 → im u → K¹ → coker u → 0` and `0 → ker u → K⁰ → im u → 0` split ⟹ `ker u` is a
     direct summand of `K⁰`, finite projective, and its formation commutes with all base change.
  5. `Module.Invertible.of_finite_of_projective_of_rankAtStalk_eq_one`
     (`ForMathlib/InvertibleOfRankOne.lean`) + `baseSectionsIsoKernelOrderedBaseCechDifferential`.

  *** GAP FOUND IN STEP 1 (external review A2) — MUST BE REPAIRED ***
  `dim ker(u ⊗ κ(p)) = 1` does NOT follow from `dim ker(d⁰ ⊗ κ(p)) = 1`: the replacement is
  applied to the CORESTRICTION `f : C⁰ → Z¹ = ker d¹`, and `i : Z¹ ↪ C¹` need not stay injective
  after non-flat base change. Reviewer's counterexample: `R = k[x,y]`,
  `R --(r ↦ (−yr,xr))--> R² --((a,b) ↦ xa+yb)--> R`; at `p = (x,y)`, `ker(d⁰⊗κ(p)) = κ(p)` but
  `ker(f⊗κ(p)) = 0`. Concretely in Lean this is the hypothesis pair of
  `shortComplexBaseChangeKernelEquiv` (`LowDegree…:834`): `[Module.Flat R (ker S.g.hom)]` and
  `hbij : Bijective (kerBaseChangeComparison A S.g.hom)`, BOTH of which AP2-A2 got from `hexact`.
  TWO REPAIRS — try (b) first:
   (b) TWO-ELEMENT COVER: if `card ι = 2` then `orderedBaseCechObject_subsingleton_of_card_le`
       makes `C^q = 0` for `q ≥ 2`, so `d¹ = 0`, `Z¹ = C¹` is flat by (F3), `i = id`, and
       `kerBaseChangeComparison A 0` is the identity. Everything discharges with no exactness.
       Open question: can `IsInvertible.exists_finiteAffineBaseCech_flat`
       (`Picard/InvertibleSheafBaseCechFlat.lean:23`) be strengthened to `card ι = 2` for a
       relative curve, or should Blocker 2 carry `card ι = 2` as a hypothesis?
   (a) MINIMAL-PRIME SANDWICH (general): `F_p := ker(f⊗κ(p)) ↪ H_p := ker(d⁰⊗κ(p))` so
       `dim F_p ≤ 1`; for a minimal prime `η ⊆ p`, `R` reduced ⟹ `R_η = κ(η)` is a FIELD hence
       flat, so `F_η = H_η` and `dim F_η = 1`; rank only drops under specialisation `η ⤳ p`, so
       `dim ker(u_p) ≥ dim ker(u_η) = 1`. Reducedness is used TWICE in Blocker 2 (here, and in 0FWG).

  *** A2′ FOLLOW-UP (checked after the review — read this before starting T4c) ***
  BOTH repairs are needed; (b) is NOT free.
   * (b) is not available off the shelf: `exists_finiteAffineBaseCech_flat` is a thin wrapper over
     `exists_finite_affine_trivializingCover`, a generic quasi-compactness argument with NO control
     on `card ι`.
   * (a) fixes only the DIMENSION identification. It does not discharge `[Module.Flat R (ker d¹)]`,
     which is a *variable* of the `LowDegreeFiniteReplacement` namespace and so is needed to build
     the replacement at all. And you cannot dodge it by replacing `d⁰ : C⁰ → C¹` directly: that
     needs `Module.Finite R (C¹/im d⁰)`, and only `H¹ = Z¹/im d⁰` is finite.
  ⇒ KM-SEESAW-2″ MUST CARRY ONE EXTRA HYPOTHESIS:
       `(hhigh : ∀ q, 1 ≤ q → Function.Exact ((C.d q (q+1)).hom) ((C.d (q+1) (q+2)).hom))`
     — exactness at positions ≥ 2 only. Strictly weaker than AP2-A2's `hexact` (which also demands
     position 1, i.e. `H¹ = 0`, FALSE here), and automatic in the application: the fibres are
     curves so `H^{≥2}(X_s,M_s) = 0`, and
     `HomologicalComplex.functionExact_of_bounded_flat_forall_field_baseChange_exact_of_finite_homology`
     lifts it to `R` exactly as `kernel_data_of_hasDegreeOneFibreCohomology` does, just over `q ≥ 1`.
     Fibre input already in the tree: `subsingleton_H_add_two_of_two_affine_open_cover`.
     From `hhigh`, `Flat R Z¹` by DESCENDING INDUCTION: `Z^N = C^N` flat; for `1 ≤ i < N`,
     `im d^i = Z^{i+1}` by exactness at `i+1`, so `0 → Z^i → C^i → Z^{i+1} → 0` with `C^i`, `Z^{i+1}`
     flat ⟹ `Z^i` flat. The same sequences give `ker(d¹ ⊗ A) = Z¹ ⊗ A`, i.e. `hbij`.
  ⇒ T4c(i) = `hhigh ⟹ Flat R Z¹ + hbij`; T4c(ii) = the minimal-prime sandwich. BOTH required.

HALF B.2 (counit iso) — write the comparison diagram EXPLICITLY, in this order (review A4):
  `P := ker u ≅ Γ(X,M)`, then `P ⊗ κ(p) ≅ ker(u_p) ≅ ker(f_p) ↪ H⁰(X_p,M_p)`, an ISO by dimension
  count; then a local basis `ℓ` restricts to a nonzero element of `H⁰(X_p,M_p) ≅ κ(p)`, so `ℓ_p`
  is nowhere vanishing; then at `y ∈ X` over `p`, evaluation is multiplication by `a` whose class
  mod `p𝒪_{X,y}` is a unit, hence `a` is a unit. DO NOT claim base change "from the splitting
  alone" — it is the splitting PLUS the one-dimensional injection.
  `hfib` is essential and cannot be replaced by `hrank`: `𝒪_E(P)` on a genus-1 curve has `h⁰ = 1`
  but its generator vanishes at `P` (already in the b2_log).

T4 — **DONE** (2026-08-08). `ForMathlib/ReducedConstantRankFree.lean`:
`free_of_isReduced_of_forall_le_finrank_fiber` — Stacks 0FWG, LOCAL form — PROVED, axiom-verified
standard-three, zero sorries, no new imports. VERIFIED ABSENT FROM MATHLIB before writing.
Two useful discoveries: `IsLocalRing.span_eq_top_of_tmul_eq_basis` does the whole Nakayama step in
one shot, and `TensorProduct.mk_surjective` gives the lift. The per-prime step is a private helper
`algebraMap_eq_zero_of_mem_ker` stated over an ARBITRARY `R`-algebra field `κ` rather than `κ(p)`
(`set` would not fold the occurrence inside `hmin p`).

T4b — **DONE** (2026-08-08). The GLOBAL form,
`projective_of_isReduced_of_isLocallyConstant_finrank_fiber` : `R` reduced Noetherian, `M` finite,
fibre dimension locally constant ⟹ `Module.Projective R M` — PROVED, axiom-verified
standard-three. **STACKS 0FWG IS COMPLETE; the mathlib gap under the seesaw is closed.**
This is the form B.1 step 3 consumes (applied to `coker u`). Four private helpers do the fibre
transports; `residueFieldAlgEquivOfSurjectiveOnStalks` (via
`RingHom.SurjectiveOnStalks.residueFieldMap_bijective` at `surjectiveOnStalks_of_isLocalization`)
is much cleaner than `IsLocalization.orderIsoOfPrime` — reuse it if a similar transport is needed.
Needed `import Mathlib.RingTheory.LocalProperties.Reduced` for `IsReduced (Localization.AtPrime I)`.

T4c(i) — **ALREADY PROVED IN THE TREE; ZERO NEW WORK** (found 2026-08-08). Do NOT write the
descending induction: `ForMathlib/BaseChangeKerCoker.lean` already has both halves, sorry-free,
stated with exactly the "exact from degree `k` onward" hypothesis and no full exactness —
  * `Module.Flat.ker_of_bounded_exact_from` (:292) → `Flat R (ker (d k))`
  * `kerBaseChangeComparison_bijective_of_bounded_exact_from` (:625) → the `hbij` for `d k`
  * (support) `Module.Flat.quotient_range_of_bounded_exact_from` (:270) → `Flat R (M (k+1) ⧸ range (d k))`
Instantiate all three at `k = 1`; `hhigh` IS their `hexact`. (Lesson `grep-the-conclusion-not-the-inputs`
again: the argument was re-derived from scratch before anyone grepped for the conclusion.)

T4c(ii) — **NOT NEEDED**. `shortComplexBaseChangeKernelEquiv`'s conclusion is about `S.f.hom` = d⁰
(not the corestriction `f`), reached via `ker_baseChange_codRestrictToKer_eq … hbij`. So `hbij` IS
the identification whose failure the reviewer's Koszul counterexample exhibits, and T4c(i) supplies
it from `hhigh`. The minimal-prime sandwich is the right repair only if one refuses `hhigh`.

T5 CORE — **DONE** (2026-08-08). `ForMathlib/ConstantKernelRankProjective.lean`, all axiom-verified
standard-three: over a reduced Noetherian ring, `u : K₀ → K₁` between finite projectives with
`dim ker(u ⊗ κ(p)) = n` at every prime has projective cokernel, projective kernel, finite kernel.
Two notes worth keeping: `Module.rankAtStalk_eq` must be RESTATED with the fibre spelled
`κ(p) ⊗[R] M` (`Ideal.Fiber` is a reducible abbrev but `omega` treats it as a distinct atom); and
the ℕ-arithmetic goes through the additive identity + `omega`, never through ℕ-subtraction.
`Module.Projective.ker_of_projective_coker` (already in `BaseChangeKerCoker.lean`) does both
splittings in one step.

**THE WHOLE GRAUERT SUBSTITUTE IS NOW IN PLACE.** What remains for Blocker 2 is wiring, not new
mathematics:

  * **T5-wire — DONE** (2026-08-08). `ForMathlib/SeesawPushforwardInvertible.lean`:
    `baseSections_invertible_of_orderedBaseCechHomologyFinite` PROVED, axiom-verified
    standard-three. Given `hrank` (KM-SEESAW-1′), `hhigh`, and Čech homology finiteness,
    `baseSections π M` is invertible over a REDUCED base with no `H¹ = 0` anywhere.
    TWO PLAN CORRECTIONS FOUND IN EXECUTION: the right short complex is `C.sc' 0 1 2`, NOT
    `C.sc' 0 0 1` (there `f` is `C.d 0 0 = 0`); and no `1 ≤ card ι` hypothesis is needed — take
    `N := card ι + 1` and extend `hhigh` via `exact_of_subsingleton_middle`.
  * **T5-bridge — DONE** (2026-08-08). `baseSections_invertible_of_kernel_finrank_of_isReduced` is
    now SORRY-FREE and axiom-verified standard-three. **BLOCKER 2's HALF B.1 IS COMPLETE.**
    Two lessons recorded in the commit: (i) the boarded "swap `Spec (.of R)` for affine `S`" fix is
    IMPOSSIBLE — the comodel-existence input runs through `SupportAdaptedChowChart`, whose fields
    are structurally `Spec (.of R)` across ~25 files, so the general statement must be DERIVED, not
    reparameterised; (ii) both blocking claims in the old sorry's docstring were STALE —
    `ModuleCat.restrictScalars` DOES have `PreservesFiniteLimits`/`Colimits`/`PreservesHomology`,
    and `ShortComplex.mapHomologyIso` gives the homology comparison. The transport is short because
    `baseModulePresheaf f M ⋙ ModuleCat.restrictScalars g.appTop.hom = baseModulePresheaf (f ≫ g) M`
    is a genuine EQUALITY of functors. New reusable exports:
    `OrderedBaseCechHomologyFinite.of_comp`, `orderedBaseCechHomologyFinite_of_isProper_of_isAffine`,
    `orderedBaseCechLowDegreeFinite_of_isProper_of_isAffine`.
    FOLLOW-UP TICKET: move the private `CompTransport` section out of
    `SchemeModuleProperLowDegreeCechFinite.lean` into
    `ForMathlib/SchemeModuleOrderedBaseCechPushforward.lean`, next to
    `orderedBaseCechComplexPushforwardIso` — it is generic ordered-Čech infrastructure and only
    landed there because of a two-file edit fence.

  * (historical, now closed) `Scheme.Modules.orderedBaseCechHomologyFinite_of_isProper`
    (`ForMathlib/SchemeModuleProperLowDegreeCechFinite.lean`) is stated for a SYNTACTIC
    `Spec (.of R)` base, as is its whole supporting chain (whose helpers are `private`), so it does
    not unify with a general `[IsAffine S]`. Three routes tried and documented at the sorry:
    direct application (no unify); composing with `S.isoSpec.hom` (the two complexes are `∏ᶜ` over
    different rings — carriers NOT defeq); `orderedBaseCechComplexBaseChangeIso` along
    `S.isoSpec.inv` (blocked — mathlib has neither
    `((F.mapHomologicalComplex c).obj C).homology n ≅ F.obj (C.homology n)` nor
    `PreservesFiniteLimits`/`Colimits` for `ModuleCat.restrictScalars`).
    FIX: generalise that lemma and its `Spec (.of R)`-shaped helpers to
    `[IsAffine S] [IsNoetherianRing Γ(S,⊤)]`. Also unblocks
    `Picard/InvertibleSheafProperCechResidueSpread.lean`. WORKAROUND meanwhile: a caller with a
    `Spec (.of R)` base uses `baseSections_invertible_of_orderedBaseCechHomologyFinite` directly,
    SORRY-FREE, feeding it `orderedBaseCechHomologyFinite_of_isProper`.
  * (superseded plan text for T5-wire, kept for the chain of reasoning) instantiate on the Čech
    complex:
      `hhigh` ⟹ `Flat R Z¹` (`Module.Flat.ker_of_bounded_exact_from` at k=1) and `hbij`
      (`kerBaseChangeComparison_bijective_of_bounded_exact_from` at k=1)
      ⟹ `shortComplexBaseChangeKernelEquiv A hbij : ker(u ⊗ A) ≅ ker(d⁰ ⊗ A)` for all `A`
      ⟹ with KM-SEESAW-1′, `dim ker(u ⊗ κ(p)) = 1` at every prime
      ⟹ `ConstantKernelRankProjective` gives `ker u` finite projective
      ⟹ `rankAtStalk (ker d⁰) = 1`, then
      `Module.Invertible.of_finite_of_projective_of_rankAtStalk_eq_one`
      (`ForMathlib/InvertibleOfRankOne.lean`) and
      `baseSectionsIsoKernelOrderedBaseCechDifferential` ⟹ `baseSections π M` invertible.
      Model to imitate throughout: `kernel_data_of_hasDegreeOneFibreCohomology`
      (`EllipticCurve/DegreeOneFibreCohomology.lean:126`) — same shape, `hexact` replaced by
      `hhigh` + reducedness. Remember to ADD `hhigh` to KM-SEESAW-2″'s signature.
  * **T6 — DONE (2026-08-08). BLOCKER 2's HALF B.2 IS CLOSED.**
    `exists_baseCover_restrict_unitObj_iso_of_invertible_baseSections` PROVED, axiom-verified
    standard-three, zero sorries. **BOTH halves of Blocker 2's descent input (B.1 `π_*M` invertible,
    B.2 `M` base-locally trivial) are now complete.**
    The boarded T6-eval ticket was largely UNNECESSARY — route simplification killed it: `hfib`
    gives a GLOBAL trivialisation `θ : g^*M ≅ 𝒪_{X_T}` on the whole fibre product, so the cofactor
    is only divided out (`isUnit_of_mul_isUnit_left`), never identified; and
    `Scheme.preimage_basicOpen` + the tautological `σ := pullback.lift (X.fromSpecResidueField y) 𝟙`
    reduces the germ comparison to the set-level `g (σ pt) = y`, with no `IsLocalHom` reasoning.
    LOAD-BEARING DEFEQ (`rfl`): the `Γ(X,W)`-action on `Γ((pushforward g).obj N, W)` IS
    `(g.app W).hom r • x`; with `Scheme.Modules.Hom.app_smul` that gives
    `u_W (a • e) = g^#(a) • u_W e` for free.
    ALREADY EXISTED, do not rebuild: `Scheme.Modules.localPullbackTrivializationT`
    (`Picard/DualPullback/LocalTrivialization.lean:264`) pulls a trivialisation back along an
    ARBITRARY morphism, with `localPullbackTrivialization_hom_unit_ofSection`/`_inv_one_ofSection`.
    CAVEAT: `ForMathlib/SeesawBaseLocallyTrivial.lean` is an ORPHAN MODULE — nothing imports it, so
    `lake build ModularCurves` skips it and leaves its olean stale. Build it EXPLICITLY before any
    cross-file `#print axioms`.

  * (historical, superseded by the line above)
    `ForMathlib/SeesawBaseLocallyTrivial.lean`. Target
    `exists_baseCover_restrict_unitObj_iso_of_invertible_baseSections` is stated as planned (not
    weakened) and now carries Half B.1's binders (`[IsReduced S] [IsNoetherian S]`, the cover,
    `hhigh`, `hrank`). `hrank` is a HYPOTHESIS, not derived from `hfib`, to avoid an import cycle —
    `ForMathlib/Seesaw.lean` will import this file. `[IsReduced S]` is genuinely needed (sole input
    to `projective_quotient_range_of_constant_finrank_ker_baseChange`).
    PROVED sorry-free: `exists_finset_dual_apply_sum_eq_one`, `bijective_smul_of_isUnit_evalSection`,
    `nonempty_restrict_unitObj_iso_of_bijective_smul`,
    `nonempty_restrict_unitObj_iso_of_isUnit_evalSection`, `tmul_one_ne_zero_of_isUnit_dual_apply`,
    and in `SeesawPushforwardInvertible.lean` the KEY comparison
    `kerBaseChangeComparison_orderedBaseCech_zero_bijective` :
    `A ⊗_{Γ(S,⊤)} Γ(X,M) ≅ ker(d⁰ ⊗ A) = H⁰(X_A, M_A)` — i.e. `π_*M ⊗ κ(s) ↪ H⁰(X_s,M_s)`, the input
    that was reported missing. Steps 1 and 2 of the blocked lemma are therefore done.
    DESIGN WIN worth keeping: the base cover comes from surjectivity of `contractLeft`
    (`1 = ∑ φ_k(ℓ_k)`, take `V_k := S.basicOpen (φ_k ℓ_k)`), NOT from a free-locus trivialisation —
    so the `ℓ_k` are honest GLOBAL sections and the proof never needs quasi-coherence of `π_*M` on a
    non-affine `X`.
    NON-RESCUES already checked (do not retry): invertibility of `π_*M` alone is insufficient (DVR:
    `P = R·t ⊆ C⁰ = R`); and the dimension count alone is insufficient (a map between 1-dimensional
    spaces can still be zero). The statement is NOT false — in the `k[ε]/(ε²)` example `π_*M` is not
    invertible, so `hinv` excludes it.
  * **T6-api — DONE** (2026-08-08). The five-link chain now has element descriptions, all
    axiom-clean, additions only to the shared files:
    `orderedBaseCechXBaseChangeIso_one_tmul_of_projection_eq` (link 2, ALL degrees),
    `baseSectionsIsoKernelOrderedBaseCechDifferential_hom_coe`/`_hom_π` + `orderedBaseCechObject_ext`
    (link 4), and the packaged
    `orderedBaseCechKernelBaseChangeEquivBaseSections` + `_apply` in
    `SeesawPushforwardInvertible.lean` — the chain sends `1 ⊗ ℓ` to the pullback of `ℓ` along
    `pullback.fst π t`. Plus `baseChange_unit_ne_zero_of_isUnit_dual_apply` composing steps 1–3.
    One `set_option maxRecDepth 1024 in` on a single `exact` in `AffineModuleCechBaseChange.lean` —
    a recursion bound, NOT a heartbeat bump, but flagged for review.

  * **T6-eval (NEW TICKET — now the only thing between here and Half B.2)**: `evalSection` versus
    pullback of modules along a GENERAL scheme morphism `g = pullback.fst π t`. Three pieces, all
    recorded in `le_basicOpen_evalSection_of_dual_baseSections`'s docstring:
      1. a PULLED-BACK TRIVIALISATION: `ψ : M.over W ≅ 𝒪` ⟹ a trivialisation of
         `(Scheme.Modules.pullback g).obj M` over `g ⁻¹ᵁ W`. The existing
         `restrictTrivialization` / `restrictIsoOfPullbackIso` / `overTrivializationOfRestrictIso`
         family covers only OPEN IMMERSIONS — that is the gap.
      2. NATURALITY OF `evalSection` ALONG `g`: `evalSection (g^*ψ) (g^*ℓ) = g^# a`.
         `evalSection_naturality` (`Picard/Evaluation.lean`) is naturality in the OPEN, not along a
         morphism of schemes. `evalSection_factor` (`Picard/PicComparison.lean`) supplies the unit
         relating it to `hfib`'s trivialisation on the same open, and that piece DOES exist.
      3. the GERM COMPARISON at a point `z` of `X ×_S Spec κ(y)` over `y`, via
         `σ = pullback.lift (X.fromSpecResidueField y) 𝟙`, plus "a local ring hom reflects units".
    Lands in `Picard/Evaluation.lean`. Size comparable to T6-api. Expect `whnf`/`maxRecDepth` walls
    in the `SheafOfModules.pullback`/`over` machinery — fix them STRUCTURALLY (`let` not `set`,
    explicit terms instead of `rw`, `have … := rfl` anchors).

  * (historical, now closed) element-level `_apply`/`_coe` lemmas for the five-link chain
    `baseChangeKernelZeroLinearEquiv → orderedBaseCechComplexBaseChangeIso →
     kernelZeroLinearEquivOfHom → baseSectionsIsoKernelOrderedBaseCechDifferential →
     baseSectionsMapIso`.
    The chain exists but its only consumer uses it for a `finrank` count, so NONE of the five links
    has an element description; `orderedBaseCechComplexBaseChangeIso` has only `…_hom_f`, reducing
    to `orderedBaseCechXBaseChangeIso`, which has none either. Needed: the chain sends `1 ⊗ ℓ` to the
    pullback of `ℓ` along `pullback.fst π t` (step 3), plus `evalSection`-vs-pullback and germ
    compatibility (step 4). Lands in `ForMathlib/AffineModuleCechBaseChange.lean` and
    `ForMathlib/SchemeModuleOrderedBaseCechZero.lean`.
    TACTIC NOTE from this pass: `isDefEq`/`whnf` stalls here are fixed STRUCTURALLY — `let` not
    `set`, explicit `LinearMap.baseChange_comp` terms instead of `rw`, and
    `cast (congrArg Function.Bijective …)` instead of `rw` on a function equation.
  * **T7** — generalise the RelPicLocal chain to a bare `f : Y ⟶ T`.
  * **T8** — assemble; then Blocker 2 closes and Blocker 3 opens.

AFTER BLOCKER 2: Blocker 3 = `exists_invertible_tensor_idealModule_add` (`Picard/SelfAdjointN.lean`),
which unblocks AP-D4/D5-existence/D6/D7. Reviewer kept the seesaw route (R1) ahead of the chart
route (R2), but R1 acquires two obligations not previously boarded: `B = C ×_U C` is NOT affine
(apply the affine seesaw on an affine open cover of `B`, then glue — ticket T8b), and the passage
from an arbitrary elliptic curve to the universal Weierstrass parameter space is only
Zariski-local (state and glue locally — ticket T9b).

INDEPENDENT LINE (Blocker 4, does NOT gate AP-D): `IsIso ((sectionVanishingIdeal M hM σ).subschemeι ≫ π)`
at `EllipticCurve/AbelEquivalence.lean`. **RE-PLANNED** — the old route is dead:
  * The board note "the local generator vanishes on `Z`, so it is not a nonzerodivisor" is WRONG
    reasoning (`t ∈ k[t]` vanishes on `V(t)` and is a nonzerodivisor).
  * The fibrewise-nonzerodivisor route IS Stacks 00MF = the principal case of the local criterion
    00ME, whose proof needs Artin–Rees. It does NOT dodge the sorried criterion.
  * Producing the section from the fibre points is NOT viable (`S_red → S` has the same
    residue-field fibres).
  * USE INSTEAD — étale transversality: `h̄` generates the maximal ideal of the DVR `𝒪_{E_s,z}`,
    so `dh̄ ≠ 0`, so `h : U → 𝔸¹_S` is étale near `z`, so `Z ∩ U = U ×_{𝔸¹_S} S` is étale over `S`;
    `Z` closed in proper `E` ⟹ proper; proper + étale ⟹ finite étale; fibre degree 1 ⟹ iso.
    (Stacks §37.38 / 055S — prove the special case from a standard-smooth Jacobian presentation;
    the general slicing lemma routes through fibrewise flatness and would reintroduce the gap.)
  * Hypothesis audit: `h⁰ = 1` and `H¹ = 0` alone are NOT enough; the statement also needs smooth
    geometrically integral genus-one fibres, `deg M_s = 1`, and the base-change iso giving `σ_s ≠ 0`.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone (grep for sorry lies — bare `sorry` and inherited `sorryAx`).
