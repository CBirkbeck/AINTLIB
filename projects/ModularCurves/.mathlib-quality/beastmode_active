FOCUS (2026-08-09, late): **AP-D7 CLOSED** (Katz–Mazur construction complete bar AP-D4 ⊇) and
**Stacks 00MK S-module core PROVED** (`injective_of_lTensor_residueField_injective_sModule`,
axiom-clean, commit 12b5355a5).

READ FIRST: `.mathlib-quality/HANDOVER-2026-08-09-ds4-weil-pairing.md`.
Kickoff message for a new worker: `.mathlib-quality/HANDOVER-MESSAGE-2026-08-09.md`.

=== IMMEDIATE NEXT STEP ===
`injective_baseChange_of_residueField_fibre_sModule` — the LAST sorry in
`ForMathlib/LocalFlatnessCriterion.lean`. Its docstring now writes out the whole obligation in four
steps, and the core was deliberately SPLIT
(`injective_of_lTensor_residueField_injective_of_separated` takes
`hsep : ∀ x, (∀ n, x ∈ 𝔪ᴿⁿ • ⊤) → x = 0` instead of the S-package) so this consumer NEVER has to
build `S ⧸ IS`. Step 1 (`𝔪_{R⧸I} = 𝔪ᴿ.map q` for a surjective local hom) is a real ~8-line mathlib
gap — loogle found only `AdicCompletion.maximalIdeal_eq_map`.

=== THEN, AND VERIFY BEFORE COMMITTING TO IT ===
A consumer grep shows **nothing in the tree consumes the sModule chain, and nothing consumes
`evalGenerator_mem_nonZeroDivisors`**. `relEffCartierDiv_of_degreeOne_package`'s body is just
`refine relEffCartierDiv_of_isIso_subschemeι _ ?_ ; sorry`. So closing the leaf supplies a TOOL;
the wiring to `IsIso (Z ≫ π)` is still owed. Mathlib's `Scheme.Hom.isIso_iff_finrank_eq` needs
`[Flat f] [IsFinite f]` and `Scheme.Hom.finrank f = 1` — LFP is NOT needed; see
`exists_section_of_degree_one` (`AbelEquivalence.lean:877`) for the exact idiom.

=== THE ONE REMAINING KM TARGET ===
`exists_torsionPoint_of_mem_kerMulByN` — `WeilPairing/KMPairing.lean:302` — AP-D4 `⊇` = Abel
(rigidity + surjectivity), plus a fibrewise degree function on `Pic` the tree does not have.
Escape hatch proved: `exists_torsionPoint_of_mem_kerMulByN_of_surjective`.

=== GOTCHA WORTH KEEPING ===
Inside `namespace Module.Flat`, the bare name `lTensor_exact` resolves to `Module.Flat.lTensor_exact`
with a DIFFERENT signature; `_root_.lTensor_exact` is load-bearing. Same shadowing family as the
`idealModule` / `sectionVanishingIdeal` incidents. Full `lake build ModularCurves` is the detector.

RULES: no `set_option maxHeartbeats`/`synthInstance.maxHeartbeats`; never `2>/dev/null` beside a
lake/lean command; push with `LEAN4_GUARDRAILS_BYPASS=1 git push origin dev/modular-curves`;
`#print axioms` at every milestone; NEVER `git add -A` while a subagent is mid-run; full
`lake build ModularCurves` before adding any import. Some `ForMathlib/` modules are orphans that
`lake build ModularCurves` skips — build them explicitly.
