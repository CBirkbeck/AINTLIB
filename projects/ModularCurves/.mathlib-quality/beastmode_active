FOCUS: AP2-B2/B3 — THE ONLY REMAINING GAP is the bridge 'degree-one package ⟹ a section': produce z : Spec R ⟶ E with sectionVanishingIdeal M hM σ = Scheme.Hom.ker z, or equip the vanishing subscheme (Scheme.IdealSheafData.subscheme of sectionVanishingIdeal) with finite+flat+lfp over the base and apply exists_section_of_degree_one (PROVED). Everything else on the line is PROVED and axiom-clean: exists_relEffCartierDiv_of_section, isInvertible_idealModule_of_section, degree_eq_one_iff_exists_section, degree_eq_one_of_ideal_eq_ker, exists_section_of_degree_one, plus the whole localization-gap chain and sectionVanishingIdeal itself. ROUTES for the bridge: (a) fibrewise — the vanishing scheme meets each fibre in one reduced point (AP2-A1 h⁰ = 1 for degree one on the genus-one fibre), so the subscheme→base map is finite flat of rank one (needs the fibrewise-flatness criterion, R-substrate form only — the _sModule variant is SORRIED); (b) via the A2 package — baseSections is invertible of rank one, so the counit f^*f_*M → M has an image ideal that is locally principal by sv1, and its subscheme is S-isomorphic by construction. Prefer (b) if it avoids the flatness criterion.
