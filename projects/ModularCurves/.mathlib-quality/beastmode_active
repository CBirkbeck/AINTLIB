FOCUS: [G3 + 3c-iv] — G3: instantiate L2e at the G2-dataset: the h-family + hsplit come from AP-D4 (exists_transitionUnit_eq_mul_inv_of_mem_torsionPoints — read its signature in KMPairing.lean and check it consumes exactly (M, hM-kappa-tie, dataset + hnorm) — if yes G3 is assembly); 3c-iv: state the div-level germ-reading of the dressed transition (germToFunctionField of transitionUnitOfCover-val vs the generator-quotient germs — the u-relations from G2 + DIV-UNIT via RP-5-atom) — design its statement against L1a-consumption (the divisor-matched G-function construction)
