FOCUS: route beta — fullLevelHom_baseChange ALREADY EXISTED (GLSchemeAction.lean:402); my duplicate is removed. NEXT: build the SQUARE version directly on fullLevelHom_baseChange (transport through fullLevelSqIso, check on inclusions with fullLevelSqIso_inv_ι) to identify the kernel-pair transition; then hdet splits into (a) the reading relation and (b) WP-D3d.
