FOCUS: DECISION GATE — adjudicate the A-cancellation (prime alternative, doc cont.6): trace res(A_i A_j^-1)-dressed transitions through L2e (FieldLeaf:279 functionFieldMap_translateByPoint_germ hypothesis-shape), the 3c-iv germ-reading, and L1b matching; note L4 (weilPairingEval_eq_torsionSplittingEval) is DATASET-INDEPENDENT so the value-side tolerates any normalized dataset — the question is only whether hnorm+hsplit+3c-iv hold for the A-dressed family OR whether the glue should use the NATIVE-middle t_W-family directly as its OWN dataset (check hnorm for t_W!). If native-family works as the dataset: transport-block (H2/H3-S/H3-T1/per-chart-as-stated) leaves the critical path entirely — re-cut [SK-per-chart] to the native-vs-native transition (iso-cancels + W-level A1-sibling). Else: L-layer route (L4a muIso-comp via mathlib LaxMonoidal.comp; L3 W-asIso functoriality).
