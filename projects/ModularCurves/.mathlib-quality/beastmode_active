FOCUS: route beta — fullLevelPairing_eq_of_levelCoord LANDED (transition columns => determinant law, no invertibility). NEXT: the piecewise argument — decompose W by levelTransitionCols of the two pulled-back level structures, apply this on each piece via fullLevelSqIso_inv_baseChange, glue with locConst_hom_ext; then only WP-D3d remains.
