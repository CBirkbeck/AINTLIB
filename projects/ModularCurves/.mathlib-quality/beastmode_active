FOCUS: route beta item (A) step 1 — obstacle localised to ONE missing 5-line lemma: comp_pointToTorsion_of_eq (index as variable + subst, pattern 6). Write it, then fullLevelHom_pullAlong closes per the recipe in FullLevelPairing.lean.
