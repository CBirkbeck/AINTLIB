FOCUS: route beta item (A) — coverTrivReading landed. NEXT: the comap identification (apply constSchemePointsEquiv_natural AT THE USE SITE, or name the point first — stating it standalone times out at isDefEq), then the pointwise transition statement c (b t) = gl2Both g (c (a t)).
