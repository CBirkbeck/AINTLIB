# Decomposition: KM-INTEGRAL — the Drinfeld upgrade of Y₁(N) (Katz–Mazur Ch. 5)

*Coordinator /develop pass, 2026-07-12. Source of record: Katz–Mazur, "Arithmetic Moduli of
Elliptic Curves" (`refs/ModularCurves/katz-mazur-arithmetic-moduli-FULL.pdf`; pdf page =
print + 11). Print pages 129–141 read at source this pass; verbatim quotes below are from
those pages. KM Ch. 1.4–1.11 / 3 / 6 quotes are the [KM-W0] charter's first-act obligation.*

## Skeleton location
`ModularCurves/Moduli/DrinfeldRegularity.lean` — 4 leaves stated `:= by sorry`
([KM-W0-1] :49, [KM-W1-1] :79, [KM-W2-1] :109, [KM-W3-1] :132).
`lake build ModularCurves.Moduli.DrinfeldRegularity` ✓ (3,088 jobs, sorries only) — verified 2026-07-12.

## Goal (KM First Main Theorem 5.1.1, Γ₁-clause; print p. 129, verbatim)
> "FIRST MAIN THEOREM 5.1.1. Each of the four moduli problems [Γ(N)], [Γ₁(N)], [bal.Γ₁(N)],
> and [Γ₀(N)] is relatively representable over (Ell). Each is finite and flat over (Ell) of
> constant rank ≥ 1, and regular (necessarily of dimension two). Each tensored with ℤ[1/N]
> is finite etale over (Ell/ℤ[1/N])."

Our target is the **[Γ₁(N)] clause**. The ℤ[1/N]-étale clause is CONSUMED from the landed
Y1 chain (`gammaOneNaive_representable` + `isGammaOne_iff_naive` + `torsionπ_etale′`).

## KM's own proof architecture (this IS the tree — transcribed, not invented)

**Reduction 1 (p. 129, §5.2):** "By 3.7.1, the theorem is true over ℤ[1/N]. In view of the
'factorization into prime powers' (3.5.1) of all the moduli problems involved, it suffices
to treat the case when N is a power of a single prime p." → leaves [KM-W0-3] (3.5.1
factorization) + the landed ℤ[1/N] theory.

**Reduction 2 — Deligne's homogeneity principle (pp. 129–130):** axioms Reg. 1–4 for a fixed
prime p:
> "Reg. 1. 𝒫 is relatively representable and finite over (Ell).
>  Reg. 2. 𝒫⊗ℤ[1/p] is finite etale over (Ell/ℤ[1/p]).
>  Reg. 3. 𝒫 depends only on the underlying p-divisible group […]
>  Reg. 4. Let k be an algebraically closed field of characteristic p, E₀/k a supersingular
>  elliptic curve, and E/W(k)[[T]] its universal formal deformation. Then (4A) 𝒫(E₀/k)
>  consists of a single element, and (4B) the (consequently local) finite W[[T]]-scheme
>  𝒫_{E/W[[T]]} is the spectrum of a regular two-dimensional local ring."

**Axiomatic Regularity Theorem 5.2.1 (p. 130, verbatim):** "Any moduli problem 𝒫 satisfying
the axioms Reg. 1 through Reg. 4 is finite and flat over (Ell) of constant rank ≥ 1, and
regular (necessarily of dimension two)."
Proof structure (pp. 130–134, read at source): auxiliary rigid étale level δ₁ (naive full
level ℓ, ℓ ≠ p odd; 𝔐(δ₁) smooth ℤ-curve by 4.7.1); the open set 𝒰 ⊆ 𝔐(δ₁) of
regular-and-flat points (complement closed by properness of finite π); Reg 2 ⟹ 𝒰 ⊇ [1/p];
strict-henselization + completion bridge (EGA IV 18.8.8, 18.8.13) + the (*) fibre-product
spectrum identity; Serre–Tate + Reg 3 ⟹ 𝒫_{E/W[[T]]} depends only on E₀[p^∞]; the k̄
dichotomy (μ_{p^∞}×ℚ_p/ℤ_p ordinary vs the height-2 formal group supersingular) ⟹ up to
W[[T]]-iso only TWO such schemes ⟹ homogeneity (H1)/(H2); Reg 4 seeds the supersingular
class; 4.12.1 gives rank constancy.

**Prop 5.2.2 (Reg 4B bis; p. 135, verbatim):** "𝒫_{E/W[[T]]} is the spectrum of a local ring
whose maximal ideal is generated by two elements" suffices — dimension ≥ 2 from finiteness +
dominance ("its image is closed (the map being finite, hence proper) and its image contains
the open dense set Spec((W[[T]])⊗ℤ[1/p])"), then "It is then automatically flat over
W[[T]], because any finite morphism between regular schemes of the same dimension is
automatically flat [A-K 1, V, 3.6]." ← miracle flatness for regular schemes: **mathlib GAP**.

**5.3 the Γ₁ case:** Reg 1–3 hold by "3.6, 3.7.1, 4.14.3" (→ [KM-W0]); Reg 4A = Lemma 5.3.1;
Reg 4B bis = Theorem 5.3.2 Case II, which reduces (pp. 137–139) to:
> "(5.3.2.2) (Rigid II). Let k be an algebraically closed field of characteristic p. If R is
> an artin local W(k)-algebra, and if E/R admits 0 as a point of 'exact order pⁿ', then R is
> a k-algebra, i.e., p = 0 in R." [+ the T-invariant clause forcing E/R constant]
via the two-generator criterion (f = X(P), g = T generate max(A)) on the pro-represented
deformation-with-level functor.

**Lemma 5.3.1, Γ₁-case (p. 136, verbatim):** "Consider next 𝒫 = [Γ₁(N)]. Then 𝒫(E₀/k) is
the set of k-valued points P of E₀[pⁿ] of 'exact order pⁿ'. Again the only possibility is
P = 0, which does in fact have 'exact order pⁿ', because it generates Ker(Fⁿ)."

**Prop 5.3.3 (p. 140, verbatim):** quoted in the skeleton docstring — zero section of exact
order pⁿ on a smooth 1-dim commutative group scheme ⟹ p = 0 in R and pⁿ[0] = Ker(Fⁿ).
"Zariski locally on R, we may choose a parameter X for the formal group. Once this is done,
our proposition results from the following one."

**Prop 5.3.4 (pp. 140–141, THE COMBINATORIAL CORE, verbatim in skeleton):** the binomial
argument — G^{pⁿ} = X^{pⁿ}A + Y^{pⁿ}B; total-degree-pⁿ comparison; A(0,0)=B(0,0)=1;
(X+Y)^{pⁿ} = X^{pⁿ}+Y^{pⁿ}; (pⁿ choose i) = 0 for 0<i<pⁿ; i=1 ⟹ pⁿ=0; i=p^{n−1} ⟹
"p × (an integer prime to p) = 0" ⟹ p = 0.

**(5.3.5) (p. 141, verbatim):** "Assertion II is just the proposition itself, applied to E/R."

## The wave/leaf table

| Node | Content | Status | Skeleton / owner |
|---|---|---|---|
| KM-W0-1 | Drinfeld transport (pullback of exact order) | READY (statable; proof = KM 1.4 divisor-base-change, CartierDivisor.lean substrate) | :49 |
| KM-W0 | integral rel-rep + finiteness of [Γ₁] (KM 3.6/3.7.1/4.14.3) + `gammaOneProblem` def + 3.5.1 factorization | WAVE-CHARTER (first act: focused --decompose of KM 1.4–1.11 + 3.5–3.7 with quotes; D2's Ch-6 pieces = seam, consume) | ticket |
| KM-W1-1 | 5.3.4 first conclusion (p = 0), raw power-series form | READY | :79 |
| KM-W1-2 | 5.3.4 second conclusion (subgroup = Ker Fⁿ) | statement joins with W2's Frobenius vocabulary | ticket |
| KM-W2-1 | 5.3.3 first conclusion, elliptic affine case | READY (chart parameter via Weierstrass machinery; divisor bridge via killed-locus layer) | :109 |
| KM-W3-1 | Rigid II first half | READY (one-line from W2-1 per 5.3.5) | :132 |
| KM-W4 | universal formal deformation E/W(k)[[T]] + T-invariant + Rigid II second half + 5.3.2 Case II two-generator assembly | **API-GAP charter** (deformation theory; Witt vectors ✓ mathlib; deformation functor + pro-representability = new) | ticket |
| KM-W5 | p-divisible groups + k̄ dichotomy + Serre–Tate | **API-GAP charter — research-scale** (all three absent from mathlib; flagship treatment recommended) | ticket |
| KM-W6 | axiomatic engine 5.2.1 + 5.2.2 | WAVE-CHARTER, gated on W5 + three named mathlib gaps: strict henselization (only `HenselianLocalRing` exists), completion-regularity transfer (EGA IV 18.8.8/18.8.13), miracle flatness for regular schemes [A-K V 3.6] (absent — fleet-confirmed at the Y1 N6 campaign) | ticket |
| KM-W7 | 5.1.1 Γ₁-clause assembly | gated on W0+W3/W4+W6; ℤ[1/N] clause consumed from landed Y1 | ticket |

## Mathlib inventory (verified this pass)
- `Mathlib.RingTheory.FormalGroup.Basic` ✓ EXISTS (`FormalGroup`, 𝔾ₐ/𝔾ₘ/map) — W1/W2 vocabulary.
- `MvPowerSeries.coeff (n : σ →₀ ℕ)` / `constantCoeff` ✓ (current implicit-σR signatures, verified by build).
- `WittVector` ✓ (full directory) — W4 vocabulary.
- `RingTheory/RegularLocalRing/` ✓ (exists in current mathlib) — W6 vocabulary partially present.
- Strict henselization construction ✗ GAP; completion-regularity iff ✗ (verify at W6 pickup); miracle flatness ✗ GAP; p-divisible groups ✗✗; Serre–Tate ✗✗; universal formal deformation of elliptic curves ✗✗.

## Attacks (this pass; the wave charters re-run per-leaf attacks at execution)
- **W1-1 statement vs source**: KM's subgroup hypothesis at the universal quotient
  B = R[[X,Y]]/(X^{pⁿ},Y^{pⁿ}) is literally "(G(X,Y))^{pⁿ} ∈ (X^{pⁿ},Y^{pⁿ})" (p. 140–141,
  quoted) — the skeleton hypothesis is that membership, verbatim. Hypotheses h0/hlin
  transcribe "G(X,Y) = X + Y + ⋯"; the proof uses exactly those two coefficients (the
  degree-pⁿ comparison). Edge attack: n = 0 breaks the i-range (1 ≤ i ≤ pⁿ−1 empty) — `hn : 1 ≤ n`
  required and present (KM: "n ≥ 1"). Hypothesis-strength attack: commutativity/associativity
  of a full FGL are NOT needed for this conclusion — the raw form is deliberately weaker-hypothesis
  than KM's statement (strictly more general; KM's FGL case follows by instantiation).
- **W2-1 vs source**: KM state 5.3.3 for any smooth commutative 1-dim group scheme; the
  skeleton states the elliptic affine case (the only case the Γ₁ application uses — Rigid II's
  E/R). The general-C form is a W2 generalization leaf, not needed for W7. Zariski-locality:
  KM's own proof reduces to a formal parameter choice — matches our chart machinery.
- **W3-1 composition**: could W2-1 hold and W3-1 fail? No — W3-1's hypotheses strictly
  strengthen W2-1's (adds artinian + local, unused); the composition is instantiation. (KM
  5.3.5 says exactly this.)
- **W0-1 name-check**: `Point.asSection E f (Point.pull E f P)` verified as the repo's
  base-change idiom (brief:3171 + GroupLaw.lean:135–153 constructions); the statement
  type-checks in the skeleton (build ✓).

## Prior-B2 consultation
`b2_log.jsonl` (3 entries) checked: T-H4/T-H6 (Γ_H naive-orbit sheafification failure — not
Γ₁-relevant; our problem is points, not orbits, and the integral problem uses the KM
definition of record) and T-B6′-IFACE (hz zero-pinning — not statement-adjacent here). No
name or shape matches. Clean.

## Feasibility
The Γ₁ path through KM Ch. 5 decomposes exactly as KM wrote it, and splits into a
**tractable near-term half** and a **research-scale far half**. Near-term (W0–W3, plus
W6's three named ring-theory gaps as standalone items): W1's combinatorial core is
elementary and fully specified; W2/W3 sit directly on the repo's chart + Cartier-divisor
substrate; W0 is a real but standard rel-rep development (KM Ch. 3 style, with D2's Ch-6
machinery as substrate). Far half: W4 (formal deformation theory of elliptic curves) and
especially W5 (p-divisible groups + Serre–Tate) are multi-charter formalization projects
with nothing in mathlib to stand on — they are the honest critical path to the full
regularity theorem and should be run as flagship charters (W5 possibly coordinated with
mathlib-community interest). Nothing in the near half is wasted regardless: W0 gives the
integral moduli problem and its finiteness (new arithmetic content over every ℤ-base),
and W1–W3 give the characteristic-p rigidity kernel that every KM Ch. 5 argument reuses.

## W2 bridge — precise decomposition (KM 5.3.3 → 5.3.4; source pp. 139–141 re-read 2026-07-13, KM worker)

**Session landed (KM worker, 2026-07-13):** [KM-W1-1] `CharP.p_eq_zero_of_pow_mem_span`
(repaired banked proof under the daily bump — `coeff_add_pow_cross` FunLike-coercion; axiom-clean;
commit ea2b28190) and [KM-W0-1] `isGammaOne_pullAlong` (= `Section.HasExactOrder.baseChange`;
axiom-clean; commit 02502fb5b). [KM-W3-1] staged as the one-line delegation to W2 (pending W2).

**What `E.IsGammaOne (p^n) 0` actually is** (unfolded via `LevelStructure/Basic.lean:70`
+ `ExactOrder.lean:104,97`): `((0 : E.Section).orderDivisor E (p^n)).IsSubgroup E`, i.e. the
relative effective Cartier divisor `p^n·[0] = sectionsDivisor E.π (fun _ : Fin (p^n) => 0)`
is a subgroup of `E/Spec A` in the KM 1.3.6 sense (`RelEffCartierDiv.IsSubgroup`,
`ExactOrder.lean:91`).

**KM Prop 5.3.3 (verbatim, print p. 140):** "Let R be an arbitrary ring, C/R a smooth commutative
one-dimensional group-scheme over R (cf. 1.4.1) … Suppose that the zero section 0∈C(R) is a point
of 'exact order p^n' in C/R, i.e., that the effective Cartier divisor p^n[0] in C/R is a
subgroup-scheme. Then p = 0 in R, and the subgroup-scheme p^n[0] is none other than Ker(F^n)."
Proof (verbatim): "The zero-section and the Cartier divisor p^n[0] both lie in the **formal group**
of C/R. **Zariski locally on R, we may choose a parameter X for the formal group.** Once this is
done, our proposition results from [5.3.4]."

**KM Prop 5.3.4 (verbatim, print pp. 140–141):** "…G(X,Y) = X + Y + ⋯ a one-parameter commutative
formal group law over R. Suppose that for some prime power p^n, the equation X^{p^n} = 0 defines a
subgroup-scheme of G. Then p = 0 in R…" Proof (verbatim): "The hypothesis means that **for any
R-algebra B, and any elements x,y ∈ B, we have the implication x^{p^n} = y^{p^n} = 0 in B ⟹
(G(x,y))^{p^n} = 0 in B.** Considering the **universal situation B = R[[X,Y]]/(X^{p^n},Y^{p^n})**,
we see that inside R[[X,Y]], we have (G(X,Y))^{p^n} ∈ (X^{p^n},Y^{p^n})…" [then the binomial
argument = **[KM-W1-1], DONE**].

**Layered sub-ticket scaffold (decl-names TBD from the substrate map; each link EXISTS/GAP):**
- **(W2-L-fg)** the formal group law `G ∈ A⟦X,Y⟧` of `E/Spec A` at the origin: `G = X+Y+⋯`
  (`constantCoeff G = 0`, both linear coeffs `= 1`), commutative 1-parameter. GAP-or-substrate:
  repo `EllipticCurve` is scheme-theoretic; HasseWeil (`projects/HasseWeil`, importable) has the
  FGL of an affine `WeierstrassCurve R`; the linchpin is a `WeierstrassCurve A`-model of `E`.
- **(W2-L-param)** parameter `X` at `0`: completed local ring of `E` at the zero section `≅ A⟦X⟧`
  (KM "Zariski locally … choose a parameter X"); base is affine so expect global-over-A, not a
  Zariski cover. GAP-or-substrate: repo chart machinery (`PoleFiltration`/`PoleSheaf`/
  `ModelVariableChange`, `coordRingToZSection`, `chart*SectionsEquiv`).
- **(W2-L-div)** `p^n·[0]` ↔ the killed locus `X^{p^n}=0`, i.e. the ideal `(X^{p^n})` in `A⟦X⟧`:
  `orderDivisor 0 (p^n)`'s ideal at the origin chart = `(X^{p^n})`. GAP.
- **(W2-L-sub)** the "universal situation" step W1-1 does NOT cover: from `IsSubgroup (p^n·[0])`
  derive `G^{p^n} ∈ (X^{p^n},Y^{p^n})` in `A⟦X,Y⟧` — via the comultiplication (group law) on the
  completed local ring of `E×E` at `(0,0) ≅ A⟦X,Y⟧` (avoid formalizing eval-at-arbitrary-nilpotents;
  work in the completed local ring directly). GAP.
- **(W1-1)** `G^{p^n} ∈ (X^{p^n},Y^{p^n})` ⟹ `p = 0`. **EXISTS** — `CharP.p_eq_zero_of_pow_mem_span`.

Note the "p^n[0] = Ker(F^n)" second conclusion of 5.3.3/5.3.4 (= [KM-W1-2]) comes *after* `p=0`
(Frobenius defined); deferred with the Frobenius vocabulary per the wave table.

## [KM-W2-core] — adversarial `/develop --decompose` pass (KM worker, 2026-07-13)

*Ran Phase 1e adversarially (disposition: oppose the plan). The tractable front (L1 `orderDivisor_zero_ideal`,
L2 `orderDivisor_zero_ideal_principal`) and the endpoint L6 (`CharP.p_eq_zero_of_pow_mem_span`) are DONE +
axiom-clean and are NOT re-attacked here. This pass adversarially verifies the residual route L3·L4·L5.*

### Step 1 — KM's actual proof of 5.3.3 (full, verbatim, re-read pp. 139–141)
The **entire** proof of 5.3.3 is the three sentences quoted above: reduce to **the formal group of C/R**,
choose a parameter X (Zariski-locally), invoke 5.3.4. KM does **not** construct the formal group here — he
uses it as standing theory ("cf. 1.4.1"; the formal group of a smooth 1-dim group scheme is Ch.1 / standard
Silverman IV material). 5.3.4's proof is the universal-situation reduction (→ L5) + the binomial argument
(= L6, DONE).

### Step 2 — decomposition tree (mirrors KM)
- **L3** relative formal completion + parameter: `Ô_{E,0} ≅ A⟦X⟧` (equivalently the formal group `Ê` of `E/Spec A`).
- **L4** the scheme group law read in `X` = a 1-param FGL `F ∈ A⟦X,Y⟧`, `F ≡ X+Y mod deg 2`.
- **L5** `IsSubgroup(pⁿ[0])` ⟹ `F^{pⁿ} ∈ (X^{pⁿ},Y^{pⁿ})` (KM's universal `B=A⟦X,Y⟧/(X^{pⁿ},Y^{pⁿ})`).
- **L6** = `CharP.p_eq_zero_of_pow_mem_span` (DONE). Plus a Zariski-glue (`p=0` local ⟹ global; `A → ∏ Aᵢ` injective on a cover).

### Step 3/4.5 — ATTACKS ATTEMPTED on the route (the binding adversarial artifact)
Red-flag #1 fired: L3/L4 need "substantial infrastructure absent from mathlib" (the formal group of a
scheme-theoretic group scheme). Per the source-faithfulness rule this "almost always means the source has an
easier route" — so the route was actively attacked:
- **[A1] Is there an easier KM route avoiding the formal group?** Re-read 5.3.3's full proof: NO. KM's proof
  IS "both lie in the formal group … choose a parameter X … results from 5.3.4." The formal group is
  KM's own, unavoidable tool. → L3/L4 are **faithful to KM**, not invented scaffolding. Red-flag #1 is a
  genuine API gap here, not the Wedhorn-style "invented route" error.
- **[A2] Can the abstract completion (L3) be dodged via the concrete inf-chart `R[t][s]/(cubic)`?** NO. The
  group law on the chart is an algebraic (not polynomial) map; its expansion at 0 IS a power series, i.e.
  the completion. The divisor is `span{t^{pⁿ}}` (L2 ✓) but the *group law* forces the formal expansion. → L3 unavoidable.
- **[A3] Can HasseWeil's `formalGroupLaw W` be used without L3?** NO. `formalGroupLaw W` is an algebraic
  chord-process series in `R⟦X,Y⟧`, built with no scheme. Using it for W2 requires proving the SCHEME mult,
  expanded in the completed coordinate, EQUALS it — which presupposes L3 (the completion) + a mult-matches-chord
  proof (L4-connect). → HasseWeil shortens L4's *coefficient facts* (h0/hlin via `formalGroupLaw_coeff_left/right_unit`)
  but does NOT remove L3 or the scheme-connection.
- **[A4] Is L5 dischargeable once L3/L4 exist?** PLAUSIBLE-but-nontrivial: KM's "for any R-algebra B" is a
  functor-of-points reading of `IsSubgroup`; instantiating it at the universal `B` and extracting the ideal
  membership needs the comultiplication on the completed local ring of `E×E`. Bounded once L3/L4 land; a leaf under them.

### Step 4/5/6 — feasibility verdict: DEEP API GAP (charter), faithful to source
**L3 + L4 = the formal group of a smooth 1-dimensional group scheme over an arbitrary base** (relative formal
completion `Ô_{E,0} ≅ A⟦X⟧` + the induced FGL, with `F ≡ X+Y`). This is **absent from repo, HasseWeil, and
mathlib** (verified via the substrate map). HasseWeil formalises the *affine Weierstrass* FGL algebraically
but neither the scheme-theoretic formal completion nor the scheme-mult ↔ chord-series identification. mathlib
has NO scheme-theoretic elliptic curve and NO Weierstrass formal group. Building L3/L4 is a **multi-week
formal-geometry sub-development** — textbook (Silverman IV, relativised), NOT research-frontier (so NOT B3;
it's a hard-formalisation charter, unlike [KM-W5] Serre–Tate). It is a genuine **API GAP** in the /develop
sense: it cannot be discharged from existing infrastructure and needs its own sub-decomposition (relative
completion theory first, then the FGL, then L4-connect).

**Skeleton note (Step 2.5):** the L3/L4 leaves cannot be stated as building `:= by sorry` declarations without
first choosing the relative-completion API (mathlib `adicCompletion` vs a bespoke `PowerSeries A` iso) — that
choice is itself the first leaf of the API-gap sub-development. So the buildable skeleton for W2-core is
deferred into that sub-development rather than force-fit here (the front L1/L2 + endpoint L6 already build).

**Recommendation:** keep [KM-W2-core] as a boarded charter (this analysis is its plan). Its own first act,
when a worker commits to it, is a `/develop --decompose` of the *relative formal group of an elliptic curve*
(L3 completion → L4 FGL → L4-connect → L5), tensioned against Silverman IV + HasseWeil's affine FGL as the
coefficient substrate. W2 (and W3, W7) stay blocked on it. The near-half marathon proceeds on W0/W6 meanwhile.

**Open question the substrate map resolves:** are (W2-L-fg)/(W2-L-param) dischargeable on
repo-chart + HasseWeil-FGL substrate (medium sub-tickets), or is "the formal group of a
scheme-theoretic elliptic curve + the divisor↔FGL dictionary" a deeper charter? The decomposition's
"W2 sits directly on the chart + Cartier substrate" is being tension-tested this session.
