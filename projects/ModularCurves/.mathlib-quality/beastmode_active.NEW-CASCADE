NEW-CASCADE on y1: step-1 merge EXECUTED but HELD (dev tree-broken since 40e44a706 — Representability.lean clobber; forensics = dev board v10.132-CASCADE). Merge preserved on wip/y1-dev-merge-cascade; this tip stays green pre-merge. PARKED at double gate: (1) dev regression fix, (2) c5β T-G4. On both: re-merge + top-up → spine verify → push → steps 2–4.
