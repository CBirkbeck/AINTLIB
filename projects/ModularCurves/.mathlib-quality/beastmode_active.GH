FOCUS: [GHA3 beta2-heart U2] next firing — the bridge lemma isFullLevel_baseChange_comp_iff per the v10.263-GH recipe (rw isFullLevel_iff_naive both sides; kill-transport via asSection=baseChangeEquiv.symm-congrBase; span-clause per kbar-point via the cL/cR AddEquiv chains + baseChangeEquiv_pull_asSection + map_closure). U1 compats LANDED clean (d85ebca57). Then U3 factor-iff assembly, then beta3 clopen. beta1/beta4/beta2-ambient/beta2-L all DONE.
