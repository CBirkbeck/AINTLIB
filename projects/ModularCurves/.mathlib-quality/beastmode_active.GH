FOCUS: STREAM-GH marathon — GHB7 assembly, fields 1-5 DONE. PUSHED (all axiom-clean unless noted): GHB5a-i/GHB5a/GHB5★, GHB7-2a(+eq), GHB7-2b(+compare_equivariant), QuotPkg layer + hbase rule-5, exists_mapDescent, α (compare_pullback_id + toPullbackAlong_self/congr), β (toPullbackAlong_comp + compare_pullback_comp), transport πT/mapT/spec/mapT_id/mapT_comp/mapT_congr, ★quotProb THE FUNCTOR (7fed16eb0), projQ + naturality (dbd01cc67), projQ_invariant (d9852f9ef), isIso_mapT_pullbackAlongπ (1cbca5a97), ★relRep_quotProb (81c043a10, sorryAx-gated on GHB6 via f₀_finite_etale ONLY). REMAINING for exists_quotientProblemData: couniversal (KM 7.1.3(1) verbatim — μ via per-X: P′-relrep-datum dP′X + section-classification: μ.app X s := ν′-transport through eqv-of-dP′ at the section; uniqueness via πX-epi-surjectivity? NO — via the ∃!-couniversal-descent: πX finite-étale-SURJ ⟹ projQ.app-X SURJECTIVE-ish? — KM: any G-equivariant map factors: μ(s) := ν′(any-preimage-a) well-defined by geom/torsor — NEEDS the fibre=orbit clause: DESIGN: use hdesc-style at the MODULI level: prob-sections-lift-étale-locally… — CHECK KM p.108-109 for the verbatim argument before writing), geom_surjective + geom_orbits (k̄-points: finite étale cover splits over k̄ + GHB4-torsor-fibres), then assembly ⟨quotProb, projQ, projQ_invariant-fun, relRep_quotProb-fun, couniv, gs, go⟩. LEAN-OPS this leg: (10) NEVER write w-ascriptions with (πg).baseHom-forms in lift-shows — creates @Y-Frankenstein pullback-args ill-typed-at-instances that hard-abort EVERY later kabstract in scope — spell plain g/k composite indices; (11) mapT-choice-terms NOT defeq-transportable across Ell-morphism-Eq — mapT_congr (subst-form, eqToHom-bridged) + eqToHom≡𝟙-via-proof-irrel-rfl-show when endpoints defeq; (12) hard-abort kabstract (Application type mismatch during rw) = some subterm has mixed @-slots — congrArg/refine-trans term-steps or ∀-u-key.
