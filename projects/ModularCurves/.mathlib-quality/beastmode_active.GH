FOCUS: STREAM-GH — ★★★ GHB7 ASSEMBLY COMPLETE (da44daabe): exists_quotientProblemData PROVEN, GHC1 gammaH_relativelyRepresentable LIVE — sorryAx ONLY via the [GHB6] f₀_finite_etale edge (single tracked gate). NEXT: [GHB6-RING] — discharge isFinite_etale_of_comp_of_finite_etale_surjective (GammaHRepresentability ~:709) chart-locally: π : Z→Z₀ finite étale surjective, f₀ : Z₀→S with π≫f₀ finite étale ⟹ f₀ finite étale. Chart-route (banked in the GHB6-RING sub-ticket, tickets.md): affine-local on S and Z₀: B := O(Z₀-chart), A := O(Z-chart) finite étale over C := O(S-chart) with B→A finite étale (faithfully flat since surjective+flat+affine); (finite-half): B is a C-module direct summand of A (B→A split mono as B-modules via the A711-FP projectivity layer / trace) ⟹ B finite over C [Module.Finite.of_surjective-ish through the summand]; (étale-half): B→A ff étale + A étale over C ⟹ B étale over C = étale descent along ff étale covers of the SOURCE: unramified-half via Ω: Ω_{B/C}⊗_B A ≅ Ω-exact-sequence + Ω_{A/C} = 0 + Ω_{A/B} = 0 ⟹ Ω_{B/C}⊗A = 0 ⟹ Ω_{B/C} = 0 [lTensor_reflects_triviality, A faithfully flat]; smooth/flat-half: B ⊆ A summand ⟹ B flat over C [flat-of-summand-of-flat] + fp [FinitePresentationCancel-file?] ⟹ B smooth?— étale = flat+unramified+lfp over a field-free base: étale ⟺ flat ∧ unramified ∧ lfp [mathlib Algebra.Etale-iff?]. INVESTIGATE mathlib: RingHom.Etale-characterization + the project ForMathlib/{FinitePresentationCancel,FaithfullyFlatFiniteDescent,FinitePresentationDescent}.lean (names suggest EXACTLY these pieces exist!). THEN: full-clean re-audit of GHC1 + report. Boards: day-2/2b/3 blocks in tickets.md.
