FOCUS: BB-DELIGNE Layer B (T-D5h..k) → box smul_eq_zero_of_factors (ExactOrder:115). Layer A DONE; T-END0 is p0.
COMMITTED efb7d7ea, 8cb532fc, +(next): deligne_point_pow_eq_one (axiom-clean) + GroupScheme/DeligneOrder.lean.
DeligneOrder.lean GREEN (2 intended sorries: affine core L99, general box L205). CLOSED: L2 subgroupAlgebra+_finite; **L3 group-object DATA COMPLETE from IsSubgroup** — bipt1/bipt2/upt (universal pts), subgroupMul m:D×_S D→D, subgroupUnit e:S→D, subgroupInv n:D→D, all +_subschemeι defining eqns (via IsSubgroup add_mem/zero_mem/neg_mem + mono subschemeι). Route VALIDATED.
NEXT: group AXIOMS as scheme eqns (m assoc, unit/inv laws via mono subschemeι + curve group axioms on pullback products) → dualize m/e/n to Δ:A→ₐA⊗_R A, ε:A→ₐR, antipode:A→ₐA over affine base (needs Γ(D×_R D)≅A⊗_R A product-of-affines global sections — CHECK mathlib) → HopfAlgebra R A + IsCocomm (L4) → L5 localise-to-free (proj not free) → L6 points↔convolution (ΓSpec adj+isoSpec, curve-law=conv) → L7 assemble via deligne_point_pow_eq_one. Plus L1 reduction general→affine. Templates: torsionAlgebra/PointsEquiv (EtaleDescent), muNPointsEquiv (MuN).
