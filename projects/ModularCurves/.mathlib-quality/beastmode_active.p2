FOCUS: BB-DELIGNE Layer B (T-D5h..k) → MILESTONE box smul_eq_zero_of_factors (ExactOrder.lean:115). Layer A DONE; T-END0 is p0.
DONE this session: deligne_point_pow_eq_one (point-GROUP form (toConv φ)^(finrank R A)=1 in mathlib WithConv(A→ₐB) CommGroup; CartierDual.lean; axiom-clean [propext,Classical.choice,Quot.sound], green 1867 jobs) — the geometry-free Layer-B consumable. CONFIRMED: IsFinite extends IsAffineHom (mathlib Finite.lean:39) so D.subscheme affine-over-S is FREE via D.finite; convGroup=mathlib Group(WithConv(A→ₐC)) antipode-inverse; AlgHom.toLinearMap_convPow bridges linear↔alghom.
ROUTE (to write as decomposition once substrate map lands): reduce box to S,T affine (via proven IsSubgroup.baseChange + Point.pull_zsmul/zero + local-on-T); D.subscheme=Spec A_D finite proj Hopf R-alg (comul dual to curve-law m:D×D→D from IsSubgroup at universal pt); localize R→free for deligne_point_pow_eq_one; identify curve-law on D(T)=convolution in WithConv(A_D→ₐB); (toConv φ)^N=1 ⟹ N•Q=0. NEXT: process Explore substrate map (agent ac394f93) → write GroupScheme/DeligneOrder.lean skeleton + focused leaf tickets, start T-D5h-mul (m from IsSubgroup) or whatever the map shows is the shortest tractable first leaf.
