FOCUS: BB-DELIGNE close deligne_operators [coordinator, from Tate text, RR-only]. DONE+green: psiAlgEquiv (M≅T algebra iso), translationEquiv (τ auto-ness via antipode monoid algebra). NOW piece 3 (coevaluation + Lemma 3.8.2): define coev = sum ιₘ(coord i)⊗b i; prove (A) psiAlgEquiv coev = pointConv includeRight [dual basis]; (LHS) psiAlgEquiv(translationEndo φ coev)=pointConv(rightTranslationAlgHom φ) [generator formula (f.ofConv x⊗1)·RT_φ(a) + dual-basis collapse]; (RHS) psiAlgEquiv(pointConv φ⊗1)=pointConv(includeLeft∘φ); then coev_relation reduces to cocommutativity sum φ(a1)⊗a2 = sum φ(a2)⊗a1. Then deligne_operators := ⟨coev-as-unit, translationEquiv φ, coev_relation⟩.
