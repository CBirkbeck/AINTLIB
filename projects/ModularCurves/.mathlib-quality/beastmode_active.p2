FOCUS: BB-DELIGNE Layer B, L4 Hopf laws. LANDED: point_add_eq_lift (point-addition underlying-map spec, axiom-clean) + subgroupMul_comm (scheme commutativity swap≫m=m, axiom-clean, transport-free via point_add_eq_lift + Over.tensorObj_ext + term-mode .trans; KEY GOTCHA: rw/simp BOTH fail on tensor .left compositions — semireducibility wall — use pure term-mode congrArg/.trans, proj helpers via Over.comp_left.symm+congrArg). NEXT L4: associativity (m∘(m×id)=m∘(id×m)), unit laws (e is identity), inverse laws (n is inverse) — same point_add_eq_lift technique — then dualize all via Δ/ε/antipode pins to Coalgebra/Bialgebra/HopfAlgebra R A + IsCocomm (subgroupMul_comm→IsCocomm). Then L5/L6/L7→affine core→discharge 4 sorries→Milestone 1 BB-DELIGNE DISCHARGED.
