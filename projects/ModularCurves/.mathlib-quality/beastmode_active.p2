FOCUS: BB-DELIGNE Layer B (T-D5h..k) → box smul_eq_zero_of_factors (ExactOrder:115). Layer A DONE; T-END0 is p0.
COMMITTED efb7d7ea + (next commit): deligne_point_pow_eq_one (point-GROUP form, axiom-clean) + GroupScheme/DeligneOrder.lean.
DeligneOrder.lean GREEN (2 intended sorries: affine core L99, general box L166). CLOSED so far: L2 subgroupAlgebra+_finite (A=Γ(D.subscheme) finite R-alg over Spec R); **L3 CRUX subgroupMul = m:D×_S D→D from IsSubgroup PROVEN** (bipt1/bipt2 universal pts, exists_factor_bimul via IsSubgroup+add_mem, subgroupMul via mono subschemeι, subgroupMul_subschemeι). Route VALIDATED end-to-end.
NEXT (downstream of validated crux): Delta=m^sharp : A→ₐ A⊗_R A over affine base (needs Γ(D×_R D)≅A⊗_R A = product-of-affines global sections) → HopfAlgebra R A + IsCocomm (L4) → localise-to-free (L5, projective_of_finitePresentation gives PROJ not FREE) → points↔convolution (L6, ΓSpec adj + isoSpec, curve-law=conv dual to Δ) → assemble via deligne_point_pow_eq_one (L7). Plus L1 reduction general→affine (base-change along g via Point.asSection/baseChangeEquiv + IsSubgroup.baseChange, then cover S by affines + locality). Multi-session, each leaf bounded. Templates: torsionAlgebra/torsionAlgebraPointsEquiv (EtaleDescent.lean), muNPointsEquiv (MuN.lean).
