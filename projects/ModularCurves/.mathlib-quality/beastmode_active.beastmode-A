FOCUS: T-W7.1b-b2 — piece (ii) COMPLETE: mem_poleOrderFiltration_iff PROVEN (both directions + subsingleton split). The filtration is now INTRINSIC: F_n-membership = regularity of f*s^n on the infinity chart. REMAINING for b2: piece (i) [criterion is stalk/section-local — may be SKIPPABLE: recheck whether (iii) can transport the ALGEBRAIC criterion directly: the e-transport needs overlapMap-W-of-Phi(f) related to overlapMap-W-of-f-image — via pointedIsoGamma + a Y-chart-side transport... NOTE e need NOT preserve the Y-chart, so the algebraic criterion transports only if the B-range/root-powers correspond — think via: criterion is equivalent over ANY chart covering the section; e preserves the SECTION; consider reformulating the transport through the ideal-sheaf stalks per original blueprint (i)+(iii)] then pointedIsoCoordEquiv_filtration assembly. THEN b3x/b3y/main/injectivity.
