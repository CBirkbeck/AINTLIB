FOCUS: T-W7.1b-b2 (ii-b): overlap_coordOfShift_eq PROVEN (set-based clearing: hterm per-term cleared_term+rho^n-merge, pow_mul_mul_overlapInvT, basis-compare). NEXT: the endgame lemma mem_of_overlapMap_mul_root_pow (criterion => membership): obtain p q via exists_smul_basis_eq, rewrite hab via lift_of/coe_eval2RingHom (mirror overlap_pair endgame lines ~1250-1290 of the file), N := max(deg p, deg q + 1), hstar := overlap_coordOfShift_eq; step-1 q-bound: by_contra n < 2m+3, case 2d vs 2m+3 parity-kills with coordOfShift_coeff_lead/_eq_zero at slots (d+n)%3 order N-d+(d+n)/3 resp (m+n)%3 order N-1-m+(m+n)/3, RHS-vanish X_pow_dvd_iff k<N (needs n<2d+j resp n<2m+3+j — check omega); step-2 p-bound with q-bound in hand; span-write: f = p.1 + q.mkY = sum-monomials via as_sum_range + hcast/hx-pattern (PoleFiltration:201-207). Then public iff mem_poleOrderFiltration_iff (subsingleton-split + both directions). THEN b2 pieces (i)+(iii).
