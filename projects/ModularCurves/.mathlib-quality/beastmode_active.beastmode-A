FOCUS: T-W7.1b b2-(γ) INC-3g-3 — (g4) combined value: zero.appLE(e⁻¹BO′)⊤ value of (e.app w′) = ΓSpecIso.inv(aug′ (ΓB′sec w′-avatar)) via pointedIso_zero_appLE_chartY (elementwise via appLE_le_rfl_apply) + projModelZero_appLE_chartYSection W′. Then (g5) the aug-r vanishing pack: localized augmentation on Γ(bo r) + ker = (ŝ,t̂)-localized + σ,τ ∈ ker via (g4). Then mirror + α-unit + witness assembly (3g-final), δ-assembly (3h).

KEY ANTIDOTES (append-only): kernel timeout on appLE-composition with CONCRETE morphisms (projModelZero/e.hom) → state categorical facts for OPAQUE variables in own decl (appLE_appLE_of_comp_eq pattern), instantiate after; subst-trick (∀ f, hf : A = f → …; intro; subst; rfl) for morphism-rewrites under dependent appLE proofs; point-level opens proofs NOT Opens-rfl-casts (kernel unfolds comp.base); rw on big Γ-goals times out → forward congrArg chains + congrArg₂ + Subsingleton.elim thin-homs + presheaf.map_id forward; stale olean: lake build PF before MVC.
DONE: chartPointOf pkg; aug=zeroChartHom; fromSpec-awayι bridge; overlap=bo(t-sect); ε-def+bo-computation+unit-aug-nbhds; overlap-eq transport (d-generalized); sections dictionary; zero-appLE value (g2 W-side); hez-appLE square (g3).
