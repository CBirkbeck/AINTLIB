FOCUS: [T-G3d-infra] Piece 3 — FULL ROUTE-INDEPENDENT REDUCTION COMPLETE + axiom-clean.
isColimit_of_isHopfGalois PROVED (HopfGaloisQuotient.lean): IsHopfGalois ρ ⟹ affine-local IsColimit,
via isRegularEpi_specEqualizerπ (023Q half 1, faithfullyFlat) + isKernelPair_specEqualizerπ (023Q half
2: tensor pushout → Spec kernel pair, transported along galoisEquiv) + IsKernelPair.toCoequalizer'.
The ENTIRE abstract Hopf-Galois → coequalizer chain is done (5 new ForMathlib files this session:
ComoduleCoinvariants, Coaction, HopfGalois, SpecEqualizer, HopfGaloisQuotient). Composed with
exists_unique_lift_of_isColimit + isInvariant_iff_coequalizes ⟹ SubgroupQuotient pins per affine chart.
REMAINING (all need E-geometry, connecting abstract ρ/B⊗A to the curve):
  (1) ρ = translation co-action [3a-ii E-geometry: O(G×ₛSpecB)≅B⊗A on a G-stable chart]
  (2) IsHopfGalois(translation co-action) [crux PROOF, scoping-blocked v10.38: general vs E[N] shortcut]
  (3) glue [3a-iii/iv: G-stable affine cover + SchemeQuotient GlueData; consumes 1+2]
NEXT candidate: 3a-ii (co-action = act^# on affine chart) OR the geometry setup for the G-stable cover.
Both need E's affine charts — deep geometry. Crux (2) scoping-blocked; glue (3) consumes crux.
CONSTRAINTS: p2 Layer-B files READ-ONLY (their sentinel live); new files only; HEAD-guard + pathspec commits;
#print axioms verify. NIsogeny/EngineDescent/AdditionChartOverlap/SchemeActionFree = concurrent workers', don't touch.
