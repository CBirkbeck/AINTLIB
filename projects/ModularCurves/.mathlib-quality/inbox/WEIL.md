# INBOX: WEIL (fresh worker — the Weil-pairing backend charter. Read fully before touching anything.)
- [2026-07-12, coordinator, v10.153] **CHARTER-WEIL: [T-C1-KM28] — the Weil pairing e_N : E[N] × E[N] → μ_N as a perfect pairing of finite flat group schemes** (KM 2.8; p2-seat takeover, their pushed scratch = substrate, credited). WHY NOW: it gates GHA3/DS4's étale conjuncts (Γ_H) and YFULL's CLOPEN route α — the highest-leverage unstaffed item in the general-curves push. WHAT YOU INHERIT (all landed, consume by name): E[N] as a group scheme with the full mulOver law set (c5β C5B-2 + the closer's K-chain); `torsionπ_isFinite_of_nIsInvertible` + flatness (the N5–N7 ladder, MulByHomFlat/MulByHomSmooth); the GL₂(ℤ/N) scheme action (glSchemeSmul); NEW-HOPF's Hopf/comodule layer + p2's divisor-indexed Hopf substrate (read-only, cite); HasseWeil's pairing-adjacent results (cross-project — verify statements + axiom profiles FIRST, board the bridge, never re-derive). SETUP: shared main worktree, dev/modular-curves; NEW files (`EllipticCurve/WeilPairing.lean` or similar); sentinel `beastmode_active.WEIL`; rule-5 claim before files. FIRST ACT (v10.8): `/develop --decompose` vs KM 2.8 + Mumford AV §20 + refs (verbatim quotes; route decision — Cartier-duality-native vs cubical/biextension vs divisor-symbol — boarded with an adversarial route audit, the PIC0 v10.99 standard). BOUNDARIES: p0/p2 files read-only; PIC0's Pic⁰/Cartier seam (v10.36 never-build-duality-twice: their CHARTER-PIC-3 builds Pic⁰_{E/S} — if your route needs Cartier duals of E[N], BOARD THE SEAM before building; the pairing itself is yours). Discipline: v10.24(a–e); v10.115-a; atomic pathspec commits (+`git add` first for new files); stop-lines 3–4 iterations → ledger; spine-verify at merges; v10.52; v10.35b. REPORT (charter mode): at decompose completion, at each named milestone, at completion = e_N perfect + bilinear + alternating + Galois-equivariant (or the KM-2.8 clause set your decompose fixes), axiom-clean.
