# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**Four active seats (as of v10.265, 2026-07-16):** ALL FOUR FREE + re-dispatched. **★★★ BRICK 6 IS ONE
SORRY FROM DONE** — `brick6_intertwining` PROVEN axiom-clean (the whole FFM chain landed); the sole
remaining sorry in `MulByHomDegree.lean` is **`brick6_from_intertwining` (:1356, the finrank-tower
assembly), with TWO exact fixes banked** (instance-pin + coercion-defeq; verbatim resume plan in the KM
sentinel + decomposition-keystone.md). **Both Bootstrap final assemblies are TURNKEY** (the only sorries
are exactly the keystone inputs: hL :86, hArb :91, E[2]-gen :~178). **BB-DIFF is assembled to ONE leaf**
(`MulByHomUnramifiedField.lean:45`; Torsion.lean sorry-free) and **G0's fire-gate is OPEN** (KM stabilized —
the "broken save" concern is verified STALE). **DISCOVERY: Bootstrap:112 = engine AXIOM 2** (finite-étale
rel-rep) **consumes G0's BB-DIFF chain (T-B5)** — the leaf feeds both instantiations' AX2, not just étale.
Cascade on brick 6: field anchor → BB-DEG → `endDeg_mulBy` → hL/hArb one-liners → Bootstrap:75 + Legendre
→ **all three levels representable.** Seat map: **KM** = close :1356 (the domino; fire ONE); **G0** = the
leaf NOW; **OMEGA** = Bootstrap owner (pre-fill killing-halves + scope AX2); **GH** = U2→U3→β3. ONE per seat.

---

## STREAM-G0 — ★★ BB-DIFF assembled → FIRE THE LEAF NOW (your gate is OPEN; it feeds engine AX2)
> You are G0, resuming STREAM-G0. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-G0.md` IN FULL + boards v10.264-G0/v10.265. Rule-5 claim + sentinel `beastmode_active.G0` — **fire ONE G0 session only**. **★★ BB-DIFF ASSEMBLED + ratified** — `mulByHom_formallyUnramified`/`mulBy_etale`/`torsionπ_etale` are real proofs, Torsion.lean is SORRY-FREE (verified), the L-A torsor + T-DISC + κ̄-descent chain is banked. **YOUR FIRE-GATE IS OPEN:** KM has stabilized (verified 0/0 pushed, no dirty files — the "broken intermediate save" concern is STALE, disregard it). **DISPATCH — the ONE leaf, NOW:** `modelMulByHom_formallyUnramified_of_isAlgClosed` (`MulByHomUnramifiedField.lean:45`) — [N] formally unramified on the model over κ̄ ∋ 1/N. Route of record (your docstring): HasseWeil `mulByInt_isSeparable` ([N] scales ω by N≠0) → unramified at the generic point → the unramified locus is open + stable under translation by E(κ), which is transitive on closed points → everything. Your banked point-count route is the alternative. **PRIORITY RAISED (v10.265 discovery): your leaf feeds ENGINE AXIOM 2** — Bootstrap:112 (`naiveLevelThree_relativelyRepresentable_finiteEtale`) routes through E[3] finite étale of rank 9 = T-B5 = exactly your torsionπ_etale chain (+ its Legendre twin) — so the leaf unblocks the second engine axiom for BOTH instantiations, plus GH's β3. THEN: the `#print axioms` receipt you boarded as pending. Marathon v10.162. /beastmode on the leaf.

## STREAM-KM — ★★★ ONE SORRY LEFT (:1356) with two exact fixes banked — CLOSE IT (fire ONE session)
> You are KM, resuming STREAM-KM. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read your own sentinel `beastmode_active.KM` FIRST (it carries your verbatim resume plan), then `projects/ModularCurves/.mathlib-quality/inbox/STREAM-KM.md` + decomposition-keystone.md (v10.256/07-16 append) + board v10.265. Rule-5 claim — **⚠ FIRE EXACTLY ONE KM SESSION** (single-file work; a second session WOULD collide). **★★★ brick6_intertwining is PROVEN axiom-clean** (the whole T1/T2/T3/PHI/τ-hits/(M)/KEY-EVAL/FFM chain, ratified v10.265). **THE ENTIRE PROJECT IS ONE SORRY FROM THE CASCADE: `brick6_from_intertwining` (`MulByHomDegree.lean:1356`, the A+B finrank-tower assembly).** Your towerBC draft is green-through-the-instance-stack; execute the TWO exact boarded fixes: **(1)** pin the middle Module/SMul in towRR'S' + the `finrank_of_isFractionRing` call with @-instance-args (`letI algR'S' := functionFieldMap.toAlgebra` BEFORE any K(pM)-over-K(pM) mention; shadow the id-algebra); **(2)** the hFaith coercion (ConcreteCategory.hom vs algebraMap defeq — exact-with-show) + hFRZ letI-reorder. Then the degree-tail (degree-def + pullback=ψ via dif_neg replay) closes towerBC; then the (A)-transport (`finrank_eq_of_equiv_equiv`, i := zChart-topIso-equiv, j := (pullbackRestrictIsoRestrict [N] zChart).inv.appTop-asIso trans V-topIso, hc via `pullbackRestrictIsoRestrict_hom_morphismRestrict` + `morphismRestrict_appTop`) + hfin-discharge closes :1356 → the K4 crux wires → **FIELD ANCHOR DONE → G0's BB-DEG auto-cleans → `endDeg_mulBy` → OMEGA's Bootstrap + GH's inputs go LIVE → all three levels representable.** Marathon v10.162. /beastmode on :1356 — close the domino.

## STREAM-GH — β2-heart U1 landed (transport DISSOLVED) → U2 per the pinned recipe → U3 → β3
> You are GH, resuming STREAM-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read your sentinel (it carries the U2 recipe VERBATIM) + `projects/ModularCurves/.mathlib-quality/inbox/STREAM-GH.md` IN FULL + boards v10.263-GH/v10.265. Rule-5 claim + sentinel `beastmode_active.GH`. **★ U1 RATIFIED** (d85ebca57/cdfa9f8a3): the feared double-base-change transport DISSOLVED — `Point.baseChangeEquiv` (GroupLaw.lean, T-H2b) already does it; `Point.congrBase` + `Point.pullAlong` + `baseChangeEquiv_pull_asSection` axiom-clean. **DISPATCH — execute the pinned ladder:** **(1) U2** = `isFullLevel_baseChange_comp_iff` per your boarded recipe (isFullLevel_iff_naive both sides + kill-conjunct transport + per-geometric-point span-clause via the cL/cR equiv chains + U1 + map_closure). **(2) U3** = the factor-iff assembly feeding `exists_iso_of_factor_iff`. **(3) β3** = the constant-form clopen enumeration — NOTE G0's BB-DIFF leaf is firing now and `torsionπ_etale` is real modulo it, strengthening your β3 leg. On U2+U3+β3, [GHA3] closes the last GH-owned GHC1 edge → the Γ_H rel-rep sorryAx chain shrinks to the boxes. HOLD the general-S degree pins ([FR-GEN]-gated, non-critical). You remain THE CLOSER. Marathon v10.162. /beastmode on U2.

## STREAM-OMEGA — assemblies TURNKEY ✓ → Bootstrap OWNER: pre-fill killing-halves + SCOPE the two AX2s
> You are OMEGA, resuming STREAM-OMEGA. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-OMEGA.md` IN FULL + board v10.265. Rule-5 claim + sentinel `beastmode_active.OMEGA`. **★★ TURNKEY ASSEMBLIES RATIFIED** (verified: Bootstrap:75 = `refine …_of_conditions R hR ?_ ?_` with exactly hL :86 + hArb :91; Legendre :168 with only E[2]-gen; `transportE_baseChange` consumed by GH; push hygiene verified — the ls-remote protocol stuck, good). **DISPATCH — you are the Bootstrap OWNER-OF-RECORD; two jobs while brick 6 closes:** **(1) PRE-FILL the keystone-FREE parts of hL/hArb:** the section-level killing halves on the universal ℰ₃ ([3]P=[3]Q=0 — the flex-relation algebra on your own universal object, analog of Legendre's `two_zsmul` ✓); structure hL/hArb so ONLY the generation/bridge clauses wait on BB-DEG. **(2) SCOPE THE TWO AX2s (v10.265 discovery — the engine's second axiom is still sorried):** Bootstrap:112 `naiveLevelThree_relativelyRepresentable_finiteEtale` + its Legendre twin (T-E14-AX2, below :185). Route: E[3] finite étale (G0's torsionπ_etale chain — its leaf is firing now) + the Isom-scheme packaging + the open-closed cut. The docstring names a Weil-pairing cut (stream C) — **assess whether it's avoidable like NORM's ζ₃ was** (your void-finding suggests an incidence/discriminant cut); board the decomposition either way. Consume G0's leaf + KM's BB-DEG as they land → then the hL/hArb one-liners → both instantiations discharge. Marathon; /beastmode on the killing-halves + AX2 scope.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
