# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**TERMINAL-CHARTER MODE (v10.313, 2026-07-17 — owner: "stop micromanaging; longer tasks so at the end we
are done").** Each seat below holds ONE charter = its ENTIRE remaining contribution to the headline, worked
to completion across as many sessions as it takes. **BUILD-AHEAD PROTOCOL (the anti-15-min rule): a cross-seat
block is NOT a terminal condition — switch to another sub-goal in your charter; build every consuming side
turnkey; PHASE-8-terminate ONLY when every sub-goal is simultaneously done-or-blocked-and-turnkey. Report at
your charter's NAMED MILESTONES only.** State (v10.313): Bootstrap = 2 sorries (hArb :95 → OMEGA; Legendre-AX2
:212 → G0); T-D8 dead both halves (zero boxes on the rel-rep side); hLN off the headline; T-H8/T-H9 Rigid
conjuncts are free-riders. The headline = these 4 charters; KM is upstream of all. Owner re-fires a seat on
its TERMINAL report (or a genuine all-fronts block), not per-item. **★ v10.316 FINISH-LINE RE-ALLOCATION:** the project is down to TWO deep self-contained builds — **RING-DBL** (affineSection doubling → hArb; TRANSFERRED to OMEGA, its wheelhouse, KM's route banked → zero-wait) and **endDual_comp_self** (KM 100%). Both are now dependency-free (long sessions). G0 ASSEMBLES :206 now (unblocked — abscissaDiff landed). GH finishes independent work then arms (no watching). Two hard builds + one assembly to the headline.

---

## CHARTER-G (G0) — ASSEMBLE :206 NOW (you are UNBLOCKED) · terminal when :206 is CLOSED (⟹ Bootstrap Legendre side DONE)
> You are G0 under CHARTER-G (v10.316 re-allocation). Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read `inbox/STREAM-G0.md` IN FULL + boards v10.313->316. Rule-5 claim + sentinel `beastmode_active.G0` — **you are UNBLOCKED — do NOT wait for further OMEGA input.** OMEGA's O1 `abscissaDiff` (the ω^{⊗-2} chart-d data you were waiting on) is ON ORIGIN (Moduli/AbscissaDifference.lean), alongside your own `SqrtUnitCover` + `sqrtPairCongr` + the proven funnel `legendreDelta_relRep_finiteEtale_of_scaleTorsor`. **DISPATCH — ASSEMBLE :206 (G2):** glue the per-adapted-chart `SqrtUnitCover`s via `sqrtPairCongr` (the c²-transition = the abscissaDiff cocycle) into the global finite-étale double cover Z₂ → fullLevelLocus 2; feed it + OMEGA's `IsLegendreDatum.neg`/`unit_sq_eq_one` (fibres are honestly μ₂) into the funnel → `legendreDelta_relRep_finiteEtale` (Bootstrap:212) closes. **TERMINAL:** :206 CLOSED (Bootstrap Legendre-AX2 discharged). Coordinate residual T-E12 spellings with OMEGA via inbox only if a specific lemma is missing — do NOT block on it, build around. **Report ONLY at:** :206-CLOSED (= your charter terminal). Marathon v10.162. /beastmode — assemble and close.

## CHARTER-K (KM) — endDual_comp_self, 100% (RING-DBL handed to OMEGA) · terminal when endDual + hH/hbound/5-generals are done
> You are KM under CHARTER-K (v10.316 re-allocation). Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel FIRST, then `inbox/STREAM-KM.md` IN FULL + boards v10.313->316. Rule-5 claim + sentinel `beastmode_active.KM` — **marathon charter; ONE deep self-contained build, no cross-seat block — grind it to completion, do NOT PHASE-8-terminate early.** **★ K1 is DONE + HANDED OFF:** your Psi3 certificate (ThreeTorsionRingCertificate.lean) reduced the bridges to RING-DBL, and **RING-DBL now belongs to OMEGA** (it's affineSection-doubling = OMEGA's Stage-B wheelhouse; your generic-point route in decomposition-km-integral.md is banked for them). **Do NOT touch RING-DBL / the affineSection doubling file** (collision guard). **YOUR FULL FOCUS = (K2) endDual_comp_self** — the dual keystone (KM 2.6 Cayley-Hamilton of the degree form): **START by reading KM 2.6 itself** (source-faithfulness); 7 pins + the biquadratic route banked; **CONSUME GH's `ForMathlib/FinrankDegenerate.lean`, do NOT rebuild.** **(K3) on endDual landing** — discharge the 5 EndomorphismDegree generals (:336-:446, route A) + hH + hbound. **(K4 backstop) W0 -> T-H8** (never-run-dry). **TERMINAL:** endDual proven + K3 complete. **Report ONLY at:** K2-landed, K3-complete. Marathon v10.162. /beastmode — one deep build, run it to the terminal.

## CHARTER-GH (GH) — finish independent work, then ARM (do NOT watch) · terminal when the headliners are sorry-free + receipts
> You are GH under CHARTER-GH (v10.316 re-allocation). Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel + `inbox/STREAM-GH.md` IN FULL + boards v10.313->316. Rule-5 claim + sentinel `beastmode_active.GH` — **do NOT spend a session watching; exhaust your independent work first.** **Independent sub-goals (do these NOW, in order):** **(GH2) the straggler sweep** — drive every non-headline sorry to closed-or-explicitly-archived; board the classification (this is real, unblocked work). **(the engine-⇐ wire)** — build `representable_iff_rigidNoeth`'s ⇐ direction in EllCategory as far as it goes on the CURRENT engine (T-E14 is landed; wire everything not gated on Bootstrap's two remaining sorries). **(GH4-script) THE CAPSTONE, PRE-SCRIPTED** — write the 7-receipt capstone as a literal one-command fire (the exact `#print axioms` + the `gammaBot`/`gammaH`/`gammaOneDrinfeld`(+T-H8) wirings), so THE HEADLINE is a single command the moment Bootstrap empties + KM's keystone lands. **(GH1) T-H6** stays owner-flagged (SUSPECT-B2, boarded v10.314) — no forcing. **ONLY WHEN ALL OF THE ABOVE ARE DONE** is armed-and-waiting legitimate (the closer genuinely depends on the engine+keystone; that idle is honest). **TERMINAL:** the three (four) headliners sorry-free + receipts printed. **Report ONLY at:** independent-work-exhausted (with what remains + what it waits on), THE-HEADLINE. Marathon v10.162. /beastmode — exhaust the independent work, then arm.

## CHARTER-O (OMEGA) — BUILD RING-DBL (your wheelhouse), then close hArb :95 · terminal when hArb is CLOSED
> You are OMEGA under CHARTER-O (v10.316 re-allocation). Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel + `inbox/STREAM-OMEGA.md` IN FULL + boards v10.313->316. Rule-5 claim + sentinel `beastmode_active.OMEGA` — **marathon charter; STOP WATCHING — you now BUILD the lemma you were waiting for. ONE deep self-contained build, dependency-free.** Your O1 (abscissaDiff — G0's torsor datum) + O2 (hArb TURNKEY: Bootstrap:95 = `refine isE3Datum_of_bridges X hR L ?_ ?_`) are DONE. **★ NEW OWNERSHIP (v10.316): you OWN RING-DBL** — `2•(affineSection p q) = affineSection(addX, addY)` (ring-level, unit-d locus). It is affineSection-doubling algebra — exactly your Stage-B territory (AffineSectionSpecPoints / modelPointAddEquiv / projModelAffineSection_injective), and you already did the 3-torsion doubling for `three_zsmul_universalE3P/Q`. **CONSUME KM's banked generic-point route (decomposition-km-integral.md, exact anchors, pushed)** — RING-DBL is a torsion-free IDENTITY (not ε-trapped), so the universal-domain/generic-point proof is valid. Put it in a ForMathlib/affineSection file (KM does NOT touch it). **THEN:** RING-DBL → KM's `bridgeP_of_dbl_origin`/`bridgeQ_cubic_of_Ψ₃` fire (on origin) → your two Bootstrap:95 bridge-sorries close as one-liners → **hArb DONE → the ℤ[1/3] engine instantiation is complete.** **TERMINAL:** hArb :95 closed. **Report ONLY at:** RING-DBL-landed, hArb-CLOSED. Marathon; /beastmode — build RING-DBL, then close.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
