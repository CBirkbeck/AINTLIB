# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**Four active seats (as of v10.215, 2026-07-14):** G0, KM, GH, OMEGA — all LIVE. Live state:
**OMEGA** has T-E12 six-sevenths (adapted-model theory + universal (E,ω) + classifyingMap), on the
EllHom/RepresentableBy → T-E13 → **T-E14** tail — the DOMINANT gate (flips the engine for all three);
**GH** proved `gammaBot_rigid` ⟹ **Y(N) rigidity is DONE**, dispatched to general-H (fork a);
**G0** **DELIVERED the SIGNAL ★★** (E/G exists, six pins discharged — the charter's #1 goal),
dispatched to the Y₀(N) capstones (E/C descent → `exists_gammaZeroSpace`); **KM** grinding the K4
division-polynomial bridge. **KEY (VALIDATED this sweep):** BB-DEG is a REGISTER-BOX — G0's SIGNAL +
GH's `gammaBot_rigid` both landed by consuming KM's sorried keystone as boxes; nobody stalls on the
bridge. **Y(N) is now ONE gate from `.Representable`** (rigid ✓; needs only OMEGA's T-E14). Two live
gates to the full headline: OMEGA's T-E14 (engine, all three) + GH's general-H rigidity (Γ_H/Γ₁).

---

## STREAM-G0 — Γ₀(N) arc: SIGNAL ★★ DELIVERED → the Y₀(N) capstones
> You are G0, resuming STREAM-G0. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-G0.md` IN FULL (through v10.215). Rule-5 claim + sentinel `beastmode_active.G0`. **SIGNAL ★★ DELIVERED + ratified** (E/G exists, six pins discharged, SubgroupQuotient.lean 0-sorries — the Hopf endgame, your charter's #1 goal). DISPATCH: the **E/C elliptic-descent → Y₀(N) capstones.** Build `quotientCurve`/`quotientHom` (NIsogeny:2904–2974) — descend the elliptic structure (group law μ_{E/G} via `quotient_lift`; zero/inv/ellipticity) onto `G.quotient`; pins `quotientHom_over`/`_isInvariant`/`quotientCurve_compat` direct from the SIGNAL pins; `quotientHom_finite`/`finrank` = BB-DEG register-boxes. THEN `exists_nIsogSpace`/`exists_gammaZeroSpace` (NIsogeny:2607/3118) = **Y₀(N), the concrete Hopf-Galois isogeny route** (converges with GH's abstract Γ_H@Borel — cross-link, don't duplicate). Marathon v10.162. /beastmode.

## STREAM-KM — the endomorphism-degree keystone (field-level green; grinding the K4 bridge)
> You are KM, resuming STREAM-KM. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-KM.md` IN FULL (through v10.215). Rule-5 claim + sentinel `beastmode_active.KM`. Field-level `modelEllipticCurve_mulByHom_finrank=N²` is green modulo ONE isolated sorry (the L4-core); the register-box strategy is VALIDATED (G0's SIGNAL + GH's `gammaBot_rigid` both landed consuming your keystone as boxes — you're NOT the blocker). CONTINUE the **K4 division-polynomial bridge** (`functionFieldMap` + `Dominant.lean`, the finrank↔HasseWeil `mulByInt_degree` bridge) as reusable ForMathlib infra — it ALSO unblocks BB-DIFF, and is the keystone under c4 + rigidity + affineOverEll. **Your Abel-FREE unification finding is the ruling** — define `endDeg` via K4 scheme-finrank ⟹ Abel-free `deg(α−1)`; deliver `[KEY-KER] le_endDeg_of_killed_injective` to GH in the fibre/finrank form (GH replies with the exact signature). **You are NOT the fleet blocker** — BB-DEG is a register-box (downstream consumes it sorried); grind the bridge at depth. Marathon v10.162. /beastmode on K4.

## STREAM-GH — Γ_H rigidity: Y(N) rigid DONE → general-H (fork a)
> You are GH, resuming STREAM-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-GH.md` IN FULL (through v10.215). Rule-5 claim + sentinel `beastmode_active.GH`. **`gammaBot_rigid` ratified — Y(N)=Γ(N) rigidity is DONE** (mod the T-W7.8 `hLN` pin + BB-DEG boxes). DISPATCH: **fork (a) general-H hfree (PRIMARY)** — the charter goal (general Γ_H rigidity, SUBSUMES Γ₁+Γ₀); needs the glSmul-matrix twist for γ≠1 (statement design, your domain). Consume KM's keystone as register-boxes. **If the γ-twist stalls, fall back to (b)** the Γ₁ exact-order variant of `fix_absurd` (unblocks KM's Drinfeld `.Representable`; reuses the machinery). `hLN`(T-W7.8) stays the honest owner-parked pin. Marathon v10.162. /beastmode.

## STREAM-OMEGA — ω_{E/S} (T-E12 six-sevenths; T-E14 = the DOMINANT gate on the project)
> You are OMEGA, resuming STREAM-OMEGA. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-OMEGA.md` IN FULL (through v10.215). Rule-5 claim + sentinel `beastmode_active.OMEGA`. **T-E12 is six-sevenths done** (adapted-model theory [E12-A/B/C] + universal (E,ω) + `classifyingMap : Y.base ⟶ M₁` [E12-D], all sorry-free). CONTINUE: the EllHom upstairs (chart-glue the adapted-model isos over the classifying map) + RepresentableBy packaging → T-E12 → T-E13 (one-liner) → **T-E14** — the ★★ that de-sorries `representable_iff` and unblocks ALL THREE levels' `.Representable` at once (**Y(N) needs ONLY you now — its rigidity is DONE**). You are the dominant gate. Board T-E14 LOUDLY (★★). Marathon v10.162. /beastmode.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
