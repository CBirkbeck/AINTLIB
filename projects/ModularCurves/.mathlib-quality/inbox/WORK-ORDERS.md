# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**★ v10.320 OWNER CONSOLIDATION (2026-07-18 — owner: "I want to finish this project off … fresh
strong worker … the bulk of the remaining work").** ONE seat — **FIN** — now holds the entire
remaining headline cone as a marathon charter (terminal-charter mode v10.313 + force-concentration
v10.316, taken to the limit). State at dispatch (source-verified): Bootstrap = 1 sorry (:222,
Legendre AX2); the degree keystone re-pointed AT k̄ by THE RULING (board v10.320 — T-W7.8 stays
parked; general-S endDual OFF the headline; **SUSPECT-RIG2**: the frozen hbound contract must be
NARROWED before discharge); the engine gate = the mouth (QuotientProblem:1040) + two TorsorData
packages + the ⇐-consumption map + recollement. Charters G/K/O/GH are SUPERSEDED-INTO-FIN (below).

---

## CHARTER-FIN (the closer) — THE WHOLE REMAINING CONE (v10.320) · terminal when the capstone receipts print
> You are FIN under CHARTER-FIN (v10.320 owner consolidation — you hold the BULK: the entire remaining headline cone; the project finishes when you do). Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read `inbox/STREAM-FIN.md` IN FULL (your route book: verified census, THE RULING, SUSPECT-RIG2, per-front anchors), then board entries v10.316→v10.320 (grep `^## v10.3` in `.mathlib-quality/tickets.md`). Rule-5 claim + sentinel `beastmode_active.FIN`. **Marathon charter, build-ahead protocol: a blocked front is NOT terminal — switch fronts; PHASE-8-terminate only when every front is done-or-blocked-and-turnkey.** **FIRST ACTION (ground truth):** `bash projects/ModularCurves/.mathlib-quality/scripts/capstone-receipts.sh` — the live receipts + cone census. **FOUR FRONTS, leverage order:** **(F1) KEYSTONE-AT-k̄** — under THE RULING (board v10.320): work ONLY over `S = Spec k̄` (every consumer is a `∀ k̄` pin — hbound GammaHMaster:1220-shape, hH :822-shape); **T-W7.8 stays PARKED; do NOT build general-S `endDual_comp_self`; do NOT grind the 7 general-S EndomorphismDegree sorries** (they re-tag [KEY-GEN-PARKED] after your rewire). START with **SUSPECT-RIG2**: re-derive the coordinator's refutation of the frozen hbound contract (ε = [N+1] any N ≥ 4; ε = [−1] at N = 4 — board v10.320), NARROW it to the true shape (ISO ε + exact-geometric-order-N point P with (ε−1)P = 0) at its GammaHMaster/GammaHClosure sites + re-thread `gammaOneDrinfeld_fix_absurd` (it holds the iso + the T-D6b exact-order data); run the same review on hH against `gammaH_representable_of_orderOf`'s side condition. THEN discharge at k̄: the L1 fibre dictionary (repo endDeg/endTrace ↔ HasseWeil degree over k̄ — anchors at the bottom of `decomposition-km-integral.md`), the k̄ zero-or-isogeny dichotomy (feeds the PROVEN `endDeg_comp_of_isIntegral`), IMPORT HasseWeil `hasse_bound`/`degree_quadratic_closed`/`DualIsogeny` (never re-prove), discharge the [T-G3d-Niso] box at k̄, assemble on the PROVEN `T-G3a` + `le_endDeg_of_killed_injective` + `TorsionDivisibility._of_isIso` — full inventory + build order in STREAM-FIN §F1. **(F2) Bootstrap:222 (Legendre AX2)** — COLLISION GUARD FIRST (`beastmode_active.G0` mtime + origin log + board; if G0 is live and landing, YIELD + inbox): otherwise TAKE OVER the banked build — funnel/`SqrtUnitCover`/glue-1a/1b/2a PROVEN; execute the mapped steps (2b) Spec-pullback square → (3) RelativeGluingData → Z₂ finite étale over `fullLevelLocus 2` → (4) sections-spec + b↔u dictionary (OMEGA's adapted-value refinement NOT landed — build it or around) → (5) feed the funnel ⟹ :222 closes (verbatim map: STREAM-FIN §F2 + `beastmode_active.G0` + STREAM-G0.md). **(F3) THE ENGINE GATE** — de-sorry the mouth `representable_of_rigidNoeth_of_torsor` (QuotientProblem:1040; banked KM pp.112–116 route in its docstring; simul + freeness + T-Q5 quotient PROVEN; gates: `ForMathlib/InvariantTorsor.lean` + `EngineDescent.lean:646`); build the two TorsorData packages (T-E15b `gammaFullNaiveGlAction` equivariance over IsUnit 3; T-E14-ACT' — the COUPLED Legendre action, NOT the ambient product — after F2); execute the :324 ⇐-consumption map (documented verbatim in-source at EllCategory:324) + recollement (Recollement:445); derive :1022/:298 as weakening corollaries — never separate builds. **(F4) FINAL WIRING + CAPSTONE** — instantiate the proven hH/hbound (unpinned corollaries for receipts 3/5/6), drive all seven receipts to {propext, Classical.choice, Quot.sound}, FIRE the script, board the receipts = THE HEADLINE. **The bar per landing:** `lake build` green; ZERO new sorries outside your claim (the SUSPECT-RIG2 statement-narrowing is boarded, never silent); `#print axioms` clean; push + VERIFY `git ls-remote origin dev/modular-curves`. T-H6 (SUSPECT-B2) stays owner-flagged — do not force. External-quiet stands. **Report ONLY at:** KEYSTONE-k̄-LANDED, :222-CLOSED, MOUTH-CLOSED, THE-HEADLINE. Marathon v10.162. /beastmode — four fronts, run them to the receipts.

---

## Superseded / parked (v10.320 consolidation — no standing orders)
- **CHARTER-G (G0)** — SUPERSEDED-INTO-FIN(F2). Mid-build state banked + pushed:
  `beastmode_active.G0` + `STREAM-G0.md` (funnel/1a/1b/2a proven; 2b→5 mapped). If the owner
  chooses to re-fire G0 instead, G0 keeps :222 and FIN yields that front (inbox coordination);
  G0's old opener is in git history (v10.316 version of this file).
- **CHARTER-K (KM)** — SUPERSEDED by THE RULING (board v10.320): k̄ suffices, T-W7.8 stays parked,
  general-S endDual off the headline. KM's L1 anchors + decomposition (bottom of
  `decomposition-km-integral.md`) survive into FIN(F1). Do not re-fire on the old charter.
- **CHARTER-O (OMEGA)** — TERMINAL with honor (the ℤ[1/3] engine complete, v10.318).
- **CHARTER-GH (GH)** — independent work exhausted (v10.317); the armed-receipts duty transfers
  to FIN(F4). The capstone script stands ready for any seat.
- **YN's Y(N) charter** — BEASTMODE-DONE (PR-B #5790); remaining conjuncts auto-complete on the
  engine landing.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0).
