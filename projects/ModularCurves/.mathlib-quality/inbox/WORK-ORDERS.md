# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**Four active seats (as of v10.256, 2026-07-15):** ALL FOUR FREE + re-dispatched. **★★ L4-iii IS DOWN TO
BRICK 6** — KM's 5 bricks landed (verified on origin; `MulByHomDegree.lean` = 1 sorry :660; the feared
2000-line AdditionChart* thread was AVOIDED via the points-dictionary + HasseWeil Jacobian formula).
**Brick 6 is THE critical-path item, one brick from triggering the whole BB-DEG cascade:** brick 6 →
KM field anchor → G0's BB-DEG + BB-FLAT → `endDeg_mulBy=N²` → OMEGA's NORM + GH's inputs → normalization
tickets → **all three levels representable.** Prior: engine `representable_iff` SORRY-FREE, Rigid ✓, RelRep ✓,
BB-QF ✓, foundation spine ✓. **OMEGA B2 owner-APPROVED** (invert e3Gamma — E3ModuliRing over-represented the
level-3 functor). **[FR-GEN]** (finrank finite-flat-API wall) routed to an in-tree ForMathlib sub-ticket,
KM-owned, NON-critical (gates only endDual_comp_self/degree-completeness, not representability). Seat map:
**KM** = LAND BRICK 6; **OMEGA** = the B2 fix, then BB-DEG-gated NORM; **G0** = BB-FLAT :150 → BB-DEG :155
(consumes KM's anchor); **GH** = [GHA3] route-β β1 + hold general-S degree pins. Fire ONE per seat.

---

## STREAM-G0 — BB-FLAT wall-break banked → assemble BB-FLAT :150 THEN BB-DEG :155 (consumes KM's brick 6)
> You are G0, resuming STREAM-G0. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-G0.md` IN FULL + boards v10.251-G0/v10.256. Rule-5 claim + sentinel `beastmode_active.G0` — **fire ONE G0 session only**. **★★★ BB-QF DONE + BB-FLAT WALL-BREAK BANKED** — `free_of_flat_of_fibre_flat` (the fibrewise criterion for fp modules over a local ring, axiom-clean, `FiniteFibrewiseFlat.lean` on origin). **DISPATCH — your banked next-session plan, the two Torsion recipes in order:** **(1) BB-FLAT** `mulByHom_flat` (`Torsion.lean:150`) — the :150 chart-assembly from your banked wall-break + fibre-leg substrate. **(2) BB-DEG** `mulByHom_finrank=N²` (`Torsion.lean:155`) — the scheme-level degree; **it CONSUMES KM's brick-6 field-level anchor** (KM's `MulByHomDegree` L4-iii lands the field case; your :155 is the arbitrary-E/S reduction on top — coordinate the handoff, don't re-derive; same N², two forms). **BB-DEG :155 landing → `endDeg_mulBy`'s box auto-cleans → OMEGA's NORM + GH's degree inputs go live** — you + KM's brick 6 jointly trigger the cascade. Also `_locallyOfFinitePresentation`/`FormallyUnramified` (:217/:232) if in reach. Marathon v10.162. /beastmode on BB-FLAT then BB-DEG.

## STREAM-KM — ★★ L4-iii DOWN TO BRICK 6 — LAND IT (the single critical-path item)
> You are KM, resuming STREAM-KM. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-KM.md` IN FULL + boards v10.252/253/256. Rule-5 claim + sentinel `beastmode_active.KM` — **fire ONE KM session** (v10.253: two collided). **★★ FIVE L4-iii BRICKS LANDED** (zsmul_genericPoint, genericSpecPoint, chartSpecPoint kit, genericSpecPoint_comp_mulByHom, mulByInt_equation — all axiom-clean; the feared 2000-line AdditionChart* thread was AVOIDED). **ONE BRICK LEFT — LAND BRICK 6** (`MulByHomDegree.lean:660`, the sole remaining sorry, THE critical path): the section-level transfer (`chartHomEquiv` ↔ section-pullback compat through `Proj.awayι`: φ_τ(appTop [N] s) = mulByInt_pullback(φ_τ s) on the generators) + the fraction-field assembly (both `IsFractionRing` legs + `functionField_algHom_ext`, all BANKED). **On landing: :660 dies → your field-level anchor → G0's BB-DEG reduction → `endDeg_mulBy` auto-cleans → OMEGA's + GH's inputs go LIVE** — the whole cascade. THEN: spawn the **[FR-GEN]** in-tree ForMathlib sub-ticket (public non-finite-flat finrank reduction; NOT a mathlib PR — external-quiet holds) + `endDual_comp_self` (the char-poly, [FR-GEN]-gated, NON-critical per v10.256). Coordinate the brick-6 → BB-DEG:155 handoff with G0. Brick 6 first — everything waits on it. Marathon v10.162. /beastmode on brick 6.

## STREAM-GH — [E-INT] landed → [GHA3] route-β β1 (unblocked) + hold the general-S degree pins for KM
> You are GH, resuming STREAM-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-GH.md` IN FULL + boards v10.255-GH/v10.256. Rule-5 claim + sentinel `beastmode_active.GH`. **★ [E-INT] `isIntegral_of_field` DELIVERED** (`IntegralOverField.lean`, axiom-clean) — the degree tranche now auto-synthesizes at geometric fibres (`endDeg_comp_of_isIntegral` etc. apply over Spec k with only the isogeny [Flat]/[IsFinite] instances); you answered the seam question (KM/G0 need not build it). You remain THE CLOSER (`.Representable` one-liners written). **DISPATCH — [GHA3] `levelSpaceΓπ_etale` route-β EXECUTION (your sentinel's active focus, the last GH-owned GHC1 edge, unblocked NOW):** β1 = globalize the affine trivialization `torsion_etaleLocal_triv_affine` (proven, 0-sorry), then the clopen constant-form incidence locus + mathlib's `DescendsAlong`/`FlatDescent` for the final étale-descent leg (four-step map banked in your sentinel). **HOLD the general-S degree pins** (`endDeg_comp` etc.) — they wait on KM's `endDual_comp_self` (which is [FR-GEN]-gated, non-critical); your engines for the trace pins + T-G3d-Niso are already in place and fire the moment it lands. `hH` consumable @KernelBound:100; minimise the GHC1 `qpd` sorryAx if in reach. Marathon v10.162. /beastmode on [GHA3] β1.

## STREAM-OMEGA — B2 FIX (owner-approved) → then the BB-DEG-gated NORM scheme-lift
> You are OMEGA, resuming STREAM-OMEGA. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-OMEGA.md` IN FULL + boards v10.256 + b2_log.jsonl. Rule-5 claim + sentinel `beastmode_active.OMEGA`. **★ NORM CORE COMPLETE** (6 axiom-clean lemmas) + **KEY DISCOVERY: the ζ₃/Weil gate is VOID** (sheet-pinning u=−A/B is Zariski-rational — NORM needs NO Weil datum; the 8-sorry WeilPairing dependency was a phantom). **★ B2 OWNER-APPROVED — FIX IT NOW:** your ratified `E3ModuliRing` over-represents the level-3 functor (the degenerate γ=0 component, Q=−P∈⟨P⟩, only ℤ/3). **Invert `e3Gamma`** — `E3ModuliRing := Localization.Away ((a₁³−27a₃)·a₃·e3Gamma)` — and add the γ-unit / Q∉⟨P⟩ condition to `IsE3Datum`. Re-verify the three ★★★ landmarks (`universalE3`, `e3_vc_marked`, `e3ClassifyingEllHom` — all take unit hyps, so adding `IsUnit γ` is expected non-breaking). Your 6 NORM lemmas are unaffected. **THEN** the NORM scheme-lift (`universalE3_isE3Datum` + `IsE3Datum` for arbitrary level-3 → `naiveLevelThree_representable_by_affine`) — **BB-DEG-gated:** it needs the torsion→coord bridges (3•P=0⟹μ_P=0, 3•Q=0⟹cubic) = KM's brick 6 → BB-DEG; consume as it lands. [T-E14-LVL-b] (E[2]-gen = #E[2]=4) is the symmetric handoff. Marathon; /beastmode on the B2 fix.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
