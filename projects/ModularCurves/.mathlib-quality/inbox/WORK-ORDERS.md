# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**TERMINAL-CHARTER MODE (v10.313, 2026-07-17 — owner: "stop micromanaging; longer tasks so at the end we
are done").** Each seat below holds ONE charter = its ENTIRE remaining contribution to the headline, worked
to completion across as many sessions as it takes. **BUILD-AHEAD PROTOCOL (the anti-15-min rule): a cross-seat
block is NOT a terminal condition — switch to another sub-goal in your charter; build every consuming side
turnkey; PHASE-8-terminate ONLY when every sub-goal is simultaneously done-or-blocked-and-turnkey. Report at
your charter's NAMED MILESTONES only.** State (v10.313): Bootstrap = 2 sorries (hArb :95 → OMEGA; Legendre-AX2
:212 → G0); T-D8 dead both halves (zero boxes on the rel-rep side); hLN off the headline; T-H8/T-H9 Rigid
conjuncts are free-riders. The headline = these 4 charters; KM is upstream of all. Owner re-fires a seat on
its TERMINAL report (or a genuine all-fronts block), not per-item.

---

## CHARTER-G (G0) — CLOSE THE LEGENDRE ENGINE SIDE · terminal when :206 is CLOSED (=> Bootstrap EMPTY)
> You are G0 under CHARTER-G. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read `inbox/STREAM-G0.md` IN FULL (through v10.313) + boards v10.295->313. Rule-5 claim + sentinel `beastmode_active.G0` — **marathon charter; an OMEGA block => switch sub-goals, do NOT terminate.** Your AX2-E3 is done (the four-step is the proven template); `legendreDelta_relRep_finiteEtale_of_scaleTorsor` is proven — :206 reduces to ONE input: a finite-etale Z2->fullLevelLocus-2 whose sections classify the +/-omega bases (rank 12 = 6x2). The datum-PINNING is OMEGA's O1, but **the SCHEME is yours — BUILD IT AHEAD:** **(G1) construct the mu2-torsor scheme geometry independently** — the square-roots of the omega^{-2}-valued abscissa-difference; prove Z2->fullLevelLocus-2 finite etale (pure scheme geometry, your `TorsionCombination` machinery at N=2, E[2] rank 4 done from BB-DEG; mu2-torsor fibres, no connectedness needed). **(G2) consume OMEGA's O1 omega-datum** -> the torsor classifies the +/-omega pair -> feed `legendreDelta_relRep_finiteEtale_of_scaleTorsor` -> :206 dies. Coordinate T-E12-layer questions with OMEGA via inbox (don't excavate). **TERMINAL:** :206 closed (Bootstrap EMPTY => both engine instantiations complete). **Report ONLY at:** torsor-scheme-built, :206-CLOSED. Marathon v10.162. /beastmode — run it to the terminal.

## CHARTER-K (KM) — THE KEYSTONE + BRIDGES · terminal when endDual_comp_self + hH/hbound/5-generals + the 2 hArb bridge-Props are all done
> You are KM under CHARTER-K. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel FIRST, then `inbox/STREAM-KM.md` IN FULL + boards v10.302->313. Rule-5 claim + sentinel `beastmode_active.KM` — fire ONE KM session; **this is a marathon charter, not a task list — do NOT PHASE-8-terminate until the terminal condition below; a sibling block => switch sub-goals, never stop.** You are UPSTREAM of the whole headline. **Ordered sub-goals:** **(K1) BRIDGE-P + BRIDGE-Q FIRST** (they unblock OMEGA's hArb :95): the a2-obstruction-vanishes + flex-cubic ring identities — L4-iii division-polynomial content, ring-form pinned by OMEGA (a2*a3^2-a4*a1*a3-a4^2=0 + the flex cubic; the epsilon-example proves fields/reduced-bases can't win — it's the poly crux). Deliver into `E3DatumAssembly.isE3Datum_of_bridges` (already in Lean). **(K2) endDual_comp_self** — the dual keystone (KM 2.6 Cayley-Hamilton of the degree form): **START by reading KM 2.6 itself** (source-faithfulness — not memory); 7 pins mapped + the biquadratic route in decomposition-km-integral.md; **CONSUME GH's `ForMathlib/FinrankDegenerate.lean` for the fibre-rank side (do NOT rebuild).** **(K3) on endDual landing** — discharge the 5 EndomorphismDegree generals (:336-:446, route A = KM 2.6 algebra) + hH + hbound. **(K4, backstop when K1-K3 all block) the W0 integral arc -> T-H8** (Prod-translate comaximality -> D_M=Z_M -> integral glue -> F3 converse + F4 — genuinely multi-session; your never-run-dry work). **TERMINAL:** K1 delivered + K2 proven + K3 complete. **Report ONLY at:** K1-delivered, K2-landed, K3-complete. Marathon v10.162. /beastmode — run it to the terminal.

## CHARTER-GH (GH) — THE CLOSER · terminal when the three .Representable headliners are sorry-free with printed receipts
> You are GH under CHARTER-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel + `inbox/STREAM-GH.md` IN FULL + boards v10.303->313. Rule-5 claim + sentinel `beastmode_active.GH` — **marathon charter; when blocked on a sibling landing, switch to audit/staging, do NOT terminate.** You OWN the finish: [GHA3] is closed (zero boxes), hLN is off, T-H8/T-H9 Rigid conjuncts are free-riders — the `GammaHClosure` capstones are staged. **Sub-goals:** **(GH1) T-H6 SUSPECT-B2** — scope `gammaH_representable_of_rigid` (GammaHRep:4128) against the orbit-presheaf Rigid=>Representable question; prepare the corrected wiring; **FLAG for owner (do NOT force a statement change — B2 protocol).** **(GH2) the YN-side/legacy straggler sweep** — drive every non-headline sorry to closed-or-explicitly-archived; board the classification. **(GH3) receipts-closer (re-arm each sibling landing):** the instant KM's K2/K3 lands or Bootstrap empties, re-print the affected receipts + board the box-shed LOUDLY. **(GH4) THE FINAL CAPSTONE** — when Bootstrap is empty (OMEGA+G0) AND KM's keystone (hH/hbound) lands: wire the `GammaHClosure` capstones + `gammaBot_representable` / `gammaH_representable_of_orderOf` / `gammaOneDrinfeld_representable` (+ T-H8 `gammaFullDrinfeld`) to sorry-free, and print the final `#print axioms` on all three. **TERMINAL:** the three (four) headliners sorry-free + receipts printed = THE HEADLINE. **Report ONLY at:** T-H6-scoped, each box-shed, THE-HEADLINE. Marathon v10.162. /beastmode — run it to the terminal.

## CHARTER-O (OMEGA) — CLOSE THE Z[1/3] ENGINE SIDE · terminal when hArb :95 is CLOSED
> You are OMEGA under CHARTER-O. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull; read your sentinel + `inbox/STREAM-OMEGA.md` IN FULL (through v10.313) + boards v10.295->313. Rule-5 claim + sentinel `beastmode_active.OMEGA` — **marathon charter; a KM block => switch sub-goals, do NOT terminate.** hL :87 + E[2]-gen + AX1 are DONE (ratified); your only open Bootstrap sorry is hArb :95, whose FINAL plug-in waits on KM's K1 bridges — so BUILD AHEAD: **(O1) deliver ALL of G0's scale-torsor omega-datum** (unblocks CHARTER-G fully): `IsLegendreDatum.neg` (u=-1 VC — you verified the Legendre curve is u=-1-invariant; entry points `basisUnitAt_ofVC`/`projModelVCIso_affineSection`), `unit_sq_eq_one` done, the +/-omega uniqueness-up-to-sign, and any universal-d / trivialization data (if you have it, G0's torsor drops in and :206 dies immediately — highest cross-seat leverage). **(O2) stage hArb TURNKEY** — `isE3Datum_of_bridges` (E3DatumAssembly:413) is in Lean; wire the marking pipeline ([hArb-1/2a/2b/2c] -> translation -> a3-cert -> a4-shear -> flex chart -> cover) so KM's BRIDGE-P/Q drop in as a one-liner. **(O3) close hArb :95** the instant K1 lands (RULED route: ideal-comparison + rank-rigidity — consume KM's `FiniteFlatRigidity` module core, on origin; NO IsReduced, NO etale-descent). **(O4, backstop) adapted-model/T-E12 hardening + receipts.** **TERMINAL:** hArb :95 closed (Z[1/3] instantiation fully discharged). **Report ONLY at:** O1-delivered (G0-torsor-data), hArb-CLOSED. Marathon; /beastmode — run it to the terminal.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
