# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**Four active seats (as of v10.262, 2026-07-16):** ALL FOUR FREE + re-dispatched. **★★★★ OMEGA's [T-E15-NORM]
MACHINE IS COMPLETE + G0's E[N] SUBSTRATE IS DONE — the WHOLE PROJECT now funnels onto KM's BRICK 6.** OMEGA's
entire ℰ₃ representability machine is on origin, conditional on hL+hArb ONLY (= the keystone + bridges);
the ζ₃/Weil gate is proven VOID; B2 fixed. G0's BB-QF/FLAT/DEG are all real proofs (no sorry) modulo KM's
:660 anchor. **BRICK 6 = the single project domino, reduced to exactly 4 turnkey leaves** (`MulByHomDegree.lean`:
FFM_X/Y/C in `brick6_intertwining` + the A+B finrank tower; playbook τStalkMap→FFM_X/Y/C→A+B banked). **On
landing:** BB-DEG goes axiom-clean fleet-wide → `endDeg_mulBy=N²` → OMEGA's hL/hArb assembly closes Bootstrap:74
(ℤ[1/3]) + Legendre (ℤ[1/6]) → `representable_iff` instantiations → **all three levels representable.** Nothing
structural remains below brick 6. Seat map: **KM** = BRICK 6 (fire ONE — two collided again); **OMEGA** =
turnkey final-assembly prep; **G0** = BB-DIFF :247 (independent); **GH** = [GHA3] β2-heart + β3. ⚠ **FIRE
EXACTLY ONE KM SESSION** — brick 6 is single-file, two sessions WOULD collide. Fire ONE per seat.

---

## STREAM-G0 — ★★★ SUBSTRATE DONE (BB-QF/FLAT/DEG) → RE-CHARTER to BB-DIFF :247 (independent)
> You are G0, resuming STREAM-G0. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-G0.md` IN FULL + boards v10.252-G0/v10.262. Rule-5 claim + sentinel `beastmode_active.G0` — **fire ONE G0 session only**. **★★★ YOUR E[N] SUBSTRATE IS COMPLETE** — BB-QF ✓ BB-FLAT ✓ (`mulByHom_flat`) BB-DEG ✓ (`mulByHom_finrank=N²`), all real proofs (no sorry) modulo KM's :660 anchor; you own no other open substrate sorries. (Your v10.256 dispatch was stale — you'd already landed these; correctly reconciled + wrote the KM handoff.) **RE-CHARTER — BB-DIFF :247** (`mulByHom_formallyUnramified`, T-B5 = Loeffler 3.4.2(2), verified OPEN): [N] is unramified for N invertible — the last gate on `mulBy_etale`/`torsionπ_etale`, INDEPENDENT of brick 6 (start now, no waiting). Completes Loeffler 3.4.2(2) and feeds the étaleness GH's [GHA3] consumes. **Alternatives if you prefer** (owner may redirect): (b) support OMEGA's Bootstrap:74 by building the torsion→coord E[N]-bridges (hArb: 3•P=0⟹μ_P=0, 3•Q=0⟹cubic — your E[N] territory, but partly brick-6-gated); (c) the v10.251-G0 cleanup dedup flag (two `mulByHom_surjective`s). Default BB-DIFF. Marathon v10.162. /beastmode on BB-DIFF.

## STREAM-KM — ★★★ BRICK 6 = THE SINGLE PROJECT DOMINO, 4 turnkey leaves — LAND IT (fire ONE session)
> You are KM, resuming STREAM-KM. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-KM.md` IN FULL + decomposition-keystone.md + boards v10.259-KM/v10.262. Rule-5 claim + sentinel `beastmode_active.KM` — **⚠ FIRE EXACTLY ONE KM SESSION** (v10.253 AND v10.256/259 both saw two collide — and brick 6 is single-file `MulByHomDegree.lean`, so two WOULD conflict now). **★★★★ THE WHOLE PROJECT NOW FUNNELS ONTO YOU** — OMEGA's entire [T-E15-NORM] machine is COMPLETE (conditional on hL+hArb only) and G0's BB-QF/FLAT/DEG are all done modulo your :660 anchor. **BRICK 6 = the single domino, reduced to 4 turnkey leaves** (verified): FFM_X (:658), FFM_Y (:662), FFM_C (:667) inside `brick6_intertwining`, + the A+B finrank tower. **Full playbook banked** (decomposition-keystone.md v10.256/258/259): **τStalkMap** (IsLocalization.lift of the tautological-point evaluation = `projModelFunctionFieldEquiv` by `IsLocalization.ringHom_ext` — the ONE unbuilt construction) → **FFM_X/Y** (germ evaluates through brick 5's `τ.1≫[N]=σ.1` to σ's coord = mulByInt_x/y; generators `chartZRingEquiv_x/_y` exist) → **FFM_C** (K-linearity via `mulByHom_π`) → **A+B** (the finrank tower via `functionField_algHom_ext` + `finrank_of_isFractionRing` + `functionFieldMap_comp_germToFunctionField`, instance-heavy but routine). **On landing: BB-DEG axiom-clean fleet-wide → `endDeg_mulBy=N²` → OMEGA's Bootstrap:74 (ℤ[1/3]) + Legendre (ℤ[1/6]) → ALL THREE LEVELS REPRESENTABLE.** THEN (non-critical): the [FR-GEN] in-tree ForMathlib sub-ticket + endDual_comp_self. Marathon v10.162. /beastmode on brick 6 — the single domino.

## STREAM-GH — [GHA3] route-β 3/5 done → β2-heart + β3 (the last two legs) + hold general-S pins
> You are GH, resuming STREAM-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-GH.md` IN FULL + boards v10.257/258/262. Rule-5 claim + sentinel `beastmode_active.GH`. **★ [GHA3] route-β 3/5 EXECUTED** (`LevelSpaceEtale.lean`, all axiom-clean): β1 (already-proven, stale-docstring correction boarded) + β4 (`etale_of_etale_pullback_snd_of_cover`) + β2-ambient (`torsionPair_baseChange_isPullback`) + β2-L (`levelSpacePullbackIso` + projection triangles) + `exists_iso_of_factor_iff`. **DISPATCH — the last two legs:** **(1) β2-heart** = the factor-iff through `levelSpaceΓ_spec` on both curves, bridged by the T-D8 fibrewise-generation box — its one genuine leaf is the double-base-change geometric-pull transport (`transportE_baseChange` flagged as the candidate machinery). **(2) β3** = the constant-form clopen enumeration. Full designs banked in your sentinel + board. On landing, [GHA3] closes the last GH-owned GHC1 edge. **HOLD the general-S degree pins** (`endDeg_comp` etc.) — they wait on KM's `endDual_comp_self` ([FR-GEN]-gated, non-critical); your trace-pin + T-G3d-Niso engines fire the moment it lands. You remain THE CLOSER (`.Representable` one-liners written). Marathon v10.162. /beastmode on β2-heart.

## STREAM-OMEGA — ★★★★ MACHINE COMPLETE → prep the TURNKEY final assembly (both instantiations)
> You are OMEGA, resuming STREAM-OMEGA. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-OMEGA.md` IN FULL + board v10.262. Rule-5 claim + sentinel `beastmode_active.OMEGA`. **★★★★ [T-E15-NORM] MACHINE COMPLETE + ratified** — the entire ℰ₃ representability machine (B2 fix + rt2 ladder + both RepresentableBy discharges, `naiveLevelThreeRepresentableBy`) is on origin, conditional on **hL+hArb ONLY**; the ζ₃/Weil gate is VOID. (NB coordinator protective-pushed your final 10 commits — they were unpushed despite the report; verify via `git ls-remote` next time.) **DISPATCH — make the final assembly TURNKEY** so brick 6 → a one-liner → representable: (1) write `naiveLevelThree_representable_by_affine R hR hL hArb` (Bootstrap:74) as an exact term taking hL/hArb as hypotheses; (2) do the symmetric **Legendre ℤ[1/6]** final assembly (`legendreDelta_representable_by_affine` modulo its hL = E[2]-generation) the same way. Both hL/hArb bottom out at KM's brick 6 → BB-DEG (torsion→coord bridges 3•P=0⟹μ_P=0, 3•Q=0⟹cubic; generation = #E[N]); **consume them the instant brick 6 lands** → both engine instantiations discharge → the headline. Then stand ready / support GH's β2-heart (scheme-transport, your domain). Marathon; /beastmode on the turnkey assembly.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
