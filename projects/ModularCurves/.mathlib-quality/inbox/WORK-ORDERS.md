# CURRENT WORK ORDERS — the single place to find every seat's session opener

**This is the findable home for the paste-ready openers.** The coordinator keeps it current each
dispatch. To (re)fire a worker: open a `claude` session **from the worktree**
`/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (except YN/y1 work, which also uses
`/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3`), and paste that seat's opener. Each opener points
to the seat's `inbox/STREAM-*.md` for the full banked state; the board (`tickets.md` Amendments) is
the attribution-of-record.

**Four active seats (as of v10.252, 2026-07-15):** ALL FOUR ON THE KEYSTONE — and **★★ KM's FOUNDATION
SPINE HAS LANDED (the critical-path unblock fired).** `endDeg`/`endTrace`/`endDual` are CONSTRUCTED
(Abel-free, scheme fibre-rank), sorry 17→10, `le_endDeg_of_killed_injective` AXIOM-CLEAN
(`KernelBound.lean:100` — GH's `hH` consumable), L4 reduced to ONE deep leaf (L4-iii). **⟹ GH's 6
downstream pins are NOW LIVE, and three seats parallelize.** Prior context: OMEGA drove the shared engine
`representable_iff` (`EllCategory.lean:274`) to SORRY-FREE (T-E12/13/14 + T-E15a-cores) then went terminal;
RelRep ✓✓✓, Rigid ✓, engine ✓, BB-QF ✓. **The two deep leaves left in the keystone: L4-iii + `endDual_comp_self`
(the char-poly).** Seat map: **KM** = the deep leaves (L4-iii division-polynomials + `endDual_comp_self`
char-poly + T-DEG0); **GH** = the 6 downstream identities (LIVE) + the `.Representable` closer; **OMEGA** =
the two normalization tickets (consume `endDeg_mulBy`); **G0** = BB-DEG + BB-FLAT (feed `endDeg_mulBy`'s box).
**Keystone lands ⟹ all three levels representable, same commit** (GH's one-liners written). Fire ONE per seat.

---

## STREAM-G0 — BB-QF ✅ DONE → the E[N]-scheme substrate (BB-FLAT + BB-DEG) for the keystone
> You are G0, resuming STREAM-G0. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-G0.md` IN FULL (through v10.232) + board v10.250. Rule-5 claim + sentinel `beastmode_active.G0` — **fire ONE G0 session only**. **★★★ BB-QF DISCHARGED + ratified** — `mulByHom_locallyQuasiFinite` + `mulByHom_isFinite` de-sorried, axiom-clean (KM 2.3.1 ZMT); BB-FLAT fibre-leg substrate (FF-2/FF-alg/surjective/dominance) BANKED, assembly turnkey. **NEW DISPATCH (owner converged the fleet on the keystone) — finish the E[N]-scheme substrate the keystone cluster consumes:** **(1) BB-FLAT** — `mulByHom_flat` (Torsion.lean:150) — assemble from your banked fibre-leg substrate (it's turnkey per board v10.232; wire FF-2/FF-alg/surjective through). **(2) BB-DEG** — `mulByHom_finrank=N²` (Torsion.lean:155) at the scheme level — the E[N]-as-finite-flat-scheme degree, coordinate with KM's `endDeg_mulBy=n²` (same n²; KM's is the categorical/K4 form, yours is the fibre-finrank form — cross-link, don't re-derive). Both feed OMEGA's E[N]-classification normalization tickets (E[N] finite-flat is the substrate for order-3⟹flex + E[2]-generation). Also close `mulByHom_locallyOfFinitePresentation`/`FormallyUnramified` (:217/:232) if in reach. Marathon v10.162. /beastmode on the E[N]-scheme substrate.

## STREAM-KM — ★★ FOUNDATION SPINE LANDED → the two DEEP LEAVES (L4-iii + endDual_comp_self)
> You are KM, resuming STREAM-KM. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-KM.md` IN FULL + boards v10.250/v10.251/v10.252. Rule-5 claim + sentinel `beastmode_active.KM`. **★★ FOUNDATION SPINE RATIFIED** — `endDeg`/`endTrace`/`endDual` CONSTRUCTED Abel-free (6595238b2); sorry 17→10; `le_endDeg_of_killed_injective` AXIOM-CLEAN (`KernelBound.lean:100` — GH's `hH` handshake DONE from your side); `endDeg_mulBy=n²` proven modulo the 3 auto-cleaning boxes; L4 reduced to ONE leaf. The critical-path unblock has FIRED (GH's 6 pins + OMEGA now consume your work). CONTINUE — **the two deep leaves left in the keystone:** **(1) L4-iii (next dive)** — `functionFieldMap [N]` on the generators = the division polynomials, through the AdditionChart* group law at the generic point (engine `functionField_algHom_ext` + `mulByInt_pullbackAlgHom_x_gen/_y_gen` ✓ banked; `DominantFunctionField.lean`). Landing it auto-cleans BB-DEG → `endDeg_mulBy`'s box → OMEGA's + GH's numeric inputs. **(2) `endDual_comp_self` (:177)** — the char-poly f+fᵗ=[tr] (where ALL the quadratic/Abel content concentrates; your 3-part gap analysis on the board) — unblocks GH's `endDeg_comp`. Plus T-DEG0 (`mulByHom_zero_finrank` :168, Cartier-nzd route mapped) in passing. **Coordinate the L4-iii ↔ BB-DEG handshake with G0** (same n², two forms). You remain the critical path. Marathon v10.162. /beastmode on the deep leaves.

## STREAM-GH — ★ GO NOW: KM's foundation LANDED → your 6 downstream pins are LIVE + hold the CLOSER
> You are GH, resuming STREAM-GH. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-GH.md` IN FULL + boards v10.250/v10.252. Rule-5 claim + sentinel `beastmode_active.GH`. **★★ rigidity + ALL-LEVEL `.Representable` one-liners DELIVERED** — you are THE CLOSER (`gammaH_representable_of_orderOf`/`gammaBot_representable` close the instant the keystone lands). **★ THE UNBLOCK HAS FIRED: KM landed `endDeg`/`endTrace`/`endDual` — your 6 downstream pins are NOW LIVE.** GO on the tranche (pure Ell-category degree algebra, your wheelhouse), all in `EndomorphismDegree.lean`: **`endDeg_comp` (:233, multiplicativity — depends on KM's `endDual_comp_self`, coordinate timing), `endDeg_comp_mulBy` (:298), `endTrace_comp_mulBy` (:304), `endTrace_sq_le` (:322, Hasse bound), `eq_zero_of_endDeg_eq_zero` (:328), `exists_eq_one_add_mulBy_comp_of_fixesTorsion` (:336, feeds your own rigidity).** Your `hH` is consumable NOW — `le_endDeg_of_killed_injective` is AXIOM-CLEAN at `KernelBound.lean:100`. Also: minimise the GHC1 `qpd` sorryAx (GammaHMaster, 1 sorry) + hold the `.Representable` closer ready; hLN=T-W7.8 owner-parked. Marathon v10.162. /beastmode on the 6 pins.

## STREAM-OMEGA — engine SORRY-FREE ★★★★ → RE-TASKED to the two E[N]-normalization tickets
> You are OMEGA, resuming STREAM-OMEGA. Worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` (branch dev/modular-curves). Pull, read `projects/ModularCurves/.mathlib-quality/inbox/STREAM-OMEGA.md` IN FULL (through v10.248) + board v10.250. Rule-5 claim + sentinel `beastmode_active.OMEGA`. **★★★★ ENGINE COMPLETE + ratified** — you drove `representable_iff` to sorry-free: T-E12/T-E13 (omegaProblem repr+rigid over ℤ[1/6]), T-E14 (AX1 machine + corrected δ), T-E4a/b + T-H3 de-sorried, T-E15a cores (object/uniqueness/forward-UP/RT1) — all axiom-clean. Your v10.248 terminal was correct: the remainder is E[N]-keystone-gated, NOT NORM-free. **NEW DISPATCH (owner converged the fleet on the keystone) — the two E[N]-normalization tickets YOU flagged, now YOURS to own** (your just-built E3/Legendre territory): **(1) [T-E15-NORM]** — order-3⟹flex existence (the universal marked (P,Q) IS a naive level-3 structure; `universalE3_isE3Datum` + `IsE3Datum` for arbitrary level-3 → discharges `naiveLevelThree_representable_by_affine`). The flex relation is already Q's equation; the uniqueness certificate `e3_vc_marked` ✓. **(2) [T-E14-LVL-b]** — E[2]-generation (the Legendre level clause `legendreDeltaRepresentableBy`'s hL). **Consume KM's `endDeg_mulBy=n²` + G0's BB-DEG/BB-FLAT as they land** (both feed the E[N]-scheme classification these need). Landing both ⟹ both engine instantiations discharge ⟹ headline. Board LOUDLY (★★). Marathon; /beastmode on the normalization tickets.

---

## Parked / done (no order)
- **YN's Y(N) charter** — BEASTMODE-DONE ([YF-ETALE]★ + T-D8-bridge + MASTER wired; in PR-B #5790). Its remaining conjuncts auto-complete when OMEGA lands. The seat is re-tasked to OMEGA above.
- **fable-PIC0's Pic⁰ [NAT]-arc** — parked-with-honor (resume ledger board v10.169-PIC0); resumable if a 5th seat frees.
