# INBOX: NEW-CASCADE (fresh account — takeover of the P3b3 seat: the étale cascade / [T-B6′]. Read fully before touching anything)
- [2026-07-10, coordinator, v10.121] You take over the **P3b3 seat** (the account is retired — owner confirmation; P3b3's return-reclaim clauses are VOID). **Your work order is `inbox/P3b3.md`'s v10.120 entry** (gate-audit-first dispatch: audit whether `geomFibrePointAddEquiv.map_add'` ([T-B6′]) is dischargeable from the landed C6 dictionary layer; execute if yes; named-gate + unconditional prep if no) — read it in full, it is written to be self-contained, then work it under YOUR name.
  SETUP (this machine): the shared main worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` is on dev/modular-curves (LIVE SIBLING WORKERS — never `git add` broadly, pathspec-commit only your own files); the [T-B6′] sorry lives on **dev/modular-curves-y1** in worktree `/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3` (NEW-Y1 is PARKED there — rule-5 claim + collision watch); the cascade branch is **dev/modular-curves-b5da** (pushed through 1a38dc29a; PR #5223 open to dev). `ln -s ../AINTLIB/refs refs` if absent; LSP live (lean_goal etc.).
  READS (order): (1) canonical inbox + board via `git show origin/dev/modular-curves:…` at session start AND every commit boundary (v10.86 protocol); (2) `inbox/P3b3.md` (the work order); (3) board tail v10.111 → end (the C6 dictionary landing, the 0c-i atlas laws, v10.120's audit framing); (4) the inherited state: `ForMathlib/GeometricFibreComparison.lean` (y1 — the sorry site; bijection PROVEN, only `map_add'` open), `EllipticCurve/Torsion.lean:228` (dev — the BB-DIFF MASTER, assembled awaiting L-BC), the b5da branch tips (T-B5D-A + T-DISC, both proven).
  INHERITANCE NOTE: treat **origin as ground truth** — anything the retired account left unpushed is unrecoverable and must be re-derived if missed; the fleet's commit-early+push discipline means the pushed state above is believed complete (T-W7.1b landed and already consumed by the atlas; the cascade legs L-A/T-DISC pushed on b5da).
  RULES: rule-5 claim (board section + sentinel `beastmode_active.NEW-CASCADE`) BEFORE touching files; board your execution plan before building (charter mode — report at the audit verdict, at milestones, at walls, at completion); v10.24(a–e); v10.115-a term-mode-as-probe; NO maxHeartbeats; v10.52 commit-early+push; v10.35b external-quiet; single-target builds; p0/p2/held files read-only per the standing map; [OWNER-FLW] owner-reserved — consume as pin.
- [2026-07-10, coordinator, v10.127] AUDIT + PREP RATIFIED (★ — model gate-audit; the verdict re-verified at source this activation). Three rulings for your fill session: (1) **B2 EVENT #3 APPROVED** — the hz-fix (`hz : E.zero ≫ eqToHom hE = projModelZero W` or equivalent) is owner-delegate approved, logged in b2_log.jsonl; execute it in the SAME COMMIT as the map_add' fill, quoting the log entry in the commit message; NEW-Y1 (IFACE holder) is informed. (2) **dev → y1 merge approved** as your fill step 1 (the C6 layer's absence on y1 is verified); do it as a MERGE (not cherry-pick) so y1 absorbs the whole current dev state once — if the merge conflicts anywhere outside board files, stop and board it. (3) Your park-gate is CONFIRMED and imminent: c5β's T-G4 Over-level transports are in flight via their bg agent (mulModelHom_comm general-R already landed). The coordinator watches the board and signals the owner to re-fire you the moment T-G4 boards — your pinned four-step recipe (merge, record packaging, hz+fill, hfib discharge) is the work order as-is.
- [2026-07-10, coordinator, v10.131] EARLY-FIRE AMENDMENT — your recipe's STEP 1 IS GATE-FREE; execute it NOW, ahead of the T-G4 signal. c5β's of_map breakthrough (their sentinel + f1dd3eab4) puts T-G4 completion plausibly inside their current session; the dev → y1 MERGE (your slowest mechanical step, ~900 commits of drift, board-file conflicts expected) should not sit on the critical path behind it. THIS SESSION: (1) execute the approved dev → y1 merge (v10.127 rules: MERGE not cherry-pick; resolve tickets.md append-only; conflicts outside board files ⟹ stop + board); verify green (single-target: the y1 spine — YOneTatePoint + GeometricFibreComparison + their imports); push. (1b) NOTE: c5β's T-G4 tail will land on dev AFTER your merge — at gate-fire do a small TOP-UP merge of dev (trivial after the big one) before step 2. (2) IF the T-G4 board line lands while you are still in-session: proceed directly to steps 2–4 (record packaging at fields; hz-fix + map_add' fill SAME-COMMIT quoting b2_log entry #3; hfib discharge → L-BC → MASTER → torsionπ_etale). IF not: park at the (now much closer) gate, sentinel updated. Shared-index discipline: atomic pathspec commits per the v10.130 fleet recipe — `git commit -m "<msg>" -- <paths>`, never separate add+commit. Rule-5 claim + collision watch in aintlib-mc-b3 (NEW-Y1 parked there).
- [2026-07-10, coordinator, v10.134] GATE 1 CLEARED — re-enter now. The 40e44a706 regression is FIXED and VERIFIED (d10e7fc87: Representability.lean restored verbatim — EllHom.pullSection :148 / gammaOneNaiveProblem :212 / the MASTER :252 all back; capstone relocated to Moduli/QuotientRepresentability.lean; FULL root module 3,766 jobs green; TREE-RED lifted, board v10.134). THIS SESSION (the merge redo is gate-free, same logic as the v10.131 early-fire): (1) re-merge from your preserved wip/y1-dev-merge-cascade onto y1, top-up merge of dev's tail (which now includes the repair + everything since), re-resolve the two board files append-only per your own precedent; (2) SPINE VERIFY (your v10.132-CASCADE pass — it caught the last one) + push y1; (3) then: if c5β's T-G4 board line has landed (check the tail — they are mid-integration, transports landing one by one), proceed DIRECTLY to fill steps 2–4 (record packaging at fields; hz-fix + map_add' SAME-COMMIT quoting b2_log entry #3; hfib discharge → L-BC → MASTER → torsionπ_etale); if not, park at the single gate with sentinel updated. Discipline: atomic pathspec commits (v10.130); rule-5 re-claim on y1; collision watch (NEW-Y1 parked).
