# INBOX: NEW-CASCADE (fresh account — takeover of the P3b3 seat: the étale cascade / [T-B6′]. Read fully before touching anything)
- [2026-07-10, coordinator, v10.121] You take over the **P3b3 seat** (the account is retired — owner confirmation; P3b3's return-reclaim clauses are VOID). **Your work order is `inbox/P3b3.md`'s v10.120 entry** (gate-audit-first dispatch: audit whether `geomFibrePointAddEquiv.map_add'` ([T-B6′]) is dischargeable from the landed C6 dictionary layer; execute if yes; named-gate + unconditional prep if no) — read it in full, it is written to be self-contained, then work it under YOUR name.
  SETUP (this machine): the shared main worktree `/Users/mcu22seu/Documents/GitHub/aintlib-modular-curves` is on dev/modular-curves (LIVE SIBLING WORKERS — never `git add` broadly, pathspec-commit only your own files); the [T-B6′] sorry lives on **dev/modular-curves-y1** in worktree `/Users/mcu22seu/Documents/GitHub/aintlib-mc-b3` (NEW-Y1 is PARKED there — rule-5 claim + collision watch); the cascade branch is **dev/modular-curves-b5da** (pushed through 1a38dc29a; PR #5223 open to dev). `ln -s ../AINTLIB/refs refs` if absent; LSP live (lean_goal etc.).
  READS (order): (1) canonical inbox + board via `git show origin/dev/modular-curves:…` at session start AND every commit boundary (v10.86 protocol); (2) `inbox/P3b3.md` (the work order); (3) board tail v10.111 → end (the C6 dictionary landing, the 0c-i atlas laws, v10.120's audit framing); (4) the inherited state: `ForMathlib/GeometricFibreComparison.lean` (y1 — the sorry site; bijection PROVEN, only `map_add'` open), `EllipticCurve/Torsion.lean:228` (dev — the BB-DIFF MASTER, assembled awaiting L-BC), the b5da branch tips (T-B5D-A + T-DISC, both proven).
  INHERITANCE NOTE: treat **origin as ground truth** — anything the retired account left unpushed is unrecoverable and must be re-derived if missed; the fleet's commit-early+push discipline means the pushed state above is believed complete (T-W7.1b landed and already consumed by the atlas; the cascade legs L-A/T-DISC pushed on b5da).
  RULES: rule-5 claim (board section + sentinel `beastmode_active.NEW-CASCADE`) BEFORE touching files; board your execution plan before building (charter mode — report at the audit verdict, at milestones, at walls, at completion); v10.24(a–e); v10.115-a term-mode-as-probe; NO maxHeartbeats; v10.52 commit-early+push; v10.35b external-quiet; single-target builds; p0/p2/held files read-only per the standing map; [OWNER-FLW] owner-reserved — consume as pin.
