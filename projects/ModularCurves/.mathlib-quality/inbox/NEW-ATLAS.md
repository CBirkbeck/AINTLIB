# INBOX: NEW-ATLAS (fresh worker — read this whole file, then the reads below, before touching anything)
- [2026-07-09, coordinator, v10.80] CHARTER: [Y1-ATLAS] iii/iv/v — the classifying clause of `exists_tatePoint` (Loeffler Cor 3.3.5, scheme-level), the last large unblocked subtree of Y₁(N) (T-E7). vi is DONE (don't touch it); i/ii landed earlier.
  SETUP (this machine): cd ~/Documents/GitHub/AINTLIB && git fetch origin && git worktree add ../aintlib-mc-atlas -b dev/modular-curves-y1-atlas origin/dev/modular-curves-y1 && cd ../aintlib-mc-atlas && ln -s ../AINTLIB/refs refs && lake exe cache get (first build compiles the project tree once; then incremental).
  READS (in order): CLAUDE.md · .mathlib-quality/COORDINATOR-HANDOVER.md · tickets.md §v10.37 [STREAM-Y1] card + Amendments v10.71→end · decomposition-y1-assembly.md ([Y1-ATLAS] subtree — per-leaf plans + verbatim Loeffler quotes) · this inbox at every commit boundary.
  RULES: rule-5 claim on the board BEFORE touching files (sentinel beastmode_active.NEW-ATLAS); work in a NEW file (e.g. ModularCurve/YOneAtlasClassify.lean) on YOUR branch — do NOT edit YOneAssembly.lean (NEW-Y1's, single-writer); consume Comparison.lean's proven T-W7.1b lemmas; v10.24(a–e) discipline (decompose-don't-grind, opaque interfaces, term-built isos, named handles); NO maxHeartbeats ever; v10.52 commit-early + PUSH each green increment; v10.35b external-quiet; single-target builds. CREDIT-FRUGAL: bounded focused sessions, no exploratory side-quests, stop at clean boundaries.
  DELIVERABLE: the master classifying lemma(s) per the artifact, sorry-free or with registered-gate sorries + plans (v10.8). PR to dev/modular-curves-y1 at subtree completion or a milestone boundary; board-report per the stream card (milestone or post-decomposition wall only).
