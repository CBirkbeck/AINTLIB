FOCUS: AGE-4 brick beta4d — the tsum reindex: sum_v-ite over idealZLattice(mk0 J) = sum over idealSet x Z^rank of gauss at shifted u (via euclidConeEquiv + sum_placeWeights_unit_smul with eps := prod fs^n and the cone point y from mem_idealSet); needs cone-point preimage as K-element (mem_idealLattice) + Equiv.tsum_eq. Then beta4e: swap tsum/integral + sum_n int_box(u + n-shift) = int_logSpace (integral_eq_tsum with basisUnitLattice box). Then gamma Jacobian, delta counting, epsilon assembly. ALL DERIVATIONS IN TICKET SP1-AGE + HANDOVER.
