FOCUS: AGE-4 brick beta4f — FD unfolding per cone point: for nonneg periodic-free integrand, sum over n of int_box f(u + logEmb(prod fs^n)) du = int_logSpace f du (IsAddFundamentalDomain (basisUnitLattice box).integral_eq_tsum + logEmbedding_prod_fundSystem: logEmb(prod fs^n) = sum n_j basis_j = the generic lattice element); then Tonelli tsum<->integral swaps to get w^-1 * int_box (Theta-1) as sum_a int_logSpace gauss; then gamma: the (t,u)->y change of variables per cone point (Jacobian kappa, Gamma integrals). Ticket SP1-AGE + HANDOVER carry all derivations.
