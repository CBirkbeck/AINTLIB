FOCUS: SP1-AC A5 — Landau local partial fractions: for s near the slab at height T, ζ-log-deriv/H-log-deriv = ∑_{ρ ∈ ball-zeros} 1/(s−ρ) + O_K(log(2+|T|)). Route: Complex.borelCaratheodory (mathlib, Analysis/Complex/BorelCaratheodory.lean — check exact statement first) applied to log(g/∏(z−ρ)^{m_ρ}) on the Jensen ball at A+iT (g = normalized H, zeros removed → zero-free on ball → holomorphic log exists on the SIMPLY-CONNECTED ball); Re log = log‖·‖ bounded by A4-machinery; BC upgrades to |log| → derivative bound via Cauchy on smaller ball → H′/H(s) − ∑ 1/(s−ρ) bounded on the slab. Tao 246B Notes 1 Thm-9-adjacent / Titchmarsh §3.9 scheme. A4 COMPLETE (f563ce33).
