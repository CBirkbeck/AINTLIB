FOCUS: SP2-RECT R-f proper — exists_contour_height in ZeroCapture.lean: obtain (A1,C1,hmain) := exists_logDeriv_partial_fractions (count = (hmain T hT).1, pf = .2) + (A2,c2,hgood) := exists_good_height (two-sided); A := max A1 A2 (+handle A1+5<=|T| for T in [T0,T0+1] and -T); statement: exists A c C, forall T0>=A+5, exists T in [T0,T0+1], (two-sided sep) and forall sigma in [-1/4,5/4], forall eps in {1,-1}, H(sigma+eps iT) != 0 and ||logDeriv H (sigma+eps iT)|| <= C log^2(2+T0). Proof per edge eps: s := sigma + eps iT in closedBall(A1 + eps iT, A1+5/4); H s != 0 from sep (if H s = 0 then |eps T - s.im|... s.im = eps T so sep gives c2/log <= 0 false); triangle: ||logDeriv H s|| <= ||sum|| + C1 log(2+|eps T|); per-term: |s - rho| >= |Im(s-rho)| = |eps T - rho.im| = |T - eps rho.im| >= c2/log(2+T0) (two-sided!); ||sum|| <= count * log/c2 <= C1 log(2+|T|) * log/c2; log(2+|eps T|) = log(2+T) <= log(2+T0+1) <= 2log(2+T0). Then R-g.
