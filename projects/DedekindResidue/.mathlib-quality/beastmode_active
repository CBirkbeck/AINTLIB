FOCUS: AGE-4 brick beta4c — per-point shift: gauss(pW(heckeWeights t u), euclidMixedEquiv.symm((prod fs^n) smul a)) = gauss(pW(heckeWeights t (u + sum n_i (basisUnitLattice-vector i))), euclidMixedEquiv.symm a) via mulCoords-compat (mulCoords def IS the euclidMixedEquiv-conjugated mult) + heckeWeights_add_logEmbedding + logEmbedding_fundSystem; then the ite-tsum reindex along euclidConeEquiv; then sum_n int_box = int_logSpace (IsAddFundamentalDomain.integral_eq_tsum with basisUnitLattice box). Then gamma: (t,u)->y Jacobian.
