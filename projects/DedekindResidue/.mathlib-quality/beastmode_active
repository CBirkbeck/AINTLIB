FOCUS: SP1-AC A4 FINAL ASSEMBLY: (i) H-decaying-upper on [2, A+R]-side: H = s(s−1)pref·γ·ζ with norm_Gamma_le_mul_exp_add_nat-packaged uppers (mirror exists_norm_Gamma_le already handles arbitrary σ ≥ 1/2 — write exists_gammaFactor_upper_at (σ) then exists_H_upper_right (σ ∈ [2, σ₁], |t| ≥ 2)); (ii) merge with exists_H_strip_decay → sup-bound on closedBall (A+iT) (A+2) (ball ⊆ {−1 ≤ re ≤ 2A+... pick R := A+2, ball reaches re ∈ [−2, 2A+2]: need strip-decay extended to re ≥ −2?? NO — ball ⊆ [−2,...]: LEFT edge re ≥ A−(A+2) = −2 — exists_H_strip_decay covers only re ≥ −1!! FIX: radius R := A+1 → re ≥ −1 ✓ slab-coverage: dist from A+iT to (0,T±1)-corners = √(A²+1) < A+1 ✓ (A ≥ 2) so slab {0 ≤ re ≤ 1, |im−T| ≤ 1} ⊂ ball(A+iT, √(A²+1)) ⊂ ball radius r := √(A²+1) < R := A+1 ✓ log(R/r) > 0 K-const ✓); (iii) AnalyticOnNhd.sum_divisor_le application → m_K(T) ≤ C_K·log(2+|T|)-shaped (Δ-dependence inside C_K fine for now). exists_H_center_lower LANDED (this push).
