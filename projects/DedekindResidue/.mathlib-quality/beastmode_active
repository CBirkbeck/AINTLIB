FOCUS: SP1-AC A5-ii — c1/c2 LANDED (ball-sup A+3, count A+2). NEXT brick b-prime: divisor split div_big = div_mid + div_ann, P_big = P_mid*P_ann pointwise (zpow_add-nonneg case split), h := P_ann*g_big analytic on ball(A+3) zero-free on ball(A+2), H = P_mid*h. Then max-principle sphere A+2.5, log h, BC recentered, Schwarz, logDeriv unwind.
